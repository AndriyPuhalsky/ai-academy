# Журнал інфра-змін (CHANGELOG)

Цей файл — reverse-chronological журнал подій для бекенд-інфраструктури проєкту AIA:
SQL-міграції в Supabase, деплої/оновлення Edge Functions (`tg/telegram_index.ts`,
`tg/contact_index.ts` тощо), зміни секретів/конфігурації Supabase та Cloudflare, версії.
Це доповнення до наративного розділу "## Стан бекенду" в кореневому `CLAUDE.md` — там
опис поточного стану, тут — хронологія, як до нього дійшли.

Нові записи додаються **зверху** (найновіші перші), під заголовком з датою у форматі
`РРРР-ММ-ДД`.

## 2026-09-05

- **Застосовано міграцію `005-2-module-titles.sql`** (задача 005, хвіст Д5.4 п. 1) —
  назви шести модулів курсу «AI Термінал» вирівняно з `claude-code.config.json`.
  Виконано з сесії через Supabase Dashboard → SQL Editor за **разовим прямим дозволом
  власника саме на цей файл** (прецедент — 005-1 від 2026-09-03). Одна транзакція.
  Файл: `dev/build/005-ai-terminal/02-backend/db/005-2-module-titles.sql`.
- **Що змінилось у базі:** рівно шість значень `title` у `public.modules` курсу
  `claude-code` — `c04`, `c08`, `c14`, `c16`, `c19`, `c22`. Більше нічого: `code`,
  `number`, `slug`, `passing_score`, `sort_order`, `course_id` не чіпались; таблиці з
  даними учнів (`progress`, `quiz_attempts`, `certificates`, `enrollments`) у міграції
  не згадуються взагалі. Функції, політики, гранти й секрети не змінювались.
  Edge Function не чіпалась і передеплою не потребує. Cloudflare не чіпався.
- **Було → стало:** `Перша сесія: проси → дивись → підтверджуй` → `Перша сесія: проси,
  дивись, дозволяй` (`c04`) · `Plan Mode й пісочниця` → `Режим плану й пісочниця` (`c08`) ·
  `Skills, команди, плагіни` → `Навички, команди, плагіни` (`c14`) · `Hooks` →
  `Перехоплювачі подій (hooks)` (`c16`) · `Headless і CI: Claude як утиліта` → `Запуск без
  діалогу: Claude як утиліта` (`c19`) · `Безпека, великі репо, куди далі` → `Безпека,
  великі проєкти, куди далі` (`c22`). Джерело назв — рішення власника від 2026-09-03
  (`dev/design/005-ai-terminal/00-decisions.md`), яке вже втілене в конфізі й у `<h1>`
  сторінок; база відставала від них із моменту застосування 005-1.
- **Розходжень виявилось рівно шість — стільки й називав SUMMARY, і всі шість кодів
  у ньому були правильні.** Це не приймалось на віру: SUMMARY прямо попереджав, що коди
  «виведені співставленням і на живих даних не перевірені». Перевірка зроблена **порівнянням
  `md5(title)` бази з md5 назв конфіга**, а не оком — саме тому видно, що решта 17 назв
  збігаються байт-у-байт, включно з апострофом у `c12` («пам'ять»), який візуально є
  типовим кандидатом на прихований розсинхрон, але насправді ідентичний.
- **Чому це варто було робити, хоча користувач розсинхрону не бачив:** назви модулів на
  сайті бере `js/module.js` з конфіга, тож `modules.title` сьогодні не читає жоден
  клієнтський шлях. Дефекту в проді не було й немає. Але будь-який майбутній звіт із бази,
  адмін-екран чи вибірка для бота показали б назви, яких на сайті немає, без жодного
  натяку на причину.
- **Перевірка після застосування (незалежно від браузера, через MCP):** усі 23 `md5(title)`
  бази = md5 конфіга, розбіжностей 0 · жодної зі старих шести назв у базі не лишилось ·
  структура ціла: 23 модулі, нумерація суцільна 1…23, коди й номери унікальні,
  `passing_score` 70/85 як був · інші курси не зачеплені (`ai-essentials` 12,
  `ai-architect` 22, `claude-code` 23 — разом ті самі 57 модулів).
- **Дослівний результат запуску:** помилки не було, транзакція пройшла разом із вбудованим
  запобіжником (`raise exception`, якщо оновлених рядків ≠ 6 або модулів у курсі ≠ 23).
  Панель Results показала **`23 rows`** — це вивід останнього з чотирьох перевірочних
  `select` у файлі. Рядка `Success. No rows returned` тут бути не могло саме тому, що
  файл закінчується вибірками, а не DDL.
- **Спосіб вставки, який знову себе виправдав:** `pbcopy < файл` → `cmd+a` → `cmd+v` і
  звірка `window.monaco.editor.getModels()[0].getValue().length` **до** натискання Run.
  Збіглося: 9544 символи в редакторі = 9544 символи у файлі (12544 байти UTF-8 — байти й
  символи тут не одне й те саме, звіряти треба символи). Перша спроба вставки провалилась
  мовчки: сторінка домалювалась після кліку, `cmd+a` виділив текст усієї сторінки, і в
  моделі лишився порожній рядок. **Без перевірки довжини це виглядало б як успішний Run
  порожнього запиту** — власне тому вона й робиться до Run, а не після.

## 2026-09-03

- **Застосовано міграцію `005-1-course-ai-terminal.sql`** (задача 005, третій курс
  «AI Термінал»). Виконано з сесії через SQL Editor за прямим дозволом власника —
  раніше міграції застосовував лише він. Одна транзакція, `Success. No rows returned`.
  Файл: `dev/build/005-ai-terminal/02-backend/db/005-1-course-ai-terminal.sql`.
- **Що змінилось у базі:** новий рядок у `courses` (`slug = claude-code`, `is_paid = false`,
  `sort_order = 3`) · 23 рядки в `modules` (`c01…c23`, `number` 1…23, `passing_score` 70,
  іспит `c23` — 85) · backfill `enrollments` для всіх 5 наявних профілів ·
  `maybe_issue_certificate` і `admin_user_report` перевизначені.
- **`maybe_issue_certificate`:** `coalesce(full_name, email, 'Студент')` →
  `coalesce(nullif(btrim(full_name), ''), 'Студент')`. Закриває пункт 4 задачі 004:
  email більше не може потрапити в сертифікат і, через `verify_certificate`, до будь-кого,
  хто знає публічний код. Зроблено до появи першого сертифіката (`certificates` = 0).
- **`admin_user_report`:** прибрано захардкоджений `slug = 'ai-essentials'`, функція стала
  курсо-агностичною. Наслідок у боті: `total` тепер 57 (усі курси) замість 12, а рядок
  «Завершили курс» де-факто означає «завершили всі курси». Сім колонок і «рядок =
  користувач» збережені навмисно — `toCsv` у `tg/telegram_index.ts` хардкодить їх за
  іменами. **Edge Function не змінювалась і передеплою не потребує.**
- **`search_path` збережений точно.** Крок 0 показав `proconfig = ["search_path=public"]`;
  чернетка міграції ставила `public, pg_temp`, і це мовчки змінило б конфігурацію, якої
  ніхто не просив міняти. Виправлено на `public` у міграції і у блоці відкату.
- **Перевірка після застосування — 8 із 8:** курс `claude-code` є й безкоштовний · модулів
  23, нумерація суцільна 1…23, коди унікальні · глобальних дублів коду 0 · зараховано 5 із
  5 профілів, усі `active` · `has_email_fallback` = false · захардкодженого слага в боті
  немає · `search_path=public` на обох функціях · `report_total` 57 = `all_modules` 57.
- **Секрети й конфігурація Supabase не змінювались.** Cloudflare не чіпався.

## 2026-08-26
- **Реліз у прод: задача 003 «Роадмап розробки» — нова публічна сторінка `/roadmap`.**
  Мерджі в `main` — `9f0f1be` (уся задача, fast-forward `dev` → `main`) і `892e2a5`
  (підйом версій + закриті статуси й журнали). Деплой — автоматичний Cloudflare Workers.
  **SQL-міграцій не застосовувалось, Edge Functions не деплоїлись, секрети й конфігурація
  Supabase не змінювались** — Supabase не чіпався в цій задачі взагалі, сторінка не читає
  й не пише в базу нічого.
- **Нове в роздачі сайту:** `roadmap.html`, `roadmap.json`, `css/roadmap.css`,
  `js/roadmap-render.js`, `js/roadmap-motion.js`. Усі роздаються за замовчуванням —
  `.assetsignore` **не чіпався** (перевірено: приватне як і раніше 404).
  **Перше підключення GSAP** у проєкті: `gsap` + `ScrollTrigger` + `CustomEase` 3.13.0 з
  CDN. Ліцензія перевірена на `gsap.com/licensing` — безкоштовна для комерційного
  використання, атрибуції не вимагає.
- **Версії сайту підняті** (рішення власника при релізі, питання 5 плану 003):
  `config.json` (AI Академія) `0.2.0` → **`0.3.0`**, `architect.config.json`
  (AI Architect) `1.1.0` → **`1.2.0`**, поле `updated` в обох — `2026-08-26`.
  `roadmap.json` `meta.updated` уже був `2026-08-26`. Живі конфіги на проді віддають саме
  `0.3.0` / `1.2.0` (перевірено curl-ом).
- **Пастка деплою, яку варто пам'ятати: Cloudflare Workers Builds збирає один раз на
  коміт.** Fast-forward-мерж дає `main` і `dev` **однаковий SHA**, тому мерж `9f0f1be`
  нового білда не викликав: GitHub check-run для цього коміта світив `success` (це білд,
  зроблений раніше для `dev`), а прод 10+ хвилин віддавав старий сайт — усі нові адреси
  404. Прод оновився за **54 с** після того, як на `main` прийшов новий коміт `892e2a5`.
  **Висновок для наступних релізів: після мержу перевіряти сам прод, а не статус
  check-run**; якщо SHA не змінився — потрібен окремий коміт.
- **Друга пастка, цього разу в самій перевірці:** Workers редіректить `.html` → адресу без
  розширення, тому `/roadmap.html` віддає **307**, а не 200 (рівно як `/index.html`,
  `/architect.html`, `/verify.html`). Перевіряти треба `/roadmap`.
- **Перевірка прода після деплою (curl, коди відповіді).**
  Роадмап → 200: `/roadmap`, `/roadmap.json`, `/css/roadmap.css`, `/js/roadmap-render.js`,
  `/js/roadmap-motion.js`. Регресії немає → 200: `/`, `/architect`, `/config.json`,
  `/architect.config.json`, `/css/custom.css`, `/js/config.js`, `/js/contact.js`,
  `/js/auth.js`, `/js/auth-ui.js`.
  Регістрові пастки → 404: `/Roadmap`, `/Roadmap.json`, `/CSS/roadmap.css`,
  `/JS/roadmap-render.js`, `/CSS/custom.css`, `/JS/config.js` (Cloudflare чутливий до
  регістру — застереження з `CLAUDE.md` тримається).
  Приватне не роздається → 404: `/CLAUDE.md`, `/wrangler.toml`, `/.assetsignore`,
  `/.gitignore`, `/tg/CHANGELOG.md`, `/dev/build/JOURNAL.md`,
  `/dev/build/003-roadmap/content.md`, `/dev/design/003-roadmap/task.md`,
  `/deploy-guide.local.html`.
  Дані на проді: `roadmap.json` віддає **18 пунктів = 6 `done` / 2 `progress` / 10 `ahead`**
  — збігається з `dev/build/003-roadmap/content.md`.
- **Хвости задачі 003, свідомо НЕ виправлені перед релізом** (жоден не блокував merge, усі
  описані в `dev/build/003-roadmap/SUMMARY.md`): дві латентні пастки в
  `js/roadmap-render.js` — згортання квартальних груп за індексом (`gi >= 2`) згорне
  найсвіжіший квартал, коли їх стане три, і гілка `dropped` сортує «Зроблено» у зворотному
  порядку; обидві невидимі на поточних даних. Плюс макет у `dev/design/` лишився з дефектом
  D-01, виправленим лише в коді сайту (`dc8cd12`) — повторне перенесення того файла
  принесе дефект назад.

## 2026-08-25
- **Реліз у прод: задача 001 «Вхід і реєстрація через Google».** Мерджі в `main` —
  `661cf45` (код задачі: `js/auth.js` як шар даних, новий `js/auth-ui.js`, слот
  авторизації і токени макета в 38 HTML) і `d7febfd` (версії у футері + закриті статуси
  в індексах). Деплой — автоматичний Cloudflare Workers після push у `main`.
  **SQL-міграцій не застосовувалось, Edge Functions не деплоїлись, секрети й
  конфігурація Supabase не змінювались** — реліз суто клієнтський.
- **Версії сайту підняті:** `config.json` (AI Академія) `0.1.0` → **`0.2.0`**,
  `architect.config.json` (AI Architect) `1.0.0` → **`1.1.0`**, поле `updated` в обох —
  `2026-08-25`. Живий `config.json` на проді віддає саме `0.2.0` (перевірено curl-ом).
- **Перевірка прода після деплою (curl, коди відповіді):** `/` → 200 ·
  `/architect.html` → 307 на `/architect` → 200 · `/config.json` → 200 ·
  `/js/auth-ui.js` → 200. Регістрові пастки `/JS/auth.js` і `/CSS/custom.css` → 404
  (Cloudflare чутливий до регістру — застереження з `CLAUDE.md` тримається).
  Приватне не роздається: `/CLAUDE.md`, `/tg/CHANGELOG.md`, `/dev/build/JOURNAL.md` → 404,
  тобто `.assetsignore` покриває і нові файли майстерні `dev/build/`.
- **Домен і TLS підтверджено остаточно** (це закриває хвіст із `CLAUDE.md` від 2026-08-18):
  NS через 8.8.8.8 — `elliot.ns.cloudflare.com` / `ziggy.ns.cloudflare.com`, A-записи
  `172.67.190.19`, `104.21.57.106`; сертифікат `CN=ai-academia.com.ua`, видавець
  Google Trust Services (WE1), дійсний 2026-08-18 → 2026-11-16. HTTPS віддає 200.
- **Хвости задачі 001, свідомо НЕ виправлені перед релізом** (жоден не блокував merge, усі
  описані в `dev/build/001-oauth-google/SUMMARY.md`): критерій приймання 15 нездійсненний
  за конструкцією (`signInWithOAuth` не робить мережевого запиту) — дефект формулювання,
  не коду · ціль кнопки імені 130 × 18 px проти мінімуму 24 × 24 (WCAG 2.2 AA) · фон під
  модалкою прокручується. Останні два — так у затвердженому макеті, тож правити їх без
  рішення дизайну не можна.

## 2026-08-24
- **Застосовано міграцію `002-lock-profiles-role.sql` — закрито ескалацію привілеїв через
  `public.profiles.role`.** Виконав власник у SQL Editor; файл міграції —
  `dev/build/002-rls-role-escalation/02-backend/db/002-lock-profiles-role.sql`, блок відкату
  в тому ж файлі. Простій нульовий: клієнт у `profiles` напряму не писав.
  - **Що було:** `anon` і `authenticated` мали `UPDATE` на **всі** колонки `profiles`,
    включно з `role`, а політика `profiles_update` не мала `WITH CHECK` — Postgres підставляв
    туди `USING (id = auth.uid())`, яке лишається істинним і після зміни власної ролі. Один
    `PATCH /rest/v1/profiles` робив будь-якого залогіненого учня адміном, а `is_admin()`
    відкривала йому персональні дані всіх (`profiles`, `contact_messages` з IP, `progress`,
    `certificates`). Дірка була відкрита в проді з моменту заведення таблиці.
  - **Що стало (звірено з живою базою через MCP після застосування):** у
    `information_schema.column_privileges` для `profiles` лишився рівно один рядок —
    `authenticated / full_name / UPDATE`; `INSERT` для `anon`/`authenticated` забраний;
    `profiles_update` має **явний** `with_check = (id = auth.uid())`.
  - **Чому `full_name` свідомо лишили:** це право легальної фічі «зміни своє імʼя» із задачі
    001. Створення профілю при реєстрації не постраждало — `handle_new_user()` виявився
    `SECURITY DEFINER` (перевірено окремо, це знімало ризик Р9 плану 001).
  - **RLS тут ні до чого:** RLS не вміє обмежувати колонки, це робота `GRANT`. Саме тому
    екран політик у Dashboard показував коректну картину, а дірка все одно існувала.
- **Google OAuth піднято з нуля — задача `001-oauth-google`, бекенд-частина інфраструктури.**
  Робив власник руками через браузер разом із кореневою сесією; секрет у репозиторій не
  потрапляв і не потрапить.
  - **Google Cloud:** створено проєкт `ai-academia-506420` (акаунт `andriy.puhalsky@gmail.com`
    активовано в Google Cloud уперше — ToS прийняв власник особисто). Налаштовано
    OAuth consent screen: App name `AI Academia`, user type **External**, support-email і
    контакт — пошта власника. Створено OAuth 2.0 client типу **Web application** з іменем
    `Supabase Auth — ai-academia.com.ua`; єдиний Authorized redirect URI —
    `https://hpcyrnxschpxlrxudmqk.supabase.co/auth/v1/callback`. Authorized JavaScript
    origins свідомо порожні: ми йдемо класичним server-side redirect flow через Supabase,
    а не через Google Identity Services / One Tap, тому origins не потрібні.
  - **Статус публікації — `In production`.** Спочатку застосунок був у `Testing`, де вхід
    доступний лише списку тест-користувачів, а refresh-токени живуть 7 днів. Опубліковано
    через `Publish app`. Верифікація Google не потрібна: запитуються лише базові неконфіденційні
    скоупи (email, profile), логотипа немає, доменів менше десяти.
  - **Supabase → Authentication → Sign In / Providers → Google:** увімкнено, вставлено
    Client ID і Client Secret. Секрет копіювався через буфер обміну безпосередньо з діалогу
    Google у поле Supabase — ніде не зберігався у файлах.
- **Виправлено `Site URL`, який ламав би весь OAuth: було `http://localhost:8000`.** Це
  значення прод-проєкту; будь-який редірект, що не збігся з allow-list, ішов би користувачу
  на його ж локальний порт. Стало `https://ai-academia.com.ua`. Побічно це лагодить і
  листи скидання пароля — шаблон підставляє `{{ .SiteURL }}`, тобто досі слав людей на
  localhost.
- **`Redirect URLs` були порожні — додано чотири:** `https://ai-academia.com.ua/**`,
  `https://ai-academy.andriy-puhalsky.workers.dev/**`,
  `https://dev-ai-academy.andriy-puhalsky.workers.dev/**`, `http://localhost:8000/**`.
  Третій — щоб OAuth можна було чесно тестувати на dev-превʼю; четвертий — для локального
  `python3 -m http.server 8000`. Гілкові превʼю, крім `dev`, свідомо не покривались
  wildcard-ом — з'явиться потреба, додамо явно.
- **Перевірено незалежно від Dashboard, не «на вигляд»:**
  `GET /auth/v1/settings` → `"google": true` (до змін було `false`);
  `GET /auth/v1/authorize?provider=google&redirect_to=<dev-превʼю>` → `302` на
  `accounts.google.com/o/oauth2/v2/auth` з тим самим `client_id`, тобто і провайдер, і
  allow-list редіректів справді робочі.
- SQL-міграцій не застосовувалось, Edge Functions не деплоїлись. Дірка з
  `002-rls-role-escalation` **лишається відкритою** — цей запис її не стосується.

## 2026-08-22
- **`.assetsignore` створено — закрито публічну роздачу службових файлів.** Це виконання
  пункту, запланованого 2026-08-19 і доти не зробленого. Оскільки `wrangler.toml` має
  `[assets] directory = "."`, корінь assets = корінь репозиторію; у `.assetsignore`
  (синтаксис як у `.gitignore`) виключено `dev/`, `tg/`, `deploy-guide.local.html`,
  `.claude/`, `.mcp.json`, `CLAUDE.md`, `wrangler.toml`, `.gitignore`, `.assetsignore`,
  `.DS_Store`. Замість запланованого «`*.md`, `*.sql`» узято точніше `tg/` — воно накриває
  `GOING_LIVE.md`, `contact_messages.sql`, `telegram_index.ts` і сам цей журнал. Свідомо
  НЕ виключено `readme.md` (публічний за призначенням) і `config.json` /
  `architect.config.json` (сайт їх fetch-ить, без них нічого не працює).
- **Перевірено curl-ом до і після, на dev і на проді.** До: `tg/CHANGELOG.md` і `CLAUDE.md`
  віддавали 200 на обох середовищах. Після пушу в `dev` — 404 уже через ~15 с (Cloudflare
  перебудував preview сам, без ручних дій). Після merge `dev` → `main` — те саме на
  `ai-academia.com.ua`. Сайт цілий: `/`, `/architect`, `/verify`, `/modules/module-01.html`,
  `config.json`, `css/custom.css`, `js/auth.js` — усі 200. Побічно підтверджено, що
  `architect.html` → 307 на `/architect` — це штатний clean-URL редірект Cloudflare, не регресія.
- **`dev/design/` заведено в git.** `.gitignore` тепер `dev/*` + `!dev/design/`. Причина —
  власник працює з двох ноутбуків. Репозиторій **публічний**: перемикання в private
  розглядалось і відкладено власником, тому стратегічні `dev/*.local.md`
  (`strategy-analysis`, `ideas`, `python-course-plan`) свідомо лишені поза git — на другому
  ноуті їх не буде. Наслідок, який треба тримати в голові: усе, що потрапляє в
  `dev/design/`, стає публічно читабельним на GitHub.
- Змін у Supabase (SQL-міграції, RPC, секрети) і деплоїв Edge Functions не було.

## 2026-08-19
- **Змін інфраструктури не вносилось** (жодної SQL-міграції, деплою Edge Function чи
  зміни секретів) — сесія проєктна. Нижче лише результати перевірок і зафіксовані плани.
- Перевірено обсяг публікації статики: `wrangler.toml` має `[assets] directory = "."`,
  тому в публічний доступ іде **весь корінь репозиторію**. Підтверджено curl-ом на
  dev-URL: `CLAUDE.md`, `tg/CHANGELOG.md`, `tg/GOING_LIVE.md`, `tg/contact_messages.sql`,
  `tg/telegram_index.ts`, `wrangler.toml` — усі віддають 200. Секретів у них немає
  (вони в Supabase Secrets), але карта інфраструктури читається зовні.
  **Заплановано:** `.assetsignore` у корені (`tg/`, `*.md`, `*.sql`, `dev/`, `.claude/`)
  з перевіркою тим самим curl-ом на dev перед merge у `main`. Ще не зроблено.
- Уточнення до запису 2026-08-18 про RLS: там звірялись політики виду `using (...)`,
  а `USING` керує SELECT/UPDATE/DELETE. Для INSERT працює окрема умова `with check`,
  і вона **ще не звірена** — тобто питання, чи можна писати в `progress`/`certificates`
  прямим REST-запитом з публічним anon-ключем в обхід `submit_quiz`, лишається
  відкритим. **Заплановано:** знімок реальних політик і сигнатур RPC з Dashboard у
  `dev/schema-snapshot.local.md` (приватний, у `.gitignore`); заодно перевірити, чи
  `submit_quiz` рахує «попередній модуль» у межах курсу, а не глобально.
- Спроєктовано третю платформу (курс «Python Basic» з AI-ментором): план у приватному
  `dev/python-course-plan.local.md`, короткий запис у `dev/ideas.local.md`. Стан — ідея,
  не почато. Майбутні інфра-наслідки, коли дійде до реалізації: рядок у `courses` + 8
  рядків у `modules` (коди `p01…p08`), нові таблиці під AI-шар (`learning_profiles`,
  `study_plans`, `homework_reviews`, `exam_items`, `exam_attempts`,
  `capstone_submissions`), нові RPC для серверної перевірки екзамену. Нової Edge
  Function не потрібно — LLM викликається з браузера ключем самого учня.
- Додано таблицю `contact_messages` + RLS-політика `contact_messages: admin read`
  (`using (is_admin())`) — міграція `tg/contact_messages.sql`. INSERT-політики немає навмисно:
  записи вставляє лише Edge Function через service_role-ключ (в обхід Turnstile/honeypot/
  rate-limit було б небезпечно).
- Задеплоєно нову Edge Function "contact" (`tg/contact_index.ts`) — приймає форму
  «Написати нам», з опційною перевіркою Cloudflare Turnstile (пропускається, якщо
  `TURNSTILE_SECRET_KEY` не задано).
- Оновлено Edge Function "telegram" (`tg/telegram_index.ts`) — додано кнопку
  «✉️ Повідомлення» / команду `/messages`: показує останні звернення з таблиці
  `contact_messages`.
- Підключено Cloudflare Turnstile: `TURNSTILE_SECRET_KEY` додано в секрети функції
  "contact", публічний Site key вписано в `config.json` → `contact.turnstileSiteKey`.
  Додано перевірку `data.hostname` у відповіді `siteverify` (без неї публічний site key
  можна було б вставити на чужому сайті й реплеїти токени в обхід капчі).
- Наскрізно перевірено локально (localhost): Turnstile проходить, POST доходить до
  Edge Function, запис падає в `contact_messages`, тестовий рядок після перевірки видалено.

## 2026-08-18
- RLS перевірено вручну через Dashboard → Database → Policies: ключові таблиці
  (`profiles`, `certificates`, `progress`, `payments`, `enrollments`, `quiz_attempts`) мають
  політики виду `using ((id = auth.uid()) OR is_admin())` — безпечно, попри те, що в списку
  роль показана як "public".
- `TELEGRAM_SECRET_TOKEN` задеплоєно й активовано: `setWebhook` викликано з `secret_token`,
  `getWebhookInfo` підтвердив коректний URL без помилок.
