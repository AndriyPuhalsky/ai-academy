# 009 — План проходження конвеєра

Хроніка запуску + інструкція, як довести задачу до кінця, якщо сесія обірветься.

---

## Що вже зроблено (2026-09-07, сесія запуску)

1. **Перевірка Figma до старту агентів** (вимога власника, Р14). Результат — `00-review.md`,
   розділ «Figma: що перевірено фактично». Малювати можна; єдине обмеження — один режим
   змінних. **Файл створено, новий не створювати:**
   `https://www.figma.com/design/FktfudHg9ZuZ7bYJufGIk6` (`file_key` `FktfudHg9ZuZ7bYJufGIk6`).
2. **Папку задачі заведено:** `00-decisions.md` (15 рішень), `00-review.md` (перевірені
   числа), `task.md` (бриф, 12 полів), цей файл.
3. **Конвеєр приведено в робочий стан:**
   - памʼять агентів консолідована в `dev/design/.claude/agent-memory/`
     (аналітик 8 · білдер 10 · валідатор 9 · хантер 7; оригінали в підпапках задач лишились);
   - скіл-привид `web-design-guidelines` прибраний із усіх чотирьох;
   - `mcp__claude_ai_Figma` доданий білдеру й валідатору; білдеру доданий живий браузер;
   - `effort: xhigh` у всіх чотирьох.
4. **README** (рядок 009), **JOURNAL** (009 в «Активне», 005 стиснуто в «Зроблено»),
   **CLAUDE.md** майстерні (розділ «Figma: зміна рішення», три платформи, скіл-привид).
5. **Агент №1 запущено.**

---

## Порядок агентів (Р11 — жодних воріт до варіантів)

| # | Агент | Вихід | Особливе для 009 |
| - | ----- | ----- | ---------------- |
| 1 | `aia-brief-analyst` | `01-spec.md` | питання закриває **дефолтами** й позначає окремим розділом · механізм reduced-motion **без поіменного списку** · схема шарів і таблиця перейменувань для колізій · інвентар компонентів з реального коду |
| 2 | `aia-reference-hunter` | `02-references/` | шрифти — **з рендер-тестом кирилиці й дослівною ліцензією** · темний «важкий люкс» · **не збирати вдруге** те, що вже розібрано в 001/003/005 |
| 3 | `aia-design-builder` | `03-build/` (A–E) | **Figma спершу**, HTML — дзеркало · працює у вже створеному файлі · `NOTES.md` з URL і node-id |
| 4 | `aia-design-validator` | `04-variants/` | вісь «характер мови» (типопара + палітра) · паритет Figma ↔ `tokens.css` · перевірки у **видимому** вікні · тест «стан після `render()`» |

Орієнтир за 005: 20–60 хв на агента, конвеєр — робочий день.
Після кожного агента — запис у `JOURNAL.md`.

---

## Якщо сесія обірвалась

1. `pwd` — має бути `/Users/ander1.sage/Downloads/AIA/dev/design`. **Це критично:** памʼять
   агентів шукається відносно cwd сесії, з `AIA/dev` вони побачать іншу, неповну.
2. `git status` і гілка (правило кореневого `CLAUDE.md`). Гілка `dev`, коміти робить
   **коренева сесія**, не ця.
3. Подивитись, які файли вже є в `009-design-system/`, і запустити наступного агента з
   таблиці вище. Перезапускати того, чий вихід уже лежить на диску, не треба.

## Показ власнику

`python3 -m http.server 8000` з `04-variants/` → `compare.html`; посилання на Figma — з
`03-build/NOTES.md`. Artifact — **лише за окремою згодою власника** (правило №3 пісочниці).
Власник обирає варіант → статус `дизайн готовий`, далі `SUMMARY.md` як вхід для
`dev/build/009-design-system/`.

## Межі, які не порушувати

- Пісочниця: пишемо лише в `dev/design/`. Код сайту — **тільки читати**.
- Жодних git-команд із цієї сесії.
- Репозиторій публічний: ні ключів, ні даних учнів, ні фінансів.
- Обсяг v1 **без шаблонів сторінок** — усе, що тягне за собою сторінку, позначати «→ 010».
