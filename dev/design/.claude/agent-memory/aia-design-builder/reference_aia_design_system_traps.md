---
name: aia-design-system-traps
description: Пастки дизайн-системи AIA, які видно тільки в коді — reduced-motion за поіменним списком, специфічність .prose-aia pre, ButtonFace, list-style
metadata:
  type: reference
---

Чотири речі в `css/custom.css`, на яких легко зламати макет мовчки. Перевірені 2026-09-03.

1. **`@media (prefers-reduced-motion: reduce)` працює за ПОІМЕННИМ списком токенів**
   (`css/custom.css:1034+`). Новий токен тривалості туди сам не потрапляє й лишається
   живим. Це вже було критерієм приймання 003 і стало Б-13 у 005. Виняток, який роблю
   свідомо: токен-швидкість, на який ділять (`--speed-term-cps`) — нуль дав би `Infinity`,
   тож його вимикає гілка в JS.

2. **`.prose-aia pre` має специфічність (0,1,1) і б'є будь-який одинарний клас (0,1,0)**
   всередині сторінок уроків: накидає другу рамку, другий фон і підміняє `white-space: pre`
   на `pre-wrap`, від чого моноширинні колонки розсипаються. Розв'язання — оголошувати
   полотно як `.term > .term__body` (0,2,0) з ЯВНИМ скиданням семи властивостей:
   margin, border, background, border-radius, padding, white-space, word-break.

3. **`<button>` без явного `background` бере світлий ButtonFace браузера** — на темній
   темі текст ivory зникає на світлому. У `<a>` тієї ж проблеми немає, тому баг ловиться
   не одразу: та сама кнопка виглядає правильно, поки вона посилання.

4. **`.prose-aia li::marker { color: #D97757 }` (css/custom.css:191), схоже, мертве:**
   Tailwind preflight ставить `list-style: none`, а `.prose-aia ul` повертає лише
   `padding-left`. Маркерів у списках уроків немає. Не моя зона — фіксую для сесії
   реалізації.

5. **Медіа-запит НЕ додає специфічності.** `@media (max-width: 640px) { .x { min-width: 8ch } }`,
   що стоїть у файлі **вище** за звичайне `.x { min-width: 0 }`, програє йому — і правило
   мовчки не діє. Спіймано в 009 на `.ds-hdr__brand`: бренд у мобільній шапці стискався до
   нуля. Правило: блок мобільних перевизначень компонента ставити **після** всіх його
   базових оголошень, а не поруч із базовим правилом блока.

6. **`minmax(<фікс>, 1fr)` у `repeat(auto-fill, …)` не стискається нижче фікса.**
   `minmax(var(--w-narrow), 1fr)` при 672px дає +376px горизонтального скролу на 320.
   Завжди `minmax(min(<фікс>, 100%), 1fr)`. Те саме для будь-якої фіксованої `width`
   усередині контейнера з паддінгом (у 009 так розпирав `.ds-paper__rule` 280px —
   репліка хайрлайна з PDF сертифіката).

7. **Чотири пастки, доміряні на 010 (2026-09-07), усі в системі 009:**
   `--scrub` не зареєстрований через `@property`, тому `getComputedStyle` дає
   рядок `calc(1 * 0.8)` і JS тихо бере fallback — жива П-01 ·
   `--c-paper-rule` як колір тексту дає **1.56:1** на папері (це колір
   хайрлайна; текстових кольорів на папері рівно два) ·
   `.ds-tbl thead th { position: sticky }` не спрацьовує **жодного разу** на
   182 таблицях, бо скролпорт — сама обгортка з `overflow-x: auto` ·
   `.ds-prose li::marker { color }` без повернення `list-style` — мертвий код,
   preflight Tailwind ставить `none`.

Плюс правило проєкту, яке з цього випливає: **одна задача — один префікс класів**
(`aia-` у 001, `rm-` у 003, `term-` + `cc-` у 005), і компонент та сторінка — два окремі
CSS-файли, префікси не змішуються.

Пов'язане: [[aia-005-terminal-build]]

---

## Дефекти самої системи 009, знайдені вимірюванням у 010 (Б1, 2026-09-07)

Усі чотири невидимі в CSS і на скріншоті; кожен ловиться тільки заміром.

7. **`.ds-hdr > .ds-nav { display:none }` — ДОЧІРНІЙ селектор.** Щойно в шапці
   зʼявляється будь-яка обгортка (напр. `.ds-hdr__inner` для центрування по
   `--w-content`), правило мовчки перестає діяти: десктопна навігація лишається на
   мобілці й дає **+273 px горизонтального скролу на 320**. Помилка — в одному символі.

8. **Закрите `.ds-nav__menu` має лише `opacity:0` + `pointer-events:none`** — три його
   пункти лишаються **фокусованими** й невидимими. Мишею не ловиться взагалі; видно
   тільки програмним обходом порядку табуляції.

9. **`visibility` у `transition-property` ЛАМАЄ клавіатуру.** Наївний фікс п.8
   (`transition-property: opacity, transform, visibility`) робить меню таким, що воно
   не відкривається з клавіатури взагалі: `visibility` інтерполюється ступінчасто, тож
   у момент `list[0].focus()` елемент іще hidden, а `.focus()` на невидимому мовчки
   нічого не робить. Правильна форма: `visibility 0s linear var(--dur-state)` у базі
   + `transition-delay: 0s` у відкритому стані.

10. **`AIA.motion.play.typing()` рахує тривалість як `увесь_текст / cps`,
    а `.term__clip` робить ГОРИЗОНТАЛЬНУ ШТОРУ через увесь блок**, не друк рядок за
    рядком. Для однорядкового термінала збігається; для сесії на 450 символів дає
    **14 секунд** шторки. Правильний дільник — довжина НАЙДОВШОГО рядка.

Плюс: у базі **жодне з 10 правил `:hover` не захищене `@media (hover: hover)`**, а
Tailwind CDN v3 сам цього не робить — на тач-екрані hover «залипає» після тапу.
Дістати до сотень утиліт `hover:*` можна одним рядком:
`tailwind.config = { future: { hoverOnlyWhenSupported: true }, … }`.
