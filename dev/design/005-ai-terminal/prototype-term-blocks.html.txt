<div class="term">
            <div class="term-h">demo-repo — claude</div>
            <pre class="term-b"><span class="c">$</span> <span class="u">cd ~/demo-repo &amp;&amp; claude</span>

<span class="u">&gt; додай у README розділ «Як запустити локально» з трьох кроків,
  нічого іншого не змінюй</span>

<span class="t">⏺ Read</span>(README.md)
  <span class="c">└─ 48 рядків</span>
<span class="t">⏺ Read</span>(package.json)
  <span class="c">└─ знайдено скрипти: dev, build, test</span>
<span class="t">⏺ Update</span>(README.md)
  <span class="c">└─ додано 9 рядків</span>

<span class="c">  ── README.md ─────────────────────────────</span>
  <span class="g">+ ## Як запустити локально</span>
  <span class="g">+</span>
  <span class="g">+ 1. `npm install`</span>
  <span class="g">+ 2. `npm run dev`</span>
  <span class="g">+ 3. Відкрити http://localhost:5173</span>
  <span class="c">──────────────────────────────────────────</span>

  <span class="u">Дозволити зміну README.md?</span>
  <span class="c">❯ 1. Так</span>
  <span class="c">  2. Так, і не питати про цей файл</span>
  <span class="c">  3. Ні, і скажу чому</span></pre>
          </div>

<!-- ───────────────────────────────── -->

<div class="term">
            <div class="term-h">demo-repo — claude</div>
            <pre class="term-b"><span class="u">&gt; причеши тут усе</span>

<span class="t">⏺ Search</span>(**/*.js)
  <span class="c">└─ 34 файли</span>
<span class="t">⏺ Update</span>(src/api.js)
  <span class="c">└─ 62 рядки змінено</span>
<span class="t">⏺ Update</span>(src/utils.js)
  <span class="c">└─ 15 рядків змінено</span>
<span class="t">⏺ Bash</span>(npm test)
  <span class="r">└─ 3 тести впали</span>

<span class="c">  Виправлю тести під новий код…</span></pre>
          </div>

<!-- ───────────────────────────────── -->

<div class="term">
            <div class="term-h">та сама задача, сформульована як задача</div>
            <pre class="term-b"><span class="u">&gt; у файлі src/api.js прибери дублювання в обробці помилок.
  Тести не змінюй. Спершу покажи план, потім роби.</span></pre>
          </div>

<!-- ───────────────────────────────── -->

<div class="term">
            <div class="term-h">macOS · Linux · WSL</div>
            <pre class="term-b"><span class="c">$</span> <span class="u">curl -fsSL https://claude.ai/install.sh | bash</span></pre>
          </div>

<!-- ───────────────────────────────── -->

<div class="term">
            <div class="term-h">Windows · PowerShell</div>
            <pre class="term-b"><span class="c">PS C:\&gt;</span> <span class="u">irm https://claude.ai/install.ps1 | iex</span></pre>
          </div>

<!-- ───────────────────────────────── -->

<div class="term">
            <div class="term-h">Windows · CMD</div>
            <pre class="term-b"><span class="c">C:\&gt;</span> <span class="u">curl -fsSL https://claude.ai/install.cmd -o install.cmd &amp;&amp; install.cmd &amp;&amp; del install.cmd</span></pre>
          </div>

<!-- ───────────────────────────────── -->

<div class="term">
            <div class="term-h">перевірка встановлення</div>
            <pre class="term-b"><span class="c">$</span> <span class="u">claude --version</span>
<span class="c">$</span> <span class="u">claude doctor</span>
<span class="c">$</span> <span class="u">claude auth status</span></pre>
          </div>

<!-- ───────────────────────────────── -->

<div class="term">
            <div class="term-h">правильно й неправильно</div>
            <pre class="term-b"><span class="c"># правильно: спершу зайшли в проєкт</span>
<span class="c">$</span> <span class="u">cd ~/ai-terminal/my-project</span>
<span class="c">$</span> <span class="u">claude</span>

<span class="c"># неправильно: запустились у домашній папці</span>
<span class="c">$</span> <span class="u">cd ~</span>
<span class="c">$</span> <span class="u">claude</span>
<span class="c">  → агент не бачить проєкту й почне «шукати» по всьому диску</span></pre>
          </div>

<!-- ───────────────────────────────── -->

<div class="term">
            <div class="term-h">.claude/settings.json — фрагмент</div>
            <pre class="term-b"><span class="c">{</span>
  <span class="c">"permissions": {</span>
    <span class="c">"allow": [</span> <span class="g">"Bash(npm run test)"</span><span class="c">,</span> <span class="g">"Bash(git log *)"</span> <span class="c">],</span>
    <span class="c">"ask":   [</span> <span class="u">"Bash(git push *)"</span> <span class="c">],</span>
    <span class="c">"deny":  [</span> <span class="r">"Bash(rm -rf *)"</span><span class="c">,</span> <span class="r">"Read(./.env)"</span> <span class="c">]</span>
  <span class="c">}</span>
<span class="c">}</span></pre>
          </div>

<!-- ───────────────────────────────── -->

<div class="term">
            <div class="term-h">.claude/rules/api.md</div>
            <pre class="term-b"><span class="c">---</span>
<span class="u">paths:</span>
  <span class="u">- "src/api/**/*.ts"</span>
<span class="c">---</span>

<span class="g"># Правила для API</span>

<span class="g">- Кожен обробник перевіряє вхідні дані</span>
<span class="g">- Формат помилки — стандартний, з `code` і `message`</span></pre>
          </div>

<!-- ───────────────────────────────── -->

<div class="term">
            <div class="term-h">.claude/skills/pered-komitom/SKILL.md</div>
            <pre class="term-b"><span class="c">---</span>
<span class="u">name: pered-komitom</span>
<span class="u">description: Перевірка перед комітом: тести, лінтер, диф, повідомлення</span>
<span class="c">---</span>

<span class="g"># Перед комітом</span>

<span class="g">1. Запусти `npm test` і покажи результат.</span>
<span class="g">2. Запусти лінтер; якщо є помилки — виправ і покажи, що саме.</span>
<span class="g">3. Покажи `git diff --stat` і перелічи, що змінилось, одним рядком на файл.</span>
<span class="g">4. Перевір, чи не потрапили в диф `.env`, ключі, паролі, службові файли.</span>
<span class="g">5. Запропонуй повідомлення коміту українською, до 72 символів.</span>
<span class="g">6. Нічого не комітуй сам — покажи команду й чекай мого «так».</span></pre>
          </div>

<!-- ───────────────────────────────── -->

<div class="term">
            <div class="term-h">.claude/agents/reviewer.md</div>
            <pre class="term-b"><span class="c">---</span>
<span class="u">name: reviewer</span>
<span class="u">description: Рев'ю змін на помилки й безпеку. Нічого не змінює.</span>
<span class="u">tools: Read, Grep, Glob</span>
<span class="u">model: sonnet</span>
<span class="c">---</span>

<span class="g">Ти рев'ювер. Прочитай зміни й дай конкретні зауваження:</span>
<span class="g">помилки коректності, небезпечні місця, порушення домовленостей</span>
<span class="g">проєкту. На кожне зауваження — файл і рядок. Не пропонуй</span>
<span class="g">переписати все; пропонуй мінімальний фікс.</span>
<span class="g">Нічого не редагуй: у тебе немає таких інструментів.</span></pre>
          </div>

<!-- ───────────────────────────────── -->

<div class="term">
            <div class="term-h">.claude/hooks/no-rm.sh</div>
            <pre class="term-b"><span class="c">#!/bin/bash</span>
<span class="u">input=$(cat)</span>
<span class="u">cmd=$(echo "$input" | jq -r '.tool_input.command')</span>

<span class="u">if [[ "$cmd" == *"rm -rf"* ]]; then</span>
  <span class="r">echo "Заборонено правилом проєкту: rm -rf" &gt;&amp;2</span>
  <span class="r">exit 2</span>
<span class="u">fi</span>
<span class="g">exit 0</span></pre>
          </div>

<!-- ───────────────────────────────── -->

<div class="term">
            <div class="term-h">.claude/settings.json</div>
            <pre class="term-b"><span class="c">{</span>
  <span class="c">"hooks": {</span>
    <span class="c">"PreToolUse": [{</span>
      <span class="u">"matcher": "Bash"</span><span class="c">,</span>
      <span class="c">"hooks": [{</span> <span class="u">"type": "command"</span><span class="c">,</span>
                  <span class="u">"command": "${CLAUDE_PROJECT_DIR}/.claude/hooks/no-rm.sh"</span> <span class="c">}]</span>
    <span class="c">}]</span>
  <span class="c">}</span>
<span class="c">}</span></pre>
          </div>

<!-- ───────────────────────────────── -->

<div class="term">
            <div class="term-h">основні команди</div>
            <pre class="term-b"><span class="c"># віддалений сервер (HTTP)</span>
<span class="c">$</span> <span class="u">claude mcp add --transport http назва https://приклад/mcp</span>

<span class="c"># локальний процес (stdio) — зверни увагу на «--»</span>
<span class="c">$</span> <span class="u">claude mcp add --transport stdio назва -- npx -y пакет-сервера</span>

<span class="c"># подивитись і перевірити</span>
<span class="c">$</span> <span class="u">claude mcp list</span>
<span class="c">$</span> <span class="u">claude mcp get назва</span>
<span class="c">$</span> <span class="u">claude mcp remove назва</span>

<span class="c"># вхід, якщо сервер вимагає авторизації</span>
<span class="c">$</span> <span class="u">claude mcp login назва</span></pre>
          </div>

<!-- ───────────────────────────────── -->

<div class="term">
            <div class="term-h">.mcp.json — вигляд</div>
            <pre class="term-b"><span class="c">{</span>
  <span class="c">"mcpServers": {</span>
    <span class="c">"приклад": {</span>
      <span class="u">"type": "http"</span><span class="c">,</span>
      <span class="u">"url": "https://приклад/mcp"</span><span class="c">,</span>
      <span class="c">"headers": {</span> <span class="u">"Authorization": "Bearer ${API_KEY}"</span> <span class="c">}</span>
    <span class="c">}</span>
  <span class="c">}</span>
<span class="c">}</span></pre>
          </div>

<!-- ───────────────────────────────── -->

<div class="term">
            <div class="term-h">чотири приклади, які варто набрати живцем</div>
            <pre class="term-b"><span class="c"># відповів і вийшов</span>
<span class="c">$</span> <span class="u">claude -p "перелічи файли, змінені в останньому коміті"</span>

<span class="c"># з конвеєра: аналіз логу</span>
<span class="c">$</span> <span class="u">tail -200 app.log | claude -p "знайди аномалії, коротко"</span>

<span class="c"># машинний вивід — щоб обробити скриптом</span>
<span class="c">$</span> <span class="u">claude -p "перелічи TODO у проєкті" --output-format json</span>

<span class="c"># перевірка змінених файлів</span>
<span class="c">$</span> <span class="u">git diff main --name-only | claude -p "перевір ці файли на проблеми безпеки"</span></pre>
          </div>

<!-- ───────────────────────────────── -->

<div class="term">
            <div class="term-h">типовий запуск у CI</div>
            <pre class="term-b"><span class="c">$</span> <span class="u">claude -p "прогони тести й скажи, що впало"</span> <span class="g">\</span>
    <span class="u">--permission-mode dontAsk</span> <span class="g">\</span>
    <span class="u">--allowedTools "Bash(npm test)" "Read"</span> <span class="g">\</span>
    <span class="u">--max-turns 10</span></pre>
          </div>