# 011 · Звіт кореневої сесії 2026-09-10 — доопрацювання реєстру хвостів

Сесія працювала за вказівкою власника: «спочатку роби всі свої задачі (рядки «Я»), потім
напишеш», з двома правилами зверху — **не погіршувати наявний UI/UX і функціонал** і
**пропонувати зміни з урахуванням наслідків**. Усе нижче — на гілці `dev`, без мержу.

Метод: власний Chrome по CDP (рецепт QA 010) + локальний `http.server`. Кожна правка має
замір «до» (незмінна копія `HEAD` у другому сервері, порт 8312) і «після» (робоче дерево,
порт 8311). Стенд перенесено в `stand/` цієї папки, щоб наступна сесія могла повторити.

## Стан рядків реєстру

| # | Рядок | Стан | Доказ / примітка |
| - | ----- | ---- | ---------------- |
| 1 | Підказка про горизонтальний скрол | **у коді, візуально перевірити** | `css/components.css` §41 + `js/ui.js` (initScrollFades). Патерн: край із вмістом ГАСНЕ (mask на скролері; рамка, кільце фокуса й смуга прокрутки — поза маскою через тришарову маску). Стани `data-scroll-fade="right/left/both"` на всіх шести скролерах; замір на 5 сторінках: 0 розбіжностей стану з геометрією. Знімки — `stand/shots/t8-*`. ⚠ Слабке місце: там, де вміст обрізаної колонки не дотягується до краю (таблиці `min-w-[54rem]` у `ref-hooks`), гасіння не видно. Пропозиція (не зроблена): додати ЗАВЖДИ видиму тонку смугу прокрутки через `::-webkit-scrollbar` з токенами — див. «Рішення власника» |
| 4 | Порожній рендер Mermaid | **зроблено, перевірено** | `mermaid.render(id)` з власним лічильником `aia-mmd-N`, перевірка «є `<svg>`, не error» ПІСЛЯ рендеру, джерело `<pre>` не чіпається до успіху; тимчасовий контейнер Mermaid після відмови прибирається (`sweep`). 300/300 діаграм на 3 ширинах, 0 помилок консолі, 0 сміття в `<body>`. Проба з навмисно зламаною діаграмою — `probe-broken-diagram.html`: фолбек лише на зламаній, сусідні цілі |
| 5 | Текст фолбека | **зроблено** | «…нижче її текстовий запис (назви кроків і звʼязки між ними)» — і це тепер правда, бо джерело лишається |
| 6 | Дві діаграми не центруються | **зроблено, перевірено** | `<pre>` розширений на падінги, коли контейнер не прокручується; 0 нецентрованих (було 3: ще й `architect-20#0` @768) |
| 7 | Шторка мобільного меню без замка | **зроблено, перевірити ще раз** | Знайдено гірше: панель у потоці на ПОЧАТКУ документа, тож відкрита на прокрученій сторінці зʼявлялась на 1181 px вище екрана — тап нічого не показував (усі три лендінги). Тепер: `position: sticky` під шапкою + стеля висоти + замок скролу спільним лічильником + детермінована компенсація прокрутки (Chrome-якоріння на `index.html` не відкочувало на закритті — стрибок 402 px). У спокої (scrollY = 0) вигляд і потік ті самі. ⚠ Остання версія `set()` з компенсацією написана ПІСЛЯ заміру t5 — перезняти `stand/t5-verify.mjs` (секція 6) |
| 8 | CLS футера на уроках (гейт) | **у коді, замір повторити** | Причина: замок ставився після відповіді Supabase (~1,4 с) → футер стрибав у вʼюпорт (0,19). Тепер для гостя без ключа сесії в сховищі замок ставиться СИНХРОННО при розборі сторінки (номер модуля з коду в `data-module`), а мапа модулів і сесія в `auth.js` тягнуться паралельно. ⚠ Замір `stand/t10-gate.mjs` дав старі числа, бо стенд перед тим сам записав в origin фальшивий ключ `sb-probe-auth-token` (пастка стенда); скрипт виправлено (removeItem), перезняти. Очікування: замок ≤ FCP, CLS ≈ 0 |
| 9, 10a | Резерв решти hero Термінала | **зроблено, перевірено (один хвіст)** | Контейнерні запити на текстовій колонці hero; пороги зі 115 ширин (`measure-hero.md`). Перевірка з заблокованим конфігом (`stand/t9.out.json`): резерв = фактичній висоті на **114 зі 115 ширин**, розбіжність на 1116 px (пояснення +22,8) виправлена порогом 236. CLS після: 320/390/600 ≈ 0; 1024–1920 — 0,015–0,042 (джерело: порожня шапка термінала hero, +8,8 px — додано `.term__title:empty { min-height: 1lh }`, **перезняти**); **768 — 0,119, і це вже не резерв, а підміна шрифту** заголовка (Prata приходить пізніше за Times, рядків 3 → 2) — платформне, лікується `size-adjust` для запасних шрифтів, окреме рішення |
| 10 | `roadmap.html` без `data-page-in` | **рекомендую не робити** | Роадмап має власний вхід (003: рядки заголовка через маску, лід, лічильники). M1 на `<main>` (0→1 за 520 мс) наклався б на нього й притемнив перші пів секунди хореографії; заміри геометрії змійки відносні, але ScrollTrigger рахує від документа під час transform. Ризик > користь |
| 10b | Корінь схеми обрізається зліва | **зроблено, перевірено** | ціль `showRoot` не заходить правіше за лівий край кореня; 0 обрізаних (було 6 на 390) |
| 13a | `nowrap` першої колонки | **зроблено консервативно, дозняти 1280** | Спершу спробовано системне правило (клітинка переноситься, `<code>` — ні): 7 зламаних полагоджено, але чесний підрахунок рядків через Range показав, що у **80 зі 179 таблиць** підписи першої колонки, які стояли в один рядок, переносяться на 2–5 (авто-розкладка віддає ширину прозовим колонкам) — регресія вигляду там, де не боліло. **Відкинуто.** Лишено `nowrap` усталеним, доданий модифікатор `.ds-tbl--wrap` (перша колонка переноситься), поставлений руками на **25 таблиць** за критерієм «друга колонка ≤ 34 % при першій ≥ 45 % на 1280» (`stand/wrap-set.json`): 7 повністю зламаних + 18 помірних. Решта 154 — піксель у піксель як у проді. ⚠ Замір «після» для цього варіанта не знімався — `t3b-lines.mjs` на 1280 |
| 14 | `/roadmap?from=claude-code` | **зроблено, перевірено** | таблиця трьох курсів у `js/roadmap-render.js`, `backTerminal` у `roadmap.json`; невідомий `?from=` → Академія. ⚠ Контент роадмапа й досі каже «дві платформи» (`copy.eyebrow`, `lead`, `pageTitle`) — рішення власника |
| 15 | 28 Tailwind-утиліт із JS | **закрито ще 010** | grep по `js/*.js`: з JS у DOM приходить лише `sr-only`, і він є у статичній розмітці кожної сторінки |
| 17 | `term__meta` без `aria-hidden` | **закрито без коду** | Контракт уроку (`005-ai-terminal/01-authoring/lesson-contract.md`) вимагає `aria-hidden` лише на маркерах `⏺ └─ ❯`, а `.term__meta` («тьмяне: `$`, службові підписи») лишає читабельним — 22 уроки відповідають контракту; «розбіжність» з 008 не підтверджена |
| 18 | `/certificate` двічі · імʼя · бренд | **два з трьох закриті 006** | `loadedFor` у `js/certificate.js`; самолікування імені — `js/auth.js:250`. Бренд сторінки сертифіката — рішення власника |
| 2, 3, 11, 12, 13 | рішення власника | без змін | — |
| 16 | довідники: факти без джерела | **не починав** | агентська хвиля (`aia-content-author`), потрібен апрув; запускати з сесії в `dev/build/` |
| 19–22 | недоступне агентам | без змін | — |

## Знахідки поза реєстром (не правив, для власника)

- **`aia:config-ready` на уроках чекає завантаження `mermaid.min.js` з CDN** (класичний `<script>` у
  кінці body блокує `DOMContentLoaded`): на холодному кеші сайдбар, навігація й замок для
  залогіненого зʼявляються на ~1,2–1,5 с пізніше, ніж могли б. Лікується `async`-завантаженням
  Mermaid із самого `mermaid-init.js` (57 файлів + init) — окреме рішення.
- Гість на заблокованому уроці тепер одразу бачить замок, а не урок на 1,5–4 с — це
  зачіпає й Googlebot (він гість). Раніше він теж отримував замок після гідратації, тож
  індексації це не змінює, але варто знати.
- Кеш Chrome у стенді віддає старі `css/js` (та сама пастка №1 з 010): у `stand/cdp.mjs`
  кеш тепер вимкнено завжди. Фальшивий ключ сесії, який стенд кладе в `localStorage`, щоб
  обійти замок, ЛИШАЄТЬСЯ в origin — гостьові заміри після нього хибні (t10 це спіймало).

## Рішення власника, які потрібні

1. **Рядок 1 — доповнити гасіння видимою тонкою смугою прокрутки** (`::-webkit-scrollbar`
   6 px, thumb `--c-line-strong`, track прозорий) на шести скролерах? Плюс: видно і там,
   де край порожній; мінус: постійний елемент під кожним широким блоком. Рекомендую **так**.
2. **Рядок 10** — лишити роадмап без `data-page-in` (рекомендую) чи додати.
3. **Рядок 14** — оновити копію роадмапа під три платформи (`roadmap.json`, `<title>`).
4. **Рядок 18** — бренд сторінки `/certificate` для всіх курсів.
5. Версії й `sitemap.xml`: зміни зачіпають усі три платформи (CSS/JS спільні) —
   патч-підняття `0.5.1 / 1.4.1 / 1.2.1` при релізі.

## Дозняток 2026-09-10 (друга сесія): чотири заміри, які лишались

База — `git archive 39a70a3` (коміт до правок 011) на порту 8312, «після» — робоче дерево на
8311, Chrome 152 по CDP, кеш вимкнено, `sb-probe-auth-token` прибирається перед кожним
гостьовим замiром.

| Замір | Результат | Файл |
| ----- | --------- | ---- |
| **t10 · замок гостя** (рядок 8) | **390 px: CLS 0,18–0,22 → 0,003**, замок стає **до FCP** (654 мс проти FCP 668; на холодному кеші 3095 проти 2980). Перші модулі трьох курсів — замка немає, CLS 0. «Схожий на залогіненого» (ключ у сховищі, Supabase заблокований) і OAuth-повернення (`?code=`) — замок синхронно **не** ставиться, як і задумано. **⚠ Знахідка на десктопі:** 1280 px `architect-05` **0,006 → 0,082**, `claude-code-08` 0,196 → 0,044. Причина не в замку: на ≥ 1024 сайдбар стоїть у потоці й заповнюється на `aia:config-ready` (з 0 до 836 px), а головна колонка під замком заввишки лише 176 — обгортка росте, футер їде з 336 на 900. Раніше під ним стояв увесь урок, і той самий ріст сайдбара нічого видимого не рухав. 0,082 — у зоні «добре» (< 0,1), але це новий зсув | `stand/t10.out.json` |
| **t9 · резерв hero** (рядки 9, 10a) | Резерв = фактична висота на **115 зі 115 ширин** (розбіжність на 1116 px закрита порогом 236). CLS лендінга на 9 ширинах: **320/390/600 — 0,0002; 768 — 0,008–0,009; 1024–1920 — 0,002–0,008** (було 0,119 на 768 і 0,015–0,042 на десктопі). Залишок — кнопки шапки й hero, що дістають текст з конфіга (`ds-nav__group`, `#heroReplay`, `#ctaMap`), ~0,005 | `stand/t9.out.json` |
| **t5 §6 · меню лендінга** (рядок 7) | На прокрученій сторінці (scrollY 900) панель відкривається **під шапкою** (top 64), видима на всіх трьох лендінгах. Колесо миші під замком (справжня `Input.dispatchMouseEvent`, не `scrollBy`) — **scrollY не змінюється**. Закриття повертає **рівно на 900** на всіх трьох (до компенсації було 1602 / 1200). У спокої (scrollY 0) панель у потоці штовхає hero вниз на свою висоту, як і в проді. 640×360: панель обмежена 312 px і прокручується всередині | `stand/t5-menu.out.json` (виділена секція 6, `t5-menu.mjs`) |
| **t3b · перша колонка таблиць** (рядок 13a) | 1280 px, 179 таблиць: рядки першої клітинки змінились у **23**, і всі 23 — зі списку `wrap-set.json` (2 з 25 не потребували переносу). **Поза списком — 0 змін** ширин, висот і рядків, тобто 154 таблиці піксель у піксель як у проді. Горизонтальний скрол **зник у 7 повністю зламаних** (друга колонка 35–38 px → 113–456), **зʼявився — 0**. Лишились 7 скролерів `ref-hooks` з навмисним `min-w-[54rem]` — це не дефект. Контроль на інших ширинах: **768** — ті самі 23 таблиці, скрол зник у 7, зʼявився 0; **390** — 0 змін (34 скролери як були) | `stand/after-lines-1280.json`, `after-lines-390-768.json`, `diff-lines.py` |

⚠ Попередній замір `lockedScrollAttempt` у `t5.out.json` (y 1546) нічого не доводив: програмний
`scrollBy` прокручує й під `overflow: hidden`. Правильний доказ — подія колеса через CDP.

**Шосте рішення власника** (додається до пʼяти вище): десктопний зсув 0,08 на заблокованому
уроці для гостя — прийняти (< 0,1, бачить лише гість, що прийшов на середину курсу за прямим
посиланням) чи закрити резервом висоти сайдбара на ≥ 1024 поки стоїть замок (для двох довгих
курсів сайдбар завжди впирається в `max-height: calc(100vh - var(--s-16))`, тож резерв
точний; для AI Академії з 12 модулями треба заміряти). Рекомендую **прийняти зараз, резерв —
у наступну хвилю** разом із рядком 1.

## Як продовжити (наступна сесія)

1. `cd dev/build && claude` — конвеєр і памʼять агентів працюють лише звідти.
2. Стенд: `python3 -m http.server 8311` з кореня; Chrome за рецептом
   `dev/build/.claude/agent-memory/aia-build-qa/method_cdp_own_chrome.md` (порт 9333);
   скрипти — `dev/build/011-open-tails/stand/*.mjs` (`S=<тека для знімків> node t5-verify.mjs`).
3. ~~Дозняти t10 / t9 / t5 §6 / t3b~~ — **знято 2026-09-10, див. «Дозняток»**. Лишилось
   `t8-fade.mjs` — очима по знімках `stand/shots/t8-*` (рішення 1).
4. Далі — шість рішень власника → хвиля фіксів (рядки 1, 16, шосте рішення) → QA-хвиля на
   dev-превʼю (апрув власника) → реліз із патч-версіями.
