# 011 — Реєстр відкладених хвостів

## Блок 1 — заповнює власник

- **Статус:** у коді (на `dev`, без мержу) — стан по рядках і що дозняти: `report-2026-09-10.md`
- **Заведено:** 2026-09-08
- **Платформа:** обидві (точніше — усі три)
- **Дизайн:** потрібен частково — див. позначку «Д» у реєстрі нижче

### ТЗ

Рішення власника 2026-09-08: після мержу 010 звести в **один реєстр** усе, що свідомо
відкладалось під час 005–010 і живе зараз лише у звітах. Причина: після закриття 010
індекс задач стає повністю зеленим, і без цього файла відкладене просто зникне з поля
зору — так само, як це вже було з реєстром П-1…П-6 у 008, доки його не виписали окремо.

Це **реєстр, а не задача з одним рішенням**: кожен рядок закривається самостійно, у своєму
темпі, а частина рядків — це рішення власника, які кодом не закриваються взагалі.

### Чого точно НЕ робити

- Не братися за все одразу: реєстр існує, щоб можна було брати по одному рядку.
- Не переоткривати рішення, вже ухвалені у 006–010 (маркер-форма квізів, `p_score: 100`,
  датовані екрани, ширина лендінга Термінала, PM для 010).

---

## Реєстр

Позначки: **Д** — потрібен дизайн · **В** — рішення власника, кодом не закривається ·
**А** — робота агента · **Я** — коренева сесія.

### З 010 — дизайн-система

| # | Що | Звідки | Тип |
| - | -- | ------ | --- |
| 1 | **Підказка про горизонтальний скрол.** Прокручуються 64–99 діаграм (залежно від ширини), 111 таблиць і 101 термінал — і жодного натяку на це в системі 009 немає. Власник погодив 2026-09-08 робити **без макета**, одним патерном на всі шість скролерів, на токенах | D-23, коло 2 | Я |
| 2 | **Ширина діаграм на телефоні.** 96 зі 100 не влазять у 348 px; медіана діаграми — 862 px. Розворот напрямку перевірено й **не працює** як загальне рішення. Живі варіанти: розширити контейнер на великих екранах (766 → 1152 дає 76 діаграм без скролу замість 45) · повноекранний перегляд на тап · перемалювати 5–8 найгірших. Власник 2026-09-08: «поки нічого не робимо» | D-05, D-15 | В + Д |
| 3 | **Mermaid не малює жодної діаграми в Safari 26.5.2.** Прод у тому ж Safari малює всі — тобто регресія міграції 010. На iOS іншого рушія немає. Власник: Safari не є цільовим браузером (2026-09-08) | знахідка кореневої сесії | В |
| 4 | **Порожній рендер Mermaid.** `data-processed="true"`, `<svg>` немає, виключення не кинуте → `.ds-diag__fallback` мовчить, і учень бачить сирий CSS. У Chrome частота **0 на ~290 завантаженнях**; ловиться детерміновано на першій навігації нового iframe. Рішення назвав сам QA: `await mermaid.render()` із власним лічильником замість `run({nodes})` + перевірка «є `<svg>`» **після** рендеру | D-17 | Я |
| 5 | **Текст фолбека обіцяє опис, якого немає:** «нижче її текстовий опис», але Mermaid до того вже замінив вміст `<pre>` своєю картинкою помилки | D-18 | Я |
| 6 | **Дві діаграми не центруються** на межі «переобмеженого бокса»: `architect-03#0` @768 і `claude-code-08#0` @1440, різниця 15 px, нічого не обрізано | D-19 | Я |
| 7 | **Шторка мобільного меню не ставить скрол-замок** — фон під відкритим меню прокручується, на відміну від обох модалок | коло 2 | Я |
| 8 | **CLS футера на уроках 0,20–0,25** при 390 px (пізня гідратація). У проді гірше (0,296), тобто 010 покращила, але поріг 0,1 не пройдено | коло 2 | Я |
| 9 | **Залишковий CLS лендінга Термінала.** Резерв заголовка закрив регресію проти проду, але лишились чотири порожні вузли hero (підзаголовок, кнопки, пояснення, статистика). Їхні висоти між ширинами стрибають немонотонно (768 → 355 px, 1024 → 722), тому суцільний резерв ловитиме **діру замість зсуву** | D-21, залишок | Я + В |
| 10 | **`roadmap.html` — єдина з 66 сторінок без `data-page-in`** | 010 | Я |
| 10a | **`.cc-hero__lead` теж порожній у розмітці й резерву не має** — це вся решта CLS лендінга Термінала (0 → 287 px на 1024). Доведено A/B через CSSOM. Не блокує: прод гірший у 2,4–4,3 раза. Разом із рядком 9 — одна робота | D-25, коло 3 | Я |
| 10b | **Корінь схеми, ширший за колонку, обрізається зліва** — `showRoot()` центрує вузол, тож у 6 зі 100 діаграм на 390 px початок його тексту йде за лівий край. Лікується вирівнюванням по лівому краю вузла, коли він ширший за контейнер | D-26, коло 3 | Я |
| 11 | **Акцент PDF сертифіката:** теракота (як затверджено в 005) чи акцент курсу. ⚠ Обидва теракотові відтінки дають контраст 3,90 і 3,53 при нормі AA 4,5 для дрібного тексту | 010 | В |
| 12 | **Перемикач «Вхід / Реєстрація» більше не таблист** — це `ds-btn` з `aria-pressed`. QA підтвердив, що читається коректно, але це зміна доступності робочої модалки задачі 001 | 010 | В |
| 13a | **`white-space: nowrap` на першій колонці таблиці розпирає її, коли там не команда, а речення.** Правило `_base` §13 (`.ds-tbl td:first-child`) задумане для команд і ключів налаштувань, але в частині таблиць перша колонка — проза, і тоді друга стискається до одного символа в рядок. Заміряно 2026-09-09 у власному Chrome по CDP, вʼюпорт 1280, усі 180 таблиць сайту: **5 ламаються повністю** (друга колонка 36–38 px + горизонтальний скрол) — `claude-code-07#l3` (перша колонка 929 px), `claude-code-22#l3` (1020), `claude-code-ref-commands#g5` (713) і `#g6` (673), `claude-code-ref-settings#traps` (765); **ще ~12 помірно** (друга колонка 11–33 %): `claude-code-17#l1`, `claude-code-22#l8`, `claude-code-20#l3` і `#l4`, `module-05#l3`, `claude-code-03#l6`, `claude-code-06#l3`, `claude-code-18#l7`, `claude-code-09#l6`, `claude-code-13#l3`, `ref-hooks#nomatcher`, `ref-commands#cli`. Шостий випадок (`claude-code-02#l4`, перша колонка 734 px) закритий 2026-09-09 **без дотику до компонента** — таблицю замінено списком `<ul>` на самій сторінці, за рішенням власника. Два шляхи для решти: так само переводити прозові таблиці в списки або дати `.ds-tbl` модифікатор, що знімає `nowrap` для першої колонки | замір кореневої сесії 2026-09-09 | Я + В |
| 13 | **Знята хореографія hero лендінга Термінала:** було 12 таймлайнів GSAP, стала одна штора. Відкат ≈ 200 рядків GSAP і девʼять імен у CSS | 010 | В |

### Хвости 005–008

| # | Що | Звідки | Тип |
| - | -- | ------ | --- |
| 14 | **`/roadmap?from=claude-code` віддає бренд і посилання AI Академії** — код знає лише гілку `architect`. У проді так само | D-24 | Я |
| 15 | **28 Tailwind-утиліт приходять у DOM лише з JS** — CDN генерує клас із затримкою ≈ 53 мс. Модалка «Написати нам» перші ~50 мс нестилізована; `#syllabusList` перебудовується на 328 px | 006/008 | Я |
| 16 | **Довідники: факти без джерела й пропуски повноти** — `/skill-doctor`, пʼять нових підкоманд термінала, `bashOutputMaxChars` / `taskOutputMaxChars` | 008 | А |
| 17 | **`term__meta` без `aria-hidden` у 22 з 23 уроків** — конвенція проєкту розходиться з контрактом | 008 | Я |
| 18 | **`/certificate` двічі тягне `certificates`** · **`profiles.full_name` і `user_metadata.full_name` розходяться** · **сторінка сертифіката брендована «AI Академія» для всіх курсів** | П-10…П-12 з 005 | Я + В |

### Те, що агенти закрити не можуть за конструкцією

| # | Що | Чому |
| - | -- | ---- |
| 19 | **Уся перевірка за акаунтом:** вхід поштою і через Google, вихід, «Позначити завершеним» → `submit_quiz` → бейдж, PDF сертифіката (два аркуші, ~0,4 МБ, посилання, QR), `/certificate` у стані «сертифікат є» | превʼю ділить базу з продом: реєстрація агента створила б живий рядок у проді, а екран Google їм закритий дозволами |
| 20 | **Windows: компенсація смуги прокрутки** (`html.ds-lock > body { padding-right }`) | на macOS смуги накладні, `--sbw` = 0 — перевірити неможливо в принципі |
| 21 | **Дотик на реальному телефоні:** горизонтальний свайп по діаграмі, жест «назад» на iOS | непрямий доказ є (`overscroll-behavior-x: contain` на всіх скролерах), прямого немає |
| 22 | **Скрінрідер наживо** — зокрема подвійне читання підпису діаграми (імʼя + вміст), наслідок `aria-labelledby` на власний підпис | перевірено дерево доступності й доступні імена, не озвучення |

---

## Блок 2 — заповнює PM-агент

Реєстр PM не потребує: кожен рядок має власне джерело у звітах 005–010, а нарізка вже
зроблена самим поділом на групи. Заводити `01-plan.md` варто лише для тих рядків, що
переростуть у самостійну задачу (найімовірніші кандидати — 2 і 19).

---

## Хід робіт

### 2026-09-10 · коренева сесія · рядки «Я» зроблені, три заміри дозняти

Повний звіт з доказами — **`report-2026-09-10.md`**; стенд замірів (скрипти CDP, базові
JSON, знімки) — **`stand/`**; сітка резерву hero — `measure-hero.md`.

Коротко: **зроблено й перевірено** — 4, 5, 6, 10b, 14; **зроблено, замір повторити** — 1
(підказка скролу: гасіння краю, знімки є), 7 (меню: sticky + замок + компенсація прокрутки;
знайдено, що на прокрученій сторінці меню відкривалось за 1181 px над екраном), 8 (замок
гостя синхронно, стенд забруднив сховище — перезняти), 9/10a (резерв hero контейнерними
запитами, перевірка не завершилась), 13a (перша колонка переноситься, `<code>` — ні;
регресій у ширинах не знайдено, чесний підрахунок рядків першої клітинки на момент здачі
ще йшов). **Закрито без коду** — 15 (уже 010), 17 (контракт і конвенція збігаються),
18 (два з трьох — 006). **Не робити (рекомендація)** — 10. **Не починав** — 16 (агент).
Пʼять рішень власника — у звіті, розділ «Рішення власника, які потрібні».
