# 008 — Редакторський прохід по 26 сторінках AI Терміналу · Зведений звіт

Коренева сесія зводить фінальні повідомлення `aia-content-reviewer` (Opus 5 / `xhigh`), по
одній сторінці на агента, хвилями по 4–5. Правило: агент править рівно свій файл, блок
`#quizData` не чіпає (007 щойно вирівняла дистрактори — `check-quiz.py --verify` має лишатись
чистим), факти не додає, git не виконує; коренева сесія комітить після хвилі, перед тим —
`check-lessons.py` і `check-quiz.py --verify`.

Вердикти: **готово** / **готово з зауваженнями** / **повертається автору**.

## Зведена таблиця

| # | Сторінка | Вердикт | Непідтверджених фактів | Пропущених тем | Виправив сам | Коміт |
| - | -------- | ------- | ---------------------- | -------------- | ------------ | ----- |
| 1 | `claude-code-01` Термінал з нуля | **готово з зауваженнями** | 5 (+3 виправлено за джерелом) | 0 | 3 факти, 5 формулювань | `961e0c6` |
| 2 | `claude-code-02` Що таке Claude Code | **готово з зауваженнями** | 3 живі запуски автора (не відтворити) + 3 дрібні | 0 | 1 факт, 8 слів, 2 формулювання | `39c2a3a` |
| 3 | `claude-code-03` Встановлення й перший вхід | **готово з зауваженнями** | 0 твердих (3 мʼякі) | 0 (`claude update` лише мимохідь) | 4 формулювання | `dbd73a4` |
| 4 | `claude-code-04` Перша сесія | **готово з зауваженнями** | 0 (3 дрібні примітки) | 0 | 3 навігація, 4 слова, 3 точність | `84bd261` |
| 5 | `claude-code-05` Інтерфейс і навігація | **готово з зауваженнями** | 0 вигаданих; 4 датовані знімки; «слово Manual» неточне | 0 (базові клавіші редагування рядка — розсипом) | 2 числа за екраном, 1 зворот, 5 слів, 3 формулювання, 2 підписи | _(нижче)_ |
| 6 | `claude-code-06` Контекстне вікно й компакція | **готово з зауваженнями** | **1 хибний («екран чистить `Ctrl+L`»)** + 1 без джерела | 0 | 3 формулювання, 2 слова | _(нижче)_ |
| 8 | `claude-code-08` Plan Mode й пісочниця | **готово з зауваженнями** | 0 | 0 | 7 формулювань (хост, клієнт, батьківський процес, область, «вужчі» → «інші», домен, складена команда) | _(нижче)_ |
| 7 | `claude-code-07` Дозволи й межа довіри | **готово з зауваженнями** | 0 вигаданих; 6 неповних/версійних + 1 хибний `explain` у квізі | 0 | 5 формулювань, 3 слова | _(нижче)_ |
| 12 | `claude-code-12` CLAUDE.md і памʼять | **готово з зауваженнями** | 0 (4 екрани — лише власний запуск автора) | 0 | 4 слова (закомічений, YAML, транскрипт, symlink), тон «ти», підпис `p3` | _(нижче)_ |
| 9 | `claude-code-09` Сесії, історія, відкат | **готово з зауваженнями** | 0 вигаданих; 3 для автора (`mkdir -p` у PowerShell, `/subtask` у діаграмі, крок 6 вправи) | 0 | 2 факти за джерелом, 6 формулювань, `aria-label` таблиці | _(нижче)_ |
| 10–11, 13–26 | `c10`, `c11` (були в роботі на зупинку дня), `c13`–`c23`, три довідники | _(не зроблено)_ | | | | |

## Хвиля 1 — `c01`–`c04`

### `claude-code-01` Термінал з нуля — готово з зауваженнями

**Виправив сам (за живим джерелом, показаним у звіті):** «окремої програми `cd` на диску не
існує» — хибно на macOS (`/usr/bin/cd` є; лишено суть «оболонка виконує сама»); дата «редакція
серпня 2026» під таблицею PowerShell — лише одна з трьох сторінок Microsoft серпнева, дату
прибрано; «дослівні команди з документації» → «взяті з документації» (у доксах повний рядок
присвоєння); «всі інструкції Claude Code розраховані на PowerShell» → «основні» (докси дають
і CMD-команду). Формулювання: «аргумент» (вводиться лише в блоці 6) → «назва папки»;
«псевдонім» пояснено; «консольна», «дистрибутив», «файловий менеджер» замінені на слова без
терміна. Заборонених зворотів — нуль. Diff 14+/13−, `check-lessons` ✓, `check-quiz --verify` OK.

**Непідтверджене, не правлено (рішення автора/власника):** «`PATH` — рядок, склеєний
двокрапками» правда лише для macOS/Linux, а абзацом вище названий PowerShell (там `;`);
«виділити мишкою зазвичай виходить, а поставити курсор кліком — ні» стоїть одразу за «документація
каже прямо», хоча в доксах лише перше речення; «на старих macOS був bash» — без опори;
`explain` квіза п. 6 «права адміністратора не потрібні» — прямого рядка в `terminal-guide`
немає; екран `t2` (`/opt/homebrew/bin/claude`, 2.1.236) більше не відтворюється після переїзду
на native-збірку 2026-09-05 — датований знімок, не помилка.

**Суперечність контракту §4 — три кнопки «Копіювати»:** `t1` — сесія з шести команд (у читача
`cd images` впаде), `t2` — три незалежні діагностичні команди, серед них `claude --version`,
якого в `c01` ще не встановлено, `t3` — «перевірка після кроку 4», але крок 4 створює порожню
папку без `README.md`. Найдешевший фікс — прибрати кнопку й `.term--cmd` у `t1`–`t3`; це
видима зміна, рішення власника.

**Пропущених тем немає**, усі шість пунктів програми покриті. Урок ніде не лінкує
`terminal-guide`, заради якого існує (URL живий) — додати посилання може лише автор.
Живий запуск підтвердив усе відтворюване: `/etc/paths` посимвольно, чотири помилки блоку 6
дослівно, псевдоніми PowerShell за Microsoft.

### `claude-code-02` Що таке Claude Code — готово з зауваженнями

**Виправив сам (28+/15−):** факт «243 рядки виводу `--help`» → 242 (рядок 1 у знімку — сама
команда; 62 ключі й 13 підкоманд звірені, правильні); непояснені «ключ» (абзац після першої
`-p`), «утиліта», «репозиторій, якого немає локально», «історія комітів», «токен», «суцільний
JSON», «скрипти», «керування контекстом» (вказівник на блок 3); зворот «правило простих слів —
жодного вигаданого виводу»; заголовок граблі 4 не відповідав тексту (порівнюються дві поверхні
Claude Code, не чат) — вирівняно. Заборонених зворотів нуль.

**Не відтворити (дозволене джерело №3 — живий запуск автора):** `which claude` →
`/opt/homebrew/bin/claude`, журнал `stream-json` «чотирма ходами і 12,6 с», `claude -p` про
`notes.txt` → `2`. **Не правлено:** «лімітів немає в самій документації» — правда, але джерело
насправді сторінка тарифів (`no fixed message count`), не докси; «Розуміння коду» в таблиці
інструментів подано як вбудоване, а в доксах позначено «requires code intelligence plugins»;
«модулі з 14-го по 18-й» захоплюють `c17` (діагностика). Суперечність `c02`/`c11` з
`cross-findings.md` — **закрита**. Живі докси (`how-claude-code-works`, `best-practices`,
`quickstart`, `platforms`) підтвердили решту дослівно; слово «vibe» — 0 збігів і сьогодні.

**Кнопка копіювання** на `t1` (дві діагностичні команди) і `t3` (`ls` + запит про неіснуючий
`notes.txt`) — той самий системний випадок. **Квіз (не чіпав):** маркери лише в правильному —
п. 2 тире, п. 3 крапка з комою, п. 5 лапки «…».

### `claude-code-03` Встановлення й перший вхід — готово з зауваженнями

**Факти:** твердих непідтверджених немає — кожне твердження звірене з живими доксами
(`setup`, `cli-reference`, `authentication`, `troubleshoot-install`), екранами `screens/` і
власними читальними запусками; всі десять рядків таблиці помилок блоку 6 є в доксах; stable-
каска Homebrew досі 2.1.236 проти `@latest` 2.1.261 — розрив каналів реальний. Три мʼякі місця
без прямого джерела: «майже щодня» (арифметика автора), пояснення «чому менеджер пакетів не дає
самооновлюватись» (докси кажуть лише *що*), «`claude install` додасть другу, рідну» (висновок).

**Виправив сам (19+/7−):** «змінна оточення» — перша поява в курсі саме тут, додано пояснення
через введену в `c01` «оболонку»; «WSL» вперше тут, а розшифровано аж у `c08` — додано тією
самою формулою, що в `c08`; «репозиторій» означав то сховище коду, то сховище пакетів —
розведено; найщільніший абзац FAQ про `curl … | bash` отримав коротку відповідь на початку.

**Пропущених тем немає.** Тонке місце: власна команда `claude update` (аліас `upgrade`)
названа один раз мимохідь у пункті про `minimumVersion`, а не як спосіб оновити рідну збірку —
джерело в `screens/` готове, дописати може автор.

**Системне, не дефект `c03`:** кнопка «Копіювати» на блоках із 2–4 незалежними діагностичними
командами — так у **17 з 23 уроків**; рішення власника одним проходом по курсу (перегукується з
`c01`). **Квіз (не чіпав):** у п. 1, 2, 4 тире лише в правильному варіанті; слово «просто» у
варіанті п. 5 тримає вічне попередження `check-lessons.py`. Без глосарію лишились Node.js і
SSH/контейнер (додавання фактів — автор). Екран `which -a claude` → `/opt/homebrew/bin/claude`
не відтворюється після переїзду на native-збірку — датований знімок.

### `claude-code-04` Перша сесія — готово з зауваженнями

**Факти:** непідтверджених немає — 25 тверджень звірені з живими доксами (`permission-modes`,
`keybindings`, `security`, `permissions`, `interactive-mode`, `checkpointing`, `commands`,
`quickstart`, `best-practices`), екранами `claude--help`, `permission-mode-enum-test`,
`t-bogus-flag-probe`. Розбіжність про `Esc` між `keybindings` і `how-claude-code-works` досі
жива в доксах — знахідка чинна. Три примітки без правки: «ніж очікування кінця» — гладка
інтерпретація; «докси прикладів іншими мовами не наводять» — негативне твердження, перевірено
6 сторінок; `/diff` з v2.1.260 має diff-панель у fullscreen — кандидат «освіжати першим».

**Виправив сам (35+/23−):** «в модулі 5» → «у блоці 5» (ішлося про власний блок); «`/cd`,
`/add-dir` докладно — модуль 5» → модуль 7 + довідник команд (`/cd` більше ніде в курсі, `/add-dir`
розібраний у `c07`); рядок, що `~/my-site` у копійованому блоці — приклад; глоси «прапорці»,
«кодова база», «репозиторій», «система контролю версій» → `git`; класифікатор — «у фоні» замінено
на те, що в доксах («перед виконанням, блокує те, що виходить за межі запиту»); callout про
першу сесію розплутано, зняте хибне «дослівно»; «питання вмикає лише читання» → точне (читання
в робочій папці дозволу не потребує).

**Пропущених тем немає**, вимога програми «модуль починається з `Shift+Tab` у Manual» виконана
двічі. **Квіз:** `answer=1` було у 3/6 — питання зі згадкою «другий» (промпт із самого
питання, не позиція) ротація пропустила; **коренева сесія докрутила його вручну** (`84bd261`,
тепер 2/2/1/1; аналогічне в `c08` уже стояло на цілі), `--verify` OK; «просто» всередині
дистрактора п. 2 — вічне попередження чекера, правити лише в 007-подібній задачі.

## Хвиля 2 — `c05`–`c09`

### `claude-code-05` Інтерфейс і навігація — готово з зауваженнями

**Виправив сам (27+/23−):** два маркери скорочення були арифметично хибні за `screens/claude--help.txt`
(«ще 60 опцій» при 62 усього й 3 показаних, до того ж пропусків два; «ще пʼять підкоманд» при
13 усього й 7 показаних → шість) — переформульовано чесно, підписи під блоками вирівняно;
знахідка 007 про кириличну розкладку закрита — застереження «з 2.1.247» тепер стоїть **перед**
описом механізму; «консольна» → «програма термінала», «слеш» — глоса при першій появі в курсі,
«нативно» прибрано, «буфер» → «буфер обміну», «розбір рядка» переписано; двозначна метафора
«два поверхи» зникла; дата «звірено 4 вересня» тепер покриває всі три числа вступу; «рішення
просте» → «одне».

**Датовані знімки, не помилки (не правлено):** контекстів клавіш у доксах уже **20**
(`DiffPanel`, v2.1.260+), а «19» стоїть у трьох файлах — урок, `claude-code.config.json`,
`claude-code-ref-commands.html` — правити лише всі три разом; на збірці 2.1.263 `claude --help`
дає 65 опцій і 18 підкоманд проти 62/13 у тексті; **`--restricted` тепер існує** — другий
рядок блоку `t3` (`error: unknown option`) не відтворюється, версія біля блоку стоїть; дата
`t3` «2026-09-04» проти «2026-09-03» у `screens/` — рядки посимвольно ті самі.
**Неточність, що потребує рішення на три сторінки:** «показник режиму — слово `Manual`»
у `<code>` читається як рядок екрана, а на екрані `⏸ manual mode on` (докси); того ж рядка
немає й у `c04`/`c07`.

**Пропущених тем немає.** Тонке місце: «базові клавіші» дано розсипом, без набору редагування
рядка `Ctrl+A/E/K/W` (у фактурі є) — абзац може дописати лише автор. Кнопка копіювання на `t1`
і `t3` — прикордонні випадки конвенції. Термінологія `--…`: `c03`/`c04` «прапорці», `c05`
«опції» — рішення на весь курс. **Квіз (не чіпав):** `explain` п. 4 «з блоку 5» — коректно
(знахідка 007 знята); SSH без розшифровки в п. 5, «просте» в п. 6.

### `claude-code-06` Контекстне вікно й компакція — готово з зауваженнями

**🔴 Хибна несуча теза блоку 6, не правлено (рішення власника/автора):** «Екран чистить
`Ctrl+L`» стоїть у трьох місцях уроку і в `explain` квіза п. 1. Живі докси: у класичному
рендерері `Ctrl+L` **перемальовує** екран, чистить — лише у fullscreen і лише **з v2.1.260**;
а **до v2.1.238 подвійне `Ctrl+L` за дві секунди запускало `/clear`** — тобто на версії уроку
(2.1.236) порада «не тисни `/clear`, тисни `Ctrl+L`» вела рівно в ту біду, від якої застерігає.
Потрібне рішення, як подавати (fullscreen + версія, або жива перевірка на 2.1.263) — тягне
за собою заморожений `explain`. **🟡 Без джерела:** «українською теж працює» для `/compact
<інструкція>` — докси про мову мовчать, а на цьому стоїть крок 4 «Зроби сам». **🟢** Відкритий
хвіст `c06` у `cross-findings.md` («частина моделей — 200K») можна закривати: урок переказує
першу половину речення `model-config.md` і не бреше.

**Виправив сам (7+/6−):** «ви» → «ви з ним» двічі (курс на «ти»); «модель входить у ключ кешу»
→ пояснення з `prompt-caching.md` («у кожної моделі свій кеш…»); «промпт» отримав означення у
вузлі діаграми (у курсі ніде не означений); рядок таблиці про hooks дістав адресу «модуль 16»,
«вибір моделі» → «вибір моделі й рівня зусиль — модуль 10».

**Звірено й зійшлося:** 41 твердження — обидва рядки блоку 5 і `--autocompact` **живим
запуском на 2.1.263 байт у байт**; помилки межі контексту (`errors.md`), рецепти проти
thrashing, таблиця «що переживає компакцію» рядок у рядок, усі 8 чисел діаграми з масиву
`EVENTS`, пороги й 967K (`model-config.md`), `Resume from a summary` (`sessions.md`).
**Пропущених тем немає.** **Квіз (не чіпав):** «Просто закрити термінал…» у дистракторі п. 3
(вічне попередження чекера); 967K дублюється в `explain` п. 5; `explain` п. 1 успадковує
проблему `Ctrl+L`.

### `claude-code-08` Plan Mode й пісочниця — готово з зауваженнями

**Факти:** непідтверджених немає — понад 40 тверджень зійшлися дослівно з живими доксами
(`sandboxing`, `permission-modes`, `settings`, `settings-reference`, `commands`, `keybindings`,
`sandbox-environments`); три екрани відтворені на 2.1.263 (`--sandbox` → `unknown option`
посимвольно; `strings`-проба — контрольний нуль той самий, лічильники інші, як і має бути для
іншої збірки). Межу з `c07` витримано. **Пропущених тем немає.**

**Виправив сам (16+/14−):** «хост»/«клієнт» (у курсі не вводились) → «адреса в мережі» /
«програма, що виходить у мережу»; «батьківський процес» → «ті самі змінні середовища, що й сам
Claude Code»; «має область <…>» → «читається лише з трьох місць…»; заголовок «Свої захищені
шляхи — вужчі» → «інші» (списки не вкладені); глоса до «домен»; «складеної команди» →
«склеєної з кількох».

**Не правлено, рішення на весь курс:** запрошення `%` проти `$` (по курсу 42 × `%` і 140 × `$`);
«змінна оточення» (`c03`) проти «змінні середовища» (`c08` і ще вісім уроків); «просто» у
дистракторі п. 5 квіза; кнопка «Копіювати» на `p1`, `p3`. **Для наступного оновлення курсу:**
докси вже описують v2.1.260 (команди через `!` у strict sandbox — поза пісочницею) і
`--restricted` (v2.1.248+).

### `claude-code-07` Дозволи й межа довіри — готово з зауваженнями

**Факти:** вигаданих немає — усі великі твердження (шість режимів, стартова таблиця, `Shift+Tab`,
`deny → ask → allow`, зірочки, read-only набір, protected/critical paths, класифікатор 3/20,
`bypassPermissions`, `--add-dir`, `--label` 2.1.208+, `--restricted` 2.1.248+) зійшлися з доксами й
`screens/`; `claude auto-mode defaults` на 2.1.263 дав `17/69/1/21` проти `17/66/1/20` в уроці —
рух версій. **Шість неповних місць для автора:** вкладка `Auto mode` без застереження
«v2.1.246+» (на версії уроку її немає); рядок `plan` у таблиці захищених шляхів без першого
варіанта «дозволено, якщо в сесії доступний bypass»; «іноді питають» подано універсально, а в
доксах «In Manual mode»; дослівна цитата «from the added directory» у доксах уже «from additional
directories»; «файл, з якого прийшло правило» проти «by scope» у доксах; FAQ про
`settings.local.json` без винятку «поза git-репозиторієм або на Windows».

**🔴 Квіз, п. 6 (`explain`, заморожено):** «`git log main` без зірочки посередині якраз НЕ
підходить» — для правила з питання `Bash(git * main)` цей рядок **підходить**; «не підходить» він
для сусіднього `Bash(git log * main)`. Правильна відповідь лишається правильною, хибне лише
обґрунтування. Дрібне: `explain` п. 1 про «скаргу на нечитабельні налаштування» — джерела нема.

**Виправив сам (25+/16−):** подвоєння «задокументовано прямо… прямо ж»; «рівні» → «шари» (як у
заголовку й таблиці); «помилка тут не видно»; звʼязано приклад доксів `Bash(git commit *)` з
прикладом уроку про перенаправлення; хибне перехресне посилання на «блок 2 з `--help`» (там форма
без `--help`); прибрано хибне «`grep` знайомий за модулем 1» (у `c01` слова немає); глоси
«фіче-прапорці», «Agent SDK»; навігаційне речення під таблицею стартових режимів.
**Пропущених тем немає**, межа з `c08` витримана. Кнопка «Копіювати» на `p1`, `p2`, `p5` — конвенція.

### `claude-code-12` CLAUDE.md і памʼять — готово з зауваженнями

**Факти:** таких, що суперечать джерелам, немає — 2.1.206 (`/doctor` trim), 2.1.210 (індекс
памʼяті), `--safe-mode`, правка посеред сесії, порядок `~/.claude/rules/`, 200 рядків / 25 KB,
4 MiB, чотири стрибки `@`, `claudeMdExcludes`, AGENTS.md — усе дослівно в живих доксах. Чотири
екрани `p1`, `p2`, `p4`, `p5` — лише власний запуск автора (у `screens/` їх немає); приватність
§9.5 чиста. **Пропущених тем немає**; на розсуд автора — пункт **Structure** з «Write effective
instructions» і `CLAUDE_CODE_NEW_INIT=1`.

**Виправив сам (14+/11−):** «закомічений файл» → «файл у спільному репозиторії» (коміт пояснюється
аж у блоці 4); «YAML» — перша поява в курсі, описано словами; «транскрипт» пояснено; «символьне
посилання» — нагадування формулою з `c09`; єдине «ви» → «ти»; підпис блоку `p3` (приклад з доксів)
дістав джерело й дату. **Не правлено:** кнопка «Копіювати» на `p4` (дві діагностичні команди);
«просто» й «продові конфіги» всередині `quizData`.

---

## Зупинка дня 2026-09-06 (власник: «90 % лімітів, більше нікого не запускай»)

Готово й закомічено: `c01`–`c09`, `c12` (10 із 26). `c10`, `c11` були в роботі на момент
зупинки — їхні файли можуть лежати в дереві незакоміченими; перед комітом — `check-quiz.py
--files … --verify baseline.json`, `check-lessons.py`, диф без `Bin`. Не починались: `c13`–`c23`
і три довідники `claude-code-ref-*.html`.

**Наскрізні речі для рішення власника (повторюються з уроку в урок):** кнопка «Копіювати» на
блоках із кількома незалежними командами (17 із 23 уроків); «просто» всередині `quizData`
(вічне попередження чекера; 007 заморозила квізи); термінологія «прапорці»/«опції», «змінна
оточення»/«змінні середовища», запрошення `$`/`%`; «19 контекстів клавіш» → 20 у доксах (урок
`c05`, `claude-code.config.json`, `claude-code-ref-commands.html`); екрани `/opt/homebrew/bin/claude`
2.1.236 більше не відтворюються після переїзду на native-збірку.
**Змістовні помилки:** `c06` «екран чистить `Ctrl+L`» (хибне для 2.1.236, до 2.1.238 подвійне
`Ctrl+L` = `/clear`) · `c07` `explain` квіза п. 6 (для `Bash(git * main)` рядок `git log main`
підходить) · `c01` три кнопки копіювання на сесіях/діагностиці · `c02` «Розуміння коду» подано
як вбудоване, у доксах «requires code intelligence plugins» · `c05` «слово `Manual`» проти
`⏸ manual mode on`.

### `claude-code-09` Сесії, історія, відкат — готово з зауваженнями

**Факти:** вигаданих немає — `s1` посимвольно з `screens/claude--help.txt`, `s3` і `s5` відтворені
живими командами на 2.1.263 (код виходу 1 підтверджено), `s2` — папка в `~/.claude/projects/`
існує з тим самим `.jsonl`; десятки тверджень звірені з `sessions`, `checkpointing`, `errors`,
`claude-directory`, `commands`. **Три для автора (не правлено):** `mkdir -p ~/claude-sesii` у
PowerShell не працює (`-p` немає; дешевше прибрати `-p`); `/subtask` у діаграмі блоку 3 названий,
але не пояснений, і мітка гілки описує не `/fork` (докси розводять їх); крок 6 вправи «`Restore
code` до першої правки» видалить `notes.txt` — читач цього не очікує. Дрібне: обірвана цитата
про `history.jsonl`, `jq` без пояснення (пояснює `c21`), `/` «або будь-яка друкована літера»
вужче за докси, `claude -p --continue` включає `/loop`-сесії.

**Виправив сам (30+/26−):** два факти за знайденим джерелом — `/resume` **всередині сесії**
продовжує розмову в режимі поточної сесії (було злите з пікером; `sessions.md`), «чотири види
записів» `purge` → три плюс підсумок (як у цитованому виводі `Dry run: 3 item(s)`);
формулювання — `JSON` пояснено при першій появі (формулою `c13`), `worktrees` як у `c12`,
заголовок «Чотири схожі команди» проти таблиці на пʼять, «Allow for this session» більше не
приписується `c07`, «директорія» → «папка»; `aria-label` таблиці був скопійований із чужого
заголовка. **Пропущених тем немає.**
