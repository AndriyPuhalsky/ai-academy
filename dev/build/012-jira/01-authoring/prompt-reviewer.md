# Шаблон промпта для `aia-content-reviewer` (012) — використано на j01–j03 2026-09-17

Замінити `jNN` / `jira-NN` / `«ТЕМА»` / блок «Особливість» (п. 7). Спавнити з сесії в `dev/build`
(або з симлінком памʼяті, див. `JOURNAL.md`). Один агент = один урок; кілька паралельно — можна,
бо кожен править лише свій файл.

---

Задача 012 «Jira з нуля» — рецензія уроку **jNN** «ТЕМА»: файл `/Users/ander1.sage/Downloads/AIA/modules/jira-NN.html`.

Ти працюєш ПАРАЛЕЛЬНО з іншими рецензентами в одному робочому дереві. Тому:
- правиш РІВНО один файл: `/Users/ander1.sage/Downloads/AIA/modules/jira-NN.html`;
- звіт пишеш файлом через Write: `/Users/ander1.sage/Downloads/AIA/dev/build/012-jira/01-authoring/reports/jNN-review.md` (пряма вимога власника — файл-звіт обовʼязковий; якщо запис відхилено дозволами — скажи прямо у фінальному повідомленні);
- усе інше — лише читання: не чіпай `program.md`, `lesson-contract.md`, `cross-findings.md`, `facts-free-plan.md`, `jira.config.json`, інші уроки, `css/`, `js/`; git не виконуєш узагалі (ні add, ні commit).

Це курс про **Jira Cloud Free для людей не з ІТ**, а не про Claude Code — твоє визначення й памʼять з 008 писались для Терміналу. Що тут інакше:

1. **Джерела правди** (контракт §7, рівно три): (а) живі докси Atlassian через WebFetch — `support.atlassian.com/<product>/docs/…`; індексні адреси `…/docs/` дають 404, робочий вхід `…/<product>/resources/` або WebSearch з `allowed_domains: ["support.atlassian.com"]`; **багатоколонкові таблиці планів WebFetch читає неправильно** — таблиці брати `curl`-ом з Bash і розбирати `<tr>/<td>` (галочка = `<path d="M9.707 11.293…">`, прочерк = `-`); (б) текстові знімки sandbox `dev/build/012-jira/00-research/screens/*.md` (**.md, не .txt**) — написи у вікнах беруться звідси дослівно; (в) живий sandbox — у тебе доступу до Chrome немає, тож усе, що можна перевірити лише в sandbox, виноси в розділ «Що перевірити не вдалося» з точним формулюванням, що зняти. Плюс перевірені факти з датою `00-research/facts-free-plan.md` і `00-research/glossary.md`.
2. **Замість `.term` — компонент «вікно застосунку»** (контракт §4: `figure.win[data-state]` → `figcaption` → `.win__body` …; шість станів board · list · item · form · settings · portal). Механіку вікон перевіряє чекер: `cd /Users/ander1.sage/Downloads/AIA && python3 dev/build/012-jira/01-authoring/check-lessons.py --pattern 'modules/jira-NN.html'` — запусти ДО і ПІСЛЯ своїх правок, має бути 0 ✗ / 0 !. Пункти 5–6 твого стандартного списку механіки (`.term--enter`, `.term__line`) тут не застосовуються, крім `.term` для JQL/CLI (§4.4). Решта пунктів чинні: `data-config="../jira.config.json"` і `data-course="jira"` на `<html>`, `data-module="jNN"` на `<body>`, viewport без `user-scalable`, вісім блоків з `data-lesson`, Mermaid ≥ 1 з текстом вузлів у лапках, квіз 5–6 питань, регістр шляхів нижній, жодного `<img>` усередині вікон, дата «звірено 2026-MM-DD» у заголовку кожного вікна.
3. **Квіз у 012 щойно написаний і НЕ заморожений** (твоя памʼять «квіз заморожено 007» стосується Терміналу). Пункт 8 механіки застосовується повністю; числа — з `python3 dev/build/007-quiz-distractors/check-quiz.py --files modules/jira-NN.html`. Текст правильної відповіді, `q`, індекс `answer`, кількість питань і варіантів не чіпай; формулювання дистракторів і `explain` — можна, якщо дефект.
4. **Правила стійкості (контракт §0.1) — це критерії дефекту:** числа лімітів у тексті уроку заборонені (єдині винятки: «10 користувачів» Jira Free і «3 агенти» JSM Free); термінологія «поточна + колишня в дужках при першій згадці в курсі» — work item (issue), space (project), work type (issue type), flow (automation rule) — пари work item (issue) і space (project) **уже введені в j01**; англійські написи UI як на екрані, українські пояснення; посилання — на розділи доксів, не на сторінки; «Ask AI», «Improve Task», «Add agent», «See plans», «Try Confluence now» — апсели, не функції Free.
5. **Сюжет «Марципан»** (кавʼярня-пекарня, 8 людей, 6 спейсів — `program.md` розділ 3) — навчальний сюжет; це не факт про Jira, не позначай як непідтверджене; але звіряй, що імена, ролі й ключі спейсів у вікнах збігаються з розділом 3 програми.
6. **Читач — людина не з ІТ.** «Непояснене слово» рахуй від читача, який прочитав лише попередні уроки. Хто «дім» кожного поняття — `program.md` розділ 4 і розділ «Межі між сусідами»; тема іншого уроку має звучати одним рядком з відсиланням, не розгортатись.
7. **Особливість jNN за звітом автора:** «…» (взяти з `reports/jNN.md` §4–§6: що описано словами, що ⚠, які розбіжності).

Порядок роботи:
1. Спершу прочитай `dev/build/012-jira/01-authoring/lesson-contract.md` цілком (особливо §0, §0.1, §3, §4, §6, §7, §8), потім у `dev/build/012-jira/00-research/program.md` — розділи 1–5, картку уроку jNN, розділи «Наскрізні правила авторингу» і «Межі між сусідами».
2. Прочитай звіт автора `dev/build/012-jira/01-authoring/reports/jNN.md` (розділи 4–7) і `dev/build/012-jira/01-authoring/cross-findings.md`. Те, що автор сам позначив ⚠ і чесно пояснив читачеві у виносці, — свідоме рішення, не дефект; перевір, що виноска є.
3. Перший прохід — як читач, без правок. Другий — по фактах: кожне перевірюване твердження про Jira → джерело (живі докси або знімок), не памʼять. Третій — по словах: заборонені звороти пошуком (просто · очевидно · як відомо · елементарно · всі знають · звісно · не забудь, що), непояснені терміни очима. Четвертий — механіка: чекер, check-quiz, пункти вище.
4. Виправ те, що у твоїй зоні (формулювання, звороти, механіка; факт — лише якщо знайшов джерело і показав його у звіті). Решту — у звіт. Сумнів між «виправити» і «повідомити» — повідомити.
5. Прогони чекер і check-quiz на фінальному файлі, встав їхній вивід у звіт. Апостроф у тексті уроку — той самий символ, що вже використаний у файлі (чекер це перевіряє); не міняй.

**Додано після хвиль 2–3 (2026-09-18):** читати `program.md` «Уточнення» п. 1–14 і правила 2а–2в та `cross-findings.md`
розділи хвиль 2–3 (записане там як рішення — не дефект) · **цитати звіряти посимвольно** і за **банером сторінки**
(у хвилі 2 в цитату було вставлене слово, якого в доксах немає; software-цитати приписувались business-дошці) ·
`jira/kb/…` — за банером «Cloud Only» · **маркери стилю у квізі в обидва боки** (`check-quiz.py` бачить лише
довжину; з хвилі 4 є скрипт: `python3 dev/build/012-jira/01-authoring/check-quiz-markers.py --files
modules/jira-NN.html` → «Маркерів: 0», вивід — у звіт; правити лише дистрактори) · довідники за назвою · «цифри» для ①②③ · парні форми звертання · робочі файли звірки — у власній
підпапці скретчпада (сусід перезаписав `*.txt` і звірка дала хибні «MISS») · заголовок статті ≠ слаг — звіряти
заголовок у `<main>`.

**Додано після хвилі 4 (2026-09-19):** читати `program.md` «Уточнення» п. 1–**16** і оновлений §3 (канон статусів `FIN`,
автор заявок `FIN-1`/`FIN-2`, `REM-6`) та `cross-findings.md` розділи хвилі 4 · **головний клас дефектів хвилі 4 —
твердження «зі здорового глузду» без джерела** (у вікнах і цитатах було 0 розбіжностей): кожне речення про поведінку
продукту, яке не стоїть біля цитати, перевіряти окремо; окремо — **твердження про самі докси й екрани** («у довідці це
звуть…», «кнопка живе на екрані…», «є лише тоді, коли…») — грепати по завантажених сторінках і звіряти зі `screens/` ·
звірка цитат **регістрочутлива**; два варіанти зняття тегів (`теги → нічого`, `теги → пробіл`) — під **різними** ключами
словника; нормалізувати `\s+([,.'])`; тег може розірвати слово (звіряти по сирому HTML) · картки фаз 4–5 відстали від
продукту — заміни, зведені в «Уточненнях» п. 15–16, є рішенням · живий Marketplace: статус бейджа лише в
`/rest/2/addons/<key>` (у списку `programs: {}` порожній); **пошук REST за текстом може не повернути картку, яка існує, —
перевіряти за ключем** · у промпт кореневою сесією дописується блок «Особливість jNN» зі звіту автора §4–§6 (зразки — у
`JOURNAL.md`, хвиля 4) і рядок про сусідів, яких зводять паралельно (чужий файл не чіпати).

**Додано після хвилі 5 (2026-09-19):** читати `program.md` «Уточнення» п. 1–**18** і §3 у чинній редакції та `cross-findings.md`
розділи хвилі 5 · **фірма сюжету з 2026-09-19 — «Марципан»** (рішення власника; була «Кориця» — у старих звітах це історія):
під час проходу «по словах» перевіряти узгодження навколо назви — рід чоловічий («Марципан задає сам», «у самому Марципані»),
відмінки «Марципану / у Марципані / з Марципаном / про Марципан», займенники «він / його», адреса `marcypan.atlassian.net` ·
рішення власника того ж дня — не дефект: канон статусів `FIN`, авторка заявок Соломія, «млин «Золотий колос»», «службовий
спейс (service space)», мітка в JQL **у лапках** (`labels != "архів" OR labels IS EMPTY`) · **Atlassian переписує сторінки на
ходу** — цитату, якої не знайшов, спершу шукати в новому формулюванні на тій самій сторінці (і показати обидва тексти у
звіті) · сторінки `user-management` містять дві версії тексту в одному HTML («Centralized» / «Original») · уроки, написані без
живого середовища (j19, j22): головна перевірка — **жодного вигаданого напису чи виводу** (кожен напис вікна «за
документацією» — з опорою; у `term` — лише введення людини й реально отриманий вивід) · реальний `claude mcp
add/remove/login/list` заборонений · якщо Write відмовив на `.md` — записати звіт через Bash heredoc · для іспиту j23:
кожне питання звіряти з чинним текстом уроку, на який воно спирається (а не з карткою), правильна відповідь не повинна
стояти на твердженні під ⚠.

**Додано після хвилі 6 (2026-09-20):** читати `cross-findings.md` розділи від 2026-09-20 і `01-authoring/open-claims.md` (рядки про
свій урок) · **«повідомляю, не правлю» у звіті ≠ закриття:** кожне твердження, яке ти лишив у тексті без (прямого) джерела,
**продублюй у §7 окремим рядком** формату `- **jNN → open-claims:** рядок HTML · твердження дослівно · де шукав · чим
закривається (жива довідка / sandbox / рішення власника / зняти)` — коренева сесія перенесе його в реєстр; запис, який лишився
тільки в розділі 2 звіту, губиться (так «одинадцяту людину Jira додати не дасть» прожило пʼять хвиль) · окремий прохід по
квізу: **правильна відповідь не стоїть на твердженні під ⚠ і на категоричному «лише / завжди / жодного» без цитати в тексті
уроку** (шість таких питань знайдено в шести уроках) · довідка може суперечити сама собі (адреса сайту: 3 проти 15 змін) —
якщо знайшов дві сторінки з різними числами, число з уроку знімається · KB-статті з позначкою «generated by AI» — не джерело ·
**для іспиту:** джерело правди одне — чинний текст уроків (текстові зліпки дає коренева сесія); головна перевірка —
**кожен дистрактор однозначно хибний за текстом уроку** (людина з правильним знанням не має втрачати бал) і `explain`
називає справжній дім теми; шаблон завдання — у `JOURNAL.md` (хвиля 6) і в контракті §3 «Скелет іспиту».

Формат звіту (файл `jNN-review.md` + той самий зміст фінальним повідомленням):
1. Вердикт одним рядком: готово / готово з зауваженнями / повертається автору.
2. Непідтверджені факти — цитата з уроку + чого бракує (+ джерело, якщо знайшов і воно суперечить тексту).
3. Пропущені теми з картки jNN у `program.md` — списком (або «немає»).
4. Що виправив сам — по класах, з кількістю правок і 3–5 прикладами «було → стало».
5. Механіка — по пунктах, що пройшло, що ні; вивід чекера й check-quiz дослівно.
6. Що перевірити не вдалося — і чому; окремо для кореневої сесії: що саме зняти в sandbox (сторінка / шлях / елемент).
7. Знахідки для сусідніх уроків — у форматі `cross-findings.md` (`- **jNN → jMM:** факт · джерело · що з ним робити`); сам файл `cross-findings.md` не редагуй, коренева сесія перенесе.

Мова — українська.
