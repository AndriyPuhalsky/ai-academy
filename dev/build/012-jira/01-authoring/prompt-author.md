# Шаблон промпта для `aia-content-author` (012) — для хвиль j04–j08 … j18–j23 і довідників

Відтворено кореневою сесією 2026-09-17 (вечір) за контрактом §1/§7/§8/§9 і за досвідом хвилі 1
(оригінальні промпти хвилі 1 у репозиторії не збережені). Замінити `jNN` / `jira-NN` / «ТЕМА» / «Фаза N».
Спавнити з сесії в `dev/build` (памʼять `aia-content-author` за 012 лежить у
`dev/build/.claude/agent-memory/aia-content-author/`); з `AIA/dev` теж працює — там стоїть симлінк на цю папку
(стара памʼять Терміналу відкладена в `aia-content-author.terminal-2026-09-04/`); shell перед спавном має стояти
в `AIA/dev` або `dev/build`, не в папці задачі. Хвиля = 4–5 авторів паралельно, **з апрувом власника на кожну
хвилю**; кожен пише лише свій файл. До промпта дописувати: (а) рядок про паралельних сусідів («файлів jNN…jMM ще
немає — посилайся за назвою уроку»), (б) сьогоднішню дату, (в) **блок «Особливе для jNN»** — знахідки сусідів
(`grep 'jNN' cross-findings.md`), межі, знімки `screens/`, що на sandbox не зняте. Зразки — у `JOURNAL.md`
(хвиля 4) і в `reports/j15…j18.md`.

---

Задача 012 «Jira з нуля» — написати урок **jNN** «ТЕМА» (Фаза N) як файл `/Users/ander1.sage/Downloads/AIA/modules/jira-NN.html` + звіт `/Users/ander1.sage/Downloads/AIA/dev/build/012-jira/01-authoring/reports/jNN.md`.

Ти працюєш ПАРАЛЕЛЬНО з іншими авторами в одному дереві: пишеш РІВНО два файли (урок і звіт), усе інше — лише читання (`program.md`, `lesson-contract.md`, `cross-findings.md`, `facts-free-plan.md`, `jira.config.json`, інші уроки). Git не виконуєш.

Це курс про **Jira Cloud Free для людей не з ІТ**, а не про Claude Code — твоє визначення писалось для Терміналу. Читай у такому порядку, до першого рядка уроку:
1. `dev/build/012-jira/01-authoring/lesson-contract.md` **цілком** — особливо §0/§0.1 (читач і принципи стійкості), §2 (каркас: база — `modules/jira-01.html`, у файлі змінюються рівно чотири речі + заміни §2), §3 (вісім блоків), **§4 (тимчасова розмітка «вікна» — класи лише з §4.3, своїх не вигадувати)**, §5 (Mermaid), §6 (квіз без підказки довжиною), **§7 (джерела — памʼять моделі джерелом не є)**, §8, §9 (формат звіту).
2. `dev/build/012-jira/00-research/program.md` — розділи 1–5, **картка jNN** (результат · теми · вікна зі станом, заголовком і мітками · Mermaid · «Зроби сам» · граблі · квіз · джерела), «Траєкторія учня» (стан сайту після jNN−1 — з нього стартує «Зроби сам»), «Матриця вікон», «Наскрізні правила авторингу», **«Межі між сусідами»** (що належить сусідам — одним рядком з відсиланням).
3. `dev/build/012-jira/01-authoring/cross-findings.md` — усе, що вже знайшли інші (не шукати вдруге, не переказувати чужу тему); `00-research/facts-free-plan.md` (свій рядок перепровірити по живих доксах), `glossary.md`, `screens/README.md` і потрібні `screens/*.md`; `01-authoring/facts-map.md`.
4. Еталон — `modules/jira-01.html`, `jira-02.html`, `jira-03.html` (пройшли рецензента): звідти тон, щільність, як зроблені вікна, виноски ⚠ і квіз.

Що визначає якість:
- **Джерела рівно три** (§7): живі докси Atlassian через WebFetch (`…/<product>/resources/` або WebSearch з `allowed_domains: ["support.atlassian.com"]`; індексні `…/docs/` → 404; **таблиці планів — лише `curl` + `<td>`**), `screens/*.md` (написи у вікнах дослівно), живий sandbox — лише через кореневу сесію: чого бракує, попроси зняти у звіті й познач ⚠ у тексті. Кожен факт — з URL і датою в звіті.
- **Вікна:** 1–5 на урок за карткою; стан, заголовок «Jira Cloud · … · звірено 2026-MM-DD», мітки ① ② ③ (`aria-hidden`) + легенда `<ol>` (підписи, не речення); імена й ключі спейсів — лише з сюжету «Марципан» (розділ 3 програми); написи EN як на екрані; жодного `<img>`; апсели («Ask AI», «Improve Task», «Add agent», «See plans») — не функції Free.
- **Числа лімітів в уроці заборонені** (винятки: 10 користувачів, 3 агенти) — «обмежено, дивись довідник 3». Термінологія: поточна + колишня в дужках при першій згадці **в курсі** (пари work item (issue), space (project) вже введені в j01; work type (issue type) — j04? flow (automation rule) — j14 — звір з розділом 4 програми).
- **Квіз:** 5–6 питань на розуміння, `answer` з нуля, `explain` у кожного, правильна не найдовша й не найкоротша, індекси розкидані — перевір `python3 dev/build/007-quiz-distractors/check-quiz.py --files modules/jira-NN.html`.
- **Механіка:** `cd /Users/ander1.sage/Downloads/AIA && python3 dev/build/012-jira/01-authoring/check-lessons.py --pattern 'modules/jira-NN.html'` → 0 ✗ / 0 ! на фінальному файлі; `python3 -m json.tool` на `#quizData`; апостроф — як у базовому файлі.
- Читач не з ІТ: після кожного абзацу — «а звідки читач це знає?»; заборонені звороти (просто · очевидно · як відомо · елементарно · всі знають · звісно · не забудь, що).

**Що читати після хвиль 2–3 (додано 2026-09-18) — обовʼязково, до першого рядка:** `program.md` розділи **«Уточнення
до карток після хвилі 2» (п. 1–14, пріоритет над карткою)** і **«Наскрізні правила» 2а–2в** (вікна показують фірму
цілком; дані сюжету — лише з §3; числа поведінки продукту з цитатою — дозволені, ліміти плану — ні);
`cross-findings.md` розділи «Від авторів хвилі 2/3» і «Від рецензентів хвилі 2/3» (наскрізне + рядки з твоїм
номером). Правила, які коштували найдорожче: **у JQL — `project`**, докси `jql-fields` зіпсовані автозаміною ·
`Summary` як на екрані (докси — `Title`) · канонічний префікс бізнес-доксів `jira-software-cloud/docs/`
(`jira-core-cloud` — аліас) · **банер сторінки** («This page is for business/software/company-managed spaces»)
дивитись до цитування · `jira/kb/…` — за банером «Cloud Only»/«Data Center» · **цитати звіряти посимвольно** перед
здачею (витягнути всі `«…»` латиницею і прогнати `in` по збережених текстах; зняття тегів лишає пробіл перед
апострофом/комою — нормалізувати) · шаблон task management дає 2 статуси, project management — 3 · епіка в бізнес-
спейсі за замовчуванням немає (`+ Add work type`) · довідники називати **назвою**, не номером · для ①②③ писати
«цифри», не «мітки» (слово зайняте полем `Labels`) · терміни, що ловились без пояснення: гліф, туторіал, API,
чеклист, чіп, випадайки, рейка, полотно, мінімапа, прапорець, докси · **парні форми звертання** («зробив(ла)») ·
**маркери стилю у квізі в обидва боки** (знак лише в правильному — або лише правильна без знака; з хвилі 4 —
скрипт `python3 dev/build/012-jira/01-authoring/check-quiz-markers.py --files modules/jira-NN.html` → «Маркерів: 0») · робочі файли —
у власній підпапці скретчпада (`scratchpad/jNNdocs/`), бо паралельні агенти ділять одну папку.

**Додано після хвилі 4 (2026-09-19) — обовʼязково, до першого рядка:** `program.md` «Уточнення» **п. 15–16** і оновлений
§3 (`REM-6`, автор `FIN-1`/`FIN-2`, деталі форми `FIN-2`, **робочий канон статусів `FIN`: Open → Work in progress →
Waiting for customer → Done**); `cross-findings.md` «Від авторів хвилі 4» і «Від рецензентів хвилі 4». Що коштувало
найдорожче: **картка задає теми, а не факти** — картки фаз 4–5 відстали від продукту сильніше за ранні (гілок «за JQL»
не існує; картка про JSM Free суперечила таблиці планів; граблі без джерела): кожне твердження картки перевіряти, писати
за продуктом, заміну пояснювати у звіті §6 · **метод доксів, що дав 0 розбіжностей у 234 цитатах:** хаб
`…/<product>/resources/` → індекс живих слагів; сторінка — `curl -sL` → `<main>` без `<nav>` → зняти теги; таблиці —
розбір `<tr>/<td>` · **звірка цитат регістрочутлива** (без `lower()`); тег може розірвати слово (хибний MISS — звіряти
по сирому HTML); `&#x27;` не декодується сама; `developer.atlassian.com/…` мовчки редіректить — `%{url_effective}` ·
JSM-докси: банер team-managed / company-managed на кожній сторінці (слаги з `next-gen` — це team-managed) · термін —
**«службовий спейс (service space)»** («сервіс-спейс» — лише мова карток) · база знань JSM — «at a minimum, you'll
need to have a Free plan of Confluence on the same site» (слів «купувати не треба» не вживати) · живий Marketplace
читається без браузера (публічний REST `/rest/2/addons?text=…`, фасети сторінки пошуку в HTML) · у вікнах «за
документацією» кожен напис мусить мати опору в живих доксах; третього стану заголовка («знімок + докси») поки немає —
гібрид пояснювати в підписі · числа поведінки продукту (правило 2в) — лише з дослівною цитатою поруч · **головний клас дефектів хвилі 4 — твердження «зі здорового глузду» без джерела** («лист усе одно
надсилається», «резервна копія не рятує», «у довідці це скорочують до…», «категорія є лише тоді, коли…»): після кожного
абзацу питати не лише «звідки читач це знає», а й **«звідки це знаю я»** — немає цитати чи знімка → не стверджувати ·
пошук REST Marketplace за текстом може не повернути картку, яка існує, — перевіряти за ключем (`/rest/2/addons/<key>`).

**Додано після хвилі 5 (2026-09-19) — обовʼязково, до першого рядка:** `program.md` «Уточнення» **п. 17–18** і §3 у чинній
редакції; `cross-findings.md` «Від авторів хвилі 5» і «Від рецензентів хвилі 5». **Фірма сюжету з 2026-09-19 — «Марципан»**
(рішення власника; була «Кориця» — у старих звітах і записах це історія): рід чоловічий («Марципан задає сам»), відмінки —
«бухгалтерія Марципану», «у Марципані», «з Марципаном», «про Марципан»; адреса сайту сюжету — `marcypan.atlassian.net`;
дашборд з уроку 8 — «Марципан — тиждень». Рішення власника того ж дня — не переоткривати: статуси `FIN` — канон `Open → Work
in progress → Waiting for customer → Done`; авторка `FIN-1`/`FIN-2` — Соломія; «млин «Золотий колос»» лишається; термін —
«службовий спейс (service space)»; блок «Хто веде» — заглушка (посилання на профіль Champion не ставити); мітка в JQL — **у
лапках** (`labels != "архів" OR labels IS EMPTY`, канон j07). Що коштувало найдорожче: **Atlassian переписує сторінки на
ходу** (KB про видалені роботи змінилась за добу, довідка адмінки перейменовує «product» → «app») — цитати звіряти в день
здачі; докси MCP переїхали в `support.atlassian.com/atlassian-ai-gateway/` (хаб `…/resources/` працює й там, і для
`user-management`, `organization-administration`, `atlassian-account`); сторінки `user-management` містять **дві версії
тексту в одному HTML** («Centralized» / «Original») — шлях брати з однієї половини · цитат із U+2019 / U+2011 уникати
(обрізати з «…»); корпус звірки цитат чистити від текстів інших уроків · **без живого середовища нічого не вигадувати:** у
`term` — лише введення людини й реально отриманий вивід; вікно, написів якого немає в доксах, не малювати — проза з ⚠ і
сценарій дозйомки у звіті §5 · живість MCP-endpoint перевіряється без підключення (`curl -s -D - -o /dev/null <url>`: 401 +
`www-authenticate` = живий) · реальний `claude mcp add/remove/login/list` і будь-що, що змінює чи друкує конфігурацію
власника, — заборонено · якщо Write відмовив на `.md` — записати звіт через Bash heredoc · шлях скретчпада в кожній сесії
свій — коренева сесія підставляє актуальний у промпт.
**Для іспиту `j23`:** писати лише ПІСЛЯ рецензії j19–j22 (рішення власника); 26 питань, розподіл по фазах — `program.md`
«Фінальний іспит», без вікон і Mermaid, `passing_score` 85, нового матеріалу немає; форма питань ситуаційна («Марта створила
flow … і він не спрацював — що сталось?»), не «скільки»; автор читає всі 22 уроки у чинній редакції (закласти ≈ 500 тис.
токенів); безпечні теми питання про j22 — MCP як місток, доступ на Free з обмеженням частоти, що вирішує згода, читання
перед записом, `Ask AI` ≠ MCP; **не питати** про покоління endpoint-а, числа лімітів і все, що в уроках стоїть під ⚠;
обидва квіз-скрипти обовʼязкові (у 005 іспит мав маркери у 12 із 26 питань).
**Для довідників:** обіцянки уроків довідникам зібрані в `cross-findings.md` («Від рецензентів хвилі 3» → «Наскрізне», «Від
авторів хвилі 4/5», «Від рецензентів хвилі 4/5»); числа лімітів живуть ЛИШЕ в довіднику-карті й у `facts-free-plan.md`;
перша колонка `.ds-tbl` — лише ідентифікатор (`nowrap`), речення туди не класти; довідник-карта — після JSM/Confluence на
sandbox.

**Додано після хвилі 6 (2026-09-20: іспит j23, реєстр `open-claims.md`) — обовʼязково, до першого рядка:** `program.md` «Уточнення» **п. 19**; `cross-findings.md`
розділи «Закриття хвостів кореневою сесією (2026-09-20)», «Від автора іспиту j23» і «Реєстр незакритих тверджень без джерела»;
`01-authoring/open-claims.md` — рядки про свій урок. Що коштувало найдорожче: **категоричне «лише / завжди / жодного / не дасть»
про продукт без цитати поруч** — таких тверджень у курсі знайшлось шість, і на кожному стояв **правильний варіант квізу**
(«одинадцяту людину Jira додати не дасть» прожило пʼять хвиль і суперечило довідці про рахунки: «Adding more users will
automatically move you from a Free plan to a Standard plan trial»). Правила звідси: правильна відповідь квізу не стоїть на
твердженні під ⚠ і на категоричному слові без цитати; джерело часто лежить у **сусідньому** продукті доксів
(`subscriptions-and-billing`, `organization-administration`, `user-management`), а не в тому, про який урок; **довідка може
суперечити сама собі** (адреса сайту: «more than 3 times» проти «up to 15 times») — тоді число в урок не йде взагалі; KB-статті
з позначкою «The content of this article has been generated by AI» джерелом не брати — шукати людську сторінку ·
**публічна сторінка, яку малює браузер** (`curl` дає десятки символів), більше не блокер: попроси кореневу сесію у звіті §5 —
вона зніме її власним headless Chrome (так закрито сторінку сертифікацій у j22); sandbox і все за входом — як і раніше, лише
через власника · агент, що читає багато уроків (іспит, довідники), отримує від кореневої сесії **текстові зліпки уроків**
(маркери секцій, вікон, виносок, квіз із ✓) — читати їх, а цитати звіряти `grep`-ом по HTML.
**Для довідників (уточнено):** вхід — `01-authoring/ref-promises.md` (генерує `ref-promises.py`: кожне речення уроків, яке
відсилає до довідника за назвою, — довідник мусить містити саме обіцяне; на 2026-09-20: карта — 48 згадок із 22 сторінок,
автоматизація — 5, JQL — 2) + рядки «→ довідник …» у `cross-findings.md` + `open-claims.md` розділ 8 (довідка змінилась під
курсом: Rovo потребує підтвердженого бізнес-домену; список фільтрів у бічній панелі team-managed спейсу; хто створює спейс).

Звіт `reports/jNN.md` — за §9 контракту (8 розділів: файл · джерела · вікна · що описано словами · чого не знайшов підтвердження ⚠ · розбіжності · що в контракті незручно · знахідки для сусідів). Той самий зміст — фінальним повідомленням. Мова — українська.
