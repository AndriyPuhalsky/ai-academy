# Рецензія уроку `j12` «Практикум: шаблони під сферу»

**Зроблено 2026-09-18** агентом `aia-content-reviewer` (хвиля 3). Файл під рецензією —
`/Users/ander1.sage/Downloads/AIA/modules/jira-12.html`. Правився **лише** він; `program.md`,
`lesson-contract.md`, `cross-findings.md`, конфіг, інші уроки, `css/`, `js/` — тільки читання,
git не виконувався.

---

## 1. Вердикт

**Готово з зауваженнями.** Урок фактично чистий: усі **39** англомовних цитат простежені до
живих доксів Atlassian (`curl -sL`, 2026-09-18) або до `screens/23`; заборонених зворотів немає;
механіка проходить обидва чекери. Виправлено **13** місць (формулювання, атрибуція цитат,
порядок написів у вікні, дві внутрішні суперечності уроку). Лишаються: один висновок без
прямого джерела (він у тексті вже помʼякшений автором), дві неповні теми картки і три
розбіжності з сусідніми уроками, які рецензент закрити не може.

---

## 2. Непідтверджені факти

Перевірка робилась так: усі рядки в «…» з латиницею вигребені регуляркою й прогнані `in` по
текстах 20 сторінок доксів, знятих `curl -sL` 2026-09-18 (`<main>` → зняті `<script>`, `<style>`,
`<nav>` → зняті теги → нормалізовані апострофи U+2019 → U+0027 і пробіл перед комою/апострофом).
**Результат: 39 із 39 знайдено** (дві фрази — `Plan and deliver business projects`,
`Organize and track team or personal tasks` — звірені зі `screens/23`, вони з екрана, не з доксів).

### 2.1 Єдине твердження без прямого джерела (лишено, не правив)

> «У документації Jira такого не описано: шаблон приносить налаштування, а не роботу.
> Найпрямше це видно в описі власних шаблонів, де окремо перелічено, що саме *не*
> переноситься: «Work items from the source space». **Тобто новий спейс приходить порожнім.**»
> (блок 7, ЧаПи «Чи створює шаблон приклади карток?»)

Цитата справжня (`…/docs/custom-project-templates/`, 2026-09-18), але вона зі сторінки про
**власні шаблони Enterprise**, а не про звичайні шаблони галереї. Висновок «новий спейс
приходить порожнім» — інференція з невідповідного джерела. Автор це бачив (звіт §5 п. 1) і
поставив у тому ж абзаці страховку («Якщо ж у твоєму новому спейсі раптом виявились чужі
приклади, видали їх до того, як дивитись звіти»), тому читачеві нічого не обіцяно категорично.
**Чого бракує:** одного кліку в sandbox — створити спейс із ділового шаблону й подивитись, чи є
в ньому картки одразу (той самий пункт уже стоїть у `facts-free-plan.md`, «Дописано після хвилі 3»).

### 2.2 Перевірено й підтверджено (вибірково, щоб не шукали вдруге)

| Твердження уроку | Джерело (2026-09-18) |
| ---------------- | -------------------- |
| «Creating a new column on the Board will create an associated status…» + `Create status` праворуч від останньої колонки | `…/docs/add-rename-or-delete-a-column-in-team-managed-business-projects/` («Select Create status ( ) next to the rightmost column») |
| шлях у кошик: `More actions (•••)` → `Manage spaces` → `More actions` → `Move to trash` | `…/docs/delete-project/` — дослівно той самий порядок |
| «Only one category can be added to a work item», `+` і `More actions → Edit field` | `…/docs/categorize-work-items-in-the-list-view/` |
| «Your saved filters are specific to the space…», «you can only apply one at a time» | `…/docs/save-your-filters-in-business-projects/` |
| «The sample space is an IT service management template project…» | `jira-service-management-cloud/docs/test-explore-and-learn-with-your-sample-space/` (банер «Cloud Data Center», не KB) |
| два кроки / три кроки роботи у двох ділових шаблонах | `…/use-business-projects-for-task-management/`, `…-for-project-management/` |
| «Jira components are only available in company-managed spaces» проти поради фінансової сторінки | `…/what-are-jira-components/` + `…/use-business-projects-for-finance-projects/` |
| «кандидат = одна робота» у шаблоні добору | `…/use-business-projects-for-hr-projects/` («Create a work item for each candidate…») |
| 17 категорій · ~30 плиток · `Custom templates` [Enterprise] · `Import data ↗` · описи шести плиток | `screens/23-create-space-templates.md` (17 назв у CATEGORIES перелічені поіменно, у Work management — 30) |
| «кнопка `Create space` стояла в бічній панелі» | `screens/22-sidebar.md` (пункт 6 «Spaces» → кнопка **Create space +**) |
| три сезонні оновлення на рік | `facts-free-plan.md`, розділ «Зміни, які визначають стабільність» |

Числа лімітів у тексті відсутні (§0.1 п. 3): строк кошика й кредити Rovo віддані довіднику за
**назвою** («Карта інтерфейсу, глосарій і ліміти Free», 4 згадки), «довідник 3» у тексті немає.
Числа «сімнадцять категорій / близько трьох десятків плиток» подані з датою знімка — це опис
галереї, не ліміт плану; формулювання перевірив, воно датоване й у підписі, і в блоці 8.

---

## 3. Пропущені теми картки `j12`

Дописати може лише автор — для них потрібні джерела.

1. **Software-шаблони названі лише категорією.** Картка вимагає «software: Kanban/Scrum/bug
   tracking». В уроці є `Software development` у рейці вікна W1 і `Kanban` у ЧаПи («шаблон, дошка
   чи метод»); **`Scrum` і `bug tracking` як шаблони не згадані жодного разу**. Матеріал готовий:
   `what-are-the-project-templates` описує рівно три software-шаблони, `screens/23` дає плитки
   `Kanban` і `Scrum` у «Made for you» (це вже лежить у `cross-findings.md`, §8 автора j12).
   Межа з `j13` при цьому не порушується: там метод, тут — плитка в галереї.
2. **«Перейменувати статуси англійською» з теми «чистка шаблону» не прозвучало.** Блок 6 вчить
   прибирати зайві вигляди, стани й поля, але про перейменування станів (і про пастку «статуси
   народжуються мовою акаунта», яку курс уже показував у `j03`/`j10`) в уроці немає нічого.
3. **Дрібне:** у переліку ділових шаблонів картки є «продажі», в уроці цієї сфери немає ні в
   таблиці пʼяти сценаріїв, ні в плитках W1 (у галереї це `Sales pipeline`, `screens/23`).
   Пункт найлегший з трьох: один рядок у таблиці блоку 3 або плитка у вікні.

Решта тем картки прозвучала: майстер `Create space` · галерея за призначенням · ділові шаблони
(керування проєктом, облік задач, кампанії/контент, добір, бюджет) · service → `j18` ·
пʼять сценаріїв · чеклист із восьми кроків · sample data (у чесній формі) · перейменування й
чистка шаблону (частково, див. п. 2) · десять карток.

---

## 4. Що виправив сам (13 правок)

### Факт із показаним джерелом (3)

1. **Атрибуція входів у галерею.** Було: «…натиснути `Create space`. **Там же** описаний
   коротший шлях — значок `+` біля `Spaces`». На `create-a-new-project` такого шляху немає — він
   на сусідній сторінці: «Select the plus icon + in the space sidebar»
   (`…/docs/create-a-business-project/`, 2026-09-18). Стало: обидва додаткові входи віднесені до
   сусідньої сторінки, коротший подано дослівною цитатою.
2. **Розташування `Create with Rovo`.** Було: «Дві речі **в цій колонці**…» — але в лівій колонці
   галереї (`screens/23` перелічує її повністю) кнопки `Create with Rovo` немає. Докси кажуть
   «Under Space templates, select Create with Rovo» (`create-a-new-project`). Стало: «Дві речі в
   **цьому діалозі**…», `Custom templates` явно позначений як пункт лівої колонки, кнопка Rovo —
   з цитатою про її місце.
3. **Порядок категорій у вікні W1 приведений до `screens/23`.** Було: Work management ·
   Service management · Software development · Human resources · Finance · Marketing. На екрані (знімок
   `screens/23`, 2026-09-17) порядок інший: Software development · Service management ·
   **Work management** · Product management · Marketing · Customer service · Human resources ·
   Finance… Заголовок вікна каже «звірено», тож підмножина тепер іде у порядку екрана
   (Software development · Service management · **Work management** (активна) · Marketing ·
   Human resources · Finance); прозовий перелік під вікном вирівняний з тим самим порядком.

### Цитати (1)

4. **Вимоги до ключа розбиті на три дослівні фрагменти.** Було одне суцільне «be at least two
   characters long; start with an uppercase letter; contain only uppercase letters or numbers» —
   у доксах це маркований список із трьох рядків, крапок з комою там немає. Стало: «be at least two characters long», «start with an uppercase letter»,
   «contain only uppercase letters or numbers» (`edit-a-projects-details`).

### Внутрішні суперечності уроку (3)

5. **«Ключ — це дві-три великі літери»** суперечив власному сюжету курсу: `MARK` — чотири літери,
   і сам урок показує `MARK-8` у вікні W2. Докси верхньої межі не називають. Стало: «кілька
   великих літер перед номером роботи (`REM-2`, `MARK-8`)».
6. **Крок 4 проти блоку 5.** Блок 5 вимагає «щонайменше три стани», а сценарій «планер» у блоці 3
   стартує з шаблону на **два** кроки роботи — читач, який іде тим сценарієм, провалює перевірку,
   нічого не порушивши. У крок 4 додано одне речення: якщо шаблон дав два кроки, стан «уже роблю»
   додають саме тут (жодного нового факту — обидва факти вже в блоці 2 з цитатами).
7. **Крок 3 проти таблиці сценаріїв.** Таблиця блоку 3 двічі прописує батьківську роботу
   («кампанія як батьківська робота над постами», «етапи як батьківські роботи»), а крок 3 казав
   «нічого додавати не обовʼязково». У діловому спейсі типу для великої справи за замовчуванням
   немає: у довідці про такий шаблон у рядку `Work types` стоїть рівно «Task and subtask»
   (`use-business-projects-for-project-management`, 2026-09-18), решта типів додається кнопкою
   `+ Add work type` (`j11` вчить це поіменно, 5 згадок). Абзац із цим кроком додано перед
   «порожній крок теж результат». Це закриває наскрізну вимогу `cross-findings.md` («Будь-який
   «Зроби сам» з епіком у бізнес-спейсі мусить містити цей крок (j12, j13, j21)»).

### Формулювання й термінологія (6)

8. **Місток «стан» → «статус».** Курс до цього вживав «статус» (j10 — 103 рази), j12 пише «стан»
   (62 рази). При першій появі в блоці 1 тепер сказано: «перелік станів — тих самих статусів з
   уроку 10». Наскрізне вирівнювання — не правка одного файла (див. §7).
9. **Репліка Марти в блоці 1** суперечила траєкторії: вона просить «спейс під контент», хоча
   `MARK` існує з уроку 6 (і вікно W2 показує саме його «після збирання»). Переписано під той
   самий абзац, який каже, що жоден спейс не робили «цілком».
10. **Посилання на урок 2 щодо кредитів Rovo.** В `j02` слова «Rovo» немає взагалі (перевірив
    `grep`), зате там введене поняття **апсел**. Тепер речення веде читача саме до нього.
11. **Заголовок ЧаПи** «Kanban — це шаблон, **вигляд** чи метод?» розходився з власною
    відповіддю («метод… назва шаблону… назва **дошки**») і з карткою («і шаблон, і тип дошки, і
    метод»). Стало «шаблон, дошка чи метод?».
12. **Час читання** `≈ 22 хв` → `≈ 25 хв`: 5 126 слів — другий за обсягом урок курсу, а сусіди
    такого розміру (`j05` 4 847 → 25, `j11` 5 441 → 25) чесніші до читача.
13. **Переноси рядків** у трьох абзацах, які після правок вилізли за ширину файла.

Чого **не** чіпав: правильні відповіді, `q`, `answer`, `explain`, кількість питань квіза; текст
вікон поза порядком рейки; `#completeTitle`; жодного факту без джерела не додав.

---

## 5. Механіка

Обидва чекери — на **фінальному** файлі, після всіх правок.

```
$ python3 dev/build/012-jira/01-authoring/check-lessons.py --pattern 'modules/jira-12.html'
✓ jira-12.html  (61.3 KB)
Готово. З помилками: 0 із 1.        ← 0 ✗ / 0 !

$ python3 dev/build/007-quiz-distractors/check-quiz.py --files modules/jira-12.html
питань 6 | найдовша 0/6 (0.0 %) | +нічиї 0/6 | найкоротша 0/6 (0.0 %)
answer 0/1/2/3 = 1/2/2/1 | довж. прав./хибн. 93.2 / 91.1
```

| # | Пункт | Стан |
| - | ----- | ---- |
| 1 | `data-config="../jira.config.json"` на `<html>` (+ `data-course="jira"`) | ✅ |
| 2 | `data-module="j12"` на `<body>`; глобально унікальний | ✅ (`grep` по `modules/*.html` — рівно один файл) |
| 3 | `<meta viewport>` без `user-scalable` / `maximum-scale` | ✅ |
| 4 | Вісім блоків у порядку + квіз, у кожного `id` і `data-lesson` | ✅ `l1…l8`, `#quiz` |
| 5 | `.term--enter` / `.term--hero` | ✅ блоків `term` у файлі немає взагалі |
| 6 | (вікна замість термінала) `<img>`, `<svg>`, `<canvas>` у вікнах | ✅ немає; єдиний `<svg>` у файлі — бургер шапки з бази |
| 7 | Mermaid ≥ 1, текст вузлів у лапках, без `<br/>` | ✅ дві діаграми, всі вузли в лапках, `<br` — 0 |
| 8 | Квіз 5–6 · JSON валідний · `answer` з нуля · `explain` у кожного · довжина не підказує · індекс ≤ 40 % · маркери стилю | ✅ 6 питань, `json.loads` OK, `explain` 6/6, 0 %/0 %, максимум 2 із 6 (33 %); окремий прохід по маркерах: двокрапка стоїть у 2 з 4 варіантів (П2, П3, П4), «готово» в лапках — лише в дистракторах, однакових початків немає |
| 9 | Дата біля кожного екрана | ✅ три вікна: «звірено 2026-09-17», «за документацією, 2026-09-18» ×2 |
| 10 | Регістр шляхів — нижній | ✅ `../css/`, `../js/`, `../assets/`, `../jira.html`, `../jira.config.json` |

Додатково: апострофи — `U+0027` 40, `U+2019`/`U+2018`/`U+02BC` — 0. Кількість цифр ①②③ у вікнах
дорівнює кількості пунктів легенди (3/3, 2/2, 2/2). Слово «мітки» в прозі не вживається.
Заборонені звороти (`просто`, `очевидно`, `як відомо`, `елементарно`, `всі знають`, `звісно`,
`не забудь`) — **0 входжень**, шукав регуляркою по всьому файлу. Термінів із чорного списку
хвилі 2 (гліф, туторіал, API, чеклист, чіп, випадайки, рейка, полотно, прапорець) — 0.

**Дрібниця, яку не правив (системна):** у W1 цифри йдуть у DOM у порядку ③ (`Custom templates`) →
① (`Work management`) → ② (плитка), тобто нумерація не збігається з порядком читання. Рецензенти
хвилі 2 зафіксували те саме у `j05` W2 і віддали рішення кореневій сесії («якщо нумерувати за
читанням, правити j04 і j05 разом») — правити один файл наодинці тут шкідливо.

---

## 6. Що перевірити не вдалося

- **Живий sandbox** — доступу в рецензента немає (як і в автора). Усі екранні твердження звірені
  зі `screens/`, усі решта — з доксів.
- **Верстка вікон і таблиці** — CSS компонента «вікно» ще не існує (порядок 012: контент до
  дизайну), тому як `win__table` на 4 колонки поводиться на 390 px, ніхто не бачив.
- **Питання №4 квіза** (правильна відповідь «довелось відкрити кожну з десяти карток заново»)
  формально ігнорує масову зміну, якої вчив `j07` (до 1 000 робіт за раз). Правильну відповідь і
  `explain` рецензент не чіпає — рішення за автором або кореневою сесією.

### Що зняти в sandbox (доповнення до списку автора)

Автор просив пʼять кадрів (§8 його звіту): картки одразу після створення з ділового шаблону ·
поле ключа в майстрі · екран після `Use template` · меню `Group` на діловій дошці · колонка
`Category` і панель `Edit field`. **Додаю два:**

1. **Ліва колонка діалогу `Space templates` цілком, з підзаголовками `CATEGORIES` і `PRODUCTS`** —
   зараз у вікні W1 це один список без груп, а порядок категорій узятий зі `screens/23` текстом.
   Один знімок закриє і порядок, і групування (потрібно ще й дизайну — див. §7 автора).
2. **Діалог `Create status` на діловій дошці** (крок 4 «Зроби сам») — вибір категорії статусу в
   момент створення описаний словами з доксів, живого екрана немає в жодного уроку курсу.

---

## 7. Знахідки для сусідів

Файл `cross-findings.md` не редагував.

- **j12 → j04, кореневій сесії:** дедлайни тих самих робіт `REM` розходяться між уроками —
  `REM-4` «Вивіска: замовити макет» у `j04` має `Sep 22`, у `j12` — `Oct 10, 2026`; `REM-6`
  «Меблі: доставка й монтаж» у `j04` — `Oct 10`, у `j12` — `Oct 24, 2026`. Поруч уже є той самий
  розлад поза j12: `REM-5` — `Sep 30` у `j04` і `Sep 15, 2026` у `j08`. Дати в `program.md` §3 не
  зафіксовані, тож це наскрізне рішення (або «спейс живе й дати рухаються» як правило, або один
  канон дат у програмі), а не правка одного файла.
- **j12 → j01, j05, j06:** `MARK-6` «Флаєри для другої точки» у трьох попередніх уроках стоїть на
  **МЛ**, у вікні W2 `j12` — на **СБ**. Або переприсвоєння свідоме (тоді варте пів речення в
  тексті), або вирівнювати.
- **j12 → усім, кореневій сесії:** термінологія **«стан» проти «статус»**. `j09` — 60 «статус»,
  `j10` — 103, `j11` — 4; `j12` — 62 «стан» і 4 «статус». Обидва слова законні, але для читача не
  з ІТ це дві різні речі, поки їх хтось не зʼєднає. Я поставив місток одним рядком у блоці 1
  (`j12`), далі — рішення на весь курс.
- **j12 → j09:** для третього входу в майстер є **дослівна цитата** — «Select the plus icon + in
  the space sidebar» (`…/docs/create-a-business-project/`, 2026-09-18); у `cross-findings` він
  записаний переказом.
- **j12 → j13, j21:** крок `+ Add work type` для великої справи в **діловому** спейсі тепер стоїть
  у `j12` (блок 4, крок 3) з опорою «Work types — Task and subtask»
  (`use-business-projects-for-project-management`). Формулювання можна брати як є.
- **j12 → j13:** тема «Scrum / bug tracking як шаблони галереї» лишилась незакритою в `j12`
  (див. §3 п. 1) — якщо `j13` уже називає ці плитки, достатньо перехресного посилання, а не
  повторення.
- **j12 → довідник «Карта інтерфейсу, глосарій і ліміти Free»:** підтверджую список автора
  (17 категорій · 4 продукти · ~30 плиток станом на 2026-09-17 · `Custom templates` = Enterprise ·
  кошик спейсів 60 днів · `Create with Rovo` = 10 кредитів) — усі ці числа в уроці **відсутні**,
  як і вимагає §0.1 п. 3.
- **j12 → дизайну (підтверджую §7 автора):** у стані `form` бракує контейнера «діалог із бічною
  колонкою», групи пунктів колонки (`CATEGORIES` / `PRODUCTS`) і бейджа на плитці (зараз —
  `win__chip`); `win__tile` у цьому уроці несе **два** рядки (назва + речення опису), однорядкова
  плитка галерею розсипле. `win__topbar` у `j12` не вжитий — як і в усіх попередніх уроках.
