# 005 — AI Термінал: повний курс Claude Code CLI з нуля

## Блок 1 — заповнює власник

- **Статус:** конвеєр пройдено 2026-09-05 (QA 3/3) · чекає мержу в `main` — власник: «не зараз»
- **Заведено:** 2026-09-03
- **Платформа:** новий третій блок «AI Термінал» (окремий конфіг `claude-code.config.json`)
- **Дизайн:** `dev/design/005-ai-terminal/` — компонент «термінал» потоком Б зі схваленого
  прототипу, лендінг `claude-code.html` — окремий бриф

### ТЗ

Розписати повноцінний курс «AI Термінал» про Claude Code CLI — ідеальну інструкцію для
людини, яка взагалі нічого не знає про Claude Code, і яка в кінці володіє ним на
професійному рівні. Скрізь явні приклади роботи в терміналі, максимум діаграм і схем,
гарний дизайн з анімацією сторінки, тести, сертифікат.

### Рішення власника 2026-09-03 (відповіді на уточнення)

1. **Ритм:** каркас + 3 еталонні модулі як планка якості → показ власнику → далі хвилями
   по 3–4 модулі до всіх 22. Не «усі одним прогоном».
2. **Дизайн:** компонент «термінал» переносимо потоком Б зі схваленого прототипу в
   `~/Desktop/ai-terminal-course.html`; на лендінг `claude-code.html` — окремий бриф з
   анімацією; сторінки модулів — наявна дизайн-система + GSAP-рух.
3. **Перевірка знань:** 5–6 питань на модуль з поясненням кожної відповіді · у кожному
   уроці блок «зроби сам» із критерієм «як перевірити, що вийшло» · окремий **фінальний
   іспит-модуль** (~25 питань, вищий `passing_score`) → сертифікат за 22 модулі + іспит.
4. **Джерело екранів:** реальний запуск безпечних команд локально + звірка з офіційною
   документацією. GUI-скріншоти (VS Code, Desktop, `/usage`) поки не робимо.

### Рішення власника 2026-09-03, ухвалені після звірки фактури

5. **Програма — 22 модулі + фінальний іспит**, а не 16. Обидва дослідники незалежно довели,
   що 16 не вміщають матеріал. Повна програма — `00-research/program.md`, **вона головніша
   за план на робочому столі**. Наслідок, названий прямо: зникає відповідність «заняття
   школи N = модуль cNN»; мапа зв'язку — в кінці `program.md`.
6. **Курс орієнтується на канал `latest`**, не на homebrew stable. Локальний `claude`
   перевстановлюється офіційним інсталятором **до** початку авторингу — інакше екрани
   знімаються зі збірки, що відстає від доксів на ~20 версій.
7. **Три довідники — окремі сторінки без квізу й прогресу**, поза таблицею `modules`
   і поза сертифікатом: усі слеш-команди й клавіші · таблиця 33 hook-подій · ключі
   `settings.json`.

### Рішення власника 2026-09-05 (прогін конвеєра після авторингу)

Курс написаний повністю, конвеєр по 005 ще не проходив. Вхід — `../../design/005-ai-terminal/SUMMARY.md`,
розділ Д5 (Д5.0 — межа «текст не чіпати», Д5.4 — хвости, Д5.8 — довідники).

8. **Обсяг цього прогону — чотири речі разом:** (а) три довідники за Д5.8; (б) QA всього курсу
   на живому dev-превʼю — 26 сторінок + лендінг; (в) міграція `005-2` (назви шести модулів);
   (г) `#navProgress` на `claude-code.html` — показувати «Прогрес: N/23».
9. **Усе роблять агенти конвеєра** (PM → бекендер ∥ фронтендер → QA), Opus 5, `effort: xhigh`.
   Коренева сесія лише оркеструє й комітить чуже.
10. **Вступний блок про дату й версію** на кожному з трьох довідників — **не видаляти, а перенести
    в кінець дослівно** (уточнення до Д5.8.2: «лише в кінці» означало саме це). Абзац про третю
    колонку таблиць у `-ref-commands` («Тому в третій колонці таблиць… вважай це поломкою») —
    дослівно в розділ `#how`. Кінцівки трьох сторінок після цього мають один вигляд —
    без жодного нового слова.
11. **Другий «Зміст» усередині тексту `-ref-hooks`** (розділ `#toc` з підписами до пунктів)
    після появи липкого змісту збоку — прибрати; **підписи перенести в бічний зміст**, а не
    викинути.
12. **Фронтендеру: правило «немає макета — немає верстки» тут не блокує.** Макета на довідники
    немає й не буде (дизайн їх не малювала — Д5.8). Еталон — наявна сторінка проєкту
    `claude-code-ref-commands.html`; це складання з наявних компонентів за наявним зразком,
    тобто виїмка з правила №1. Рамка широка (Д5.8.1), але текст — Д5.0: жодного слова, крім
    переміщень у п. 10–11.
13. **Міграція `005-2` — застосовує бекендер сам, через Chrome** (Supabase Dashboard → SQL
    Editor, метод 005-1). Це **разовий дозвіл власника** саме на цей файл, як 2026-09-03 для
    005-1; решта правил чинна: читання — через Supabase MCP (автентифікований 2026-09-05),
    жодного `apply_migration`, жодного DDL поза файлом, старі назви для блоку відкату зняти з
    живої бази до застосування, після — запис у `tg/CHANGELOG.md`.
14. **QA пише в продову базу — дозволено в таких межах:** агент **реєструє один новий тестовий
    акаунт** поштою (на проді `mailer_autoconfirm = true`, лист не потрібен; `full_name`
    «QA Тест 005»; у звіті — «тестовий акаунт №1», без адреси) · проходить квізи **c01→c23 по
    порядку** — серверна послідовність є предметом перевірки · **сертифікат видати** і
    перевірити через `verify.html` — власник свідомо дозволив справжній сертифікат з публічним
    кодом · усі створені рядки — у розділ «Створені рядки» звіту · сповіщення в Telegram про
    реєстрацію — очікуване, не дефект.
15. **Що QA не оформлює дефектом 005:** найдовший варіант = правильна відповідь (Д5.4 п.8,
    платформне, окрема задача) і «поріг квіза не є воротами» (Д5.4 п.9, поведінка платформи).
16. **Довідники звіряти з еталоном `-ref-commands`**, не з макетом; уроки й лендінг — з
    варіантом B у `dev/design/005-ai-terminal/04-variants/`.

### Рішення власника 2026-09-05 після QA кола 1

17. **D-01 — обгорнути всі 100 таблиць у 23 уроках**, не лише 14 сторінок, що переповнювались
    (зроблено `02a7818`; там же три additive-правила `overflow-wrap` у `css/custom.css`, бо на
    5 сторінках розпирали нерозривні рядки в `<code>`, а не таблиці).
18. **D-02 — вузол «Фінальний іспит» на карті стає посиланням** на `modules/claude-code-23.html`,
    вигляд варіанта B зберігається, стан «пройдено» для `c23` малюється (`06ea5cf`).
19. **D-03 — вхід у три довідники з футера всіх 23 уроків**, підписи з `claude-code.config.json`
    (`a6a9755`).
20. **QA коло 2 — із записом у продову базу:** дозвіл п. 14 підтверджений у діалозі саме на цей
    прогін (один тестовий акаунт, квізи c01→c23, справжній сертифікат).

21. **Ширина лендінгу `claude-code.html` — лишити як є.** Фронтендер заміряв: контейнер той
    самий, що в роадмапу (1152 px), відчуття вузькості дає рейка осі й hero на ≥1600 px;
    власник спершу обрав ширший контейнер (О2), потім передумав — правка не вносилась.

22. **D-04 і D-05 закриті бекендером до мержу** (`3d2eeac`, `2a1aeaa`): JPEG замість PNG
    (28 МБ → 0,4 МБ) і `doc.link()` над кодом та URL перевірки — у сертифікатах усіх трьох
    курсів, бо `js/certificate.js` спільний.
23. **Тестовий сертифікат видалений із `certificates` за дорученням власника** (через `/tasks`,
    «зроби самостійно як виключення»; `6ee0dfc`, `005-3-cleanup-qa-certificate.sql`) — щоб QA
    міг видати новий уже з посиланням і проклацати його. Значення рядка — у приватному
    `dev/005-qa-certificate-deleted.local.md`, поза git.
24. **QR у сертифікатах не працював ніколи** (`certificate.html` тягне `qrcode@1.5.3/build/…` →
    404, у проді те саме) — фронтендер міняє на `1.4.4`.
25. **QA коло 3 — зі справжнім новим сертифікатом:** власник сам увійшов тестовим акаунтом
    (прогрес 23/23), QA повторно позначає c23, скачує PDF, клікає посилання, читає QR.

### Чого точно НЕ робити

- Не переписувати наявні курси (AI Академія, AI Architect) — лише крос-посилання.
- Не змінювати `js/auth.js`, `js/config.js`, `js/module.js`, `js/progress.js`,
  `js/quiz.js`, `js/certificate.js`, `js/verify.js`, `wrangler.toml` — каркас
  перевикористовується як є. Виняток лише якщо воротний крок покаже дефект у `submit_quiz`.
- Не вигадувати вивід терміналу з голови — тільки реальний запуск або дослівна документація.
- Ніяких `apply_migration` від агента: міграції — файлами, застосовує власник.

---

## Блок 2 — заповнює PM-агент (`01-plan.md`), тут лише посилання

- **Розвідка фактури:** `00-research/facts-c01-c08.md`, `00-research/facts-c09-c16.md`
- **Аудит каркаса:** `00-research/framework-audit.md`
- **Матеріал із методички:** `00-research/source-material.md`
- **План:** `01-plan.md`
- **Що піде в бекенд:** `02-backend/report.md`
- **Що піде у фронтенд:** `03-frontend/report.md`
- **Вердикт тестування:** `04-qa/report.md`
