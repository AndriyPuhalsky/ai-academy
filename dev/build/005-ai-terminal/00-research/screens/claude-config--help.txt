$ claude config --help
# (у 2.1.236 підкоманди `config` НЕМАЄ — вивід ідентичний головному `claude --help`,
#  тобто аргумент розібраний як промпт. Порівняй із контролем нижче.)
Usage: claude [options] [command] [prompt]

Claude Code - starts an interactive session by default, use -p/--print for
non-interactive output

$ claude bogus-nonexistent --help   # контроль
Usage: claude [options] [command] [prompt]

Claude Code - starts an interactive session by default, use -p/--print for
non-interactive output
