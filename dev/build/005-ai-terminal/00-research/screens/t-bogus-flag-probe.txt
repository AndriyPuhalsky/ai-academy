# Проба існування прапорців у локальній Claude Code 2.1.236
# Дата: 2026-09-03. Метод: підставити свідомо неіснуючий --zzz-bogus ПІСЛЯ досліджуваного прапорця.
# Якщо парсер скаржиться на --zzz-bogus -> досліджуваний прапорець ВІДОМИЙ.
# Якщо скаржиться на сам прапорець -> його НЕМАЄ у цій версії.
# Жодна сесія не запускається: commander падає на етапі розбору аргументів.

$ claude --version
2.1.236 (Claude Code)

$ claude --advisor --zzz-bogus
Error: The model "--zzz-bogus" cannot be used as an advisor.
  (exit=0)

$ claude --max-turns --zzz-bogus
error: option '--max-turns <turns>' argument '--zzz-bogus' is invalid. must be a number
  (exit=0)

$ claude --restricted --zzz-bogus
error: unknown option '--restricted'
  (exit=0)

$ claude --init-only --zzz-bogus
error: unknown option '--zzz-bogus'
  (exit=0)

$ claude --init --zzz-bogus
error: unknown option '--zzz-bogus'
  (exit=0)

$ claude --maintenance --zzz-bogus
error: unknown option '--zzz-bogus'
  (exit=0)

$ claude --append-system-prompt-file --zzz-bogus
Error: Append system prompt file not found: /Users/ander1.sage/Downloads/AIA/dev/--zzz-bogus
  (exit=0)

$ claude --permission-mode --zzz-bogus
error: option '--permission-mode <mode>' argument '--zzz-bogus' is invalid. Allowed choices are acceptEdits, auto, bypassPermissions, manual, dontAsk, plan.
  (exit=0)

$ claude --fork-session --zzz-bogus
error: unknown option '--zzz-bogus'
  (exit=0)


# ПОПРАВКА (додано після прогону): рядки "(exit=...)" вище НЕДОСТОВІРНІ —
# вивід йшов через `| head -3`, тож $? показує код завершення head, а не claude.
# Достовірний сигнал тут — САМ ТЕКСТ помилки парсера, і він однозначний.
