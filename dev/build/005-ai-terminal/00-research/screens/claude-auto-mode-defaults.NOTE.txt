$ claude auto-mode defaults

Дослівний вивід цієї команди (63 КБ JSON) СВІДОМО не покладений у репозиторій.

Чому: це повний текст політики класифікатора auto mode — чужа внутрішня конфігурація
продукту. Секретом вона не є (зняти може будь-хто, у кого встановлений Claude Code),
але репозиторій публічний, курсу дослівний дамп не потрібен, а правило №3 майстерні
каже: сумнівне — не публікувати.

Де лежить локально (не в гіті):
  <scratchpad сесії>/claude-auto-mode-defaults.json.txt

Що з нього треба курсу — уже переказано своїми словами у facts-c01-c08.md, розділ c06:
класифікатор читає CLAUDE.md, має списки allow/block, і саме він, а не діалог дозволу,
вирішує в auto mode. Для модуля c07 («Дозволи й межа довіри») цього досить.

Зняти заново за потреби: claude auto-mode defaults
