# Хвиля 3 — промпти `c16`, `c17` (пара) і `c19`–`c22` (четвірка)

Складено 2026-09-04 після хвилі 2. Рішення власника: **критичні `c16` і `c17` запускати
окремою парою**, щоб вони не конкурували за модель із легшими.

## Розвідка джерел, зроблена кореневою сесією (не повторювати)

Розміри сторінок, зняті `curl … | wc -c` 2026-09-04:

| сторінка | розмір | що це означає для автора |
| --- | --- | --- |
| `hooks.md` | **317 647 Б** | цілком у контекст не влазить — читати частинами |
| `errors.md` | **352 242 Б** | те саме, найбільша сторінка доксів |
| `troubleshoot-install.md` | 63 280 Б | велика, але читабельна |
| `debug-your-config.md` | 23 464 Б | компактна, читати цілком |
| `troubleshooting.md` | 11 953 Б | компактна, читати цілком |
| `goal.md` | 15 367 Б | для `c20` |
| `hooks-reference.md` | **4 Б** | **сторінки НЕ ІСНУЄ** — не витрачай на неї запит |

**Техніка читання великої сторінки** (обовʼязкова для `hooks.md` і `errors.md`):
```
curl -sL https://code.claude.com/docs/en/hooks.md -o /tmp/hooks.md   # один раз на диск
grep -n '^#' /tmp/hooks.md                                          # зміст із номерами рядків
sed -n '1200,1400p' /tmp/hooks.md                                   # тільки потрібний розділ
```
Так у контекст іде десяток кілобайт замість трьохсот. **`curl | head` не годиться** — потрібне
місце може бути в кінці.

**🔴 з `c17` знято.** `facts-map.md` каже «джерела немає» — це про фактуру, не про докси.
У доксів є цілий розділ **Troubleshooting** із чотирьох сторінок: `troubleshooting.md`,
`debug-your-config.md`, `troubleshoot-install.md`, `errors.md`. Плюс `authentication.md`.

---

## `c16` — Перехоплювачі подій (hooks)

```
Пишеш ОДИН урок курсу «AI Термінал» (Claude Code CLI). Папка /Users/ander1.sage/Downloads/AIA, гілка dev.

**Твій модуль: `c16` «Перехоплювачі подій (hooks)»** → файл `modules/claude-code-16.html`.

## Читай першим, повністю
1. `dev/build/005-ai-terminal/01-authoring/lesson-contract.md` — **розділ 0 головний**.
2. `dev/build/005-ai-terminal/01-authoring/cross-findings.md` — **свій розділ `c16`**, підрозділ **«`c16` · Hooks і `c18` · MCP — спільна знахідка»** в кінці файла, і «Для всіх авторів — доповнення».
3. `dev/build/005-ai-terminal/00-research/program.md` — рядок `c16` у Фазі 3 + рядки 142–159.
4. `modules/claude-code-01.html` — еталон форми.

## ⚠️ Головна технічна пастка твого модуля
**`hooks.md` важить 317 647 байт.** Прочитати її цілком — забити контекст до того, як напишеш перший рядок. Роби так:
```
curl -sL https://code.claude.com/docs/en/hooks.md -o /tmp/hooks.md
grep -n '^#' /tmp/hooks.md          # зміст із номерами рядків
sed -n 'X,Yp' /tmp/hooks.md         # тільки потрібні розділи
```
**`hooks-reference.md` не існує** (віддає 4 байти) — не витрачай на неї запит.

## Твоє джерело
`00-research/facts-c09-c16.md`, розділ **`c12`, рядки 477–789** — **найбільший блок фактури в усьому курсі**.

**Що всередині за програмою:** три рівні конфігу · matchers · формат входу й виходу JSON · **семантика exit-кодів** · **8 подій, які реально потрібні** — детально · три робочі приклади · безпека hooks.

⚠️ **Подій 33, а урок — не довідник.** Повна таблиця всіх 33 подій іде в окрему сторінку `claude-code-ref-hooks.html`. Твоє завдання — навчити **механіці** (коли спрацьовує, що на вході, що на виході, як exit-код змінює поведінку) на восьми подіях, які потрібні насправді. Решту назви й пошли в довідник.

**Уже знайдено сусідами — бери й перевіряй:**
- **пісочниця Bash не накриває перехоплювачі подій** — вони запускаються поза нею. Саме тому докси вимагають контейнер або VM для `--dangerously-skip-permissions`: «so that file tools, MCP servers, and hooks are also inside the boundary». Це межа, про яку читач мусить знати ДО того, як напише свій перший перехоплювач;
- `debug-your-config.md` має розділ саме про те, чому перехоплювач не спрацьовує, і команду `/hooks`.

**Межа:** `.claude/` і `settings.json` — `c13`; скіли — `c14`; безпека загалом — `c22`. Ти про **автоматичну реакцію на подію**.

## Джерела — правило без винятків
**Памʼять моделі джерелом не є.** Докси — `curl -sL https://code.claude.com/docs/en/<сторінка>.md`; WebFetch великі сторінки обрізає. Індекс — `https://code.claude.com/docs/llms.txt`.

Три джерела: живі докси · `screens/*.txt` · власний запуск. Біля кожного екрана — версія або ОС. **Не знайшов підтвердження — не пиши.**

⚠️ **Перевіряти існування прапорця треба БЕЗ `--help`.**
⚠️ **Не став справжніх перехоплювачів на цій машині.** У `.claude/` живе робоча конфігурація власника, і перехоплювач — це код, що виконується сам. Приклади пиши й перевіряй **тільки** у тимчасовій папці з власним `CLAUDE_CONFIG_DIR`, або взагалі не запускай, а показуй із доксів.

## Механіка (деталі — контракт)
`data-module="c16"` · viewport без `user-scalable` · **без** `.term--enter`, `.term--long`, `.term--hero` · апостроф тільки `'` (U+0027) · `<kbd>` немає · заголовок `<h2><span class="lesson-no">N</span>Назва</h2>` · квіз 5–6 питань, `answer` з нуля, `explain` у кожного.
`<h4>` **має стиль, можна ставити**; `<blockquote>` стилю НЕ має. JSON — звичайний `<pre><code>`.
Чекер **не бачить**, що всі правильні відповіді квіза стоять на одному індексі — **перевір руками**.

**Перевір себе:** `python3 dev/build/005-ai-terminal/01-authoring/check-lessons.py`

## Не роби
Нічого поза своїм файлом. Ніяких git-команд.

## Звіт
Фінальним повідомленням (`.md` заблоковані): URL доксів і версія · команди й ОС · що описав словами і чому · **чого не знайшов підтвердження** · **які події віддав довіднику** · знахідки для сусідів.
```

---

## `c17` — Коли все зламалось: діагностика

```
Пишеш ОДИН урок курсу «AI Термінал» (Claude Code CLI). Папка /Users/ander1.sage/Downloads/AIA, гілка dev.

**Твій модуль: `c17` «Коли все зламалось: діагностика»** → файл `modules/claude-code-17.html`.

## Це найважливіший модуль курсу за задумом
Дослідник назвав його «найбільшою дірою плану»: без нього людина, у якої щось не працює, виходить із курсу назавжди. Ти вчиш не списку команд, а **способу мислення**: як зрозуміти, що саме зламалось, і **як перевіряти факт живою командою, а не статтею з мережі**.

## Читай першим, повністю
1. `dev/build/005-ai-terminal/01-authoring/lesson-contract.md` — **розділ 0 головний**.
2. `dev/build/005-ai-terminal/01-authoring/cross-findings.md` — **свій розділ `c17`** і особливо підрозділ **«`c17` · Діагностика — три незалежні докази тези модуля»** в кінці файла. Там три перевірені наживо факти, які сусіди здобули випадково — це готовий кістяк твого головного блоку. Плюс «Для всіх авторів — доповнення».
3. `dev/build/005-ai-terminal/01-authoring/facts-map.md` — рядок `c17`: **джерело розкидане по семи місцях фактури**.
4. `dev/build/005-ai-terminal/00-research/program.md` — рядок `c17` + примітку «Чому `c17` існує і чому саме тут» + рядки 142–159.
5. `modules/claude-code-01.html` — еталон форми.

## Твоє джерело — і добра новина про нього
**У `facts-map.md` твій рядок позначено 🔴 «джерела немає». Це стосується ФАКТУРИ, не доксів.** Розвідка кореневої сесії 2026-09-04 знайшла цілий розділ **Troubleshooting**:

| сторінка | розмір | як читати |
| --- | --- | --- |
| `debug-your-config.md` | 23 КБ | **головна для тебе**, читати цілком |
| `troubleshooting.md` | 12 КБ | читати цілком |
| `troubleshoot-install.md` | 63 КБ | читати цілком, але вона довга |
| `errors.md` | **352 КБ** | **цілком НЕ читати** — див. техніку нижче |
| `authentication.md` | — | для проблем із входом |

**Техніка для `errors.md`:**
```
curl -sL https://code.claude.com/docs/en/errors.md -o /tmp/errors.md
grep -n '^#' /tmp/errors.md      # зміст
sed -n 'X,Yp' /tmp/errors.md     # потрібний розділ
```

Плюс фактура, розкидана по семи місцях: `facts-c09-c16.md` р. 22, 38, 166–203, 437–504, 760–804, 1152, 1293–1339. І екрани: `screens/claude-doctor*`, `screens/b-doctor-help*`.

**Що всередині за програмою:** `claude doctor` · `--safe-mode` · `--debug` / `--debug-file` · `--setting-sources` · `claude project purge` · як зрозуміти, який конфіг переміг · **як перевіряти факт живою командою, а не статтею з мережі**.

## Три подарунки від сусідів — перевір і став у центр уроку
1. **Жива команда каже те, чого немає в документації.** `claude -c < /dev/null` на 2.1.236 падає з кодом 1 і текстом `Error: No deferred tool marker found in the resumed session…`, якого **немає в `errors.md`** (автор `c09` перевірив пошуком по всій сторінці).
2. **В одній програмі три різні реакції на сміття** (автор `c10` перевірив наживо): невідомий **прапорець** глушиться, якщо поруч `--help` · невірне значення **`--effort`** попереджає з переліком допустимих · невірне значення **`--model`** мовчить до першого мережевого запиту, бо локальної перевірки назви моделі не існує.
3. **`claude auth status`** (автор `c11`): формат за замовчуванням — `--json`, `--text` це опція, код виходу `0` при наявному вході. Перший крок «а я взагалі увійшов». ⚠️ `--text` друкує `Email:` — **в урок його вивід не клади**.

## Джерела — правило без винятків
**Памʼять моделі джерелом не є** — і в модулі про перевірку фактів це було б смішно. Докси — `curl -sL …`; WebFetch великі сторінки обрізає.

Три джерела: живі докси · `screens/*.txt` · власний запуск. Біля кожного екрана — версія або ОС.

⚠️ **`claude doctor` і `claude project purge` показують стан цієї машини.** `doctor` можна запускати й показувати, якщо у виводі немає особистих шляхів; `purge` **без прапорця не запускай ніколи** — він видаляє. `--dry-run` безпечний.
⚠️ **Перевіряти існування прапорця треба БЕЗ `--help`** — це і твоя тема, і твій інструмент.

## Механіка (деталі — контракт)
`data-module="c17"` · viewport без `user-scalable` · **без** `.term--enter`, `.term--long`, `.term--hero` · апостроф тільки `'` (U+0027) · `<kbd>` немає · заголовок `<h2><span class="lesson-no">N</span>Назва</h2>` · квіз 5–6 питань, `answer` з нуля, `explain` у кожного.
`<h4>` **має стиль, можна ставити**; `<blockquote>` стилю НЕ має.
Чекер **не бачить**, що всі правильні відповіді квіза стоять на одному індексі — **перевір руками**.

**Перевір себе:** `python3 dev/build/005-ai-terminal/01-authoring/check-lessons.py`

## Не роби
Нічого поза своїм файлом. Ніяких git-команд. Не видаляй нічого в `~/.claude/`.

## Звіт
Фінальним повідомленням: URL доксів і версія · команди й ОС · що описав словами і чому · **чого не знайшов підтвердження** · **що з розділу Troubleshooting виявилось непридатним для новачка** · знахідки для сусідів.
```
