# Хвиля 2 — промпти `c14`, `c15`, `c18`

Складено 2026-09-04. `c12` і `c13` цієї ж хвилі беруться з `relaunch-c08-c13.md` без змін.

**Розклад трьох хвиль** (15 сторінок, що лишились після `c08`–`c11`):

| Хвиля | Сторінки | Чому так |
| --- | --- | --- |
| 2 | `c12`, `c13`, `c14`, `c15`, `c18` | важкий один — `c13` |
| 3 | `c16`, `c17`, `c19`, `c21`, `c22` | `c16` (33 події) + `c17` (🔴 джерела немає) — найризикованіша |
| 4 | `c20`, `c23`, три довідники | `c23` — іспит по всьому курсу, пишеться лише коли всі уроки готові |

Промпти хвиль 3 і 4 складати **після** відповідної попередньої хвилі — щоб увібрати
свіжі знахідки в `cross-findings.md`.

---

## `c14` — Навички, команди, плагіни

```
Пишеш ОДИН урок курсу «AI Термінал» (Claude Code CLI). Папка /Users/ander1.sage/Downloads/AIA, гілка dev.

**Твій модуль: `c14` «Навички, команди, плагіни»** → файл `modules/claude-code-14.html`.

## Читай першим, повністю
1. `dev/build/005-ai-terminal/01-authoring/lesson-contract.md` — **розділ 0 головний**.
2. `dev/build/005-ai-terminal/01-authoring/cross-findings.md` — **свій розділ `c14`** + «Для всіх авторів».
3. `dev/build/005-ai-terminal/00-research/program.md` — рядок `c14` у Фазі 3 + рядки 142–159.
4. `modules/claude-code-01.html` — еталон форми. І `modules/claude-code-13.html`, якщо він уже існує, — сусід про `.claude/`, щоб не переказувати.

## Твоє джерело
`00-research/facts-c09-c16.md`, розділ **`c10`, рядки 208–348**. Плюс екрани: усі `screens/b-plugin-*`, `screens/claude-plugin*`.

**Що всередині за програмою:** `SKILL.md` і frontmatter · хто викликає — людина чи Claude · **злиття з `.claude/commands/`**: при збігу імен виграє скіл, а `commands/` — це урізані скіли · progressive disclosure · маркетплейси, `/plugin`.

**Уже знайдено сусідами — бери й перевіряй:**
- після компакції тіла навичок вставляються назад із лімітом **5 000 токенів на навичку і 25 000 сумарно**; обрізання лишає **початок** файла → найважливіше треба класти вгору `SKILL.md`;
- **перелік описів навичок після `/compact` не перевставляється** — лишаються тільки реально викликані. Це єдиний виняток серед стартового вмісту.

**Межі з сусідами:** `CLAUDE.md` і памʼять — `c12`; структура `.claude/` і `settings.json` — `c13`; субагенти — `c15`; hooks — `c16`. Ти про **те, що розширює вміння Claude**. Місток одним реченням можна, розділ — ні.

## Джерела — правило без винятків
**Памʼять моделі джерелом не є.** Докси — **`curl -sL https://code.claude.com/docs/en/<сторінка>.md`**; WebFetch великі сторінки обрізає. Індекс — `https://code.claude.com/docs/llms.txt`.

Три джерела: живі докси · `screens/*.txt` · власний запуск. Біля кожного екрана — версія або ОС. **Не знайшов підтвердження — не пиши.**

⚠️ **Докси переписували ПІСЛЯ дослідження** — фактура старіє на очах; жива сторінка виграє завжди, змістовна розбіжність іде в урок як факт.
⚠️ **Перевіряти існування прапорця треба БЕЗ `--help`** — `--help` глушить помилку про невідоме імʼя й друкує звичайну довідку, ніби він існує.
⚠️ **Не показуй скіли й плагіни цієї машини** — там робочий інструментарій власника. Приклади пиши власні, у тимчасовій папці.

## Механіка (деталі — контракт)
`data-module="c14"` · viewport без `user-scalable` · **без** `.term--enter`, `.term--long`, `.term--hero` · апостроф тільки `'` (U+0027) · `<kbd>` немає, клавіші в `<code>` · заголовок `<h2><span class="lesson-no">N</span>Назва</h2>` · квіз 5–6 питань, `answer` з нуля, `explain` у кожного. YAML-frontmatter і Markdown-фрагменти — звичайний `<pre><code>`.

**Перевір себе:** `python3 dev/build/005-ai-terminal/01-authoring/check-lessons.py`

## Не роби
Нічого поза своїм файлом. Ніяких git-команд.

## Звіт
Фінальним повідомленням (`.md` заблоковані): URL доксів і версія · запущені команди й ОС · що описав словами і чому · **чого не знайшов підтвердження** · **де провів межу з `c13` і `c15`** · знахідки для сусідніх модулів.
```

---

## `c15` — Субагенти, worktrees, паралельність

```
Пишеш ОДИН урок курсу «AI Термінал» (Claude Code CLI). Папка /Users/ander1.sage/Downloads/AIA, гілка dev.

**Твій модуль: `c15` «Субагенти, worktrees, паралельність»** → файл `modules/claude-code-15.html`.

## Читай першим, повністю
1. `dev/build/005-ai-terminal/01-authoring/lesson-contract.md` — **розділ 0 головний**.
2. `dev/build/005-ai-terminal/01-authoring/cross-findings.md` — **свій розділ `c15`** + «Для всіх авторів».
3. `dev/build/005-ai-terminal/00-research/program.md` — рядок `c15` у Фазі 3 + рядки 142–159.
4. `modules/claude-code-01.html` — еталон форми. І `modules/claude-code-07.html` — сусід про дозволи, бо класифікатор твоєї теми стосується напряму.

## Твоє джерело
`00-research/facts-c09-c16.md`, розділ **`c11`, рядки 349–476**. Плюс екран `screens/claude-agents--help*`.

**Що всередині за програмою:** `/agents`, формат файлу агента · коли субагент виправданий, а коли шкодить · фонові агенти · **worktrees** (`.worktreeinclude`, `isolation: worktree`) · agent teams (experimental) · **dynamic workflows** · **cross-session messaging**.

**Уже знайдено сусідами — бери й перевіряй:**
- класифікатор перевіряє субагента **тричі**: опис задачі до старту, кожну дію, повну історію на виході;
- **`permissionMode` у frontmatter субагента ігнорується в auto mode** — тобто написане в файлі агента може мовчки не діяти. Це той самий клас пастки, що й `"auto"` у `settings.json` із `c13`.

**Найцінніше, що ти можеш дати читачеві** — чесна відповідь на «коли субагент шкодить». Паралельність не безкоштовна: контекст не спільний, а результат приходить текстом. Це не теорія — це головне рішення, яке людина ухвалює щодня.

**Межі:** скіли й плагіни — `c14`; hooks — `c16`; розписання, `/loop`, Remote Control — `c20`; headless і CI — `c19`. Ти про **паралельну роботу всередині сесії**.

## Джерела — правило без винятків
**Памʼять моделі джерелом не є.** Докси — **`curl -sL https://code.claude.com/docs/en/<сторінка>.md`**; WebFetch великі сторінки обрізає. Індекс — `https://code.claude.com/docs/llms.txt`.

Три джерела: живі докси · `screens/*.txt` · власний запуск. Біля кожного екрана — версія або ОС. **Не знайшов підтвердження — не пиши.**

⚠️ **Докси переписували ПІСЛЯ дослідження.** Жива сторінка виграє.
⚠️ **Перевіряти існування прапорця треба БЕЗ `--help`.**
⚠️ **Не показуй агентів цієї машини** — у `.claude/agents/` лежить робочий конвеєр власника. Приклади пиши власні, у тимчасовій папці. **Субагентів самому не запускай** — це з'їдає ліміт акаунта; форму файлу показуй з доксів.

## Механіка (деталі — контракт)
`data-module="c15"` · viewport без `user-scalable` · **без** `.term--enter`, `.term--long`, `.term--hero` · апостроф тільки `'` (U+0027) · `<kbd>` немає · заголовок `<h2><span class="lesson-no">N</span>Назва</h2>` · квіз 5–6 питань, `answer` з нуля, `explain` у кожного.

**Перевір себе:** `python3 dev/build/005-ai-terminal/01-authoring/check-lessons.py`

## Не роби
Нічого поза своїм файлом. Ніяких git-команд. Не створюй worktrees у справжньому репозиторії.

## Звіт
Фінальним повідомленням: URL доксів і версія · команди й ОС · що описав словами і чому · **чого не знайшов підтвердження** · знахідки для сусідніх модулів.
```

---

## `c18` — MCP: зовнішні інструменти

```
Пишеш ОДИН урок курсу «AI Термінал» (Claude Code CLI). Папка /Users/ander1.sage/Downloads/AIA, гілка dev.

**Твій модуль: `c18` «MCP: зовнішні інструменти»** → файл `modules/claude-code-18.html`. Це перший модуль Фази 4.

## Читай першим, повністю
1. `dev/build/005-ai-terminal/01-authoring/lesson-contract.md` — **розділ 0 головний**.
2. `dev/build/005-ai-terminal/01-authoring/cross-findings.md` — **свій розділ `c18`** + «Для всіх авторів».
3. `dev/build/005-ai-terminal/00-research/program.md` — рядок `c18` у Фазі 4 + рядки 142–159.
4. `modules/claude-code-01.html` — еталон форми. І `modules/claude-code-06.html` — сусід про контекст, бо відкладені схеми інструментів — це саме про вікно контексту.

## Твоє джерело
`00-research/facts-c09-c16.md`, розділ **`c13`, рядки 790–893**. Плюс екрани: усі `screens/b-mcp-*`, `screens/claude-mcp*`, `screens/t-mcp-add-help*`.

**Що всередині за програмою:** що це насправді · `claude mcp add` і всі форми · транспорти · scope (local / project / user) · `.mcp.json` · `/mcp` · OAuth · tool search і відкладені інструменти · живі приклади (Chrome, Supabase) · **prompt injection через MCP**.

**Уже знайдено сусідами — бери й перевіряй:**
- схеми інструментів MCP **відкладені за замовчуванням** — у контекст ідуть лише імена, а повне визначення підвантажується на вимогу; `ENABLE_TOOL_SEARCH=auto|false` це змінює;
- субагент успадковує **всі** визначення інструментів MCP від батьківської сесії й може забити своє вікно ще до першого ходу (джерело — `errors.md`).

**Prompt injection — твоя тема, але не весь її обсяг.** Загальна безпека, великі репо й «куди далі» — `c22`. Ти пояснюєш конкретний механізм: сервер MCP віддає текст, текст потрапляє в контекст, а текст може містити інструкції. Місток на `c22` одним реченням.

## Джерела — правило без винятків
**Памʼять моделі джерелом не є.** Докси — **`curl -sL https://code.claude.com/docs/en/<сторінка>.md`**; WebFetch великі сторінки обрізає (`mcp.md` велика). Індекс — `https://code.claude.com/docs/llms.txt`.

Три джерела: живі докси · `screens/*.txt` · власний запуск. Біля кожного екрана — версія або ОС. **Не знайшов підтвердження — не пиши.**

⚠️ **Докси переписували ПІСЛЯ дослідження.** Жива сторінка виграє.
⚠️ **Перевіряти існування прапорця треба БЕЗ `--help`.**
⚠️ **На цій машині справжні сервери MCP підключені** (браузер, Supabase) — у їхніх конфігах токени й ключі проєкту власника. **`.mcp.json` цієї машини й вивід `claude mcp list` із реальними серверами в урок не йдуть.** Приклади пиши власні, у тимчасовій папці. Нічого не додавай і не видаляй у справжній конфігурації MCP.

## Механіка (деталі — контракт)
`data-module="c18"` · viewport без `user-scalable` · **без** `.term--enter`, `.term--long`, `.term--hero` · апостроф тільки `'` (U+0027) · `<kbd>` немає · заголовок `<h2><span class="lesson-no">N</span>Назва</h2>` · квіз 5–6 питань, `answer` з нуля, `explain` у кожного. JSON-фрагменти — звичайний `<pre><code>`.

**Перевір себе:** `python3 dev/build/005-ai-terminal/01-authoring/check-lessons.py`

## Не роби
Нічого поза своїм файлом. Ніяких git-команд. Не чіпай справжні `.mcp.json` і `~/.claude.json`.

## Звіт
Фінальним повідомленням: URL доксів і версія · команди й ОС · що описав словами і чому · **чого не знайшов підтвердження** · **де провів межу з `c22`** · знахідки для сусідніх модулів.
```
