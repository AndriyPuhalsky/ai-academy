# Перезапуск `c08`–`c13` — готові промпти

**Складено 2026-09-04**, коли партію зупинили на 96% ліміту акаунта.

> ⚠️ **Це перезапуск, а не продовження.** Вбитий підагент не відновлюється: транскрипт
> зникає, `SendMessage` на нього віддає `No transcript found`. Шість агентів цієї партії
> мертві остаточно; їхнє дослідження втрачено. Нижче — промпти, з яких стартують **нові**.
>
> Вони стартують із кращої позиції, ніж перша партія (`c02`–`c07`): контракт із
> розвʼязаною суперечністю про інтерактивні екрани, `facts-map.md` і `cross-findings.md`
> на 309 рядків уже написані.

## Як запускати

```
Agent(subagent_type: "aia-content-author", prompt: <текст нижче>)
```

`model` і `effort` **не передавати** — вони у визначенні агента (`claude-opus-5` /
`high`). Симлінк у `AIA/.claude/agents/` уже стоїть.

**Розмір партії.** Перша шістка пройшла повністю; друга дала **4 обриви з 6** (watchdog,
600 с без прогресу) ще до ліміту. Шість одночасних — межа, і вона плаваюча. **Радимо
чотири**, а `c11` і `c13` (найважчі за джерелами) — не в одній четвірці.

**Після кожної партії:** `python3 dev/build/005-ai-terminal/01-authoring/check-lessons.py`,
потім `git status --short` на пошкодження, потім коміт **поіменно** (не `git add -A`, поки
інші агенти пишуть).

---

## Спільна частина всіх шести

Кожен промпт нижче вже містить її повністю — це просто нагадування, що саме там є:
розділ 0 контракту (читач не знає нічого) · `curl` замість WebFetch · заборона
`.term--enter` / `.term--long` / `.term--hero` · апостроф U+0027 · `.lesson-no` у заголовку ·
квіз 5–6 з `explain` · заборона виходити за свій файл і робити git · звіт фінальним
повідомленням.

---

## `c08` — Режим плану й пісочниця

```
Пишеш ОДИН урок курсу «AI Термінал» (Claude Code CLI). Папка /Users/ander1.sage/Downloads/AIA, гілка dev.

**Твій модуль: `c08` «Режим плану й пісочниця»** → файл `modules/claude-code-08.html`.

## Читай першим, повністю
1. `dev/build/005-ai-terminal/01-authoring/lesson-contract.md` — **розділ 0 головний**.
2. `dev/build/005-ai-terminal/01-authoring/cross-findings.md` — **свій розділ `c08`**: там автор `c07` явно провів межу й перелічив, що лишив тобі. Прочитай і розділ «Для всіх авторів».
3. `dev/build/005-ai-terminal/00-research/program.md` — рядки `c07` і `c08` (**обидва**, щоб бачити межу) + примітку «Чому `c07` і `c08` окремо» + рядки 142–159.
4. `modules/claude-code-07.html` — **сусідній урок, уже написаний.** Прочитай, щоб не переказувати його. І `modules/claude-code-01.html` — еталон форми.

## Твоє джерело
`00-research/facts-c01-c08.md`, розділ **`c06`, рядки 620–790** — **половина про Plan Mode й пісочницю**. Друга половина (режими, `/permissions`, protected paths, класифікатор) уже в `c07`. Плюс `screens/t-sandbox-collision-evidence.txt`.

**Що всередині за програмою:** Plan Mode як спосіб думати, а не кнопка · sandboxed Bash · `/sandbox` · пісочниця як **чесна альтернатива** вимкненню дозволів.

**Вільне для тебе повністю** (`c07` цього не займав): `Shift+Tab` у plan, `Ctrl+G`, три опції схвалення плану, `useAutoModeDuringPlan`, режими пісочниці (`auto-allow` / regular permissions), відсутність пісочниці на рідному Windows, «`/sandbox` is not a permission mode».

**На що можна коротко посилатись як на вже пояснене в `c07`:** інструменти як поняття, `deny → ask → allow`, класифікатор, critical/protected paths, `Blocked by classifier`, пороги 3/20.

## Джерела — правило без винятків
**Памʼять моделі джерелом не є.** Бери докси через **`curl -sL https://code.claude.com/docs/en/<сторінка>.md`** — це повний markdown; **WebFetch великі сторінки обрізає**. Індекс усіх сторінок — `https://code.claude.com/docs/llms.txt`.

Три джерела: живі докси · `00-research/screens/*.txt` · власний запуск через Bash. Біля кожного екрана — версія або ОС. **Не знайшов підтвердження — не пиши.**

⚠️ **Фактура застаріває на очах** — уже спіймано кілька розбіжностей за одну добу. Живі докси виграють завжди; змістовна розбіжність іде в урок як факт.

⚠️ **Перевіряти існування прапорця треба БЕЗ `--help`:** `--help` глушить помилку про невідоме імʼя прапорця й друкує звичайну довідку, ніби він існує.

## Механіка (деталі — контракт)
`data-module="c08"` · viewport без `user-scalable` · **без** `.term--enter`, `.term--long`, `.term--hero` · апостроф тільки `'` (U+0027) · `<kbd>` немає, клавіші в `<code>` · заголовок `<h2><span class="lesson-no">N</span>Назва</h2>` · квіз 5–6 питань, `answer` з нуля, `explain` у кожного.

**Перевір себе:** `python3 dev/build/005-ai-terminal/01-authoring/check-lessons.py`

## Не роби
Нічого поза своїм файлом. Ніяких git-команд.

## Звіт
Фінальним повідомленням (`.md` заблоковані): URL доксів і версія · запущені команди й ОС · що описав словами і чому · **чого не знайшов підтвердження** · **де провів межу з `c07`** · **знахідки для сусідніх модулів**.
```

---

## `c09` — Сесії, історія, відкат

```
Пишеш ОДИН урок курсу «AI Термінал» (Claude Code CLI). Папка /Users/ander1.sage/Downloads/AIA, гілка dev.

**Твій модуль: `c09` «Сесії, історія, відкат»** → файл `modules/claude-code-09.html`.

## Читай першим, повністю
1. `dev/build/005-ai-terminal/01-authoring/lesson-contract.md` — **розділ 0 головний**.
2. `dev/build/005-ai-terminal/01-authoring/cross-findings.md` — **свій розділ `c09`**: там уже лежать пʼять фактів від сусідів, яких немає у фактурі. Прочитай і «Для всіх авторів».
3. `dev/build/005-ai-terminal/00-research/program.md` — рядок `c09` у Фазі 2 + рядки 142–159.
4. `modules/claude-code-01.html` — еталон форми.

## Твоє джерело
`00-research/facts-c01-c08.md`, розділ **`c07`, рядки 791–953**.

**Що всередині за програмою:** `-c`, `-r`, `/rewind` · checkpointing · `/export` · де фізично лежить історія · що відкат **не** повертає.

**Уже знайдено сусідами — бери й перевіряй, не шукай удруге:**
- межі чекпойнтів ширші за фактуру: не відновлюються **симлінки й хардлінки** (попередження `Restored the code, but skipped N files`) і правки більшості субагентів;
- `/rewind` уміє **Summarize from here / Summarize up to here** — часткова компакція;
- після `/clear` розмову повертає не лише `/resume`, а й запис у меню `/rewind` (v2.1.191+);
- `~/.claude/projects/*.jsonl` · різниця `--continue`/`--resume` проти `--fork-session`/`/branch` · пікер `/resume` у worktrees;
- історія промптів зберігається **окремо для кожної робочої папки**; `Up` дістає промпти минулих сесій саме цієї папки.

## Джерела — правило без винятків
**Памʼять моделі джерелом не є.** Докси — **`curl -sL https://code.claude.com/docs/en/<сторінка>.md`**; WebFetch великі сторінки обрізає. Індекс — `https://code.claude.com/docs/llms.txt`.

Три джерела: живі докси · `screens/*.txt` · власний запуск. Біля кожного екрана — версія або ОС. **Не знайшов підтвердження — не пиши.**

⚠️ **Фактура застаріває на очах.** Живі докси виграють; змістовна розбіжність іде в урок як факт.
⚠️ **Історія — це файли на диску.** Якщо показуєш вміст `~/.claude/projects/`, памʼятай: там локальні шляхи, `session_id` і назви чужих проєктів. Не клади сирим — розшифровуй.

## Механіка (деталі — контракт)
`data-module="c09"` · viewport без `user-scalable` · **без** `.term--enter`, `.term--long`, `.term--hero` · апостроф тільки `'` (U+0027) · `<kbd>` немає · заголовок `<h2><span class="lesson-no">N</span>Назва</h2>` · квіз 5–6 питань, `answer` з нуля, `explain` у кожного.

**Перевір себе:** `python3 dev/build/005-ai-terminal/01-authoring/check-lessons.py`

## Не роби
Нічого поза своїм файлом. Ніяких git-команд.

## Звіт
Фінальним повідомленням: URL доксів і версія · команди й ОС · що описав словами і чому · **чого не знайшов підтвердження** · **знахідки для сусідніх модулів**.
```

---

## `c10` — Моделі й мислення

```
Пишеш ОДИН урок курсу «AI Термінал» (Claude Code CLI). Папка /Users/ander1.sage/Downloads/AIA, гілка dev.

**Твій модуль: `c10` «Моделі й мислення»** → файл `modules/claude-code-10.html`.

## Читай першим, повністю
1. `dev/build/005-ai-terminal/01-authoring/lesson-contract.md` — **розділ 0 головний**.
2. `dev/build/005-ai-terminal/01-authoring/cross-findings.md` — **свій розділ `c10`** + «Для всіх авторів».
3. `dev/build/005-ai-terminal/00-research/program.md` — рядок `c10` у Фазі 2 + рядки 142–159.
4. `modules/claude-code-01.html` — еталон форми. І `modules/claude-code-06.html` — сусід про контекст, щоб не переказувати.

## Твоє джерело
`00-research/facts-c01-c08.md`, розділ **`c08`, рядки 954–1114**.

**Що всередині за програмою:** Opus / Sonnet / Haiku / Fable · `/model`, opusplan · thinking modes і рівні effort · fast mode · як обирати під задачу.

**⚠️ Твоя фактура застаріла найсильніше з усіх.** Автор `c06` спіймав: у розділі доксів «Default auto-compact thresholds» уже згадані **Opus 4.8 і Opus 5**, яких у `facts-c01-c08.md` немає взагалі — там лише Sonnet 4.6 / Opus 4.6. **Перелік моделей бери виключно з живих доксів**, фактуру використовуй лише як підказку, де шукати.

**Уже знайдено сусідами:** пороги авто-компакції **прив'язані до моделі** (Sonnet 5 ≈967K; частина моделей — 200K), `CLAUDE_CODE_DISABLE_1M_CONTEXT=1` повертає на 200K, вибір `[1m]` — теж твоя тема. `c06` назвав «мільйон» одним абзацом і послався на тебе.

**Межа:** скільки це коштує — модуль `c11`. Ти пояснюєш, **чим моделі відрізняються і як обирати**; ціни, ліміти й `/usage` — не твоє. Місток одним реченням можна.

## Джерела — правило без винятків
**Памʼять моделі джерелом не є** — а в модулі про моделі це найнебезпечніше місце всього курсу: назви й номери версій змінюються швидше за все інше. Докси — **`curl -sL https://code.claude.com/docs/en/<сторінка>.md`**; WebFetch великі сторінки обрізає. Індекс — `https://code.claude.com/docs/llms.txt`.

Три джерела: живі докси · `screens/*.txt` · власний запуск. Біля кожного екрана — версія. **Не знайшов підтвердження — не пиши.**

## Механіка (деталі — контракт)
`data-module="c10"` · viewport без `user-scalable` · **без** `.term--enter`, `.term--long`, `.term--hero` · апостроф тільки `'` (U+0027) · `<kbd>` немає · заголовок `<h2><span class="lesson-no">N</span>Назва</h2>` · квіз 5–6 питань, `answer` з нуля, `explain` у кожного.

**Перевір себе:** `python3 dev/build/005-ai-terminal/01-authoring/check-lessons.py`

## Не роби
Нічого поза своїм файлом. Ніяких git-команд.

## Звіт
Фінальним повідомленням: URL доксів і версія · команди й ОС · що описав словами і чому · **чого не знайшов підтвердження** · **де фактура розійшлась із доксами** (для цього модуля це головне) · знахідки для сусідів.
```

---

## `c11` — Скільки це коштує

```
Пишеш ОДИН урок курсу «AI Термінал» (Claude Code CLI). Папка /Users/ander1.sage/Downloads/AIA, гілка dev.

**Твій модуль: `c11` «Скільки це коштує»** → файл `modules/claude-code-11.html`.

## Читай першим, повністю
1. `dev/build/005-ai-terminal/01-authoring/lesson-contract.md` — **розділ 0 головний**.
2. `dev/build/005-ai-terminal/01-authoring/cross-findings.md` — **свій розділ `c11`**: там уже лежить майже готовий кістяк від трьох сусідів. Прочитай і «Для всіх авторів».
3. `dev/build/005-ai-terminal/01-authoring/facts-map.md` — розділ «`c11` — пʼять місць, з яких збирати». **Твоє джерело розсипане, це головна складність модуля.**
4. `dev/build/005-ai-terminal/00-research/program.md` — рядок `c11` у Фазі 2, примітку «Чому гроші окремим модулем» + рядки 142–159.
5. `modules/claude-code-01.html` — еталон форми.

## Твоє джерело — пʼять місць
| Що | Де |
| --- | --- |
| `/usage`, `/cost` як команди | F1 § `c04`, р. 419–445 |
| Ціна контексту, prompt caching | F1 § `c05`, р. 528–615 |
| Ціни й вибір між моделями | F1 § `c08`, р. 1036–1098 |
| Ліміти підписок, висновки | F1 р. 1115–1201 |
| Витрати у звʼязці з безпекою й великими репо | F2 § `c16`, р. 1094–1137 |

(F1 = `00-research/facts-c01-c08.md`, F2 = `facts-c09-c16.md`)

**Що всередині за програмою:** ліміти підписок · `/usage`, `/cost` · prompt caching · **ціна контексту** · як не спалити ліміт за годину · коли API-ключ дешевший за підписку.

**Чому модуль існує окремо** (з програми): у старому плані ціна була розмазана по чотирьох модулях. Для безкоштовного курсу з масовою аудиторією це критична тема, і вона одна.

**Уже знайдено сусідами — бери й перевіряй:**
- `costs.md` → **«Why usage climbs in a long session»** — сім причин (компакція, cache misses, scheduled tasks, cross-session messages, goal check-ins, agent teammates). Готовий кістяк розділу.
- `prompt-caching.md` → **«Compacting the conversation»**: `/compact` на теплому кеші коштує частку від розміру контексту; після перерви, довшої за життя кешу, той самий `/compact` перечитує всю історію як некешований ввід. **Найдорожчий `/compact` — при відновленні старої сесії.**
- `costs.md` → «When a developer asks about a limit»: чотири різні ситуації і чому `/model` рятує від модельного ліміту, але не від сесійного й тижневого.
- `claude auth status --text` віддає `Login method: …` — тип плану видно з термінала; код виходу 0/1.
- **Виклики класифікатора рахуються в токени** на Enterprise / Claude API / Bedrock / Agent Platform / Foundry. Читання й правки в робочій папці класифікатор оминає.
- ⚠️ **`/status` змінився:** фактура цитує стару версію, живі докси кажуть інше. Бери живу.

## Джерела — правило без винятків
**Памʼять моделі джерелом не є.** Докси — **`curl -sL https://code.claude.com/docs/en/<сторінка>.md`**; WebFetch великі сторінки обрізає (`costs.md` і `prompt-caching.md` великі). Індекс — `https://code.claude.com/docs/llms.txt`.

⚠️ **Конкретних цін і чисел лімітів публічно може не бути** — це вже зафіксовано у хвостах фактури. **Не вигадуй жодної цифри.** Якщо число не підтверджене, вчи **механіці**: від чого залежить витрата і як її побачити своєю командою. Це для цього модуля важливіше за будь-яку цифру, бо цифри застаріють, а механіка — ні.

Три джерела: живі докси · `screens/*.txt` · власний запуск. Біля кожного екрана — версія.

## Механіка (деталі — контракт)
`data-module="c11"` · viewport без `user-scalable` · **без** `.term--enter`, `.term--long`, `.term--hero` · апостроф тільки `'` (U+0027) · `<kbd>` немає · заголовок `<h2><span class="lesson-no">N</span>Назва</h2>` · квіз 5–6 питань, `answer` з нуля, `explain` у кожного.

**Перевір себе:** `python3 dev/build/005-ai-terminal/01-authoring/check-lessons.py`

## Не роби
Нічого поза своїм файлом. Ніяких git-команд. **Не показуй свій справжній `/usage` чи `auth status` із реальним акаунтом** — там видно план і статистику власника машини.

## Звіт
Фінальним повідомленням: URL доксів і версія · команди й ОС · що описав словами і чому · **які числа не підтвердились і тому не потрапили** (для цього модуля це головне) · знахідки для сусідів.
```

---

## `c12` — CLAUDE.md і памʼять

```
Пишеш ОДИН урок курсу «AI Термінал» (Claude Code CLI). Папка /Users/ander1.sage/Downloads/AIA, гілка dev.

**Твій модуль: `c12` «CLAUDE.md і памʼять»** → файл `modules/claude-code-12.html`. Це перший модуль Фази 3 «Налаштування під себе».

## Читай першим, повністю
1. `dev/build/005-ai-terminal/01-authoring/lesson-contract.md` — **розділ 0 головний**.
2. `dev/build/005-ai-terminal/01-authoring/cross-findings.md` — **свій розділ `c12`** + «Для всіх авторів».
3. `dev/build/005-ai-terminal/00-research/program.md` — рядок `c12` у Фазі 3 + рядки 142–159.
4. `modules/claude-code-01.html` — еталон форми.

## Твоє джерело
`00-research/facts-c09-c16.md`, розділ **`c09`, рядки 49–207**.

**Що всередині за програмою:** `/init`, `/memory` · як структурувати файл · локації (проєкт / `~/.claude` / вкладені / enterprise) · імпорти через `@` · auto memory · чого туди **не** писати.

**Уже знайдено сусідами — бери й перевіряй:**
- автопамʼять вантажить **перші 200 рядків або 25 КБ `MEMORY.md`, що настане раніше**;
- **правка `CLAUDE.md` посеред сесії не діє** до `/clear`, `/compact` або перезапуску (`prompt-caching.md`, розділ «Editing CLAUDE.md mid-session»). Це і граблі, і аргумент за структуру файла.

**Межа з сусідами:** `.claude/` як директорія і `settings.json` — модуль `c13`. Ти про **памʼять і контекст для Claude**, він — про **налаштування програми**. Навички (`SKILL.md`) — `c14`. Місток одним реченням можна, розділ — ні.

## Джерела — правило без винятків
**Памʼять моделі джерелом не є.** Докси — **`curl -sL https://code.claude.com/docs/en/<сторінка>.md`**; WebFetch великі сторінки обрізає. Індекс — `https://code.claude.com/docs/llms.txt`.

Три джерела: живі докси · `screens/*.txt` · власний запуск. Біля кожного екрана — версія або ОС. **Не знайшов підтвердження — не пиши.**

⚠️ **Обережно з власним `CLAUDE.md`.** На цій машині є справжній `CLAUDE.md` проєкту AIA і особиста памʼять власника. **Не показуй їх вміст в уроці** — приклади пиши власні, у тимчасовій папці.

## Механіка (деталі — контракт)
`data-module="c12"` · viewport без `user-scalable` · **без** `.term--enter`, `.term--long`, `.term--hero` · апостроф тільки `'` (U+0027) · `<kbd>` немає · заголовок `<h2><span class="lesson-no">N</span>Назва</h2>` · квіз 5–6 питань, `answer` з нуля, `explain` у кожного.

**Перевір себе:** `python3 dev/build/005-ai-terminal/01-authoring/check-lessons.py`

## Не роби
Нічого поза своїм файлом. Ніяких git-команд. Не редагуй справжні `CLAUDE.md` чи `~/.claude/`.

## Звіт
Фінальним повідомленням: URL доксів і версія · команди й ОС · що описав словами і чому · **чого не знайшов підтвердження** · знахідки для сусідів.
```

---

## `c13` — Уся `.claude/` і `settings.json`

> Цей модуль **устиг записати 28 KB із ~70** і був обірваний посеред UTF-8 символу; файл
> видалено. Пише з нуля.

```
Пишеш ОДИН урок курсу «AI Термінал» (Claude Code CLI). Папка /Users/ander1.sage/Downloads/AIA, гілка dev.

**Твій модуль: `c13` «Уся `.claude/` і `settings.json`»** → файл `modules/claude-code-13.html`.

## Читай першим, повністю
1. `dev/build/005-ai-terminal/01-authoring/lesson-contract.md` — **розділ 0 головний**.
2. `dev/build/005-ai-terminal/01-authoring/cross-findings.md` — **свій розділ `c13`**: там уже лежить перелік ключів від трьох сусідів. Прочитай і «Для всіх авторів».
3. `dev/build/005-ai-terminal/01-authoring/facts-map.md` — рядок `c13`, позначений 🟡: **джерело розсипане, головне лежить у `screens/`, а не у фактурі.**
4. `dev/build/005-ai-terminal/00-research/program.md` — рядок `c13` у Фазі 3 + рядки 142–159.
5. `modules/claude-code-01.html` — еталон форми.

## Твоє джерело
- `00-research/facts-c09-c16.md`: **р. 31–48** («`claude config` більше не існує» — обовʼязково), **р. 73–116**, **р. 714–765**;
- **`00-research/screens/b-settings-keys-full.txt` — головне джерело**, повний перелік ключів.

**Що всередині за програмою:** структура директорії · `settings.json` проти `settings.local.json` проти enterprise · **~15 робочих ключів** (повний перелік — довідник) · `/config` · порядок перекриття.

⚠️ **Ключів багато, а урок — не довідник.** Для повного переліку є окрема сторінка `claude-code-ref-settings.html`. Твоє завдання — навчити **порядку перекриття й тому, як зрозуміти, який конфіг переміг**, і показати ~15 ключів, що справді потрібні. Решту назви й пошли в довідник. Це і є «широта як покриття, а не щільність» із розділу 0.

**Уже знайдено сусідами — бери й перевіряй:**
- ⚠️ **`"auto"` і `"bypassPermissions"`, виставлені в `.claude/settings.json` чи `settings.local.json`, мовчки не діють** — сесія стартує в Manual. Найгостріша пастка теми.
- `.claude/settings.local.json` з **v2.1.211 читається з кореня git-репозиторію**, а не з поточної папки.
- Ключі оновлень: `autoUpdatesChannel` · `minimumVersion` · `requiredMinimumVersion`/`requiredMaximumVersion` (**лише managed**) · `DISABLE_AUTOUPDATER` проти `DISABLE_UPDATES` · `CLAUDE_CODE_PACKAGE_MANAGER_AUTO_UPDATE` · `CLAUDE_CODE_GIT_BASH_PATH` · `USE_BUILTIN_RIPGREP` · `CLAUDE_CONFIG_DIR`.
- `keybindingFlavor: "readline"` (v2.1.238+) · `respondToBashCommands: false` · `footerLinksRegexes`.
- `permissions.disableAutoMode` / `disableBypassPermissionsMode` = `"disable"` — але це матеріал `c22`, лише згадай.
- **`debug-your-config.md`** — цілісна сторінка «що дивитись, коли конфіг не діє». Формально це модуль `c17`, але тобі вона дасть точні назви команд перевірки.

**Обовʼязково:** `claude config` **більше не існує**, але `claude config --help` **не падає** — друкує кореневий хелп, бо `config` розбирається як промпт. Будь-який туторіал із `claude config set` мертвий.

## Джерела — правило без винятків
**Памʼять моделі джерелом не є.** Докси — **`curl -sL https://code.claude.com/docs/en/<сторінка>.md`**; WebFetch великі сторінки обрізає (`settings.md` і `env-vars.md` величезні — `env-vars.md` 480 КБ). Індекс — `https://code.claude.com/docs/llms.txt`.

⚠️ **Перевіряти існування ключа чи прапорця треба БЕЗ `--help`** — `--help` глушить помилку про невідоме імʼя й друкує звичайну довідку, ніби він існує.

Три джерела: живі докси · `screens/*.txt` · власний запуск. Біля кожного екрана — версія.

⚠️ **Не показуй справжні `.claude/settings.json` цієї машини** — там конфігурація проєкту власника. Приклади пиши власні, у тимчасовій папці.

## Механіка (деталі — контракт)
`data-module="c13"` · viewport без `user-scalable` · **без** `.term--enter`, `.term--long`, `.term--hero` · апостроф тільки `'` (U+0027) · `<kbd>` немає · заголовок `<h2><span class="lesson-no">N</span>Назва</h2>` · квіз 5–6 питань, `answer` з нуля, `explain` у кожного.
JSON-фрагменти в уроці — звичайний `<pre><code>`, окремого компонента для них немає.

**Перевір себе:** `python3 dev/build/005-ai-terminal/01-authoring/check-lessons.py`

## Не роби
Нічого поза своїм файлом. Ніяких git-команд. Не редагуй справжні `.claude/` чи `~/.claude/`.

## Звіт
Фінальним повідомленням: URL доксів і версія · команди й ОС · що описав словами і чому · **чого не знайшов підтвердження** · **які ключі віддав довіднику** · знахідки для сусідів.
```

---

## Далі, після цієї партії

| Партія | Модулі | Нота |
| --- | --- | --- |
| наступна | `c14`–`c19` | `c16` (33 hook-події) і `c17` (🔴 джерела немає) — важкі, не в одній четвірці |
| потім | `c20`–`c23` | `c20` 🔴 найтонший, але `cross-findings` дає таблицю з `platforms.md` |
| останнє | три довідники | команд **111**, hook-подій 33, ключі `settings.json` |
