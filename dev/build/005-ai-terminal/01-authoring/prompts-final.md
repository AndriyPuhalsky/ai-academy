# Фінальні промпти — `c21`, `c22`, іспит `c23` і три довідники

Складено 2026-09-04 після пари `c19`+`c20`. **Це все, що лишилось до кінця курсу: 6 сторінок.**

## Порядок запуску — важливий

| Крок | Сторінки | Чому саме так |
| --- | --- | --- |
| 1 | `c21`, `c22` | останні два уроки; `c22` — найбагатший на знахідки в курсі |
| 2 | три довідники | компіляція; **легші за уроки** (без квізу, без 8 блоків) — можна трьома разом |
| 3 | **іспит `c23`** | **лише коли всі 22 уроки існують** — питання по всьому курсу |

**Іспит не можна писати раніше.** Він читає готові уроки, а не фактуру.

---

## `c21` — Наскрізний проєкт і робочі звички

```
Пишеш ОДИН урок курсу «AI Термінал» (Claude Code CLI). Папка /Users/ander1.sage/Downloads/AIA, гілка dev.

**Твій модуль: `c21` «Наскрізний проєкт і робочі звички»** → `modules/claude-code-21.html`.

## Читай першим
1. `dev/build/005-ai-terminal/01-authoring/lesson-contract.md` — **розділ 0 і §9**.
2. `dev/build/005-ai-terminal/01-authoring/cross-findings.md` — свій розділ `c21` + **усі чотири розділи «Знахідки…» в кінці файла** (там блоки, адресовані тобі).
3. `dev/build/005-ai-terminal/00-research/program.md` — рядок `c21` + рядки 142–159.
4. `modules/claude-code-01.html` — еталон форми.

## Твоє джерело
`00-research/facts-c09-c16.md`, розділ **`c15`, рядки 989–1073** · екран `screens/claude-ultrareview--help.txt` · жива сторінка `best-practices.md` (`curl -sL https://code.claude.com/docs/en/best-practices.md`).

**Особливість модуля: ти пишеш ОСТАННІЙ змістовний урок.** Двадцять модулів уже існують — прочитай хоча б `modules/claude-code-04.html` (перша сесія) і `modules/claude-code-12.html` (памʼять), щоб твій наскрізний проєкт **спирався на вже вивчене, а не вводив нове**. Твоя робота — скласти з розрізнених умінь одну робочу звичку.

**Уже здобуте сусідами:**
- **`/goal` — обгортка над сесійним prompt-based Stop-хуком.** Недоступний там само, де недоступні хуки з файлів налаштувань, при `disableAllHooks: true` і при `allowManagedHooksOnly`; у кожному разі **пояснює причину**, а не мовчить *(`c20`)*.
- `claude -p "/goal <умова>"` проганяє весь цикл в одному виклику; при текстовому виводі нічого не друкує до кінця — виглядає зависанням, рятує `--output-format stream-json --verbose` *(`c20`)*.

## Джерела — правило без винятків
**Памʼять моделі джерелом не є.** Докси — `curl -sL …/<сторінка>.md`. Індекс — `llms.txt`.
Три джерела: живі докси · `screens/*.txt` · власний запуск. Версія або ОС біля кожного екрана. **Не знайшов підтвердження — не пиши.**
⚠️ **Ліміт власника дорогий** — мінімум оплачених `-p`. Прапорці перевіряй `--help`, `strings` і кодами виходу.

## Механіка
`data-module="c21"` · без `.term--enter/--long/--hero` · апостроф `'` (U+0027) · `<kbd>`, `<blockquote>`, `.table-wrap` НЕ існують · `<h4>` можна · квіз 5–6 питань, `explain` **без посилань на позицію варіанта** (`js/quiz.js` тасує), розкид правильних перевір руками.
**Перевір себе:** `python3 dev/build/005-ai-terminal/01-authoring/check-lessons.py`

## Не роби
Нічого поза своїм файлом. Ніяких git-команд.

## Звіт
URL доксів і версія · команди й ОС · що описав словами · **чого не знайшов підтвердження** · знахідки для довідників і для іспиту `c23`.
```

---

## `c22` — Безпека, великі проєкти, куди далі

```
Пишеш ОДИН урок курсу «AI Термінал» (Claude Code CLI). Папка /Users/ander1.sage/Downloads/AIA, гілка dev.

**Твій модуль: `c22` «Безпека, великі проєкти, куди далі»** → `modules/claude-code-22.html`.

## Читай першим
1. `dev/build/005-ai-terminal/01-authoring/lesson-contract.md` — **розділ 0 і §9**.
2. `dev/build/005-ai-terminal/01-authoring/cross-findings.md` — **свій розділ `c22` У ВСІХ ЧОТИРЬОХ розділах «Знахідки…»**. Це найбагатший модуль курсу за переданим матеріалом: девʼять авторів складали тобі факти. **Прочитай усе, перш ніж шукати самому.**
3. `dev/build/005-ai-terminal/00-research/program.md` — рядок `c22` + рядки 142–159.
4. `modules/claude-code-07.html` (дозволи) і `modules/claude-code-18.html` (MCP) — щоб не переказувати.

## Твоє джерело
`00-research/facts-c09-c16.md`, розділ **`c16`, рядки 1074–1187** · екран `screens/b-gateway-help.txt` · живі `security.md`, `sandboxing.md` (72 КБ), `large-codebases.md`, `permissions.md`.

**Що всередині за програмою:** prompt injection · межа довіри · великі репо · куди рухатись далі.

## Дев'ять готових блоків від сусідів — бери й перевіряй
1. **Найгостріше** *(`c19`)*: `permissions.md` → «What runs before you trust a folder». У `claude -p` над недовіреною текою **`permissions.allow` і `additionalDirectories` НЕ діють** (тихе попередження в stderr), а **хуки й `.mcp.json` — діють**. Небезпечне вмикається, обмежувальне мовчки вимикається. Там же: `projects["<path>"].hasTrustDialogAccepted` у `~/.claude.json`.
2. **Сканування виводу субагента** *(`c15`)*, з 2.1.210: сканер вставляє зворотну скісну в текст, що вдає `<system-reminder>`, `Human:`, `Assistant:`, і додає маркер `[harness: subagent output matched instruction-shaped pattern(s): …]`. **Докси прямо кажуть: це не заміна обмеженню повноважень.** Маркер видно наживо у звітах авторів цього ж курсу.
3. **`sandboxing.md` → «Security limitations»** *(`c08`)*: ескалація через `allowUnixSockets` (`/var/run/docker.sock` = доступ до хоста) · через широкий `allowWrite` у теки з виконуваними в `$PATH` · `enableWeakerNestedSandbox` · `allowAppleEvents`. Плюс розділ «Protect credentials» / «Mask credentials» (маскування змінних, re-signing AWS, `network.tlsTerminate`, ліміт 8 MiB).
4. **Мотив пісочниці** *(`c16`)*, р. 488 дослівно: команда, що правила б конфіги, «додала б хук або MCP-сервер, який Claude Code виконує **поза пісочницею**».
5. **Рутини** *(`c20`)*: текст із поля `text` приходить у `<routine-fire-payload>` як **недовірені дані**; мотив — захист від витоку токена.
6. **Канали** *(`c20`)*: «permission relay» означає, що **хто може писати в канал, може схвалювати виклики інструментів у твоїй сесії**.
7. **Remote Control** *(`c20`)*: поки підʼєднаний, **запис розмови зберігається на серверах Anthropic**; ZDR-організації увімкнути не можуть.
8. **Транскрипти не шифруються** *(`c09`)*: «OS file permissions are the only protection»; прочитаний `.env` або надрукований ключ **потрапляє в транскрипт**. Таблиця «що втратиш, якщо видалиш» — `claude-directory.md`.
9. **`/heapdump`** *(`c17`)*: `.heapsnapshot` містить **усю розмову й облікові дані**; у баг-репорт кладуть лише `-diagnostics.json`. Плюс `allowManagedHooksOnly` і `blockReadsOutsideWorkingDirectories` (v2.1.257+, діє **навіть у `bypassPermissions`**).

⚠️ **Матеріалу більше, ніж на урок.** Твоє завдання — не перелічити все, а дати **модель загрози**, зрозумілу новачкові: звідки береться чужий текст у контексті, що його зупиняє, а що ні. Решту назви й пошли в довідники.

## Джерела — правило без винятків
Докси — `curl -sL …`. Три джерела, версія біля екрана, **не знайшов — не пиши**.
⚠️ **Ліміт власника дорогий** — мінімум оплачених `-p`.
⚠️ **Нічого небезпечного на цій машині не демонструй.** Ніяких реальних `allowUnixSockets`, `--dangerously-skip-permissions`, `/heapdump`.

## Механіка
`data-module="c22"` · без `.term--enter/--long/--hero` · апостроф `'` · `<kbd>`/`<blockquote>`/`.table-wrap` НЕ існують · `<h4>` можна · квіз 5–6, `explain` **без позицій**, розкид перевір руками.
**Перевір себе:** `python3 dev/build/005-ai-terminal/01-authoring/check-lessons.py`

## Не роби
Нічого поза своїм файлом. Ніяких git-команд.

## Звіт
URL доксів і версія · команди й ОС · **що НЕ взяв і чому** (для цього модуля головне) · чого не знайшов підтвердження · знахідки для довідників.
```

---

## Три довідники — запускати трьома агентами разом

> **Довідник — НЕ модуль.** Без квізу, без прогресу, без восьми блоків, **немає рядка в
> `modules`** і в сертифікат не входить. Це сторінка, до якої повертаються.
> Спільна частина промпта нижче однакова; відрізняється лише блок «Твій довідник».

**Спільна частина (вставити в кожен із трьох):**

```
Пишеш ДОВІДНИК курсу «AI Термінал» — НЕ урок. Папка /Users/ander1.sage/Downloads/AIA, гілка dev.

## Чим довідник відрізняється від уроку — прочитай перше
`00-research/program.md`, розділ **«Довідники (НЕ модулі)»**: без квізу, без прогресу, поза сертифікатом, **немає рядка в таблиці `modules`**. Причина названа там же: «90 команд не вивчають лінійно й не здають тестом — до них повертаються. Квіз по довіднику тренував би памʼять, а не навичку».

**Отже:** скелета з восьми блоків **немає**, `data-module` **немає**, `quizData` **немає**. Форму бери з `modules/claude-code-01.html` (шапка, підключення стилів, футер, навігація), а вміст будуй як **таблицю, придатну для пошуку очима**: групування за темами, стабільний порядок колонок, якорі на розділи.

⚠️ **Токен `faint` у шапці — `#9A9184`, НЕ `#7A7164`** (контраст AA, задача 004). Копіюючи шапку з будь-якого уроку, перевір це значення.

## Правила, спільні з уроками
- `lesson-contract.md` — **розділ 0 (читач не знає нічого) і §9** чинні й тут;
- **памʼять моделі джерелом не є**: докси через `curl -sL https://code.claude.com/docs/en/<стор>.md`, індекс — `llms.txt`;
- **біля кожного числа — дата й версія.** Переліки застарівають за добу: ключів `settings.json` стало 223 замість 222 за одну ніч;
- апостроф тільки `'` (U+0027) · `<kbd>`, `<blockquote>`, `.table-wrap` НЕ існують · `<h4>` можна;
- ⚠️ **ліміт власника дорогий** — жодного оплаченого `-p` без потреби;
- нічого поза своїм файлом, ніяких git-команд.

## Звіт
URL доксів і версія · що і звідки взяв · **чого не зміг підтвердити** · що свідомо не вмістив.
```

### Довідник 1 — команди

```
**Твій файл: `claude-code-ref-commands.html`.**

Джерело: `00-research/screens/b-slash-commands-full.txt` (знято 2026-09-03, 2.1.236) + живий `commands.md` (163 КБ — читай розділами через `grep -n '^#'` + `sed`) + `keybindings.md`.

**Команд 111, а не «~90»** — це перевірено двічі й записано в `cross-findings`. Прапорців `claude --help` — **62 опції й 13 підкоманд** (теж перерахунок наживо).

**Обовʼязково — колонка «поведінка в `-p`»**, зібрана трьома авторами (див. «Знахідки пари `c19`+`c20`» у `cross-findings.md`): `/model sonnet`, `/effort`, `/fast`, `/color`, `/rename` приймають значення аргументом · `/mcp` друкує текстовий підсумок · `/config ключ=значення` міняє налаштування (усе **v2.1.205+**) · `/goal` працює (exit 0) · **`/status` і `/hooks` → `isn't available in this environment`** · **`/doctor` зависає** · `/login` недоступний.

**Псевдоніми, підтверджені наживо:** `/rewind` = `/checkpoint` = `/undo` · `/resume` = `/continue` · `/clear` = `/reset` = `/new` · `/schedule` = `/routines` · `/loop` = `/proactive` · `/teleport` = `/tp` · `/remote-control` = `/rc` · `/desktop` = `/app` · `/background` = `/bg` · `/mobile` = `/ios` = `/android` · `/list-agents` = `/peers` · `/tasks` = `/bashes` · `/stats` = `/usage`.

**Пастки, які довідник мусить позначити явно:** `/rate-limit-options` **не показується в меню** — треба набирати повністю · `/subtask` і `/fork` — **різні речі** з v2.1.212 · `/remote-control` до v2.1.206 віддавав `Unknown command`, коли ти не ввійшов · MCP-промпти показуються як `/servername:promptname (MCP)`, набираються також `/mcp__server__prompt`.
```

### Довідник 2 — hook-події

```
**Твій файл: `claude-code-ref-hooks.html`.**

Джерело: живий `hooks.md` — **317 647 Б, цілком НЕ читати**. Техніка:
`curl -sL https://code.claude.com/docs/en/hooks.md -o /tmp/hooks.md` → `grep -n '^#' /tmp/hooks.md` → `sed -n 'X,Yp'`. **`hooks-reference.md` не існує** (4 байти).
Плюс `facts-c09-c16.md`, розділ `c12`, і вже написаний `modules/claude-code-16.html` — щоб довідник **доповнював** урок, а не повторював.

⚠️ **Підпис має бути точний: «33 події за документацією на 2026-09-04; встановлена 2.1.236 знає 31».** `PreModelSwitch` і `PostModelSwitch` потребують **v2.1.251**. Це перевірено двічі (`claude doctor` + `strings`) і описано в `cross-findings`.

**Три колонки, яких немає в жодній наявній таблиці** — вони і є цінність довідника:
1. **Події без підтримки `matcher`** (доданий — **мовчки ігнорується**): `UserPromptSubmit`, `PostToolBatch`, `Stop`, `TeammateIdle`, `TaskCreated`, `TaskCompleted`, `WorktreeCreate`, `WorktreeRemove`, `MessageDisplay`, `CwdChanged`.
2. **`if` працює лише на пʼяти:** `PreToolUse`, `PostToolUse`, `PostToolUseFailure`, `PermissionRequest`, `PermissionDenied`. На решті хук із `if` **не виконається жодного разу**, і це нічим не діагностується.
3. **Дефолтні `timeout`:** 600 с для `command`/`http`/`mcp_tool`, 30 для `prompt`, 60 для `agent`; знижуються до 30 на `UserPromptSubmit`/`PreModelSwitch`/`PostModelSwitch` і до 10 на `MessageDisplay`; `SessionEnd` має **спільний бюджет 1,5 с** на всі хуки.
```

### Довідник 3 — ключі `settings.json`

```
**Твій файл: `claude-code-ref-settings.html`.**

Джерело: `00-research/screens/b-settings-keys-full.txt` (222 ключі, знято 2026-09-03) + живий `settings-reference.md` (**421 997 Б — читай розділами**) + `settings.md` + `env-vars.md` (480 КБ, теж розділами).

⚠️ **Ключів уже 223, а не 222** — за одну ніч додався `permissions.blockReadsOutsideWorkingDirectories` (v2.1.257+). **Перелік застаріває за добу**, тому підпис із датою обовʼязковий, а діф проти знімка зроби сам: `comm` живого переліку проти `b-settings-keys-full.txt`.

**Колонка «область» важливіша за опис.** Найгостріша пастка теми: `"auto"` і `"bypassPermissions"`, виставлені в `.claude/settings.json` чи `settings.local.json`, **мовчки не діють**. Так само `sandbox.*` — 35 ключів із власною областю (`bwrapPath`, `socatPath`, `filesystem.allowManagedReadPathsOnly`, `network.allowManagedDomainsOnly` — **лише managed**).
**`remoteControlAtStartup` асиметричний:** у проєктному файлі поважається `false`, але **ігнорується `true`**.

**Пара, яку легко сплутати — розвести обовʼязково:** `disabledMcpServers`/`enabledMcpServers` (перемикач у `/mcp`, пишеться в `~/.claude.json` **на проєкт**) проти `disabledMcpjsonServers`/`enabledMcpjsonServers`/`enableAllProjectMcpServers` (схвалення `.mcp.json`).

Переліки ключів, зібрані авторами `c13`, `c14`, `c15`, `c18`, `c20`, лежать у `cross-findings.md` — **бери звідти, не шукай удруге**.
```

---

## Іспит `c23` — тільки коли всі 22 уроки існують

```
Пишеш ФІНАЛЬНИЙ ІСПИТ курсу «AI Термінал». Папка /Users/ander1.sage/Downloads/AIA, гілка dev.

**Твій файл: `modules/claude-code-23.html`.** Технічно це звичайний модуль каркаса (`data-module="c23"`), тому нового JS не потрібно.

## Чим іспит відрізняється від уроку
`00-research/program.md`, рядок `c23`: **~25 питань по всьому курсу · вищий `passing_score` · БЕЗ нового навчального матеріалу.**

**Отже:** восьми блоків немає. Сторінка — це короткий вступ (що це, скільки питань, який поріг, що буде після) + великий квіз + слово на завершення. Жодного нового факту: **усе, що питаєш, мусить бути вже написане в одному з 22 уроків.**

## Головне правило: джерело питань — УРОКИ, а не докси
Прочитай `modules/claude-code-01.html` … `claude-code-22.html` — принаймні блоки «підсумок» і квізи кожного. Питання став **на те, що курс справді пояснив**. Якщо факт є в доксах, але його немає в жодному уроці — **питати не можна**.

**Перевір `passing_score`** у `claude-code.config.json` і в базі (міграція `02-backend/db/005-1-course-ai-terminal.sql`) — він має бути вищий, ніж у звичайних модулів.

## Як робити хороший іспит
- **Покриття:** щонайменше по одному питанню з кожної фази курсу; жодна фаза не пропущена.
- **Не памʼять, а розуміння:** не «яка команда показує контекст», а «сесія сповільнилась і Claude забув початок розмови — з чого почнеш».
- **Правдоподібні хибні варіанти:** беріть їх із типових помилок, зібраних у блоках «граблі» уроків.
- **`explain` у кожного питання** — з посиланням на номер модуля, щоб той, хто не склав, знав, куди повернутись.
- ⚠️ **`js/quiz.js` тасує варіанти** — у `explain` жодних «перший варіант». І перевір руками, що правильні відповіді розкидані по індексах.

## Механіка
`data-module="c23"` · viewport без `user-scalable` · апостроф `'` (U+0027) · `<kbd>`, `<blockquote>`, `.table-wrap` НЕ існують · токен `faint` = `#9A9184`.
Чекер писався під 8-блочний урок — **можливі попередження про структуру; це нормально для іспиту**, але помилок бути не повинно. Розбіжності назви у звіті.
**Перевір себе:** `python3 dev/build/005-ai-terminal/01-authoring/check-lessons.py`

## Не роби
Нічого поза своїм файлом. Ніяких git-команд. **Не вводь жодного факту, якого немає в уроках.**

## Звіт
Скільки питань і як розподілені по фазах · **які уроки читав** · що не вмістилось · чи давав чекер попередження і які.
```
