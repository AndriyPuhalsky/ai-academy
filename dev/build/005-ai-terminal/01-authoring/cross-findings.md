# Наскрізні знахідки авторів — що передати сусідньому модулю

**Заведено 2026-09-04.** Автори працюють паралельно й читають ті самі докси. Коли один
натрапляє на факт, який належить чужому модулю, факт лягає сюди — інакше сусід шукатиме
його вдруге або не знайде взагалі.

> ⚠️ **Це не джерело правди, а вказівник.** Кожен запис — те, що **повідомив автор**;
> частина перевірена кореневою сесією, більшість — ні. Перед тим як ставити факт у свій
> урок, **звір його сам** за правилом §7 контракту. Позначка «✅ звірено» означає, що
> факт перевіряла коренева сесія власною командою.

---

## Для всіх авторів

### ✅ Бери докси через `curl`, а не WebFetch

```bash
curl -sL https://code.claude.com/docs/en/<сторінка>.md
```
Віддає повний markdown тієї самої сторінки. **WebFetch великі сторінки обрізає:**
`env-vars.md` важить **480 621 байт** (звірено кореневою сесією), і розділ
«First session after an install or upgrade» у WebFetch просто не потрапляє. Знайшли
незалежно автори `c03` і `c04`. Для дослівних цитат — тільки `curl` + `grep`.

### ✅ Індекс доксів і `--help`, що глушить помилку

- **Покажчик усіх сторінок:** `https://code.claude.com/docs/llms.txt` *(від `c06`)*.
- **`--help` глушить помилку про НЕВІДОМИЙ прапорець, але не про невірне ЗНАЧЕННЯ
  відомого.** Звірено кореневою сесією на 2.1.236:
  `claude --totally-bogus-flag` → `error: unknown option`, а
  `claude --totally-bogus-flag --help` → **звичайна довідка, ніби прапорець існує**.
  Натомість `claude --permission-mode bogus --help` таки лається.
  **Наслідок для всіх:** перевіряти існування прапорця треба **без `--help`**. Інакше
  напишеш урок про те, чого у твоїй збірці немає *(знайшов `c07`)*.

### ⚠️ «~90 прапорців» із фактури — неправда

Прямий перерахунок `screens/claude--help.txt`: **62 рядки опцій і 13 підкоманд** — це
збігається з `tails-closed.md` («62 опції»), а не з «~90» із розділу «Екрани для курсу»
у `facts-c01-c08.md`. **Не переносьте «90»** *(від `c02`)*.
(Окремо: «~90 слеш-команд» — це інше число, про `/`-команди в сесії, воно чинне.)

### ⚠️ Докси переписали ПІСЛЯ дослідження — фактура старіє на очах

- `how-claude-code-works` виросла: додались сесії (`~/.claude/projects`, JSONL), forking
  (`--fork-session`, `/branch`), контекстне вікно, компакція з thrashing, чекпоінти,
  auto mode на Pro/Max/Team *(від `c02`)*.
- `best-practices` перебудована: першим розділом тепер «Give Claude a way to verify its
  work» із `/verify`, `/goal`, Stop-хуком і нотою, що Claude Code перебиває хук після
  8 підряд блокувань *(від `c02`)*.
- «Default auto-compact thresholds» тепер згадує **Opus 4.8 і Opus 5**, яких у фактурі
  немає взагалі *(від `c06`)*.

**Висновок для всіх:** розбіжність фактури й доксів — норма, а не виняток. Живі докси
виграють завжди; розбіжність, якщо вона змістовна, іде в урок як факт.

### Фактура знята зі stable, а курс рекомендує latest

`brew info --cask claude-code` → **2.1.236**, `brew info --cask claude-code@latest` →
**2.1.251**. Пʼятнадцять версій розриву в один день (`c03`, 2026-09-04). Наслідок для
всіх: екрани в `screens/` і власні запуски на цій машині — зі **stable**-збірки. Якщо
докси описують щось, чого твоя команда не показує, це може бути не помилка доксів, а
розрив каналів. Називай версію біля екрана — тоді розбіжність читається як факт, а не
як недогляд.

---

## `c02` · Що таке Claude Code
- Розширення VS Code **тримає власну копію CLI всередині себе й не кладе `claude` у
  `PATH`**. Рятує від питання «поставив розширення — чому в терміналі нічого» *(від `c03`)*.

- Оглядова `how-claude-code-works` перелічує **чотири** режими дозволів (Auto, Manual,
  Accept edits, Plan), а `permission-modes` — шість. Це не помилка: оглядова сторінка
  показує підмножину. `c02` тому кількість режимів не називає взагалі *(від `c02`)*.

## Три довідники
### `claude-code-ref-commands.html` — команд **111**, не «близько 90»
✅ Перераховано кореневою сесією двічі: `screens/b-slash-commands-full.txt` → **111**,
живі докси 2026-09-04 → **111**, набори імен ідентичні. Підпис у
`claude-code.config.json` казав «близько 90» — **виправлено 2026-09-04** на «Усі 111
команд і 101 гаряча клавіша у 19 контекстах». Гарячих клавіш — **101 переприв'язувана
дія у 19 контекстах** *(порахував `c05`)*.

**Мертві рядки, які довідник мусить позначити явно**, інакше вчитиме неіснуючого:
`/vim` (видалена **v2.1.92**) · `/ultraplan` (видалена) · `/agents` (змінила поведінку
v2.1.198) · `/usage-credits` (раніше `/extra-usage`) · `/subtask` (на v2.1.161–2.1.211
звалась `/fork`).

**Псевдоніми — окремою колонкою, не в описі:** `/cost`, `/stats` = `/usage` · `/reset`,
`/new` = `/clear` · `/quit` = `/exit` · `/bashes` = `/tasks` · `/app` = `/desktop` ·
`/tp` = `/teleport` · `/bg` = `/background` · `/checkup` = `/doctor` ·
`/review` = `/code-review` · `/settings` = `/config` · `/allowed-tools` = `/permissions` ·
`/share` = `/bug` · `/continue` = `/resume` · `/proactive` = `/loop` ·
`/fullscreen` = `/tui`.

Розділ доксів **«How the command menu matches what you type»** — готовий вступ до
довідника (правила підсвітки, поведінка після одруку, приховані команди типу
`/heapdump`) *(усе від `c05`)*.

## `c05` · Інтерфейс і навігація
- **`/status` змінився.** Фактура (`facts-c01-c08.md`) цитує стару версію: «current model,
  effort level, context usage, and background tasks». Живий `commands.md` на 2026-09-04:
  «Open the Settings interface on the Status tab, showing version, model, account, and
  connectivity». **Бери живу версію, не фактуру** *(від `c04`)*.
- `/diff` описаний у `commands.md` детально, з власною клавіатурою — повний розбір твій.
- `/powerup` у `c04` лише названий як домашнє завдання.
- ⚠️ **`--help` не вичерпний.** Шість задокументованих опцій існують у 2.1.236 і
  **відсутні у виводі `claude --help`**: `--advisor`, `--max-turns`, `--init`,
  `--init-only`, `--maintenance`, `--append-system-prompt-file`. Це ядро блоку 5 уроку
  `c05` *(від `c05`)*.
- ⚠️ **Фактура помиляється: `/vim` видалена у v2.1.92**, а F1 (р. 335–512) числить її
  серед живих. Знімок `b-slash-commands-full.txt` фіксує це коректно — помилка в
  узагальненні дослідника *(від `c05`)*.
- **Докси суперечать самі собі щодо `Ctrl+T`:** у `interactive-mode.md` він і «Toggle
  Claude's task checklist», і «Toggle syntax highlighting». Контексту, що їх розрізняє,
  не наведено. `c05` не поніс це в урок — кандидат у примітку довідника *(від `c05`)*.
- `Ctrl+D` при непорожньому полі **видаляє символ під курсором**, а не виходить *(від `c05`)*.

## `c04` · Перша сесія
- `Esc` на діалозі дозволу = **«No» без коментаря**; `Tab` на діалозі відкриває поле
  коментаря до відповіді. Обидва в `interactive-mode.md`, у фактурі відсутні *(від `c05`)*.
- Живий доказ, що дозволи діють і в `-p`: агент отримав відмову на частину Bash-команди
  і **сам переписав команду** *(від `c02`)*.

## `c06` · Контекстне вікно й компакція
- `best-practices` → розділ **«Avoid common failure patterns»** дає пʼять готових
  антипатернів із фіксами; два з них (`kitchen sink session`, `correcting over and over`)
  прямо про `/clear` *(від `c04`)*.
- `/context` у fullscreen згортає розбивку по пунктах, щоб лишити сітку видимою; `all`
  розгортає *(від `c05`)*.
- Порядок витіснення з вікна: спершу старі виводи інструментів, потім підсумовування;
  плюс `/compact focus on …` і «Compact Instructions» у `CLAUDE.md` *(від `c02`)*.

## `c07` · Дозволи й межа довіри
- **Що вже сказано в `c04` — не переказуй:** ручний і автоматичний режими, `Shift+Tab`,
  рядок стану, перевірка `--permission-mode bogus`.
- **Готова пастка для твого блоку «типові граблі»:** `"auto"` і `"bypassPermissions"`,
  виставлені в `.claude/settings.json` чи `settings.local.json`, **мовчки не діють** —
  сесія стартує в Manual.
- Розділ доксів **«Which mode a session starts in»** дає повний пріоритет:
  прапорець → `permissions.defaultMode` → вбудований. У фактурі цього немає *(від `c04`)*.
- **Точний цикл `Shift+Tab`:** `default` (у показнику підписаний **Manual**) →
  `acceptEdits` → `plan` → за наявності `bypassPermissions` → `auto`; з `auto` перше
  натискання повертає в `default`. У фактурі цього порядку немає *(від `c05`)*.

## `c09` · Сесії, історія, відкат
- **Межі чекпойнтів ширші, ніж каже фактура.** Крім змін через Bash, не відновлюються
  **симлінки й хардлінки** (попередження `Restored the code, but skipped N files`) і
  правки більшості субагентів *(від `c04`)*.

- `/rewind` уміє **Summarize from here / Summarize up to here** — часткова компакція,
  альтернатива `/compact` *(від `c06`)*.
- Після `/clear` попередню розмову повертає не лише `/resume`, а й запис про попередню
  сесію в меню `/rewind` (потребує v2.1.191+) *(від `c06`)*.
- `~/.claude/projects/*.jsonl` · різниця `--continue`/`--resume` проти
  `--fork-session`/`/branch` · поведінка пікера `/resume` у worktrees *(від `c02`)*.
- Історія промптів зберігається **окремо для кожної робочої папки**; `Up` дістає промпти
  з минулих сесій саме цієї папки. Після `/clear` перелік починається з нової сесії,
  старі — під нею *(від `c05`)*.

## `c10` · Моделі й мислення
- Пороги авто-компакції **прив'язані до моделі** (Sonnet 5 ≈967K; частина моделей — 200K),
  `CLAUDE_CODE_DISABLE_1M_CONTEXT=1` повертає на 200K. Вибір `[1m]` теж ваш.
  `c06` назвав «мільйон» одним абзацом у FAQ і послався сюди *(від `c06`)*.

## `c11` · Скільки це коштує
- `claude auth status --text` віддає рядок `Login method: Claude Max account` — тип плану
  видно однією командою з термінала, без входу в сесію. Команда повертає **код завершення
  0/1**, тобто придатна для скриптів *(від `c03`)*.
- `/status` — див. запис для `c05`: опис у доксах змінився.
- `costs.md` → **«Why usage climbs in a long session»** — сім причин (компакція, cache
  misses, scheduled tasks, cross-session messages, goal check-ins, agent teammates).
  Готовий кістяк розділу *(від `c06`)*.
- `prompt-caching.md` → **«Compacting the conversation»**: `/compact` на теплому кеші
  коштує частку від розміру контексту; після перерви, довшої за життя кешу, той самий
  `/compact` перечитує всю історію як некешований ввід. **Найдорожчий `/compact` — при
  відновленні старої сесії** *(від `c06`)*.
- `costs.md` → «When a developer asks about a limit»: чотири різні ситуації і чому
  `/model` рятує від модельного ліміту, але не від сесійного й тижневого *(від `c06`)*.
- **Виклики класифікатора рахуються в токени** на Enterprise / Claude API / Bedrock /
  Agent Platform / Foundry і додають окремий обмін перед виконанням. Читання й правки в
  робочій папці класифікатор оминає — витрата йде на Bash і мережу *(від `c07`)*.

## `c12` · CLAUDE.md і памʼять
- Автопамʼять вантажить **перші 200 рядків або 25 КБ `MEMORY.md`, що настане раніше**.
- **Правка `CLAUDE.md` посеред сесії не діє** до `/clear`, `/compact` або перезапуску
  (`prompt-caching.md`, «Editing CLAUDE.md mid-session»). Це і граблі, і аргумент за
  структуру файла *(обидва від `c06`)*.

## `c13` · Уся `.claude/` і `settings.json`
- Ключі, задіяні в темі оновлень, і їх варто пояснити саме тут: `autoUpdatesChannel` ·
  `minimumVersion` · `requiredMinimumVersion` / `requiredMaximumVersion` (**лише managed
  settings**) · `DISABLE_AUTOUPDATER` проти `DISABLE_UPDATES` ·
  `CLAUDE_CODE_PACKAGE_MANAGER_AUTO_UPDATE` · `CLAUDE_CODE_GIT_BASH_PATH` ·
  `USE_BUILTIN_RIPGREP` · `CLAUDE_CONFIG_DIR` *(від `c03`, названі оглядово)*.
- Див. також пастку про `"auto"` в `settings.json` у записі для `c07`.
- `.claude/settings.local.json` з **v2.1.211 читається з кореня git-репозиторію**, а не з
  поточної папки *(від `c07`)*.
- `keybindingFlavor: "readline"` (v2.1.238+) міняє поведінку `Ctrl+W` / `Alt+F` /
  `Alt+D` / `Ctrl+Y` · `respondToBashCommands: false` вимикає автовідповідь на вивід
  `!`-команди · `footerLinksRegexes` додає клікабельні значки в підказки внизу
  *(від `c05`)*.

## `c14` · Навички, команди, плагіни
- Після компакції тіла навичок вставляються назад із лімітом **5 000 токенів на навичку і
  25 000 сумарно**; обрізання лишає **початок** файла → найважливіше вгору `SKILL.md`.
- **Перелік описів навичок після `/compact` не перевставляється** — лишаються тільки
  реально викликані. Єдиний виняток серед стартового вмісту *(обидва від `c06`)*.

## `c15` · Субагенти, worktrees, паралельність
- Класифікатор перевіряє субагента **тричі**: опис задачі до старту, кожну дію, повну
  історію на виході.
- **`permissionMode` у frontmatter субагента ігнорується в auto mode** *(від `c07`)*.

## `c16` · Перехоплювачі подій (hooks)
- `PreToolUse` з відповіддю `"allow"` **не** схвалює видалення по critical path.
- Хук, що вийшов із **кодом 2, зупиняє виклик до перевірки правил** — перебиває навіть
  `allow`.
- Поле `classifierContext` у `PostToolUse` — єдиний легальний спосіб щось сказати
  класифікатору *(усі три від `c07`)*.

## `c17` · Коли все зламалось: діагностика
- **Другий приклад до тези «звіряй живою командою», крім `claude config`:**
  `claude udpate --help` на 2.1.236 друкує **кореневий хелп** — помилково набрана
  підкоманда розбирається як звичайний промпт.
  ⚠️ **Але `cli-reference.md` на 2026-09-04 стверджує протилежне:** «If you mistype a
  subcommand, Claude Code suggests the closest match and exits… `claude udpate` prints
  `Did you mean claude update?`». Автор `c03` перевіряв **лише варіант із `--help`**, тому
  в урок не ставив. **Перевір обидва варіанти сам** — якщо розбіжність підтвердиться, це
  найкращий матеріал твого модуля.
- **`claude doctor` показує канал, якого для цієї установки не існує:**
  `Package manager: homebrew` + `Auto-update channel: latest`, при тому що встановлена
  каска `claude-code` — це stable. Докси: «Homebrew installations choose a channel by cask
  name instead of this setting». Тобто рядок друкує дефолт `autoUpdatesChannel`, який для
  brew нічого не вирішує *(від `c03`, вже стоїть у його уроці виноском)*.

- **`debug-your-config.md` — цілісна сторінка «що дивитись, коли конфіг не діє»**, з
  таблицею `/memory`, `/skills`, `/hooks`, `/mcp`, `/permissions`, `/doctor`, `/debug`,
  `/status`. **У `facts-map.md` серед семи координат її немає — а вона найкорисніша**
  *(від `c06`)*.
- Третій приклад до тези: `--help` глушить помилку про невідомий прапорець — див. розділ
  «Для всіх авторів» вище. Механізми різні: невідоме **імʼя** глушиться, невірне
  **значення** — ні *(від `c07`)*.
- **Контраст, вартий уроку:** `claude --hlep` друкує `error: unknown option '--hlep'`
  **і підказку `(Did you mean --help?)`** — тобто парсер уміє підказувати. А
  `claude config --help` мовчки друкує кореневий хелп. Два різні механізми в одній
  програмі *(від `c05`)*.

## `c18` · MCP
- Схеми інструментів MCP **відкладені за замовчуванням** — у контекст ідуть лише імена;
  `ENABLE_TOOL_SEARCH=auto|false` це змінює.
- Субагент успадковує **всі** визначення інструментів MCP від батьківської сесії й може
  забити своє вікно ще до першого ходу (`errors.md`) *(обидва від `c06`)*.

## `c19` / `c20` · Автоматизація й робота без тебе
- 🎯 **Головне для `c20`, який за мапою був найтоншим:** у `platforms.md` є готова
  таблиця **«Work when you are away from your terminal»** — шість рядків із колонками
  «що запускає / де виконується / що налаштувати / для чого»: Dispatch · Remote Control ·
  Channels (Telegram / Discord / свій сервер) · Slack · self-hosted environments ·
  scheduled tasks (CLI / Desktop / cloud-Routines). **Це закриває більшу частину дірки
  живим джерелом** *(від `c02`)*.
- ⚠️ **`--output-format stream-json` у першому ж рядку друкує локальні шляхи,
  `session_id`, перелік плагінів, скілів і субагентів користувача.** У навчальний
  матеріал сирим не класти — тільки розшифровкою *(від `c02`)*.
- `claude -p` перевірений наживо: віддає чистий текст і код виходу 0 *(від `c02`)*.
- `claude setup-token` дає токен на рік і **нікуди його не зберігає**. Токен «can only make
  model requests» — тому Remote Control і конектори claude.ai через нього **не працюють**,
  а `--bare` його взагалі не читає *(від `c03`)*.

## `c22` · Безпека, великі проєкти, куди далі
- Цілий розділ `setup.md` про **перевірку підпису**: відбиток
  `31DD DE24 DDFA B679 F42D 7BD2 BAA9 29FF 1A7E CACE`, `manifest.json.sig`, підписи
  маніфестів починаючи з версії 2.1.89, нотаризація Apple. `c03` узяв звідти лише коротку
  відповідь на «чи безпечно `curl | bash`» — решта твоя *(від `c03`)*.
- `permissions.disableAutoMode` і `permissions.disableBypassPermissionsMode` зі значенням
  `"disable"` в managed settings — те, чим організація реально закриває обидва режими
  *(від `c07`)*.

## `c21` · Наскрізний проєкт і робочі звички
- `best-practices` перебудована: чотирифазний цикл лишається, але додались `/verify`,
  `/goal`, Stop-хук і «Avoid common failure patterns» — пʼять іменованих антипатернів
  *(від `c02`)*.

## `c08` · Режим плану й пісочниця — що `c07` НЕ займав
Автор `c07` провів межу явно. Вільне повністю: `Shift+Tab` у plan, `Ctrl+G`, три опції
схвалення плану, `useAutoModeDuringPlan`, режими пісочниці (`auto-allow` / regular
permissions), відсутність пісочниці на рідному Windows. На що можна коротко посилатись як
на вже пояснене в `c07`: інструменти як поняття, `deny → ask → allow`, класифікатор,
critical/protected paths, `Blocked by classifier`, пороги 3/20 *(від `c07`)*.

---

## Розбіжності в самих доксах — матеріал курсу, а не перешкода

1. **`Esc` описаний по-різному на двох офіційних сторінках.** `keybindings` називає
   `Escape` дією `chat:cancel` («Cancel current input»), а перериванням завідує
   `app:interrupt` = `Ctrl+C`. Водночас `how-claude-code-works` і `best-practices` кажуть
   прямо: «Press `Esc` to stop Claude immediately». Обидві сторінки чинні. `c04` виніс це
   в «Часті питання» з практичним висновком *(від `c04`)*.
2. **`--permission-mode` не показує `default` у переліку choices, але приймає його.**
   Підтверджено двічі — дослідником 2026-09-03 і автором `c04` 2026-09-04.
3. **Homebrew і автооновлення — формулювання доксів неповне.** «Homebrew installations do
   not auto-update» нижче доповнюється «see below to opt in for Homebrew and WinGet» через
   `CLAUDE_CODE_PACKAGE_MANAGER_AUTO_UPDATE=1`. Тобто **сама каска не оновлюється ніколи**,
   але Claude Code можна попросити виконувати `brew upgrade` *(від `c03`)*.
4. **`quickstart` Step 5 переїхав:** посилання «First session after an install or upgrade»
   тепер веде не в `permission-modes`, а в `env-vars#first-session-after-an-install-or-upgrade`.
   Хто шукатиме за старим посиланням із фактури — не знайде *(від `c04`)*.
