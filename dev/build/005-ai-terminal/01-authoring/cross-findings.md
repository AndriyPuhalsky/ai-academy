# Наскрізні знахідки авторів — що передати сусідньому модулю

**Заведено 2026-09-04.** Автори працюють паралельно й читають ті самі докси. Коли один
натрапляє на факт, який належить чужому модулю, факт лягає сюди — інакше сусід шукатиме
його вдруге або не знайде взагалі.

> ⚠️ **Це не джерело правди, а вказівник.** Кожен запис — те, що **повідомив автор**;
> частина перевірена кореневою сесією, більшість — ні. Перед тим як ставити факт у свій
> урок, **звір його сам** за правилом §7 контракту. Позначка «✅ звірено» означає, що
> факт перевіряла коренева сесія власною командою.

---

## Для всіх авторів

### ✅ Бери докси через `curl`, а не WebFetch

```bash
curl -sL https://code.claude.com/docs/en/<сторінка>.md
```
Віддає повний markdown тієї самої сторінки. **WebFetch великі сторінки обрізає:**
`env-vars.md` важить **480 621 байт** (звірено кореневою сесією), і розділ
«First session after an install or upgrade» у WebFetch просто не потрапляє. Знайшли
незалежно автори `c03` і `c04`. Для дослівних цитат — тільки `curl` + `grep`.

### ✅ Індекс доксів і `--help`, що глушить помилку

- **Покажчик усіх сторінок:** `https://code.claude.com/docs/llms.txt` *(від `c06`)*.
- **`--help` глушить помилку про НЕВІДОМИЙ прапорець, але не про невірне ЗНАЧЕННЯ
  відомого.** Звірено кореневою сесією на 2.1.236:
  `claude --totally-bogus-flag` → `error: unknown option`, а
  `claude --totally-bogus-flag --help` → **звичайна довідка, ніби прапорець існує**.
  Натомість `claude --permission-mode bogus --help` таки лається.
  **Наслідок для всіх:** перевіряти існування прапорця треба **без `--help`**. Інакше
  напишеш урок про те, чого у твоїй збірці немає *(знайшов `c07`)*.

### ⚠️ «~90 прапорців» із фактури — неправда

Прямий перерахунок `screens/claude--help.txt`: **62 рядки опцій і 13 підкоманд** — це
збігається з `tails-closed.md` («62 опції»), а не з «~90» із розділу «Екрани для курсу»
у `facts-c01-c08.md`. **Не переносьте «90»** *(від `c02`)*.
(Окремо: «~90 слеш-команд» — це інше число, про `/`-команди в сесії, воно чинне.)

### ⚠️ Докси переписали ПІСЛЯ дослідження — фактура старіє на очах

- `how-claude-code-works` виросла: додались сесії (`~/.claude/projects`, JSONL), forking
  (`--fork-session`, `/branch`), контекстне вікно, компакція з thrashing, чекпоінти,
  auto mode на Pro/Max/Team *(від `c02`)*.
- `best-practices` перебудована: першим розділом тепер «Give Claude a way to verify its
  work» із `/verify`, `/goal`, Stop-хуком і нотою, що Claude Code перебиває хук після
  8 підряд блокувань *(від `c02`)*.
- «Default auto-compact thresholds» тепер згадує **Opus 4.8 і Opus 5**, яких у фактурі
  немає взагалі *(від `c06`)*.

**Висновок для всіх:** розбіжність фактури й доксів — норма, а не виняток. Живі докси
виграють завжди; розбіжність, якщо вона змістовна, іде в урок як факт.

### Фактура знята зі stable, а курс рекомендує latest

`brew info --cask claude-code` → **2.1.236**, `brew info --cask claude-code@latest` →
**2.1.251**. Пʼятнадцять версій розриву в один день (`c03`, 2026-09-04). Наслідок для
всіх: екрани в `screens/` і власні запуски на цій машині — зі **stable**-збірки. Якщо
докси описують щось, чого твоя команда не показує, це може бути не помилка доксів, а
розрив каналів. Називай версію біля екрана — тоді розбіжність читається як факт, а не
як недогляд.

---

## `c02` · Що таке Claude Code
- Розширення VS Code **тримає власну копію CLI всередині себе й не кладе `claude` у
  `PATH`**. Рятує від питання «поставив розширення — чому в терміналі нічого» *(від `c03`)*.

- Оглядова `how-claude-code-works` перелічує **чотири** режими дозволів (Auto, Manual,
  Accept edits, Plan), а `permission-modes` — шість. Це не помилка: оглядова сторінка
  показує підмножину. `c02` тому кількість режимів не називає взагалі *(від `c02`)*.

## Три довідники
### `claude-code-ref-commands.html` — команд **111**, не «близько 90»
✅ Перераховано кореневою сесією двічі: `screens/b-slash-commands-full.txt` → **111**,
живі докси 2026-09-04 → **111**, набори імен ідентичні. Підпис у
`claude-code.config.json` казав «близько 90» — **виправлено 2026-09-04** на «Усі 111
команд і 101 гаряча клавіша у 19 контекстах». Гарячих клавіш — **101 переприв'язувана
дія у 19 контекстах** *(порахував `c05`)*.

**Мертві рядки, які довідник мусить позначити явно**, інакше вчитиме неіснуючого:
`/vim` (видалена **v2.1.92**) · `/ultraplan` (видалена) · `/agents` (змінила поведінку
v2.1.198) · `/usage-credits` (раніше `/extra-usage`) · `/subtask` (на v2.1.161–2.1.211
звалась `/fork`).

**Псевдоніми — окремою колонкою, не в описі:** `/cost`, `/stats` = `/usage` · `/reset`,
`/new` = `/clear` · `/quit` = `/exit` · `/bashes` = `/tasks` · `/app` = `/desktop` ·
`/tp` = `/teleport` · `/bg` = `/background` · `/checkup` = `/doctor` ·
`/review` = `/code-review` · `/settings` = `/config` · `/allowed-tools` = `/permissions` ·
`/share` = `/bug` · `/continue` = `/resume` · `/proactive` = `/loop` ·
`/fullscreen` = `/tui`.

Розділ доксів **«How the command menu matches what you type»** — готовий вступ до
довідника (правила підсвітки, поведінка після одруку, приховані команди типу
`/heapdump`) *(усе від `c05`)*.

## `c05` · Інтерфейс і навігація
- **`/status` змінився.** Фактура (`facts-c01-c08.md`) цитує стару версію: «current model,
  effort level, context usage, and background tasks». Живий `commands.md` на 2026-09-04:
  «Open the Settings interface on the Status tab, showing version, model, account, and
  connectivity». **Бери живу версію, не фактуру** *(від `c04`)*.
- `/diff` описаний у `commands.md` детально, з власною клавіатурою — повний розбір твій.
- `/powerup` у `c04` лише названий як домашнє завдання.
- ⚠️ **`--help` не вичерпний.** Шість задокументованих опцій існують у 2.1.236 і
  **відсутні у виводі `claude --help`**: `--advisor`, `--max-turns`, `--init`,
  `--init-only`, `--maintenance`, `--append-system-prompt-file`. Це ядро блоку 5 уроку
  `c05` *(від `c05`)*.
- ⚠️ **Фактура помиляється: `/vim` видалена у v2.1.92**, а F1 (р. 335–512) числить її
  серед живих. Знімок `b-slash-commands-full.txt` фіксує це коректно — помилка в
  узагальненні дослідника *(від `c05`)*.
- **Докси суперечать самі собі щодо `Ctrl+T`:** у `interactive-mode.md` він і «Toggle
  Claude's task checklist», і «Toggle syntax highlighting». Контексту, що їх розрізняє,
  не наведено. `c05` не поніс це в урок — кандидат у примітку довідника *(від `c05`)*.
- `Ctrl+D` при непорожньому полі **видаляє символ під курсором**, а не виходить *(від `c05`)*.

## `c04` · Перша сесія
- `Esc` на діалозі дозволу = **«No» без коментаря**; `Tab` на діалозі відкриває поле
  коментаря до відповіді. Обидва в `interactive-mode.md`, у фактурі відсутні *(від `c05`)*.
- Живий доказ, що дозволи діють і в `-p`: агент отримав відмову на частину Bash-команди
  і **сам переписав команду** *(від `c02`)*.

## `c06` · Контекстне вікно й компакція
- `best-practices` → розділ **«Avoid common failure patterns»** дає пʼять готових
  антипатернів із фіксами; два з них (`kitchen sink session`, `correcting over and over`)
  прямо про `/clear` *(від `c04`)*.
- `/context` у fullscreen згортає розбивку по пунктах, щоб лишити сітку видимою; `all`
  розгортає *(від `c05`)*.
- Порядок витіснення з вікна: спершу старі виводи інструментів, потім підсумовування;
  плюс `/compact focus on …` і «Compact Instructions» у `CLAUDE.md` *(від `c02`)*.

## `c07` · Дозволи й межа довіри
- **Що вже сказано в `c04` — не переказуй:** ручний і автоматичний режими, `Shift+Tab`,
  рядок стану, перевірка `--permission-mode bogus`.
- **Готова пастка для твого блоку «типові граблі»:** `"auto"` і `"bypassPermissions"`,
  виставлені в `.claude/settings.json` чи `settings.local.json`, **мовчки не діють** —
  сесія стартує в Manual.
- Розділ доксів **«Which mode a session starts in»** дає повний пріоритет:
  прапорець → `permissions.defaultMode` → вбудований. У фактурі цього немає *(від `c04`)*.
- **Точний цикл `Shift+Tab`:** `default` (у показнику підписаний **Manual**) →
  `acceptEdits` → `plan` → за наявності `bypassPermissions` → `auto`; з `auto` перше
  натискання повертає в `default`. У фактурі цього порядку немає *(від `c05`)*.

## `c09` · Сесії, історія, відкат
- **Межі чекпойнтів ширші, ніж каже фактура.** Крім змін через Bash, не відновлюються
  **симлінки й хардлінки** (попередження `Restored the code, but skipped N files`) і
  правки більшості субагентів *(від `c04`)*.

- `/rewind` уміє **Summarize from here / Summarize up to here** — часткова компакція,
  альтернатива `/compact` *(від `c06`)*.
- Після `/clear` попередню розмову повертає не лише `/resume`, а й запис про попередню
  сесію в меню `/rewind` (потребує v2.1.191+) *(від `c06`)*.
- `~/.claude/projects/*.jsonl` · різниця `--continue`/`--resume` проти
  `--fork-session`/`/branch` · поведінка пікера `/resume` у worktrees *(від `c02`)*.
- Історія промптів зберігається **окремо для кожної робочої папки**; `Up` дістає промпти
  з минулих сесій саме цієї папки. Після `/clear` перелік починається з нової сесії,
  старі — під нею *(від `c05`)*.

## `c10` · Моделі й мислення
- Пороги авто-компакції **прив'язані до моделі** (Sonnet 5 ≈967K; частина моделей — 200K),
  `CLAUDE_CODE_DISABLE_1M_CONTEXT=1` повертає на 200K. Вибір `[1m]` теж ваш.
  `c06` назвав «мільйон» одним абзацом у FAQ і послався сюди *(від `c06`)*.

## `c11` · Скільки це коштує
- `claude auth status --text` віддає рядок `Login method: Claude Max account` — тип плану
  видно однією командою з термінала, без входу в сесію. Команда повертає **код завершення
  0/1**, тобто придатна для скриптів *(від `c03`)*.
- `/status` — див. запис для `c05`: опис у доксах змінився.
- `costs.md` → **«Why usage climbs in a long session»** — сім причин (компакція, cache
  misses, scheduled tasks, cross-session messages, goal check-ins, agent teammates).
  Готовий кістяк розділу *(від `c06`)*.
- `prompt-caching.md` → **«Compacting the conversation»**: `/compact` на теплому кеші
  коштує частку від розміру контексту; після перерви, довшої за життя кешу, той самий
  `/compact` перечитує всю історію як некешований ввід. **Найдорожчий `/compact` — при
  відновленні старої сесії** *(від `c06`)*.
- `costs.md` → «When a developer asks about a limit»: чотири різні ситуації і чому
  `/model` рятує від модельного ліміту, але не від сесійного й тижневого *(від `c06`)*.
- **Виклики класифікатора рахуються в токени** на Enterprise / Claude API / Bedrock /
  Agent Platform / Foundry і додають окремий обмін перед виконанням. Читання й правки в
  робочій папці класифікатор оминає — витрата йде на Bash і мережу *(від `c07`)*.

## `c12` · CLAUDE.md і памʼять
- Автопамʼять вантажить **перші 200 рядків або 25 КБ `MEMORY.md`, що настане раніше**.
- **Правка `CLAUDE.md` посеред сесії не діє** до `/clear`, `/compact` або перезапуску
  (`prompt-caching.md`, «Editing CLAUDE.md mid-session»). Це і граблі, і аргумент за
  структуру файла *(обидва від `c06`)*.

## `c13` · Уся `.claude/` і `settings.json`
- Ключі, задіяні в темі оновлень, і їх варто пояснити саме тут: `autoUpdatesChannel` ·
  `minimumVersion` · `requiredMinimumVersion` / `requiredMaximumVersion` (**лише managed
  settings**) · `DISABLE_AUTOUPDATER` проти `DISABLE_UPDATES` ·
  `CLAUDE_CODE_PACKAGE_MANAGER_AUTO_UPDATE` · `CLAUDE_CODE_GIT_BASH_PATH` ·
  `USE_BUILTIN_RIPGREP` · `CLAUDE_CONFIG_DIR` *(від `c03`, названі оглядово)*.
- Див. також пастку про `"auto"` в `settings.json` у записі для `c07`.
- `.claude/settings.local.json` з **v2.1.211 читається з кореня git-репозиторію**, а не з
  поточної папки *(від `c07`)*.
- `keybindingFlavor: "readline"` (v2.1.238+) міняє поведінку `Ctrl+W` / `Alt+F` /
  `Alt+D` / `Ctrl+Y` · `respondToBashCommands: false` вимикає автовідповідь на вивід
  `!`-команди · `footerLinksRegexes` додає клікабельні значки в підказки внизу
  *(від `c05`)*.

## `c14` · Навички, команди, плагіни
- Після компакції тіла навичок вставляються назад із лімітом **5 000 токенів на навичку і
  25 000 сумарно**; обрізання лишає **початок** файла → найважливіше вгору `SKILL.md`.
- **Перелік описів навичок після `/compact` не перевставляється** — лишаються тільки
  реально викликані. Єдиний виняток серед стартового вмісту *(обидва від `c06`)*.

## `c15` · Субагенти, worktrees, паралельність
- Класифікатор перевіряє субагента **тричі**: опис задачі до старту, кожну дію, повну
  історію на виході.
- **`permissionMode` у frontmatter субагента ігнорується в auto mode** *(від `c07`)*.

## `c16` · Перехоплювачі подій (hooks)
- `PreToolUse` з відповіддю `"allow"` **не** схвалює видалення по critical path.
- Хук, що вийшов із **кодом 2, зупиняє виклик до перевірки правил** — перебиває навіть
  `allow`.
- Поле `classifierContext` у `PostToolUse` — єдиний легальний спосіб щось сказати
  класифікатору *(усі три від `c07`)*.

## `c17` · Коли все зламалось: діагностика
- **Другий приклад до тези «звіряй живою командою», крім `claude config`:**
  `claude udpate --help` на 2.1.236 друкує **кореневий хелп** — помилково набрана
  підкоманда розбирається як звичайний промпт.
  ⚠️ **Але `cli-reference.md` на 2026-09-04 стверджує протилежне:** «If you mistype a
  subcommand, Claude Code suggests the closest match and exits… `claude udpate` prints
  `Did you mean claude update?`». Автор `c03` перевіряв **лише варіант із `--help`**, тому
  в урок не ставив. **Перевір обидва варіанти сам** — якщо розбіжність підтвердиться, це
  найкращий матеріал твого модуля.
- **`claude doctor` показує канал, якого для цієї установки не існує:**
  `Package manager: homebrew` + `Auto-update channel: latest`, при тому що встановлена
  каска `claude-code` — це stable. Докси: «Homebrew installations choose a channel by cask
  name instead of this setting». Тобто рядок друкує дефолт `autoUpdatesChannel`, який для
  brew нічого не вирішує *(від `c03`, вже стоїть у його уроці виноском)*.

- **`debug-your-config.md` — цілісна сторінка «що дивитись, коли конфіг не діє»**, з
  таблицею `/memory`, `/skills`, `/hooks`, `/mcp`, `/permissions`, `/doctor`, `/debug`,
  `/status`. **У `facts-map.md` серед семи координат її немає — а вона найкорисніша**
  *(від `c06`)*.
- Третій приклад до тези: `--help` глушить помилку про невідомий прапорець — див. розділ
  «Для всіх авторів» вище. Механізми різні: невідоме **імʼя** глушиться, невірне
  **значення** — ні *(від `c07`)*.
- **Контраст, вартий уроку:** `claude --hlep` друкує `error: unknown option '--hlep'`
  **і підказку `(Did you mean --help?)`** — тобто парсер уміє підказувати. А
  `claude config --help` мовчки друкує кореневий хелп. Два різні механізми в одній
  програмі *(від `c05`)*.

## `c18` · MCP
- Схеми інструментів MCP **відкладені за замовчуванням** — у контекст ідуть лише імена;
  `ENABLE_TOOL_SEARCH=auto|false` це змінює.
- Субагент успадковує **всі** визначення інструментів MCP від батьківської сесії й може
  забити своє вікно ще до першого ходу (`errors.md`) *(обидва від `c06`)*.

## `c19` / `c20` · Автоматизація й робота без тебе
- 🎯 **Головне для `c20`, який за мапою був найтоншим:** у `platforms.md` є готова
  таблиця **«Work when you are away from your terminal»** — шість рядків із колонками
  «що запускає / де виконується / що налаштувати / для чого»: Dispatch · Remote Control ·
  Channels (Telegram / Discord / свій сервер) · Slack · self-hosted environments ·
  scheduled tasks (CLI / Desktop / cloud-Routines). **Це закриває більшу частину дірки
  живим джерелом** *(від `c02`)*.
- ⚠️ **`--output-format stream-json` у першому ж рядку друкує локальні шляхи,
  `session_id`, перелік плагінів, скілів і субагентів користувача.** У навчальний
  матеріал сирим не класти — тільки розшифровкою *(від `c02`)*.
- `claude -p` перевірений наживо: віддає чистий текст і код виходу 0 *(від `c02`)*.
- `claude setup-token` дає токен на рік і **нікуди його не зберігає**. Токен «can only make
  model requests» — тому Remote Control і конектори claude.ai через нього **не працюють**,
  а `--bare` його взагалі не читає *(від `c03`)*.

## `c22` · Безпека, великі проєкти, куди далі
- Цілий розділ `setup.md` про **перевірку підпису**: відбиток
  `31DD DE24 DDFA B679 F42D 7BD2 BAA9 29FF 1A7E CACE`, `manifest.json.sig`, підписи
  маніфестів починаючи з версії 2.1.89, нотаризація Apple. `c03` узяв звідти лише коротку
  відповідь на «чи безпечно `curl | bash`» — решта твоя *(від `c03`)*.
- `permissions.disableAutoMode` і `permissions.disableBypassPermissionsMode` зі значенням
  `"disable"` в managed settings — те, чим організація реально закриває обидва режими
  *(від `c07`)*.

## `c21` · Наскрізний проєкт і робочі звички
- `best-practices` перебудована: чотирифазний цикл лишається, але додались `/verify`,
  `/goal`, Stop-хук і «Avoid common failure patterns» — пʼять іменованих антипатернів
  *(від `c02`)*.

## `c08` · Режим плану й пісочниця — що `c07` НЕ займав
Автор `c07` провів межу явно. Вільне повністю: `Shift+Tab` у plan, `Ctrl+G`, три опції
схвалення плану, `useAutoModeDuringPlan`, режими пісочниці (`auto-allow` / regular
permissions), відсутність пісочниці на рідному Windows. На що можна коротко посилатись як
на вже пояснене в `c07`: інструменти як поняття, `deny → ask → allow`, класифікатор,
critical/protected paths, `Blocked by classifier`, пороги 3/20 *(від `c07`)*.

---

## Розбіжності в самих доксах — матеріал курсу, а не перешкода

1. **`Esc` описаний по-різному на двох офіційних сторінках.** `keybindings` називає
   `Escape` дією `chat:cancel` («Cancel current input»), а перериванням завідує
   `app:interrupt` = `Ctrl+C`. Водночас `how-claude-code-works` і `best-practices` кажуть
   прямо: «Press `Esc` to stop Claude immediately». Обидві сторінки чинні. `c04` виніс це
   в «Часті питання» з практичним висновком *(від `c04`)*.
2. **`--permission-mode` не показує `default` у переліку choices, але приймає його.**
   Підтверджено двічі — дослідником 2026-09-03 і автором `c04` 2026-09-04.
3. **Homebrew і автооновлення — формулювання доксів неповне.** «Homebrew installations do
   not auto-update» нижче доповнюється «see below to opt in for Homebrew and WinGet» через
   `CLAUDE_CODE_PACKAGE_MANAGER_AUTO_UPDATE=1`. Тобто **сама каска не оновлюється ніколи**,
   але Claude Code можна попросити виконувати `brew upgrade` *(від `c03`)*.
4. **`quickstart` Step 5 переїхав:** посилання «First session after an install or upgrade»
   тепер веде не в `permission-modes`, а в `env-vars#first-session-after-an-install-or-upgrade`.
   Хто шукатиме за старим посиланням із фактури — не знайде *(від `c04`)*.

---

# Знахідки партії `c08`–`c11` (2026-09-04)

Зібрано кореневою сесією зі звітів чотирьох авторів. Усе перевірене ними наживо на
**Claude Code 2.1.236, macOS 26.5.2 (Darwin 25.5.0)** або взяте з живих доксів того ж дня.
**Бери звідси й перевіряй, а не шукай удруге.**

## Для всіх авторів — доповнення

### `h4` і `blockquote` в проєкті не мали стилю
Tailwind CDN тягне preflight (`h1`–`h6` → `font-size: inherit; font-weight: inherit`), а
`css/custom.css` описував лише `h2` і `h3`. Автор `c05` цього не знав і поставив чотири
`<h4>`, які рендерились як звичайний абзац. **Правило `.prose-aia h4` додане 2026-09-04** —
четвертий рівень тепер безпечний. **`<blockquote>` стилю досі не має** — не використовуй.

### Чекер став суворішим і має дві сліпі зони
`check-lessons.py` тепер нормалізує пробіли перед пошуком заборонених зворотів — «просто »
ловиться й через перенос рядка. Дві речі він **не** бачить, перевіряй руками:
- **усі правильні відповіді квіза на одному індексі** (в автора `c11` з першого разу вийшло
  шість разів `answer: 1`);
- факти. Він міряє структуру, механіку й мову, не правду.

### Три автори підряд уперлись в одне місце контракту
Норма «2–5 блоків `.term`» тисне на модулях, де предмет інтерактивний: чесно зняти можна
лише периферію (`--help`, `--version`), а суть живе всередині мальованого екрана. Рішення
кореневої сесії: **для таких тем нижня межа — 2 блоки, і це не дефект.** Дослівний
багаторядковий приклад із доксів у `.term` не клади — він читатиметься як знімок; давай
його таблицею «рядок → що означає», як зробив `c11`.

## `c12` · CLAUDE.md і памʼять
- Поруч із транскриптом у `~/.claude/projects/<проєкт>/` лежить папка **`memory/`** —
  автопамʼять сесії. Живе підтвердження: `ls` тестового проєкту віддає `<uuid>.jsonl` **і**
  `memory` *(від `c09`)*.
- Автопамʼять **виключена зі сну ретенції** й видаляється лише після того, як пробула
  порожньою весь строк (з v2.1.228).
- `CLAUDE_CODE_PROJECT_DIR_NAME` (v2.1.234+) працює **тільки разом із `CLAUDE_CONFIG_DIR`**
  і переносить і транскрипти, і автопамʼять.

## `c13` · Уся `.claude/` і `settings.json`
**Зламані налаштування** *(від `c08`)*: на старті інтерактивної сесії — вікно
**`Settings Error`** з вибором «полагодити з Claude / вийти / працювати без зламаних»;
у `claude -p` вікна немає, програма мовчки пропускає зламане й іде далі — саме тому докси
радять після цього `claude doctor`.
- **Опублікована JSON-схема (`json.schemastore.org/claude-code-settings.json`) відстає від
  релізів** — попередження редактора на щойно задокументований ключ ще не означає помилку.
- Готова таблиця **винятків із precedence**, де суворіше значення з проєктного файла виграє
  в керованого: `remoteControlAtStartup`, `crossSessionInbound`, `useAutoModeDuringPlan`,
  `syncClaudeAiSkills`.
- `.claude/settings.local.json` Claude Code сам додає в **глобальні git-виключення**
  (`core.excludesFile` → `$XDG_CONFIG_HOME/git/ignore` → `~/.config/git/ignore`) при першому
  записі — **і тільки якщо створив файл сам**. Створений руками треба ігнорувати самому.
- У `sandbox.*` **35 ключів**, і в них власна колонка області: `bwrapPath`, `socatPath`,
  `filesystem.allowManagedReadPathsOnly`, `network.allowManagedDomainsOnly` — **лише
  managed**; `allowAppleEvents`, `credentials.*`, `filesystem.disabled`,
  `network.strictAllowlist`, `network.tlsTerminate`, `ripgrep` — **user або managed**.
  Проєктний файл їх не вмикає — та сама пастка, що з `"auto"`.
- **Ключі сесій** *(від `c09`)*: `cleanupPeriodDays` (дефолт 30, мінімум 1, `0` = помилка
  валідації) · `desktopSessionCleanupPeriodDays` (лише user/managed/`--settings`, v2.1.248+) ·
  `CLAUDE_CODE_SKIP_PROMPT_HISTORY` · `CLAUDE_CONFIG_DIR` · `--no-session-persistence`
  (тільки з `--print`). Якщо строк ретенції визначити не вдається, Claude Code **зупиняє
  прибирання** і світить попередження в `/status`.
- **Ключі моделей** *(від `c10`)*: `model` · `modelSettings.<model>.effortLevel` ·
  `effortLevel` (**не приймає `max` і `ultracode`**) · `ultracode` · `alwaysThinkingEnabled` ·
  `showThinkingSummaries` · `fastMode` · `fastModePerSessionOptIn` · `switchModelsOnFlag` ·
  `fallbackModel` · `autoCompactWindow` · `availableModels` + `enforceAvailableModels` ·
  `advisorModel`.
- **Ключі грошей і кеша** *(від `c11`)*: `promptCacheTtl`, `subagentPromptCacheTtl` (обидва
  `5m`/`1h`, v2.1.242+) · `FORCE_PROMPT_CACHING_5M` · `ENABLE_PROMPT_CACHING_1H` ·
  `modelPricing` (**керовані** — підміняє прайс-лист у `/usage` і додає підпис
  `at your organization's configured rates`) · `autoContinueAtUsageLimit` ·
  `crossSessionInbound`. Повний порядок пріоритетів TTL — шість рівнів у
  `prompt-caching.md`, розділ «Choose the TTL yourself».

## `c14` · Навички, команди, плагіни
- Поле `effort` у frontmatter скіла **перебиває сесійний рівень, але не змінну середовища**
  *(від `c10`)*.

## `c15` · Субагенти, worktrees, паралельність
- **Пісочниця спільна з батьком** *(від `c08`)*: субагенти виконуються в тому самому процесі
  й із тією самою конфігурацією пісочниці. У підключеному git-worktree пісочниця дозволяє
  запис у спільну `.git` головного репозиторію (щоб `git commit` оновив refs та індекс), але
  `hooks/` і `config` усередині неї лишаються закритими.
- **Межа чекпойнта точніша, ніж у фактурі** *(від `c09`)*: відновлюється лише навичка з
  `context: fork` + `background: false`, запущена в передньому плані; фоновий
  `/code-review --fix` — приклад того, що **не** повертається.
- **Сесії й worktrees** *(від `c09`)*: сесія з іншого worktree того ж репо відновлюється
  **на місці**; якщо її worktree зник — у поточній папці; сесія з чужого проєкту не
  відновлюється, а копіює в буфер готову команду `cd` + resume. Пункт `Ctrl+W` у пікері
  показується **лише в репозиторіях із кількома worktree**.
- **Моделі субагентів** *(від `c10`)*: ланцюг `fallbackModel` застосовується й до них (до
  v2.1.247 збій, який ланцюг покриває, просто вбивав субагента) · `CLAUDE_CODE_SUBAGENT_MODEL`
  і `CLAUDE_CODE_SUBAGENT_MODEL_FORCE` садять усіх субагентів на одну модель.
- **Ціна паралельності** *(від `c11`)*: команди агентів (`agent teams`) споживають
  **приблизно всемеро більше токенів**, коли напарники працюють у режимі плану; вмикаються
  `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1`, за замовчуванням вимкнені. Субагент **не читає
  кеш батька** (різні префікси) і живе в відрі TTL «все інше» — 5 хвилин навіть на підписці,
  доки не задати `subagentPromptCacheTtl` / `cacheTtl` у frontmatter (v2.1.248+). `/fork`,
  навпаки, успадковує префікс і читає батьківський кеш.
- **Класифікатор** *(від `c08`)*: рецензує ще й кожне повідомлення `SendMessage` іншому
  агенту — і в auto mode, і в plan mode (потребує v2.1.222+).

## `c16` · Hooks і `c18` · MCP — спільна знахідка
**Пісочниця Bash їх не накриває** *(від `c08`)*: перехоплювачі подій і сервери MCP
запускаються **поза нею**. Саме тому `sandbox-environments.md` вимагає контейнер або VM для
`--dangerously-skip-permissions`: «so that file tools, MCP servers, and hooks are also inside
the boundary». Для `c18` це прямий місток у безпеку; для `c16` — межа, про яку читач мусить
знати, перш ніж писати свій перший перехоплювач.

## `c17` · Діагностика — три незалежні докази тези модуля
1. **Жива команда каже те, чого немає в статті** *(від `c09`)*: `claude -c < /dev/null`
   (тобто `--continue` без TTY і без промпту) на 2.1.236 падає з кодом 1 і повідомленням
   `Error: No deferred tool marker found in the resumed session…`. **Цього тексту немає в
   `errors.md`** — перевірено пошуком по всій сторінці.
2. **В одній програмі три різні реакції на сміття** *(від `c10`)*: невідомий **прапорець**
   глушиться, якщо поруч є `--help` · невірне **значення `--effort`** попереджає з переліком
   допустимих · невірне **значення `--model`** мовчить до першого мережевого запиту, бо
   локальної перевірки назви моделі не існує (докси це визнають прямо).
3. **`claude auth status`** *(від `c11`)*: формат за замовчуванням — **`--json`**, `--text`
   це опція; код виходу `0` при наявному вході. Придатна як перший крок «а я взагалі
   увійшов». `--text` друкує рівно три рядки: `Login method:`, `Organization:`, **`Email:`** —
   останній робить її непридатною для скріншотів в уроці.
- `claude --safe-mode` має ще одне застосування, крім конфігів: перевірити, чи не твій
  `CLAUDE.md` або скіли вмикають класифікатор безпеки й перекидають сесію на іншу модель.

## `c19` · Headless і CI
- **`claude -p --continue` у папці без сесій не помиляється** — мовчки стартує нову розмову
  й створює новий транскрипт (перевірено двічі, у двох порожніх папках) *(від `c09`)*.
  Для скриптів це означає: `--continue` не годиться як перевірка «сесія існує»; ознака —
  окремий `--resume <id>` із кодом виходу.
- **`--max-budget-usd <amount>`** існує в 2.1.236, «only works with --print» *(від `c11`)*.
  Витрати субагентів зараховуються **в ту саму стелю**; після її вибирання спроба породити
  субагента падає з `Budget limit reached`, фонові субагенти зупиняються. Потребує v2.1.217+.
- Дослівний вирок для автономних запусків *(від `c08`)*: «The sandboxed Bash tool on its own
  constrains only Bash, so it is not sufficient for fully unattended runs in either mode».

## `c20` · Коли Claude працює без тебе (🔴 найтонший)
- **`costs.md` → «Why usage climbs in a long session»** дає чотири джерела витрат у простої
  з конкретикою, якої немає в `platforms.md` *(від `c11`)*: завдання за розписанням шле
  **повний контекст на кожен інтервал** · cross-session message приходить як новий хід
  (`crossSessionInbound: hold` це гасить) · **перевірки цілі — не більше трьох у простої між
  промптами**, до v2.1.246 були без обмеження, вимикаються `CLAUDE_CODE_GOAL_CHECKIN_MINUTES=0`,
  потребують v2.1.236+ · напарники в команді агентів витрачають токени до виходу.
- **`/usage` має рядки Loops** — по найважчих `/loop` та інших завданнях за розписанням, із
  частотою, кількістю запусків і токенами. Потребує v2.1.242+. Готовий інструмент перевірки.

## `c22` · Безпека
- **Транскрипти й історія не шифруються** *(від `c09`)*: дослівно «Transcripts and history
  are not encrypted at rest. OS file permissions are the only protection», і поруч пряме
  попередження, що прочитаний `.env` або надрукований командою ключ **записується в
  транскрипт**. Там же таблиця «що втратиш, якщо видалиш» по десяти папках `~/.claude`.
- **`sandboxing.md` → розділ «Security limitations»** *(від `c08`)*, звідки взято лише два
  рядки — решта твоя й дуже щільна: privilege escalation через `allowUnixSockets`
  (`/var/run/docker.sock` = доступ до хоста) · ескалація через широкий `allowWrite` у папки
  з виконуваними файлами в `$PATH` · `enableWeakerNestedSandbox` як свідоме послаблення ·
  `allowAppleEvents` як зняття ізоляції на macOS. Плюс окремий розділ **«Protect credentials» /
  «Mask credentials»**: маскування змінних середовища, re-signing AWS-запитів,
  `network.tlsTerminate`, ліміт 8 MiB і UTF-8 для `mask`.

## Три довідники — доповнення
- **Псевдоніми, підтверджені наживо** *(від `c09`)*: `/rewind` = `/checkpoint` = `/undo` ·
  `/resume` = `/continue` · `/clear` = `/reset` = `/new`.
- **Версійні межі, які варто позначити явно:** рядок `(previous session)` у меню `/rewind`
  потребує **2.1.191+** · `/fork` у нинішньому значенні — **2.1.212+** (на 2.1.161–2.1.211
  команда звалась інакше й поводилась як `/subtask`).
- **`/rate-limit-options` не показується в меню команд** — її треба набирати повністю
  *(від `c11`)*. Без цієї позначки читач вирішить, що команди немає.
- **Число опцій `--help` перераховане наживо** *(від `c09`)*: **62 опції й 13 підкоманд** на
  2.1.236 (`grep -cE '^  (-[a-zA-Z], )?--[a-z]'`). «~90» із фактури — неправда, підтверджено
  вдруге.
- У `sandbox.*` — **35 ключів** *(від `c08`)*.

## Правки в уже написаних уроках, які лишаються відкритими
- **`c06`:** формулювання «частина моделей — 200K» неточне. Точно: **Opus 4.8 і Opus 5
  компактяться на 200K тоді, коли працюють із вікном 200K** — на Amazon Bedrock, Google Cloud
  Agent Platform і Microsoft Foundry. Справа в **постачальнику**, не в моделі *(від `c10`)*.
- **`c10`:** у прайсі зʼявилась сімʼя **`Claude Mythos` (5.1 і 5, limited availability)**,
  якої немає у фактурі взагалі *(від `c11`)*. Плюс жива примітка про токенізатор: моделі
  **4.7 і новіші** дають ~30% більше токенів, Sonnet 4.6 і старіші — старий токенізатор.
- **`c02` — закрито 2026-09-04.** Фраза «Конкретних чисел цей курс не наводить свідомо»
  суперечила `c11`, який ціни наводить. Звужено до **лімітів**, із поясненням, що їх немає
  в самій документації («no fixed message count» на `claude.com/pricing`).

---

# Знахідки хвилі 2 (`c12`–`c15`, `c18`) — 2026-09-04

Пʼять авторів, жодного обриву. Усе перевірене на **2.1.236, macOS 26.5.2** або взяте з
живих доксів того ж дня.

## ⚠️ Спростування й уточнення попередніх записів — читати перед роботою

| Було записано | Насправді |
| --- | --- |
| «описи навичок після `/compact` не перевставляються» — автор `c14` **не знайшов** підтвердження | **Факт вірний**, джерело інше: `context-window.md`, р. 56–57, поле `noSurviveCompact: true` і фраза «this listing is not re-injected after `/compact`». Автор `c14` шукав у `skills.md` / `how-claude-code-works.md` / `features-overview.md` — там цього немає. **У `c06` стоїть правильно.** |
| «поле `effort` перебиває сесійний рівень, але не змінну середовища» | Підтверджена **тільки перша половина** (`skills.md`: «Overrides the session effort level»). Взаємодія зі змінною **ніде не задокументована** — не писати. |
| «класифікатор перевіряє субагента тричі» | **Не підтверджено.** Є лише дві перевірені речі: сканування підсумкового звіту (`sub-agents.md`) і перевірка кожного `SendMessage` в auto mode (`agent-teams.md`). |
| фактура: «вкладка Stats у `/plugin`» | **Помилка атрибуції.** `Stats` належить `/stats` = `/usage`. У `/plugin` **чотири** вкладки: Discover · Installed · Marketplaces · Errors. |
| фактура: «21 поле frontmatter скіла» | **20** — перерахунок рядків живої таблиці 2026-09-04. |
| фактура: «19 полів frontmatter агента» | **17** — те саме. |
| фактура: «14 підкоманд `claude`» | **13** (і 62 опції) — перерахунок наживо, вдруге за курс. |

## 🔴 `c16` — головне число модуля розходиться

**У живих доксах подій рівно 33** (порахував заголовки `###` у розділі «Hook events»
сторінки `hooks.md`, 2026-09-04). **А `claude doctor` на встановленій 2.1.236 у відповідь
на невідому назву події друкує перелік із 31** — у ньому немає `PreModelSwitch` і
`PostModelSwitch`.

**Це не помилка жодної зі сторін, це розрив версій — і це найкращий матеріал уроку.**
Підпис «усі 33 події» в `claude-code.config.json` правильний (він про докси). Урок мусить
сказати обидва числа з датою й версією.

Решта для `c16`:
- **масив у полі `matcher` — не мовчазна помилка, а schema error, що відкидає весь файл
  налаштувань**; `claude doctor` це показує *(від `c13`)*;
- подія **`ConfigChange`** спрацьовує на кожну зміну файла налаштувань, але **не** на
  керовані, що приїхали з MDM або консолі *(від `c13`)*;
- подія **`InstructionsLoaded`** — найкращий діагностичний приклад для новачка: логує, які
  саме файли інструкцій завантажились, коли і чому. У фактурі її немає *(від `c12`)*;
- хук у frontmatter субагента живе **лише поки той працює**, і `Stop` там автоматично
  конвертується в `SubagentStop` *(від `c15`)*;
- хуки **проєктного** субагента виконуються лише після прийняття workspace trust, і
  `-p`-сесія за прийняття **не рахується** (з 2.1.218) *(від `c15`)*;
- **`WorktreeCreate`-хук повністю замінює логіку `git worktree`**, включно з місцем копій;
  `.worktreeinclude` при цьому не працює. Пастка шляхів: `${CLAUDE_PROJECT_DIR}` лишається
  на місці старту, а поле `cwd` у вхідному JSON — це вже корінь worktree *(від `c15`)*;
- `memory.md` двічі каже: для гарантії потрібен **хук `PreToolUse`, а не інструкція** в
  `CLAUDE.md`. Автор `c12` зробив це головним реченням свого модуля й передав сюди.

## `c17` — матеріалу тепер більше, ніж на один урок

- **`claude doctor` читає файли налаштувань поточної папки без запиту довіри** (дослівно з
  `claude --help`). Разом із `CLAUDE_CONFIG_DIR` на порожню теку це безкоштовний,
  безпечний і відтворюваний діагностичний екран *(від `c13`)*.
- **Чотири різні назви для чотирьох різних поломок:** `Settings Error` · `Settings Warning` ·
  `Configuration error` (це вже про `~/.claude.json`, з копією в
  `~/.claude/backups/.claude.json.corrupted.<timestamp>` і пʼятьма ротованими бекапами) ·
  окремий випадок нерозбірливих керованих налаштувань. Готовий кістяк розділу *(від `c13`)*.
- **`claude doctor` друкує `No installation issues found.` ПОРУЧ зі знайденою помилкою
  налаштувань** — це два різні розділи. Читач, який дивиться останній рядок, робить хибний
  висновок *(від `c13`)*.
- **`claude plugin validate`** — друга команда курсу з чесним кодом виходу (0/1), але
  **мовчить про те, чого не бачить**: `.md`-файл замість теки скіла, файл агента без `name`.
  «Помилок немає» ≠ «все гаразд» *(від `c14`, `c15`)*.
- **Четверта реакція на сміття** (до трьох відомих): `claude plugin init x --with skills,hooks`
  → `✘ Unknown --with component "skills,hooks". Valid: skills, agents, hooks, mcp, lsp,
  output-style, channel` — лається **і перелічує допустимі** *(від `c14`)*.
- **`strings "$(which claude)" | grep -c <ключ>` з контрольною вигаданою назвою** розрізняє
  «у моїй збірці є» і «докси описують завтрашню версію». Живий приклад:
  `CLAUDE_CODE_SUBAGENT_MODEL_FORCE` → 0 на 2.1.236 при задокументованому «requires
  v2.1.257» *(від `c15`)*.
- **`claude mcp list` друкує окремий блок `MCP config diagnostics ⚠`** зі шляхом до файла
  й деревом попереджень — ще один «жива команда каже те, чого немає в статті». Плюс
  `curl -I <адреса>`: `404`/`405` означає «сервер живий, справа в конфігурації» *(від `c18`)*.
- **`--safe-mode` не вимикає керовані хуки й політику організації** — формулювання
  «вимикає все» неточне *(від `c12`)*.
- **Рецепт чистої сесії, гостріший за `--safe-mode`:** `CLAUDE_CONFIG_DIR` на порожню теку
  + запуск із папки без `.claude`, `.mcp.json` і `CLAUDE.md`. Ознака, що спрацювало —
  зʼявляються екрани першого запуску, починаючи з вибору теми; на другому запуску з тією ж
  текою вони вже не зʼявляться *(від `c12`)*.
- `/doctor` повідомляє про файли агентів з однаковим `name` в одній теці *(від `c15`)*.

## `c19` · Запуск без діалогу
- У `claude -p`, Agent SDK і хмарних сесіях **діалогу схвалення показати нікому, тому
  проєктні сервери з `.mcp.json` вантажаться без питань**. Три способи закрити:
  `disabledMcpjsonServers`, `--setting-sources`, `--strict-mcp-config` *(від `c18`)*.
- `--strict-mcp-config` **до v2.1.246 усе одно чекав схвалення** серверів, яких не вантажив —
  через це фонові сесії зависали на старті *(від `c18`)*.
- У `--output-format stream-json` пропущений запис `--mcp-config` повідомляється полем
  **`mcp_server_errors`** у події `system/init` (2.1.219+) *(від `c18`)*.
- `claude -p --worktree` **не прибирає** копії й лишає `git worktree lock` — для CI це витік
  диска. Він же **пропускає перевірку workspace trust**, а інтерактивний `--worktree` — ні
  *(від `c15`)*.
- `claude -p` **біндить сокет вхідних повідомлень** так само, як інтерактивна сесія; виняток —
  bare mode *(від `c15`)*.

## `c20` · Робота без тебе
- **Сервер MCP може бути каналом** (`channels`): оголошує можливість `claude/channel`,
  вмикається `--channels`, і сам штовхає повідомлення в сесію (Telegram, Discord, вебхуки).
  Сторінки `channels.md` і `channels-reference.md` *(від `c18`)*.
- На рантаймі v2 канал, що домовився про ревізію протоколу 2026-07-28, **не реєструється як
  канал** *(від `c18`)*.
- Динамічні робочі процеси вимикаються **трьома** різними способами: `/config`,
  `disableWorkflows`, `CLAUDE_CODE_DISABLE_WORKFLOWS=1` *(від `c15`)*.

## `c22` · Безпека — новий великий матеріал
- **`sub-agents.md` → «Subagent output scanning» (з 2.1.210):** звіт субагента сканується,
  перш ніж його прочитає головна сесія; сканер вставляє зворотну скісну в текст, що вдає
  `<system-reminder>`, `Human:` чи `Assistant:`, і додає рядок-маркер
  `[harness: subagent output matched instruction-shaped pattern(s): …]`. **Докси прямо
  кажуть: це не заміна обмеженню повноважень** *(від `c15`)*.
  > Цей маркер видно наживо в звітах авторів цього ж курсу — готовий приклад «механізм,
  > який працює просто зараз».
- `agent-teams.md`: повідомлення від агента **ніколи не є згодою користувача**, а напарник,
  якому відмовили, **не може переказати дію іншому**, щоб обійти перевірку *(від `c15`)*.
- `cross-session-messaging.md` дає межі каналу: ~1 млн символів на повідомлення, черга 50,
  утримання до 100, дропи однакових повторів — цикл між двома сесіями зупиняється сам.
- **`allowed-tools` проєктної навички не гейтиться довірою до папки** й діє навіть у `-p`
  в теці, якій користувач ніколи не давав довіри *(від `c14`)*.
- `discover-plugins.md` § Security, дослівно: «Plugins and marketplaces are highly trusted
  components that can execute arbitrary code on your machine with your user privileges».
- **Зовнішній імпорт у `CLAUDE.md`** показує діалог схвалення один раз; сенс прямо названий
  у доксах — захист від файлів, які **інші люди комітять у спільний проєкт**. Відмова
  вимикає імпорти назавжди *(від `c12`)*.
- Таблиця «що втратиш, якщо видалиш» по всіх шляхах `~/.claude/` — `claude-directory.md`,
  розділ «Clear local data»; там же «Don't delete `~/.claude.json`,
  `~/.claude/settings.json`, or `~/.claude/plugins/`» *(від `c13`)*.
- **Новий ключ `permissions.blockReadsOutsideWorkingDirectories`** (v2.1.257+) блокує читання
  поза робочими теками **в усіх режимах, включно з `bypassPermissions`**; Claude Code сам
  пише туди `true`, коли ти відмовляєш на першому читанні поза робочою текою *(від `c13`)*.

## Довідники
- **`ref-hooks`:** 33 події в доксах / 31 у 2.1.236 — див. блок `c16` вище. Підпис довідника
  має бути з датою й версією.
- **`ref-commands`:** `/mcp reconnect <server>`, `/mcp enable|disable [<server>|all]` — форм
  немає в меню · MCP-промпти показуються як `/servername:promptname (MCP)`, набираються
  також `/mcp__server__prompt` · `/subtask` і `/fork` — **різні речі** у 2.1.212+ ·
  `/list-agents` = `/peers` · `/tasks` = `/bashes` · `/workflows` без псевдоніма ·
  `/deep-research` — вбудований робочий процес, не звичайна команда ·
  **`/rate-limit-options` не показується в меню**.
- **`ref-settings`:** пара, яку легко сплутати й треба розвести колонкою —
  `disabledMcpServers`/`enabledMcpServers` (перемикач у `/mcp`, пишеться в `~/.claude.json`
  **на проєкт**) проти `disabledMcpjsonServers`/`enabledMcpjsonServers`/
  `enableAllProjectMcpServers` (схвалення `.mcp.json`). Плюс переліки, віддані авторами
  `c13`, `c14`, `c15`, `c18` — вони в їхніх звітах, тут не дублюються.
- **223 ключі, а не 222:** діф живого `settings-reference.md` (2026-09-04) проти
  `screens/b-settings-keys-full.txt` (2026-09-03) дав рівно один новий —
  `permissions.blockReadsOutsideWorkingDirectories`, v2.1.257+. **Перелік ключів застаріває
  за добу** *(від `c13`)*.

---

# Знахідки пари `c16` + `c17` — 2026-09-04

## ✅ Розрив 33/31 закрито остаточно
Обидві відсутні події — **`PreModelSwitch` і `PostModelSwitch`** — у `hooks.md` підписані
«requires Claude Code v2.1.251 or later». Того ж дня `brew info --cask claude-code@latest`
= **2.1.251**, а встановлена — 2.1.236. Доведено двома незалежними способами:
`claude doctor` на невідому подію друкує перелік із 31, `strings` по бінарнику дає **0**
для обох назв (контрольна вигадана — теж 0). **Підпис довідника «усі 33» правильний.**

## ⚠️ Спростування попереднього запису
**Запис про те, що `cli-reference` нібито бреше про підказку при друкарській помилці —
хибний.** Автор `c17` перевірив наживо: `claude udpate` → `✘ unknown command "udpate"` +
`└ Did you mean claude update?`, exit **1**. Докси праві. Спостереження `c03` стосувалось
**лише форми з `--help`**, де помилку глушить сам `--help` — це третій прояв того самого
механізму, а не суперечність. *(В уроці `c03` хибного твердження немає — перевірено.)*

Повний механізм: слово, **схоже** на підкоманду → помилка з підказкою; слово, **не схоже**
(`config`, `zzqqxx`) → мовчки їде моделі як промпт.

## Нове, чого немає в доксах узагалі
- **`claude doctor` виходить із кодом `0` навіть коли знайшов `Invalid settings`**
  (перевірено двічі: у чистій теці й у зламаній). Для CI це означає, що `doctor` **не
  придатний як гейт** — код виходу не несе результату перевірки. *(від `c17`)*
- **`~/.claude/debug/latest`** — символьне посилання на найсвіжіший журнал; у доксах
  згадується лише файл із номером сесії. Тека `debug/` створюється **тільки** після запуску
  з `--debug` або `/debug`. *(від `c17`)*
- **Помилка схеми друкується з рядком `Suggested fix:`** і готовим зразком правильного JSON.
  Відтворюється двома командами без редактора:
  `echo '{"hooks":{"PreToolUse":[{"matcher":["Edit"],"hooks":[]}]}}' > .claude/settings.json`
  → `hooks.PreToolUse.0.matcher: Expected string, but received array`. *(від `c16`, `c17`)*
- **У `-p` зламаний хук не дає жодного повідомлення на екрані**, але в журналі `--debug`
  видно `Hooks: Found 0 total hooks in registry` — доказ «мовчазної відмови» без
  інтерактивної сесії. *(від `c17`)*

## `c19` · Headless і CI
- Перед `claude -p` над чужим репо докси радять **три** способи: перегляд `.claude/`,
  `--bare`, `--settings '{"disableAllHooks": true}'` (останній має пріоритет над проєктними
  й локальними налаштуваннями) *(від `c16`)*.
- **`terminalSequence` ігнорується в `-p` і в Agent SDK** — сповіщення з хука в CI не
  працюють у принципі *(від `c16`)*.
- `PermissionRequest`-хуки в `-p` спрацьовують **лише** якщо Agent SDK підставив
  `canUseTool`; у звичайному `-p` треба `PreToolUse`. Фонові субагенти: якщо жоден хук не
  дав рішення — виклик **відхиляється** *(від `c16`)*.
- ⚠️ **Межа техніки «зняти слеш-команду через `-p`»:** `/status` і `/hooks` віддають
  `isn't available in this environment`, `/doctor` **зависає** *(від `c17`)*.
- Рядок `Watching for changes in setting files …` у журналі `--debug` **не залежить** від
  `--setting-sources` — це файловий нагляд, а не перелік активних джерел. **Не
  використовувати як доказ precedence** *(від `c17`)*.

## `c22` · Безпека
- **Мотив, чому пісочниця забороняє запис у файли конфігурації**, названий дослівно
  (`sandboxing.md`, р. 488): команда, яка могла б їх правити, «додала б хук або MCP-сервер,
  який Claude Code виконує **поза пісочницею**». Готове речення про ескалацію *(від `c16`)*.
- На Linux/WSL2 пісочниця **видаляє** `HEAD`/`objects`/`refs`, що зʼявились у `.git` під час
  роботи *(від `c16`)*.
- `allowManagedHooksOnly` — чотири наслідки для організації: блокуються user/project/local/
  плагінні хуки · `statusLine`, `fileSuggestion`, `subagentStatusLine` звужуються до
  managed · плагіни з `command`-джерелом вимикаються · `headersHelper` маркетплейсів
  блокується *(від `c16`)*.
- **`/heapdump`:** `.heapsnapshot` містить кожен рядок із памʼяті процесу, **включно з усією
  розмовою й обліковими даними**; у публічний баг-репорт кладуть лише `-diagnostics.json`
  *(від `c17`)*.

## `c15` і `c14` — два різні правила довіри в сусідніх сутностей
- Хуки з файлів налаштувань, керованої політики й плагінів **виконуються й усередині
  субагентів**; вхідний JSON тоді несе `agent_id` та `agent_type`. Тобто `PreToolUse`-гейт
  накриває субагентів автоматично *(від `c16`)*.
- Але хук у frontmatter **проєктної навички** реєструється при виклику **включно з `-p` у
  недовіреній теці** — тоді як хуки проєктного **субагента** потребують прийнятої довіри, і
  `-p` за прийняття не рахується (2.1.218+). **Дві сусідні сутності, два різні правила**
  *(від `c16`)*.

## Довідник `ref-hooks` — три колонки, яких немає в жодній таблиці
1. **Події без підтримки `matcher`** (доданий — **мовчки ігнорується**): `UserPromptSubmit`,
   `PostToolBatch`, `Stop`, `TeammateIdle`, `TaskCreated`, `TaskCompleted`, `WorktreeCreate`,
   `WorktreeRemove`, `MessageDisplay`, `CwdChanged`.
2. **`if` працює лише на пʼяти подіях:** `PreToolUse`, `PostToolUse`, `PostToolUseFailure`,
   `PermissionRequest`, `PermissionDenied`. На решті хук із `if` **не виконається жодного
   разу**, і це нічим не діагностується.
3. **Дефолтні `timeout` різні за типом і подією:** 600 с для `command`/`http`/`mcp_tool`,
   30 для `prompt`, 60 для `agent`; знижуються до 30 на `UserPromptSubmit`/`PreModelSwitch`/
   `PostModelSwitch` і до 10 на `MessageDisplay`; `SessionEnd` має **спільний бюджет 1,5 с**
   на всі хуки (піднімається явним `timeout` до 60 с).

Підпис довідника: **«33 події за документацією на 2026-09-04; встановлена 2.1.236 знає 31»**.

## Довідник `ref-settings`
- Дослівний перелік опцій `claude project purge`: `--all`, `--dry-run`, `-i/--interactive`,
  `-y/--yes`, `-h` *(від `c17`)*.
- `--restricted` описаний у `cli-reference` з позначкою «requires v2.1.248» — у 2.1.236
  його немає *(від `c17`)*.

---

# Знахідки пари `c19` + `c20` — 2026-09-04

## ⚠️ У програмі курсу був неіснуючий пункт
Рядок `c20` містив **«tunnels»**. Автор `c20` перевірив грепом по 14 сторінках доксів і по
`llms.txt`: **такої функції немає**. Слово трапляється лише про корпоративний проксі
(`errors.md`), мережеві інтерфейси `utun` і як **заборонену** дію в `permission-modes.md`
(«Opening a tunnel or reverse shell»). Пункт закрито з іншого боку — розділом «чому Remote
Control не потребує ні тунелю, ні відкритого порту» (докси: «outbound HTTPS requests only
and never opens inbound ports»). **`program.md` виправлено.**

**Урок для решти:** пункт програми — не доказ існування функції. Програму писали під стару
фактуру; якщо не знаходиш пункт у живих доксах, це може бути не твоя невдача.

## ⚠️ Поправка до попереднього запису
Захистів перед `-p` над чужим репо **чотири, а не три**. Четвертий — **`--setting-sources
user`**, і в живому `permissions.md` він стоїть у переліку **першим** *(від `c19`)*.

## Ще два розриви версій (разом із попередніми — пʼять)
- **`--permission-prompts`** — центральний прапорець доксів для «нікому відповідати на
  запити дозволу» — у 2.1.236 **не існує** (потребує v2.1.259). Три докази: помилка розбору,
  `strings` = 0, контрольна назва = 0 *(від `c19`)*.
- **`remote-control.md` перелічує опції, яких немає у власному `--help` 2.1.236:**
  `--sandbox` / `--no-sandbox` і `manual` як псевдонім `default` для `--permission-mode`
  *(від `c20`)*.
- **`--channels` навпаки — існує у збірці (18 збігів у `strings`), але свідомо прихований
  із `--help`** (0 збігів). Докси це прямо визнають: «The flags work even though they aren't
  listed» *(від `c20`)*.

## `c22` · Безпека — три нові готові блоки
- **Найгостріше з усього, що знайдено для цього модуля** *(від `c19`)*: `permissions.md` →
  «What runs before you trust a folder» — таблиця на сім рядків. У `claude -p` над
  недовіреною текою **`permissions.allow` і `additionalDirectories` НЕ діють** (тихе
  попередження `this workspace has not been trusted` у stderr), а **хуки й `.mcp.json` —
  діють**. Тобто небезпечне вмикається, а обмежувальне мовчки вимикається. Там же ручний
  спосіб довіри: `projects["<path>"].hasTrustDialogAccepted` у `~/.claude.json`.
- **Рутини:** текст, переданий у поле `text` при запуску по API або «Run now», приходить у
  сесію загорнутим у **`<routine-fire-payload>`** і підписаний як недовірені дані; рутина
  діє на нього лише якщо її збережений промпт прямо про це просить. Мотив названий дослівно —
  захист від витоку токена. До v2.1.213 сам збережений промпт приходив як недовірене фонове
  сповіщення *(від `c20`)*.
- **Канали:** якщо канал оголошує «permission relay», **той, хто може писати в канал, може
  схвалювати виклики інструментів у твоїй сесії**. Найкоротший приклад «список дозволених
  відправників = список людей із правом підпису» *(від `c20`)*.
- **Remote Control:** поки він підʼєднаний, **запис розмови зберігається на серверах
  Anthropic**; організації з вимогою Zero Data Retention увімкнути функцію не можуть. Плюс
  окрема бета «Trusted Devices» на Team/Enterprise — цілий розділ, не займаний *(від `c20`)*.

## `c21` · Наскрізний проєкт (пише останнім — читай це першим)
- **`/goal` — це обгортка над сесійним prompt-based Stop-хуком.** Наслідок: він недоступний
  там само, де недоступні хуки з файлів налаштувань (правило довіри до теки), при
  `disableAllHooks: true` і при `allowManagedHooksOnly`. У кожному разі команда **пояснює
  причину**, а не мовчить *(від `c20`)*.
- `claude -p "/goal <умова>"` **проганяє весь цикл до завершення в одному виклику**; при
  дефолтному текстовому виводі до кінця не друкується нічого, тому виглядає зависанням —
  рятує `--output-format stream-json --verbose` *(від `c20`)*.

## `c11` · Гроші — живе число
Відповідь «OK» на легкій моделі коштувала **$0.0189734**, і майже все — це
`cache_creation_input_tokens: 8018`, тобто **стартовий вантаж, а не відповідь**. Готова
ілюстрація тези «платиш не за відповідь, а за контекст» *(від `c19`)*.
Плюс: рутини мають **окрему добову межу кількості запусків** поверх лімітів підписки, і
**одноразові запуски до неї не зараховуються** *(від `c20`)*.

## Довідники
- **`ref-commands` — псевдоніми, підтверджені доксами й знімком** *(від `c20`)*:
  `/schedule` = `/routines` · `/loop` = `/proactive` · `/teleport` = `/tp` ·
  `/remote-control` = `/rc` · `/desktop` = `/app` · `/background` = `/bg` ·
  `/mobile` = `/ios` = `/android`. Версійна межа: `/remote-control` **до v2.1.206** віддавав
  `Unknown command`, коли ти не ввійшов.
- **`ref-commands` — колонка «поведінка в `-p`»** тепер зібрана з трьох авторів:
  `/model sonnet`, `/effort`, `/fast`, `/color`, `/rename` приймають значення аргументом ·
  `/mcp` без аргументів друкує текстовий підсумок · `/config ключ=значення` міняє
  налаштування (усе **v2.1.205+**) · `/goal` працює (exit 0) · **`/status` і `/hooks`
  віддають `isn't available in this environment`** · **`/doctor` зависає** · `/login`
  недоступний.
- **`ref-settings`:** додати `channelsEnabled`, `allowedChannelPlugins`,
  `remoteControlAtStartup`, `disableRemoteControl`, `dialogExpiry`, `crossSessionInbound` +
  змінні `CLAUDE_CODE_DISABLE_CRON`, `CLAUDE_CODE_GOAL_CHECKIN_MINUTES`,
  `CLAUDE_REMOTE_CONTROL_SESSION_NAME_PREFIX`, `CLAUDE_CLIENT_PRESENCE_FILE`.
  ⚠️ **`remoteControlAtStartup` має асиметричний precedence:** у проєктному/локальному файлі
  Claude Code поважає `false`, але **ігнорує `true`** — щоб закомічений файл не вмикав
  Remote Control усім, хто відкрив репозиторій.
- Локальні задачі застосунку живуть у `~/.claude/scheduled-tasks/<імʼя>/SKILL.md`; у файлі
  лише `name`, `description` і тіло промпта — **розписання, тека, модель і стан там не
  зберігаються**.

## П'ята реакція програми на неправильний ввід (для `c17`)
`claude -p "x" --bg` віддає **осмислене пояснення конфлікту з підказкою**, як переписати
команду (`claude --bg '<task>'`), а не сухе «unknown option». Плюс `--json-schema` розрізняє
«не JSON» і «не схема» **двома різними повідомленнями**, обидва exit 1 *(від `c19`)*.

---

# Знахідки хвилі 3в — `c21` і `c22` (2026-09-04, останні два уроки)

Обидва автори працювали одночасно, тому **`c21` не бачив звіту `c22` і навпаки**. Усе, що
нижче помічене як «прийшло запізно», в уроки НЕ потрапило — це матеріал для трьох
довідників і для іспиту.

## 🔴 Розривів версій тепер сім, а не пʼять

Два нові, кожен підтверджений трьома доказами (докси · `strings` · контрольна вигадана назва):

6. **Стиль виводу `Concise`** — `output-styles.md` перелічує його серед **вбудованих** із
   приміткою «Requires Claude Code v2.1.237 or later». Встановлено **2.1.236**, на одну
   версію менше: `strings … | grep -c Concise` → **0** при `Explanatory` → **8** *(від `c21`)*.
7. **`permissions.blockReadsOutsideWorkingDirectories`** — у доксах є й діє **навіть у
   `bypassPermissions`**, позначений «requires v2.1.257»; у 2.1.236 `strings` → **0**
   *(від `c22`)*.

⚠️ **Пастка методу `strings`, яка може дати хибний доказ:** пошук чутливий до регістру й до
того, чи слово взагалі рідкісне. `grep -c Concise` → 0, але `grep -ci concise` → **83**, бо
це звичайне англійське слово всередині промптів у бінарнику. Перевіряючи назву, що збігається
зі звичайним словом, шукай **точний регістр** і став контрольну вигадану назву поруч.

## Для довідника `ref-commands`

- **`/code-review` памʼятає рівень зусиль між сесіями** і каже про це рядком
  `Reusing high effort, the level you typed last time`. Рівень, переданий у `-p`,
  запамʼятаного **не оновлює**; `ultra` не оновлює й не використовує його.
- **Розбір хвоста рядка різний:** без `ultra` все після прапорців — це ціль, навіть якщо
  починається з імені іншої команди (`/code-review /fix-issue 123` огляне текст
  `/fix-issue 123`); з `ultra` одне слово читається як гілка або номер PR, довший текст стає
  нотаткою до огляду.
- **`/ultrareview` — псевдонім `/code-review ultra`**: три безкоштовні запуски на Pro і Max,
  далі кредити. Коли запуск має списати кредити, `-p` **зупиняється до старту** (підтвердження
  потребує інтерактивної сесії); для скриптів є підкоманда `claude ultrareview`, її `--help`
  звірений наживо: `--json`, `--post`, `--no-post`, `--timeout <minutes>` (30 за замовчуванням).
- **`/verify` запускається лише за твоїм викликом** — до v2.1.215 Claude міг сам.
- **`skillOverrides: {"code-review": "user-invocable-only"}`** забороняє Claude і завданням за
  розписанням запускати огляд самостійно, лишаючи команду тобі.
- **`/insights`:** до **200** нових сесій на запуск · звіт у `~/.claude/usage-data/report.html`
  + копія з міткою часу · видаляється за `cleanupPeriodDays` · **токени рахуються тобі** ·
  недоступний у хмарних сесіях.
- **`claude project purge` окремо друкує, чого НЕ чіпає:** `shell-snapshots/` (не прив'язані
  до проєкту) і `backups/` — до пʼяти ротованих знімків `~/.claude.json`, у яких запис про
  проєкт може лишитись. Готова колонка «що не видаляє».
- **`/heapdump`** — у довідник із поміткою: `.heapsnapshot` містить усю розмову й облікові
  дані. `c22` свідомо не показав його в уроці («вчити стріляти собі в ногу»).
- **`claude gateway --help` звірений удруге 2026-09-04** — вивід не змінився з `screens/`.
- **`claude ultrareview --help` наживо = знімок від 2026-09-03**, байт у байт.

## Для довідника `ref-settings`

- **Готовий paste-ready приклад `permissions.deny`** живе в `settings-reference.md` під
  якорем `#exclude-sensitive-files` — найкорисніший фрагмент сторінки для довідника.
- **Дослівна межа, яку довідник мусить нести колонкою:** `Read`/`Edit` deny-правила діють на
  вбудовані файлові інструменти **й на упізнані файлові команди Bash** (`cat`, `head`, `tail`,
  `sed`), але **не на довільні підпроцеси** — «for OS-level enforcement enable the sandbox».
- **Прив'язка шляхів у користувацьких налаштуваннях:** `Read(/secrets/**)` там означає
  `~/.claude/secrets/**`, а не проєкт — потрібні `//` або `~/`. `Read(.env)` ≡ `Read(**/.env)`;
  `Read(//**/.env)` — усе на диску; Windows — `//c/**/.env`.
- **`claudeMdExcludes` мержиться між областями** (масиви складаються), і **керовані
  `CLAUDE.md` виключити не можна**.
- **Група «git і атрибуція»:** `attribution.commit` / `.pr` / `.sessionUrl` ·
  `includeCoAuthoredBy` (**deprecated з v2.0.62**) · `includeGitInstructions` (перебивається
  `CLAUDE_CODE_DISABLE_GIT_INSTRUCTIONS`) · `prUrlTemplate` із пʼятьма підстановками
  `{host} {owner} {repo} {number} {url}`.
- **Група доступності:** `axScreenReader`, `prefersReducedMotion`, `theme` (з
  `dark-daltonized` / `light-daltonized`), `preferredNotifChannel`, `voice.enabled` /
  `voice.mode` + змінні `CLAUDE_AX_SCREEN_READER`, `CLAUDE_AX_STARTUP_QUIET_MS` (v2.1.217+),
  `CLAUDE_AX_PREPARK_MS` (v2.1.233+), `CLAUDE_CODE_ACCESSIBILITY`.
- **Статус-лайн:** `statusLine.padding`, `statusLine.refreshInterval` (мінімум 1 с),
  `subagentStatusLine`, `footerLinksRegexes`.
- **`worktree.sparsePaths`** — 12 збігів у `strings` 2.1.236, тобто у stable-збірці ключ є.
- **Свідомо не взяте в `c22`, місце йому тут:** `sandbox.credentials` / «Mask credentials» /
  `network.tlsTerminate` / ліміт 8 MiB / re-signing AWS · `allowManagedHooksOnly` ·
  `permissions.disableAutoMode` · `disableBypassPermissionsMode` · `enableWeakerNestedSandbox` ·
  `allowAppleEvents`. ⚠️ Показувати як **межі й наслідки**, не як рецепт ескалації.

## Для довідника `ref-hooks`

- **`ConfigChange`-хуки** названі в `security.md` → «Team security» штатним способом
  **аудиту або блокування змін налаштувань під час сесії**. Це готове формулювання «навіщо
  ця подія» для рядка `ConfigChange` *(від `c22`)*.
- **`Stop`-хук, який пропонує оновлення `CLAUDE.md`** — `large-codebases.md` радить його як
  робочу звичку: хук отримує шлях до транскрипту сесії, тож скрипт може переглянути сесію й
  запропонувати правку, «поки прогалина свіжа». **Прийшло запізно, в `c21` не потрапило.**

## Для іспиту `c23` — готові питання

- **Чотири фази циклу, не пʼять**, і перевірка не серед них — найчастіша помилка переказів.
- **`Concise` у доксах є, у збірці немає** → питання про **метод**, а не про факт.
  Приманка для хибної відповіді: «запустити з `--help`» — бо `--help` глушить помилку про
  невідомий прапорець (механізм із `c17`).
- **Асиметрія довіри в `-p`:** хуки й `.mcp.json` діють, `permissions.allow` — ні. У `c22` це
  вже питання №1 квіза, тож в іспиті брати **з іншого боку** (через `--bare`).
- **`/clear` проти `claude project purge`** — розрізняє тих, хто зрозумів модель зберігання.
- **`keep-coding-instructions: false` за замовчуванням** — «Claude раптом розучився писати код».
- **Стеля `Stop`-хука — 8 блокувань поспіль** (проти інтуїції «детермінований гейт всемогутній»).
- **`/rewind` не повертає правки фонового `/code-review --fix`** — вони поза чекпойнтами сесії.

## Для `c17`, якщо колись буде доповнення

`/security-review` дає **помилку зі змінним текстом**: огляд запускає кілька git-команд одразу
й повідомляє про ту, що впала першою, тому в лапках щоразу може стояти інша команда
(відтворено наживо, exit 128). Поруч дві сусідні помилки того ж механізму:
`Shell command permission check failed for pattern "…"` — **впроваджені команди навичок ніколи
не питають дозволу**, при `ask` чи `deny` виклик навички просто переривається — і
`Skill <name> requires bash … but Git Bash was not found`.

## Матеріал, що лишився невикористаним узагалі

- **Керована служба Code Review для GitHub** (Team/Enterprise, недоступна при Zero Data
  Retention, рівні `🔴 Important / 🟡 Nit / 🟣 Pre-existing`, налаштування через `REVIEW.md`,
  `@claude review` як ручний запуск) — `c21` лишив її `c22`, `c22` не взяв. Кандидат у
  `ref-commands` одним рядком.
- **Плагін `security-guidance`** і **`/security-review`** («run an on-demand security pass over
  the changes on your current branch») — `c22` передав їх `c21`, але `c21` уже дописував.
- **Артефакти** (`enableArtifact`, пʼять умов одночасно) — не пішли ні в `c15`, ні в `c21`.
- **`Revisit after major model releases`** із `large-codebases.md`: інструкції-костилі під
  старі моделі треба видаляти, бо вони стають накладними витратами.

## Зауваження до контракту уроку (для кореневої сесії, не для авторів)

Обидва автори незалежно вперлися в те саме:

1. **Норма «2–5 блоків `.term`» тисне ЗВЕРХУ** на модулях із багатою CLI-поверхнею: чесно
   знятого матеріалу було на 7–8 блоків, довелось переводити частину в `<pre><code>` без
   рамки — і вони втратили підпис із версією.
2. **§9.5 (приватність) конфліктує з демонстраціями:** найпереконливіші перевірки друкують
   шляхи в домашню теку. Другий спосіб, вартий запису в контракт: `"$(which claude)"` замість
   абсолютного шляху до бінарника.
3. **`.example` з парою «Промпт / Відповідь» частково непридатний** за правилом свіжості —
   вигадану відповідь моделі в `.tag-model` класти не можна. Обидва автори використали
   компонент **лише для промптів**; це варто записати в §4 прямо, бо CSS-коментар пропонує пару.
4. **Дрібниці, на які кожен новий автор витрачає час:** мітки на стрілках Mermaid у всіх 20
   уроках мають форму `-->|"ні"|`, а не `-- "ні" -->`; слово «чекпойнт» у чотирьох уроках
   пишеться через «й».

---

# Знахідки фінальної хвилі — три довідники та іспит `c23` (2026-09-04)

Курс дописаний: **26 сторінок із 26**. Нижче — те, що знайшли чотири останні автори, і
поправки до вже записаного в цьому файлі.

## 🔴 Найбільша знахідка дня: квіз проходиться без читання уроку

Автор `c23` помітив у власному іспиті, що **правильна відповідь виявилась найдовшим
варіантом у 22 питаннях із 26**, і полагодив це подовженням дистракторів (стало 9 із 26 при
випадковому очікуванні ~6,5). Перевірка кореневою сесією показала, що це **не його
особливість, а властивість усієї платформи**:

| Курс | Правильна = найдовша | Стан |
| --- | --- | --- |
| AI Академія | **85 %** (82 із 96) | у проді |
| AI Architect | **99 %** (134 із 136) | у проді |
| AI Термінал, уроки `c01`–`c22` | **82 %** (130 із 158) | у `dev` |
| AI Термінал, іспит `c23` | 35 % (9 із 26) | полагоджено автором |

Разом ~390 питань. Причина механічна: правильна відповідь описує механізм і потребує
уточнень, хибна — коротке гасло. **Лікується подовженням дистракторів, не скороченням
правильних** (скорочення правильної псує точність факту).

**Рішення власника 2026-09-04: не чіпати зараз.** Дефект спадковий і однаковий у трьох
курсах, тож лагодити лише 005 — половинчасто. Іде хвостом у `dev/design/005-ai-terminal/SUMMARY.md`
як кандидат в окрему задачу на всі три курси, разом із перевіркою в `check-lessons.py`:

```python
sum(1 for q in qs if q['options'][q['answer']] == max(q['options'], key=len))
```

## ⚠️ Поправка до методу `strings` — доказом є ТІЛЬКИ нуль

Записане раніше «`strings` показує, чи є функція у збірці» **неповне й дає хибні докази**:

- `timeZone` (потребує 2.1.257) → **19** збігів у 2.1.236;
- `modelPicker` (2.1.242) → **23** збіги;
- обидва — звичайні ідентифікатори JS усередині бінарника *(від `ref-settings`)*.

Плюс раніша пастка регістру: `grep -c Concise` → 0, `grep -ci concise` → **83** *(від `c21`)*.

**Отже:** нуль при непорожній контрольній вибірці — доказ відсутності; ненульове число не
доводить нічого. Так це й описано на сторінках `ref-settings` і `ref-hooks`.

Друга половина тієї ж поправки: **версійні примітки не можна брати регуляркою.**
`apiKeyHelper` містить «Requires v2.1.246 or later», але це про оновлення протермінованого
JWT, а не про сам ключ; так само `disableAllHooks` / 2.1.242 і `blockedMarketplaces` / 2.1.223.
Автор `ref-settings` прочитав усі 56 кандидатів очима й лишив 45.

## Числа, які змінились після попередніх записів

- **Розрив версій — 24 релізи, не «~15» і не «~20»:** встановлено 2.1.236, `latest` на
  2026-09-04 = **2.1.260** (`npm view` і `registry.npmjs.org`, двома авторами незалежно).
- **Ключів `settings.json` — 223**, і це не переказ: `comm` живого переліку проти знімка
  `b-settings-keys-full.txt` дав **рівно один** новий рядок
  (`permissions.blockReadsOutsideWorkingDirectories`), решта 222 збіглися побайтово.
- **`sandbox.*` — 37 ключів, а не 35**, як записано від `c08`/`c13`: докси виросли.
- **Розклад областей `credentials.*` уточнено:** із шести ключів лише три
  (`allowPlaintextInject`, `awsPairs`, `sigv4`) — user-or-managed; `credentials`,
  `credentials.files`, `credentials.envVars` — будь-який файл. По всій темі: 4 managed ·
  8 user-or-managed · 26 будь-який.
- **Слеш-команд 111** — підтверджено втретє, набори імен у знімку й у живих доксах ідентичні.
  **`claude --help` — 62 опції й 13 підкоманд.**
- **Гарячі клавіші: 120 рядків дій у 23 групах, 112 унікальних імен, 20 контекстів**
  (двадцятий — `Scroll`). Підзаголовок у `claude-code.config.json` казав «101 клавіша у 19
  контекстах» — **виправлено кореневою сесією** на формулювання без спірного числа.

## Швидкість застарівання — тепер із доказом

Автор `ref-commands` зробив машинний діф усіх 111 описів «знімок 2026-09-03 проти живих
доксів 2026-09-04»: **110 збіглися байт у байт, один переписали** — рядок `/diff` скоротили,
подробиці про переглядач змін переїхали в `interactive-mode.md`.
**Метод, вартий повторення:** якщо для теми є і знімок, і жива сторінка — роби машинний діф
описів; дві команди дають готовий факт і ловлять переписані рядки, яких оком не видно.

## Не підтверджено — і тому не пішло в текст

- **`/fullscreen` як псевдонім `/tui`** (записано від `c05`): у живій таблиці доксів
  2026-09-04 такого псевдоніма немає, а `strings` тут не годиться — слеш-команди лежать у
  бінарнику без провідної риски. **У довідник не взято.**
- **`/ultraplan`** — докси кажуть «Removed», версії не називають. Так і написано.
- **Що 31 подія у 2.1.236 саме *працює*** — `strings` доводить лише наявність рядка.
  Прямо сказано на сторінці `ref-hooks`.

## Форма сторінки-довідника — три автори зійшлися на ній незалежно

Контракт уроку її не описує; закріпити варто саме цей набір:

- **`data-own-title` на `<html>` обовʼязковий** — інакше `js/config.js` (рядки 68–72)
  перезапише `<title>`: підстановка вимикається лише `data-module` або `data-own-title`.
- Скрипти: `progress` → `ui` → `config` → `auth-ui` → `auth`. **Без `module.js`, `quiz.js`,
  без `data-module`, без `#quizData`.**
- **Широкі таблиці:** `.table-wrap` не існує, `.prose-aia table` не прокручується →
  `<div class="overflow-x-auto" tabindex="0" role="region" aria-label="…">` + `min-w-[…]`.
  `tabindex` тут не косметика: область із прокруткою мусить фокусуватись із клавіатури
  (WCAG 2.1.1).
- **Довгі ключі рвуть верстку на телефоні** — `<wbr>` після крапок у назві
  (`permissions.blockReads…` — 46 символів моноширинним). У буфер обміну `<wbr>` нічого не додає.
- **Норма «2–5 блоків `.term`» на довідники не поширюється** — матеріал за природою табличний.
- **`<br/>` у підписах вузлів Mermaid не використовувати:** `js/mermaid-init.js` ставить
  `securityLevel: "strict"`, і жодна з 22 сторінок курсу так не робить.

## Іспит `c23` — що варто знати далі

- **26 питань**, поріг **85** (звірено з міграцією `005-1`: `c23` — 85, `c01`–`c22` — 70),
  покриття — щонайменше одне питання з кожного з 22 модулів. Питань по довідниках немає
  свідомо (програма: «квіз по довіднику тренував би памʼять»).
- 🔴 **Поріг не є воротами, і на сторінці це сказано чесно.** `js/progress.js` шле в
  `submit_quiz` `p_score = 100` незалежно від результату квіза, а завершення ставить кнопка
  «Позначити завершеним». Тому текст іспиту називає 85 % **вимогою до себе**, а не обіцянкою
  сайту. **Для QA:** це поведінка всієї платформи, не дефект 005.
- **Для QA окремо:** `c23` — останній модуль курсу, тож саме він тригерить
  `confirmCertificateName` через `isFinalClick()` у `js/module.js`. Сценарій «остання
  сторінка → підтвердження імені → сертифікат» на цьому курсі не виконувався **жодного разу**.

## Дрібне, для наступних правок

- **`ConfigChange`-хук не спрацьовує на керовані налаштування з MDM або консолі claude.ai**
  — дослівно з `settings.md` → «When edits take effect» *(від `ref-settings`)*.
- **`DirectoryAdded`** запускається успішним `/add-dir` — готове «коли спрацьовує»
  *(від `ref-commands`)*.
- **Три ключі читаються один раз на старті** й правкою файла посеред сесії не міняються:
  `model` (лікує `/model`), `effortLevel` + `modelSettings` (`/effort`), `outputStyle`
  (потрібен `/clear`). Решта, включно з `permissions` і `hooks`, підхоплюється на льоту.
- **`/status` показує рядок `Setting sources`** — які файли завантажені, але **не** який файл
  дав яке значення; для другого потрібен `claude doctor`.
- **Таймаут поводиться протилежно на двох подіях:** на `PreToolUse` завислий хук **не
  блокує** виклик, на `PreModelSwitch` скасований по таймауту хук **блокує** перемикання.
- **`WorktreeCreate`** — єдина подія, чий хук повністю заміняє поведінку git, і
  `.worktreeinclude` при цьому не обробляється.
- **Сім винятків із precedence** зібрані таблицею на сторінці `ref-settings` (`#trap3`):
  керована політика може **заборонити**, але не може **ввімкнути** те, що ти вимкнув свідомо.

## Що додати в контракт уроку (накопичено за два дні, для кореневої сесії)

1. **Розділ про довідник** — увесь набір із «Форма сторінки-довідника» вище.
2. **Розділ про іспит:** чекер тримає вимоги уроку як помилки (≥ 9 секцій `data-lesson`,
   ≥ 1 Mermaid, ≥ 1 `.term`), тож «коротка сторінка з великим квізом» неможлива без червоного
   прогону. `c23` вирішив це вісьмома блоками з іншим змістом — рішення автора, не контракту.
3. **Розкид `answer` і довжина варіантів** — §6 мовчить про обидва.
4. **`"$(which claude)"` як спосіб не світити домашній шлях** — §9.5.
5. **`.example` лише для промптів:** вигадану відповідь моделі в `.tag-model` класти не можна
   (правило свіжості), хоч CSS-коментар пропонує пару.
6. **Mermaid:** мітки на стрілках у формі `-->|"ні"|`; `<br/>` заборонений.
