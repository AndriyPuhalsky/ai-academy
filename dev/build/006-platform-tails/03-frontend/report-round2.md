# 006 — Звіт фронтенду · коло фіксів 2 (2026-09-06)

*Стислий запис фінального повідомлення `aia-build-frontend`; повні таблиці замірів — у транскрипті агента.*

**Коміти:** `a348782` (D-01) · `23bd7fc` (D-02) · `3c1d9a1` (D-03) · `1fff106` (памʼять). Превʼю віддає всі чотири файли побайтово = репозиторій.

| Дефект | Файл | Що змінено |
| --- | --- | --- |
| D-01 | `css/custom.css:370–379` | у `@media (min-width:1024px)` `.module-sidebar` дістав `z-index: 10` (базове 50 потрібне шторці на мобільних — над оверлеєм 45 і шапкою 40; десктопний блок перевизначав усе, крім `z-index`) |
| D-02 | `css/custom.css:2122–2160`, `js/config.js:422`, `js/claude-code-render.js:183` | новий клас `.navprog-label` (база = дослівні декларації `sr-only`, `@media (min-width:640px)` = `not-sr-only`); у обох копіях пілюлі `sr-only sm:not-sr-only` → `navprog-label` одним комітом; коментарі оновлені |
| D-03 | `verify.html:56` | `aria-label="Код сертифіката"` на `#verifyInput` — дерево доступності: `textbox "Код сертифіката"`, бокс без змін |

**Заміри D-01** (iframe, розблокований урок, прокрутка в низ, сітка `elementFromPoint` 5×5 у смузі шапки): до — 25 із 50 точок у сайдбар; після — 0 із 50 на `module-01`, `architect-01`, `claude-code-01` при 1440×653 і 1440×1200; `asideTop` ті самі −72,1 / −71,8 / −117,6, що в QA — змінився лише порядок накладання. Повторено на живому превʼю. Мобільна шторка (390×844, 768×1024): `z` 50/45/40, `translateX(0)`, 16/16 точок смуги шапки у шторку — як була. К9 (непрозора шапка при `max-height:820`) не чіпав.

**Заміри D-02** (`MutationObserver`, ширина в колбеку вставки): 1440 — 119,61 у кадрі вставки і через 760 мс, `slotLeft` 1057,35 без руху (у колі 1 зсув 64,81); 390 — 54,8, спан `absolute` + `clip`. `.navprog-label` у CSSOM ще до першого рендеру; резерв 119,59/54,80 проти фіналу 119,61/54,80 — як було.

**Для QA кола 2:** шторка на 390/768 над шапкою і sticky-колонка на ≥1024 під нею — в одному прогоні; плавність шторки (у фоновій вкладці переходи не йдуть — кінцевий стан доведений, рух ні); D-02 у передньому плані з реальним прогресом; латентний ризик — будь-яка нова Tailwind-утиліта лише з JS поверне CLS (`grep` класу по HTML → 0 = JIT із затримкою); `/verify` під VoiceOver.

**Поза обсягом:** на висоті > 820 px шапка напівпрозора з blur і сайдбар тепер проходить під нею — рішення власника (критерій 39); хвости кола 1 як були (`.cc-donate__link`, `table-layout`, «Нагору ↑» 16 px).
