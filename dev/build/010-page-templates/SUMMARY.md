# 010 — Увесь сайт на дизайн-систему 009 · Підсумок

**Стан на 2026-09-08, вечір: пройдено коло 1, коло фіксів, коло 2 і хвилю з чотирьох
фіксів. КОЛО 3 (останнє за лімітом) — у роботі. У `main` НЕ їхало.**

**Закрито дефектів: 14** — D-01…D-09, D-11, D-14, D-16, D-21, D-22.
**Відкрито рішень власника: 10** — усі списком у кінці цього файла.

## Коротка хронологія, щоб не читати все

| Етап | Що дав |
| ---- | ------ |
| фронтенд (5 агентів, 3 хвилі) | 66 HTML на єдиній системі, `custom.css` видалений, ≈ 3 год |
| **коло 1 QA** (2 агенти) | «не пройдено», 11 дефектів, 4 блокери |
| фікси кореневої сесії | D-01…D-04 закриті |
| **коло фіксів** (агент Ф-Е) | D-05…D-09, D-11 — шість дефектів за рішенням власника «не відкладати» |
| **коло 2 QA** (2 агенти) | 9 із 10 підтверджено закритими; блокери D-14 і D-21; нові D-22…D-24 |
| фікси кореневої сесії | D-16 (текст діаграм), D-21 (CLS), D-22, D-14 — усі з A/B-замірами |
| **коло 3 QA** | у роботі |

- **Заведено:** 2026-09-07 · **фронтенд закрито:** 2026-09-07 (той самий день)
- **Дизайн:** `dev/design/010-page-templates/SUMMARY.md` → пакет `04-package/`, варіант A «Дідона й кістка»
- **Коміти:** `3e373a8` … `2216ca7` — **20 комітів**
- **Обсяг:** 87 файлів, +13 975 / −13 214 рядків
- **Плану `01-plan.md` немає свідомо** (рішення Р-3): пакет уже містив порядок робіт, карту міграції, критерії й пастки

## Що зроблено

Усі 66 публічних HTML переведені на єдину дизайн-систему. Три платформи — AI Академія,
AI Architect, AI Термінал — тепер стоять на одному наборі токенів, і курсовий акцент
(шафран / патина / лазур) перемикається одним атрибутом `data-course` на `<html>`.

| Було | Стало |
| ---- | ----- |
| 66 інлайнових копій `tailwind.config` | один `js/tw-theme.js` з `var(--токен)` |
| `css/custom.css` — 2 535 рядків, накопичених за три роки | видалений |
| 4 файли CSS сумарно 4 213 рядків | 4 файли, 4 418 рядків, **нуль значень поза токенами** |
| 540 альфа-модифікаторів у 72 файлах | 0 (три згадки лишились у коментарі `tw-theme.js`) |
| 388 імен класів старої мови | 0 живих |
| `prefers-reduced-motion` — 57 поіменних перевизначень | два рядки в `tokens.css` |

## Хвилі

`P3-order.md` дозволяє паралелити лише після етапу 2 — усе стоїть на темі. Звідси три хвилі
з зонами запису, що не перетинаються.

| Хвиля | Агент | Етапи | Час |
| ----- | ----- | ----- | --- |
| 1 | Ф-А (соло) | 0 · 1 · 2 — тема, `verify.html`, хром 66 сторінок, 540 альфа-модифікаторів, аркуш акаунта | 50 хв |
| 2 | Ф-Б ∥ Ф-В ∥ Ф-Г | 3 · 4 (57 уроків) ∥ 5 · 6 (лендінги, довідники) ∥ 7 (роадмап) | 55 / 44 / 35 хв |
| 3 | Ф-Д | 8 · 9 — діалоги, сертифікат, видалення `custom.css` | 87 хв |

**Разом ≈ 3 години** роботи агентів проти початкової оцінки 6–10.

Звіти: `03-frontend/report-a.md` · `report-b.md` · `report-v.md` · `report-g.md` · `report-d.md`.
Кожен закінчується розділом незалежної перевірки кореневою сесією.

## Три рішення власника перед запуском — не переоткривати

1. **Аркуш акаунта — робимо.** «Сертифікати» і «Вийти» переїхали під аватар, слот 192 px,
   CLS = 0 за конструкцією. ⚠ Доручення дизайн-сесії й `open-decisions.md` тут **суперечили**
   одне одному; суперечність закрита. Виявилось, що вигадувати нічого не треба —
   `components.css:1206` уже містив `--hdr-slot-w: 192px` і коментар про переїзд, а сам
   аркуш склався з уже намальованого дропдауна `.ds-nav__group`.
2. **Кегль коду на мобілці — A, плаский 13,52.** Уже в пакеті, роботи нуль.
3. **PM-агента не запускали** — пакет уже містив усе, що робить PM.

## Що коренева сесія виправила у спільних файлах

Агентам спільні файли були відкриті лише на читання (інакше троє паралельних затирали б
роботу один одного мовчки). Усе, що вони знаходили, йшло у звіт готовим CSS.

**`js/mermaid-init.js` — три дефекти, що приїхали з пакета.** Файл скопійований побайтово,
тобто дефекти в **самій системі 009**:
- `classDef` дописувався в **кожну** діаграму, а `sequenceDiagram` його не парсить —
  **шість діаграм на пʼяти сторінках AI Architect не малювались узагалі**
  (`architect-08`: `flowchart-v2, error, error` → після фікса `flowchart-v2, sequence, sequence`);
- один спільний `.catch` на весь список вішав напис «не вдалося намалювати» на **здорові**
  діаграми (3 написи на 3 діаграми при 2 падіннях → 0);
- запуск на `DOMContentLoaded`, тобто до завантаження вебшрифта: `module-01` мав
  **23 з 39** обрізаних міток → **0 з 39**.

**`css/components.css` секція 37** — `<code>` у заголовку прози (125 заголовків у 18 файлах)
отримував абсолютні 13,52 px усередині `h1` на 64 px. Застосовано варіант, що **дослівно
відновлює прод** (`0.9em`): заміряно `h1` 64 → 57,6, `h3` 20 → 18.

**`css/components.css` секція 38** — те, що тримав видалений `custom.css`. Дві найсерйозніші
заяви звірені з вмістом видаленого файла **до** застосування:
- **три кружечки термінала** — прод малював їх одним вузлом через `box-shadow` 12/24 px,
  система малює один. 107 терміналів;
- **колізія всередині самої системи:** `.ds-ftr a` (0,1,1, рядок 463) перебиває
  `.rm-entry` (0,1,0, рядок 1330) — **специфічність сильніша за порядок**. Підкреслення
  «План розвитку →» мало ширину **0 px** на чотирьох сторінках → **100,9 px** після фікса;
- `::selection` і смуга прокрутки: прод їх мав, система ролі не описала. Відновлено
  **поведінку** через токени, не через літерали.

## Ворота, звірені кореневою сесією незалежно від звітів

| Перевірка | Було | Стало |
| --------- | ---- | ----- |
| `tailwind.config` у файлах | 66 | **0** |
| Альфа-модифікатори | 540 у 72 файлах | **3**, усі — текст у коментарі `tw-theme.js` |
| `class="…bg-ink…"` | 66 | **0** |
| Стара палітра як класи | була скрізь | **0** |
| Живих `<link>` на `custom.css` | 66 | **0** |
| `scroll-mt-24` у `modules/` | 433 | **0** |
| `<table>` / `ds-tbl__wrap` в уроках | 111 / 101 | **111 / 111** |
| `@property` у `tokens.css` | — | **29** (треба ≥ 26) |
| Регістр шляхів | 0 | **0** |
| Консоль | — | **0 помилок**, лише штатне попередження Tailwind CDN |

**Чекліст етапу 1 сходиться повністю.** Два рядки стояли відкритими з першої хвилі
(Ф-А свідомо не додав статичний «0 / 12» — лічильник, який ніколи не рахує, це брехня);
закрив їх Ф-Д. Заміряно живцем: `0 / 12` → `12 / 12` → `0 / 12`.

## Що лишається власнику — жодне не блокує QA

1. **Акцент PDF сертифіката.** Теракота (як затверджено в 005) чи акцент курсу — тоді
   сертифікат Академії став би шафрановим, Architect патиновим, Термінал лазуровим.
   Технічно це один атрибут `data-course` на офскрин-вузлі. Контраст обох теракотових
   відтінків (3,90 і 3,53) нижчий за AA 4,5 для дрібного тексту — питання «який відтінок»
   тягне питання «чи не має дрібний текст узяти `--c-paper-text`».
2. **Перемикач «Вхід / Реєстрація» більше не таблист** — рішення пакета (табів як сутності
   в 009 немає, це `ds-btn` з `aria-pressed`), але воно змінює доступність робочої модалки
   задачі 001. У QA — скрінрідером.
3. **Хореографія сесії hero на лендінгу Термінала знята.** Було 12 таймлайнів GSAP
   (посимвольний друк → пауза «машина думає» → каскад `⏺`-кроків → диф → pop-in діалогу),
   стало одна горизонтальна штора. Не свавілля: вся хореографія трималась на девʼяти класах,
   яких у 009 немає і які жили саме у видаленому `custom.css`; каретка малювалась
   псевдоелементом усередині `.term`, що прямо заборонено (Chrome бере псевдоелементи
   в буфер копіювання). Відкат коштує ~200 рядків GSAP і девʼяти імен у CSS.
4. **`roadmap.html` — єдина з 66 сторінок без `data-page-in`.**
5. **Діаграми Mermaid не центруються** — `.ds-diag` центрування не задає, а старе правило
   зникло на етапі 3.
6. **Версії платформ і `site.updated` не підвищені** — свідомо: до QA число було б
   передчасним. Підвищувати перед мержем у `main`.

## Тестування: коло 1

**Вердикт QA — «не пройдено»**, 11 дефектів. Звіти: `04-qa/report.md` (загальний, 38 скріншотів)
і `04-qa/report-diagrams.md` (прицільний аудит 100 діаграм). Обидва — з незалежною перевіркою
кореневої сесії в кінці.

**Що QA сказав про якість міграції загалом:** консоль порожня на 17 сторінках, мережа без
жодного 4xx, **60 fps** тримається на обох анімованих сторінках, `prefers-reduced-motion`
вимикає рух повністю й точно, клавіатурний контракт працює, копіювання з термінала байт у
байт, контраст без жодного справжнього провалу. Міграція **закрила два давні борги**:
П-02 із задачі 003 (модалка «Написати нам» стала повноцінним діалогом із пасткою фокуса) і
П-01 із 005 (сайдбар уроку більше не проступає крізь липку шапку).

### Чотири блокери — усі закриті кореневою сесією й перевірені на превʼю

| # | Дефект | Причина | Стан |
| - | ------ | ------- | ---- |
| **D-01** | Дві-три діаграми малювались в **один** `<svg>` — **17 колізій із 20 завантажень**, 79 зі 100 діаграм | **Регресія від фікса `8535f0e` кореневої сесії:** повузлові `mermaid.run()` без `await`, а Mermaid будує id як `mermaid-${Date.now()}` (1 мс). Δ id був 1–2 мс | `2afe4f8` — послідовний `await`. Δ id **71–129 мс**. Превʼю, 6 проходів: `8/7/5`, накладань **0** |
| **D-02** | Питання квіза розпирало документ: **+86 px** на 320, +16 на 390 | `overflow-wrap` був на `.ds-quiz__opt` і не був на `.ds-quiz__q` | `e9e546b` §39.1 — **0 і 0** |
| **D-03** | Абзац прози розпирав документ: **+13 px** на 320 | `overflow-wrap` був на `.ds-prose code` і `.ds-tbl`, але не на тексті прози | `e9e546b` §39.2 — **0** |
| **D-04** | Кільце фокуса не видно на **10 посиланнях** програми. WCAG 2.4.7 AA | Правило кільця через `:where()` важить **(0,1,0)**, роздільник останнього рядка — **(0,3,1)** | `e9e546b` §39.3 — специфічність (0,4,1), кільце перемагає на всіх 4 рядках |

**Дві з цих чотирьох — мої власні регресії**, і це варто памʼятати: D-01 вніс мій же фікс
Mermaid кількома годинами раніше, а D-02/D-03 — наслідок того, що система 009 має інші
кегль і ширину колонки, ніж стара мова.

### Пастка перевірки, яка коштувала часу двічі за день

**Програмний `el.focus()` НЕ вмикає `:focus-visible`**, а синтетичний `Tab` розширення
**не просуває фокус**. Перша перевірка D-04 показала «кільця немає» і на виправленому рядку,
і на звичайному — тобто метод, а не фікс. Довів інакше: тим самим селектором зі `:focus`
(вага псевдокласів однакова) плюс клас-двійник, що віддав точні значення кільця.

Те саме сімейство: **заморожений годинник не є тестом фікса D-01** — він ламає будь-яку
схему на основі часу, тому з ним і послідовний код дає злиття. Він доводить **механізм**.

---

## Що лишається власнику — один список, усе в одному форматі

### З фронтенду

1. **Акцент PDF сертифіката.** Теракота (затверджено в 005) чи акцент курсу — тоді сертифікат
   Академії став би шафрановим, Architect патиновим, Термінал лазуровим. Технічно — один
   атрибут `data-course` на офскрин-вузлі. ⚠ Обидва теракотові відтінки дають контраст
   **3,90 і 3,53** при нормі AA 4,5 для дрібного тексту, тож питання «який відтінок» тягне
   питання «чи не має дрібний текст узяти `--c-paper-text`». **Відкат:** одне значення.
2. **Перемикач «Вхід / Реєстрація» більше не таблист** — рішення пакета (табів як сутності
   в 009 немає, це `ds-btn` з `aria-pressed`). QA підтвердив, що читається коректно як дві
   кнопки-перемикачі. Але це зміна доступності робочої модалки задачі 001. **Відкат:** повернути
   `role="tablist"` і roving tabindex.
3. **Хореографія сесії hero Термінала знята** — було 12 таймлайнів GSAP, стало одна штора.
   Не свавілля: трималась на девʼяти класах із видаленого `custom.css`, а каретка малювалась
   псевдоелементом усередині `.term`, що заборонено. **Відкат:** ~200 рядків GSAP + девʼять імен у CSS.
4. **`roadmap.html` — єдина з 66 сторінок без `data-page-in`.** **Відкат:** один атрибут.

### З тестування

5. **D-05 · діаграми стискаються до 1,7–5,8 px замість скролу.** З погляду учня це найгірше
   з усього знайденого: **96 діаграм зі 100 нечитабельні на телефоні**, найгірша дає **4,0 px
   тексту на десктопі**. ⚠ **Але це не регресія 010 — у проді трохи гірше** (3,6 і 1,4 px),
   тобто мерж нічого не погіршує. Причина — `useMaxWidth: true` у `js/mermaid-theme.js:57`
   (він же дефолт Mermaid). **Варіанти:** `useMaxWidth: false` + реальний скрол (інфраструктура
   готова — `.ds-diag` уже має `overflow-x: auto` і `overscroll-behavior-x: contain`) · нижня
   межа масштабу · переверстати вісім найширших. **Рекомендація:** окрема задача, не в 010.
6. **D-06 · CLS 0,31–0,93 на лендінгу Термінала.** `.cc-desk` не резервує висоту, блок росте
   на 323 px. У проді 0,82 при 225 зсувах, тобто 010 вже покращив. **Фікс:** один рядок
   `min-height`. **Не блокує мерж.**
7. **D-07 · діаграми більше не центруються** (було 94/94, стало 17/175). Один рядок, але це
   **видима зміна вигляду поза затвердженим списком В1–В6**. Повернути чи прийняти — ваше.
8. **Версії платформ і `site.updated` не підвищені** — свідомо, до QA було б передчасно.
   Зараз футер показує дату, старішу за зміни. Міграція всього сайту — **щонайменше мінор**
   на трьох платформах. Плюс `roadmap.json` `meta.updated` і 65 × `lastmod` у `sitemap.xml`.
9. **Safari — єдина непокрита платформа.** Потрібен **один перемикач**: Safari → Налаштування →
   Розробка → «Дозволити віддалену автоматизацію». Без нього жоден шлях до WebKit не
   відкривається (QA перевірив три). Наступний прогін закриє платформу повністю.

### Дрібне, у тому ж списку

10. **D-08** каретка hero займає окремий рядок на 320/390 · **D-09** резерв пілюлі відрізняється
    від заповненого на 2,8 px (побачить лише той, хто вже має прогрес) · **D-11**
    `data-motion="calm"` не зупиняє GSAP-цикли (`prefers-reduced-motion` при цьому працює
    бездоганно — доступність не постраждала) · **D-10** — дефект **критерію**, не коду:
    чеклист вимагає відкритий `<details>` у довіднику, а рядок із самого пакета його згортає.

---

## Коло 2 і хвиля фіксів після нього (2026-09-08)

**Коло 2 — двоє агентів паралельно.** Загальний (`04-qa/report-round2.md`) і прицільний
аудит 100 діаграм (`04-qa/report-diagrams-round2.md`). Обидва — «не пройдено», але з
важливою різницею від кола 1: **девʼять із десяти дефектів підтверджені закритими**, і
кожен перевірений незалежним A/B, а не заявою автора.

**Головне досягнення методу.** Загальний QA підняв **власний Chrome із видимим вікном**
(`--remote-debugging-port` + вимкнене приспання фонових вкладок, керування по CDP через
`WebSocket` у Node 25) — і це зняло **всі шість пасток заміру** цієї задачі одразу:
справжній `Tab`, реальний вʼюпорт замість iframe, чесний CLS, емуляція
`prefers-reduced-motion`, блокування CDN. Тимчасовий профіль = нуль доступу до живої сесії.
Рецепт лежить у памʼяті агента (`method_cdp_own_chrome.md`) — наступні кола не мають
винаходити його заново.

### Чотири фікси кореневої сесії після кола 2

| # | Дефект | Що виявилось | Коміт |
| - | ------ | ------------ | ----- |
| **D-16** | `<br/>` у підписах діаграм не давав переносу — 58 входжень у 19 діаграмах | **Причина була не в Mermaid.** Три звіти поспіль звинувачували `securityLevel: "strict"` і `htmlLabels`; емпірика це спростувала — 6 комбінацій (strict/antiscript/loose × htmlLabels true/false) дають два рядки в усіх шести. Насправді `<br/>` у розмітці **не екранований**, браузер робить із нього справжній елемент, а код читав джерело через `textContent`, який теги викидає. Джерело псувалось **до** Mermaid | `0ba1a6e` |
| **D-21** | CLS лендінга Термінала гірший за прод на 3 ширинах із 5 | Резерв плити з кола фіксів зробив те, що обіцяв, але **важіль був інший**: до 010 завищений резерв (520 при фактичних 480,3) випадково гасив стрибок порожнього `<h1>` на −39,7 px. Правдивий резерв прибрав компенсацію. Фікс — резерв самого заголовка, у рядках | `b5d8899` |
| **D-22** | `js/tw-theme.js` кидав `ReferenceError` без Tailwind CDN | Єдиний необроблений виняток за весь прогін; вигляд при цьому не ламався | `b5d8899` |
| **D-14** | у широкої діаграми видно середину замість початку | Одне правило без розрізнення напрямку: показати верхній вузол, серед однаково верхніх — найлівіший | `298ef1f` |

**Числа, якими це доведено:**

- **D-16:** 19 діаграм із `<br/>` — 18 стали багаторядковими **і звузились**
  (`architect-04` 1256 → 886, `architect-05` 1099 → 802); на 81 діаграмі без `<br/>` стара
  й нова логіка дають **ідентичний** текст; іншої розмітки всередині `<pre class="mermaid">`
  немає в жодній зі 100.
- **D-21**, прод проти превʼю **одним інструментом**: 320 `0,037 → 0,032` · 390
  `0,002 → 0,000` · 768 `1,248 → 0,471` · 1024 `2,946 → 0,889` · 1440 `1,298 → 0,673`.
  Зсувів 370 → 15 на 1440. **Регресія проти проду закрита на всіх пʼяти ширинах.**
- **D-14:** 26 діаграм × 2 ширини — збіг із розрахунком **25 з 26**, корінь видно у 25 з 26
  (виняток — вузол 359 px у контейнері 350 px, ширший за екран сам по собі).
  ⚠ Друга половина фікса: `.ds-diag` часто прокрутний **ще до рендера** (невідмальований
  `<pre>` ширший за колонку), тому перехід «став прокрутним» витрачався на текст і **друга
  діаграма сторінки лишалась на нулі** — `#0` мала 739/319/372, `#1` нуль при потрібних
  479/614/30.

### Спростоване в колі 2 — важливіше за підтверджене

- **«Злиття рядків роздуває найширші діаграми»** (теза звіту Ф-Е) — **ні**. У восьми
  найширших `<br/>` рівно одна штука; усі 58 живуть в AI Architect, а найширші — з
  AI Термінала. Вони широкі через підписи до **97 символів** в один рядок.
- **«Mermaid зрідка віддає порожній результат, 1 на 20»** — артефакт заміру: `data-processed`
  ставиться **до** рендеру, тож «оброблено, але без `<svg>`» — це ще й вигляд середини
  роботи. Реальна частота в Chrome — **0 на ~290 завантаженнях**.
- **«iframe ламає нативний рендер Mermaid» / «не міряти в iframe ≥ 1024»** (висновки кола 1
  і Ф-Е) — **обидва хибні**. Падає лише **перша** навігація щойно створеного iframe;
  лікується `about:blank` перед цільовим URL. Через це коло 1 недоміряло 1440 дарма.
- **Розворот напрямку діаграм як спосіб звузити їх** (моя власна гіпотеза) — перевірено на
  всіх 100: жодну не робить вужчою за телефон, виграш > 40 % лише в **4**, а багатьом
  **шкодить** (`claude-code-10` 2049 → 3830).

## Що НЕ зроблено

- **Коло 3 QA — у роботі.** Це останнє за лімітом конвеєра.
- **Потребує акаунта власника:** реальний вхід поштою і через Google, вихід · «Позначити
  завершеним» → `submit_quiz` → бейдж · **PDF сертифіката** (два аркуші, ~0,4 МБ, клікабельне
  посилання, QR телефоном) · `/certificate` у стані «сертифікат є».
  ⚠ Агенти цього зробити не можуть **за конструкцією**: превʼю працює з продовою базою,
  і реєстрація створила б справжній рядок у проді.
- **Потребує Windows:** компенсація смуги прокрутки (`html.ds-lock > body { padding-right }`).
  На macOS смуги накладні, `--sbw` = 0, перевірити неможливо в принципі.
- **Потребує Safari** — див. пункт 9 вище.
- **Мерж у `main` не робився** і не робиться без прямого «так» власника.

---

## Стан на кінець дня 2026-09-08

| | |
| - | - |
| Гілка | `dev`, усе закомічено й **запушено** (`e9e546b`) |
| Превʼю | **зелене**, віддає фінальний код: секції 38 і 39 на місці, `custom.css` → 404 |
| Блокери мержу | **нуль** — усі чотири закриті й перевірені на превʼю |
| Наступний крок | **коло 2 QA** по матриці, далі рішення власника зі списку вище |
