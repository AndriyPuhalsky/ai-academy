# 010 — Увесь сайт на дизайн-систему 009 · Звіт фронтенду Ф-Б (хвиля 2, етапи 3 · 4)

> Звіт написав агент `aia-build-frontend` (Ф-Б); файл зберегла коренева сесія — харнес
> блокує підагентам запис `.md`. Текст агента дослівно; розділ «Незалежна перевірка
> кореневою сесією» в кінці дописаний нами.

- **Вхід:** `task.md` (Р-1 · Р-2 · Р-3) · `03-frontend/report-a.md` · `04-package/` · `01-spec.md` §7.3, §8.3, §8.4, §9
- **Гілка:** `dev` (звірено до першої правки)
- **Зона запису:** `modules/*.html` (57), `js/module.js`, `js/quiz.js` — і нічого більше
- **Коміти:** ⚠ див. «Аварія з комітом» — мої 59 файлів лежать у `3230a95`; памʼять — `9368945`
- **Тривалість:** 55 хв, 252 виклики інструментів

## ⚠ Аварія з комітом (першим рядком, бо це псує історію)

Я підготував свій коміт (`git add modules/ js/module.js js/quiz.js` — явні шляхи, як вимагав брифінг), а `git commit` відповів **«nothing to commit, working tree clean»**. Ф-В (етап 6) за секунди до того зробив коміт, який захопив і мої файли:

```
3230a95 010 feat: етап 6 — три довідники на дизайн-систему 009
  claude-code-ref-*.html (3)  ← його
  js/module.js, js/quiz.js    ← мої
  modules/*.html (57)         ← мої
```

**Нічого не втрачено** — вміст у `dev` цілий, звірено побайтово (`git show HEAD:…`). Але повідомлення коміта бреше про склад. Історію **не переписував** (заборонено на спільній гілці). Кореневій сесії: `3230a95` = етап 6 + етапи 3–4.

---

## Що зроблено

### Етап 3 · розмітка 57 уроків

Прогін скриптом на основі прототипу `04-package/_check/b2/tools/migrate-lesson-body.py`, з лічильниками на кожну заміну (не збіглось очікуване число — файл не зберігається, скрипт падає). Плюс три правки руками.

**Каркас сторінки (по одному входженню на файл, усі 57 однакові до символу):**

| Було | Стало |
| ---- | ----- |
| `<div class="mx-auto max-w-[88rem] …">` | `max-w-wide` |
| `sidebar-overlay lg:hidden` | `ds-snav__scrim` |
| `module-sidebar` | `ds-snav ds-snav--drawer` |
| `snav-count` | `ds-snav__count` |
| `<main id="main" class="min-w-0 py-10 lg:py-12">` | `+ data-page-in` (M1, §9 «усі 66») |
| `<article class="prose-aia max-w-3xl">` | `<article class="ds-prose">` |
| `!mt-0 mb-4 font-mono text-xs uppercase tracking-[0.18em] text-clay` | `ds-eyebrow` (капслок знято) |
| `!m-0 font-display text-3xl leading-tight text-ivory sm:text-5xl` | `ds-h1` |
| `mt-4 font-mono text-xs text-faint sm:text-sm` | `ds-small ds-num` |
| `mt-6 text-lg` | `ds-lead` |
| хвіст `<div class="max-w-3xl">` | `mt-12 grid gap-6 max-w-prose` |
| картка завершення | `ds-card sm:flex sm:items-center sm:justify-between sm:gap-6` |
| `#completeTitle` | `ds-h4` |
| `#completeBtn` `bg-clay … hover:bg-clay-deep` | `ds-btn ds-btn--primary` |
| `<nav id="moduleNav" class="mt-6 grid gap-4 sm:grid-cols-2">` | `ds-mnav` + `data-reveal-root` |

**Тіло уроку (числа фактичні, з прогону):**

| Що | × |
| -- | - |
| `class="scroll-mt-24"` знято із `<section>` цілком | **433** |
| `lesson-no` → `ds-prose__no` | 376 |
| `callout` → `ds-note` · `callout callout-sand` → `ds-note ds-note--accent` · `callout-title` → `ds-note__title` | 319 / 77 / 196 |
| `diagram` → `ds-diag` · `diagram-caption` → `ds-diag__caption` | 99 / 147 |
| `style="text-align:left"` → `data-align="start"` (єдиний інлайновий стиль) | **48** |
| `example` → `ds-code` · `example-head` → `ds-code__head` | 31 / 37 |
| голий `<pre>` → `<pre class="ds-code__pre">` | **210** |
| `<table>` → `<table class="ds-tbl">` | **111** |
| `overflow-x-auto` → `ds-tbl__wrap` | 101 |
| Mermaid `style X …` → `class X hl\|mute\|bad` | **130** (127 + 2 + 1) |
| `.term__copy` → `+ ds-btn ds-btn--sm ds-btn--quiet` | **48** |
| `text-faint` → `text-fg-3` · `text-muted` → `text-fg-2` | 8 / 20 |

**Три правки руками (скриптом не робляться — `no-automation.md`):**

1. **П-23 · 10 таблиць AI Академії без обгортки** — обгорнуті в `.ds-tbl__wrap tabindex="0" role="region" aria-label="…"`. `aria-label` кожної написаний **з контексту**, не шаблоном: `module-01` ×2 («три межі моделі — у чому суть і що робити», «три рівні моделей Claude і їхні типові задачі»), `module-02` («матриця делегування — ціна помилки проти легкості перевірки»), `module-04` («діагностика промпту — симптом, ймовірна причина, ліки»), `module-05` ×2, `module-07`, `module-08`, `module-09`, `module-10`.
2. **`modules/module-01.html:179` — блок `.tokens`.** Карта пропонує `ds-row`, але `.ds-row` — це рядок списку (`grid auto 1fr auto`, кант, hover-підкладка), а не контейнер чипів. Зробив `flex flex-wrap gap-2` + `ds-tag` — компонент системи, нуль нових класів.
3. **`modules/claude-code-22.html:744` — `<pre>` усередині `<li>`** обгорнутий у `<div class="ds-code">`. Єдиний `<pre>` у проді, який лежить не прямим нащадком `.ds-prose`/`section` і не в `.ds-code`, тобто **жодне правило `components.css` його б не оформило** (обхід DOM: 172 у секціях, 37 у `.ds-code`, 1 у `<li>`).

### Етап 4 · JS уроку

**`js/module.js`:**

| Рядок | Зроблено |
| ----- | -------- |
| `:68` | `body.style.overflow` → **спільний лічильник** `data-aia-lock` на `<html>` (той самий, що ведуть `auth-ui.js` і `contact.js`). Клас поки `aia-scroll-lock`, **не** `ds-lock` — обґрунтування нижче |
| `:117` | `matchMedia("(min-width: 1024px)")` закриває шторку при переході через 1024 |
| `:100`, `:103` | повернення фокуса на `#sidebarBtn` — **крім** двох випадків: клік по посиланню в шторці і перехід через 1024 (крав би фокус під час читання). У еталоні `btn.focus()` безумовний — свідоме відхилення |
| `:139–160` | ⚠ **`IntersectionObserver` скролспая не чіпав** |
| `:176–182` | **П-28**: `<span style="width:N%">` → `ds-prog__bar` + `--prog-v` |
| `:233` | **власний `<span class="snav-check">✓</span>` прибраний**, замість нього `<span class="sr-only">— завершено</span>` |
| `:240` | ⚠ **`aria-current="page"` ЛИШЕНО** |
| `:203–216` | **js-diff:118** — модулі з `track: null` виводяться групою «Іспит» |
| `:325` | `BTN_BASE` → `ds-btn ds-btn--primary` / `--secondary` |
| `:340–356` | екран-замок: девʼять утиліт → `ds-empty ds-empty--gate` + `ds-btn` |
| `:380`, `:384` | **П-31**: `style.display='none'` → `hidden` |

**`js/quiz.js`:** `:30` П-27 (`AIA.motion.on()`) · `:83–90` П-28 (`--prog-v`, зникли хекси `#3A342E`, `#D97757` і `transition:width`) · `:118` пояснення на три рівні `grid-template-rows: 0fr→1fr` · `:130` **П-25** `data-key` А/Б/В/Г/Д/Е · `:150–151` **П-26** дописування `"✓ "` / `"✗ "` прибране · `:181` `ds-btn ds-btn--secondary`.

---

## Потрібні правила у спільних файлах (сам не додавав)

### 🔴 1. `<code>` у заголовку прози — 125 заголовків у 18 файлах

`.ds-prose code { font-size: var(--fs-code) }` — **абсолютний** кегль 13,52 px. У проді було **відносне** `.prose-aia code { font-size: 0.9em }`, тому цього не було видно ніде.

| Місце | Заголовок | `<code>` у ньому | Було в проді |
| ----- | --------- | ---------------- | ------------ |
| `h1` | 64 px Prata | **13,52 px** із чипом | 0,9 × 48 = 43,2 px |
| `h3` (×117) | 20 px | **13,52 px** | 18 px |
| `h4` (×7) | 17 px | 13,52 px | 15,3 px |

Файли: `claude-code-05` (13), `-07` (13), `-16` (12), `-11` (9), `-17` (9), `-21` (9), `-13` (8), `-20` (7) і ще 10.

```css
/* A — дослівно відновлює прод */
:is(.ds-prose, .ds-prose > section) > :is(h1, h2, h3, h4) code { font-size: 0.9em; }

/* B — рекомендую: у заголовку чип зайвий */
:is(.ds-prose, .ds-prose > section) > :is(h1, h2, h3, h4) code {
  font-size: 0.9em; background: none; padding: 0; border-radius: 0;
}
```
Специфічність (0,1,3) — preflight не перебиває. **B виглядає краще на обох кінцях шкали**, але це візуальне рішення — не моє.

### 🔴 2. `js/mermaid-init.js` — три дефекти, усі ловлять діаграми уроків

Файл скопійований Ф-А із пакета побайтово. **Жоден не є наслідком переносу `style X` → `class X`** (конвертація 1:1, 130 = 130).

**2а. `classDef` дописується в КОЖНУ діаграму, а `sequenceDiagram` його не парсить.** `Parse error on line 11: … got 'INVALID'`. У проді **6 секвенс-діаграм** (`architect-03`, `-04`, `-08` ×2, `-10`, `-12`) малюють `svg[aria-roledescription="error"]`. До 010 працювали.

**2б. `mermaid.run({nodes})` на всі вузли + `.catch`, що чіпляє fallback до ВСІХ.** На `architect-08`: 3 діаграми, 2 падіння, **3** написи.

**2в. `run()` на `DOMContentLoaded`, тобто ДО завантаження вебшрифта.** На `module-01`: **23 з 39** `foreignObject` вужчі за вміст, найгірше **9,02 px**.

**A/B, який доводить усі три разом:**

| | Сторінка зараз | Після фікса |
| - | -------------- | ----------- |
| `architect-08`: типи SVG | `flowchart-v2`, **error**, **error** | `flowchart-v2`, **sequence**, **sequence** |
| `architect-08`: помилок | 2 | **0** |
| `module-01`: обрізаних міток | **23 / 39** | **0 / 39** |

### 3. `html.ds-lock` — перехід мусить бути одним комітом у трьох файлах

Я **свідомо лишив клас `aia-scroll-lock`**. Причина — незворотна послідовність:

> шторка відкрита (`module.js` → n=1, вішає `ds-lock`) → відкрилась модалка (`contact.js` → n=2, ранній вихід) → **шторка закрилась** (n=1, ранній вихід) → модалка закрилась (n=0, знімає **тільки `aia-scroll-lock`**) → **`ds-lock` лишається назавжди, скрол заморожений**.

**Ф-Д (етап 8):** коли міняєш `aia-scroll-lock` → `ds-lock` у `contact.js` і `auth-ui.js`, зміни в тому ж коміті `js/module.js:68–81`.

---

## Точність до пакета

| Що | У пакеті | У коді | Збіг |
| -- | -------- | ------ | ---- |
| `ds-prose` два рівні (П-32) | `:is(.ds-prose, .ds-prose > section)` | секції збережені, `display: contents` не вживався | ✓ |
| колонка тексту / широка | 672 / 768 | заміряно `.term` = 768, текст 672 | ✓ |
| `scroll-mt-24` знято | 57×N | 433, `grep` → 0 | ✓ |
| Mermaid | 130 | 127 hl + 2 mute + 1 bad | ✓ |
| `class X hl` дає акцент курсу | — | `fill rgb(46,36,23)`, `stroke rgb(191,138,58)`; на Терміналі лазур | ✓ |
| таблиці: обгортка + `tabindex`/`role`/`aria-label` | 111 | **111 із 111** повний контракт | ✓ |
| `data-key` квіза | А/Б/В/Г | заміряно на живому квізі | ✓ |
| `.ds-prog__bar` | `scaleX(--prog-v)` | `--prog-v` в обох файлах, `width` ніде | ✓ |
| `[hidden][hidden]` під замком | хвіст ховається | заміряно: `display: none`, висота 0 | ✓ |
| сайдбар 23 пункти, вікно 640 | скролиться в собі | `hdrBottom 48` = `asideTop 48`, `scrollHeight 1701 > clientHeight 571` | ✓ |
| ScrollTrigger на уроці | 0 | `0` | ✓ |
| `--scrub` при reduce | `"0"` | `"0"` | ✓ |

**Розбіжності з пакетом і чому:**

1. **`tag-user`/`tag-model` застосовано на 10 блоках із 13.** **3 блоки містять і «Промпт», і «Відповідь моделі» одночасно** (`module-03` ×2, `module-04`) — один кант на весь блок там брехав би. Роль несе саме слово мітки, що відповідає правилу «стан не кодується лише кольором».
2. **Обгортка `.tokens` — не `ds-row`** (див. вище).
3. **`🔒` лишено замість слова «замок»** з еталона: це зміна тексту продукту, якої js-diff не вимагає.
4. **`ds-snav__item--done` ставиться і на поточному модулі, якщо він пройдений.** Еталон цього не робить, і ✓ у поточного рядка зник би — а в проді він там був. Зберіг поведінку.
5. **Повернення фокуса при закритті шторки — умовне**, не безумовне.

---

## Що вирішив сам

1. `.tokens` → `flex flex-wrap gap-2` + `ds-tag` (1 місце).
2. `<pre>` в `<li>` → обгортка `.ds-code` (1 місце).
3. `tag-user`/`tag-model` на змішаних блоках — без модифікатора (3 місця).
4. 🔒 замість слова «замок».
5. `ds-snav__item--done` на поточному модулі.
6. Умовний `btn.focus()` при закритті шторки.
7. Прибрані мертві `font-mono text-2xl sm:text-4xl` з `<code>` у `h1` `claude-code-13` — вони програвали `.ds-prose code` ще в проді.
8. **`data-page-in` на `<main>` 57 уроків** — §9 каже «M1 на всіх 66», Ф-А не додав ніде (`grep` до правки: **0 файлів**). Ризику «сторінка лишиться прозорою» немає: GSAP на уроках відсутній, спрацьовує гілка `el.animate(...)`, яка не фіксує стилі; заміряно `mainOpacity: 1`.
9. **Група «Іспит» у сайдбарі** — це js-diff:118, тобто пакет; але зміна **видима**, тому називаю окремо. Відкат — зняти блок `orphans` (`js/module.js:203–216`).

## Нові токени / компоненти / ключі конфігів

**Нуль / нуль / нуль.** `css/*.css` не змінювався ні на символ. Тексти уроків і `#quizData` доведено незміненими.

## Що потрібно від бекендера

**Нічого.** Контракт не змінювався: `AIAProgress.*`, `AIAAuth.confirmCertificateName`, `aia:config-ready`, `submit_quiz`.

---

## Стани: що перевірив і як

Видимий Chrome, `python3 -m http.server 8301` (порт 8000 не займався; сервер погашено).

- **320 px, усі 57 уроків:** `scrollWidth − innerWidth` → **0,0 px на 57 із 57**. Це закриває знахідку 5 Ф-А (`module-01`, 379 px) — обгортка її прибрала.
- **390 px, усі 57:** **0 із 57**. Включно з `claude-code-15.html` (найдовший рядок термінала **222** символи; топ-5: 222 / 156 / 156 / 152 / 148).
- **Сайдбар 23 пункти, вікно 640:** низ шапки 48 = верх сайдбара 48, `max-height 571`, `scrollHeight 1701`, скролиться в собі. **Хвіст П-01 закритий.**
- **Довгий український текст:** кнопка «✓ Завершено · натисни, щоб скинути» — 293×40 на 390 і 222×60 на 1280, `insideCard: true` в обох. **П-36 закрита.**
- **Порожньо / замок:** `claude-code-13` для гостя — `ds-empty--gate`, `<article>`, `<script>` і хвіст `hidden` → `display: none`. **П-31 закрита живцем.**
- **Іспит `claude-code-23`:** у сайдбарі 6 груп, остання «Іспит», **23 пункти** (було 22), лічильник «0 з 23» тепер збігається з кількістю рядків.
- **`prefers-reduced-motion`:** `--motion: 0`, `--dur-state: 4.4e-06s`, `--scrub: "0"`. Невидимих вузлів прози — **0**.
- **П-13 · копіювання `.term`:** обійдено **79** вузлів усередині `.term` — **жодного** `::before/::after` з непорожнім `content`.
- **П-30 · маркери списків:** видно і крапки, і цифри, у кольорі акценту курсу.
- **Квіз:** `data-key` А/Б/В/Г; класи `--ok`/`--bad`; у тексті **немає** дописаних `✓ `/`✗ `; `--prog-v` = `0.125` при 1 з 8.
- **`.ds-prog__bar` рухається:** `--prog-v` → `matrix(0.375,…)`, `offsetWidth` 400 → 200.
- **Консоль:** нуль помилок на восьми сторінках. Єдина помилка на всьому обході — `[AIA] mermaid` на `architect-08`.

**Чого перевірити НЕ міг:** ⚠ вікно Chrome було спільним із двома паралельними агентами й весь час перекритим (`hasFocus() === true`, але `visibilityState === "hidden"`, `rAF` не спрацював жодного разу):
- **справжній `Tab` до сторінки не доходив** — подвійне кільце на `.ds-tbl__wrap` заміряти не вдалось. Структурний доказ: всі 111 обгорток покриті глобальним правилом `components.css:53`;
- **перехід через 1024 не перевірений живцем.** В `<iframe>` події `resize` і `matchMedia` change не приходять узагалі (`fired: 0`);
- **клік по варіанту квіза на живій сторінці не робив** — пряма заборона (превʼю з продовою базою). Обидві половини доведені окремо;
- **вхід, вихід і «Позначити завершеним»** — потрібен акаунт.

---

## Що передати тестувальнику

1. 🔴 **Діаграми Mermaid.** `architect-03`, `-04`, `-08`, `-10`, `-12` — 6 секвенс-діаграм не малюються. І на будь-якому уроці з flowchart перевірити обрізання міток.
2. 🔴 **`<code>` у заголовках** на 18 сторінках.
3. **Перехід через 1024 при відкритій шторці.** Відкрити зміст на 900, розтягнути за 1024 → шторка має закритись, `aia-scroll-lock` зникнути. Єдиний рядок етапу 4, який я не зміг виконати живцем.
4. **Смуга прогресу квіза.** Відповісти на питання — смуга має **зрушити**.
5. **Один ✓, не два.**
6. **Замок і хвіст.** Під замком не видно ні статті, ні `#moduleNav`.
7. **Копіювання термінала** — байт у байт. Саме тут регресія **пройшла б автотест**.
8. **Кеш.** `Ctrl+F5` не оновлює `css/*.css` і `js/*.js`.
9. **Вимірювання в `<iframe>` бреше про рух.** Геометрія — чесна.

---

## Знайдене поза обсягом

**Для Ф-Д (етап 8):**
1. **`html.ds-lock` — три файли одним комітом** (див. вище).
2. `js/auth-ui.js:477–493` і `js/contact.js:119–136` містять **дослівні копії** `lockScroll`/`unlockScroll`. Третя тепер у `module.js`. Кандидат на спільний хелпер.

**Для кореневої сесії / дизайн-майстерні:**
3. **`js/mermaid-init.js` у пакеті 010 має всі три дефекти** — вони поїдуть і в макет, і в будь-яку майбутню сторінку з діаграмами.
4. **`data-page-in` (M1) стоїть тільки на 57 уроках.** §9 обіцяє на всіх 66; решта девʼяти — зони Ф-В / Ф-Г / Ф-Д.
5. **`3230a95` містить етап 6 і етапи 3–4 разом.**

**Загальне:**
6. **`#completeTitle` на 25 із 57 уроків має унікальний текст** («Іспит позаду?», «Ключ у безпеці?»…), який `refreshComplete()` **затирає на «Модуль позаду?» одразу на `DOMContentLoaded`**. 25 авторських рядків не бачить ніхто, і так було до 010. Рішення власника.
7. **`.ds-snav` оголошує `display: grid; gap: var(--s-1)`, але пункти лежать усередині `<div id="sidebarNav">`** — гап не діє. Структура така сама в еталонному кадрі пакета, тому нічого не міняв.

---

## Результати всіх `grep`-перевірок числами

| # | Перевірка | Результат |
| - | --------- | --------- |
| 1 | `style X fill:#` у `modules/` | **0** |
| 2 | хекси в `modules/*.html` (крім `theme-color`) | **2**, обидва — HTML-сутність `&#160;` у `claude-code-04.html`, не кольори |
| 3 | `<table>` проти `ds-tbl__wrap` по кожному файлу | **111 = 111**, файлів із розбіжністю **0** (було 8 файлів / 10 таблиць) |
| 4 | Tailwind-утиліти в `js/module.js` + `js/quiz.js` | **0** |
| 5 | горизонтальний скрол на 320 | **0 із 57** |
| 5б | те саме на 390 | **0 із 57** |
| 6 | `scroll-mt-24` у `modules/` | **0** (було 433) |
| 7 | регістр шляхів | **0** |
| 8 | стара мова в `modules/` (23 імені) | **0 по кожному** |
| 9 | класи не `ds-*`/`term*`/`sr-only`/`mermaid`/`is-*` | **28 імен**, усі — незалежні від теми утиліти розкладки |
| 10 | обгортки таблиць із повним контрактом | **111 із 111** |
| 11 | `.term` — розмітка не змінена | 101 термінал, **0 розбіжностей** поза класами кнопки копіювання |
| 12 | `#quizData` не змінений | **0 із 57** файлів зі зміною |
| 13 | текст статей не змінений | **0 із 57** файлів зі зміною |
| 14 | `term__copy` із класами кнопки | **48 із 48** |
| 15 | Mermaid `class X` | **130** = 127 + 2 + 1 |
| 16 | `ScrollTrigger` на уроці | **0** |
| 17 | `data-page-in` у `modules/` | **57** (було 0) |

---

# Незалежна перевірка кореневою сесією (2026-09-07)

## Ворота Ф-Б — сходяться

| Перевірка | Було | Стало |
| --------- | ---- | ----- |
| `scroll-mt-24` у `modules/` | 433 | **0** |
| `<table>` / `ds-tbl__wrap` | 111 / 101 | **111 / 111** |
| `role="region"` | — | **112** (111 таблиць + одна незалежна область) |
| `data-page-in` | 0 файлів | **57 файлів** |
| `style X fill:#` | 130 | **0** |
| Стара мова (`prose-aia`, `callout`, `example-head`, `diagram-caption`, `lesson-no`, `module-sidebar`, `sidebar-overlay`, `snav-count`) | була | **0** |
| Заголовків із `<code>` | — | **125 у 18 файлах** — число Ф-Б підтверджено точно |

## Обидва «потрібні правила» застосовані кореневою сесією

Це спільні файли, тобто наша зона, не агентів.

### `js/mermaid-init.js` — три дефекти виправлені

Заяву перевірено **тричі: у коді, у DOM і після фікса.**

Структурно: `classDef` дописувався в кожну `pre.mermaid` беззастережно; `mermaid.run({nodes: nodes})` мав **один** `.catch`, який вішав fallback на **всі** вузли; запуск стояв на `DOMContentLoaded`.

**Заміряно на `architect-08.html` до фікса:**
```
svg: 3 | типи: flowchart-v2, error, error | fallback-написів: 3
```
Тобто дві діаграми з трьох не малювались, а напис «не вдалося намалювати» висів і над здоровою.

**Після фікса (той самий файл, той самий браузер):**
```
svg: 3 | типи: flowchart-v2, sequence, sequence | fallback-написів: 0
```
**`module-01.html`:** обрізаних міток **23 з 39 → 0 з 39**, найгірше відхилення `9,02 px → 0`.

Виправлення: `classDef` дописується лише для `flowchart|graph` · `mermaid.run` викликається **повузлово**, тому падіння однієї діаграми не чіпає сусідніх · запуск перенесено на `document.fonts.ready`.

⚠ **Пастка, на яку я спіймався під час перевірки** — та сама, про яку попереджали всі три агенти: після правки `js/` перше перезавантаження показало **старий результат** (23 з 39). Файл на диску був правильний. Знімається `fetch(url, {cache:'reload'})` перед перезавантаженням.

### `css/components.css` — секція 37, `<code>` у заголовку

Застосовано **варіант A** (`font-size: 0.9em`) — він **дослівно відновлює прод-поведінку** і не вносить жодного нового візуального рішення. Варіант B (додатково прибрати чип) агент рекомендував, але це вже вибір вигляду — залишено власнику.

**Заміряно після правки на `claude-code-13.html`:** `h1` 64 px → код **57,6 px** · `h3` 20 px → код **18 px** (рівно як у проді, 0,9 × 20). До правки обидва були 13,52 px.

## Змішаний коміт `3230a95`

Підтверджено: 62 файли — 57 `modules/*.html` і 2 `js/` зони Ф-Б, 3 довідники зони Ф-В. Обидва агенти виявили це самостійно й **обидва відмовились переписувати історію на спільній гілці** — правильно. Вміст дерева повний і правильний; зіпсоване лише повідомлення коміта.

**Діагноз Ф-В точний і вартий правила:** заборони `git add -A` **недостатньо**, коли в дереві кілька агентів — `git commit` без pathspec бере **весь індекс**, включно з тим, що туди поклав хтось інший. Правильна форма — `git commit -- <шляхи>`.
