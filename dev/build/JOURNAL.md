# Журнал подій інженерної майстерні

Що відбувалося в `dev/build/` і чого чекаємо далі. Зворотно-хронологічний: **найновіше
згори**. Аналог `dev/design/JOURNAL.md`, але про реалізацію, а не про дизайн.
Веде коренева сесія; PM-агент дописує по своїх задачах.

## Правило журналу

**Поки задача в роботі — розписуємо детально.** Статуси, на якому агенті стоїмо, які
дефекти знайдено, яке коло циклу QA, чого чекаємо і від кого (особливо: які міграції
лежать незастосовані й чиє рішення блокує).

**Коли задача в проді — увесь опис стирається** і замінюється одним записом:
**дата · що за задача · що зроблено. Максимум 2–3 речення.** Задача переїжджає з
«Активного» у «Зроблено».

Сенс правила: хроніка потрібна, поки нею користуються. Після здачі подробиці лишаються
в папці задачі (`01-plan.md`, `02-backend/`, `03-frontend/`, `04-qa/`, `SUMMARY.md`).

**Репозиторій публічний:** ні ключів, ні даних учнів, ні фінансів — і в журналі теж.

---

## Активне

### 2026-09-06 · 006 / 007 / 008 · хвости після 005 — заведено, стартує 006

Три задачі одним рішенням власника (2026-09-06), усі без дизайну:
- **006 `platform-tails`** — код: П-01, П-02, П-08…П-12 зі звіту QA 005, хвіст 004 (цілі
  20 px у футерах трьох лендінгів), омогліфи `/verify`, кнопка «Відкрити ↗» для банки на
  `claude-code.html`, `roadmap.json` (`a11y-improvements` → `done`), `tg/DEPLOY_CHECKLIST.md`.
  Конвеєр: PM → бекендер ∥ фронтендер → QA. **Стан 2026-09-06 (сесія з `dev/build/`):
  PM пройдено** — `01-plan.md` (12 контрактів К1–К12, 60 критеріїв, 14 ризиків, нуль
  спільних файлів між зонами), пропатчений кореневою сесією трьома уточненнями власника
  (мержу поки не буде → реліз прибраний з плану; leaked password protection → К12 у зоні
  бекендера через Chrome; тестовий акаунт уже залогінений). Повідомлення PM після спавну
  класифікатор заблокував — уточнення внесені в файл напряму. Далі: бекендер ∥ фронтендер
  на `xhigh` (рішення власника того ж дня; PM дожив на `high`). Історія: перший запуск
  із сесії в `AIA/dev` зупинено власником: памʼять агентів конвеєра (`memory: project`)
  привʼязана до cwd сесії, і повна лежить у `dev/build/.claude/agent-memory/` (pm 7 файлів,
  backend, frontend, qa), а сесія з `AIA/dev` бачить лише `dev/.claude/agent-memory/` (pm
  порожній). **Рішення власника 2026-09-06: сесії конвеєра запускати з `dev/build/`.**
  Вхід для наступної сесії: `006-platform-tails/task.md` (розвідка з file:line і рішення
  власника вже там) + план кореневої сесії `~/.claude/plans/005-reflective-prism.md`
  (006/007/008, верифікація). Перший крок — `aia-build-pm` на `task.md` → `01-plan.md`.
  **Бекендер завершив 2026-09-06** (4 коміти з pathspec: `29ed4f2` К1+К2, `a02c88f` К3,
  `49cc7b4` К4, `17436e9` CHANGELOG): слаги `ai-essentials` / `ai-architect` / `claude-code`
  звірені з базою (Р-1 закрито); `window.AIACert` для стаб-перевірки; К2 і К4 виміряні
  наживо; К3 не прогнано (сесії в агента нема — QA). **К12 НЕ ввімкнено:** leaked password
  protection доступний лише на плані Pro (~$25/міс), проєкт `FREE` — грошове рішення
  власника; у Dashboard нічого не змінено, запис у `tg/CHANGELOG.md`. `tg/DEPLOY_CHECKLIST.md`
  створено кореневою сесією з тексту бекендера (`97c1af2`): чесно названо, що базової схеми
  в репозиторії немає жодним файлом (окрема задача — дамп схеми без даних). **Питання
  власнику:** `courses.title` першого курсу — «AI Essentials», на сайті такої назви немає:
  після К1 PDF читатиметься «видавець AI Академія, курс AI Essentials». Хвіст: `tg/GOING_LIVE.md`
  описує Cloudflare Pages і неіснуючий порядок міграцій. **Фронтендер завершив** (9 комітів
  `bace7ec`…`59620f6`; +3 px у `module-07` — таблиця з min-content 373 px, обгортка як у 005;
  «Нагору ↑» 16 px лишено як інлайнове). Звіти обох — `02-backend/report.md`,
  `03-frontend/report.md`; критерії 48 і 60 уточнені під факти. **QA коло 1 — НЕ ПРОЙДЕНО**
  (`1f23ff6`, 57/60 з доказами): **D-01** сайдбар уроку `z-index: 50` малюється поверх шапки
  `z-40` — К9 (непрозора шапка) сам П-01 не лікує, корінь старший за 006 і живий у проді на
  трьох курсах; **D-02** `sm:not-sr-only` генерується Tailwind CDN через ~53 мс після вставки з
  JS — на цей час шапка зсунута на 64,8 px. Перший прогін QA обірвав ліміт API, другий пройшов
  повністю; сесія тестового акаунта жива. **Коло 2 фронтендера запущено** (z-index у десктопному
  блоці; власний клас замість JIT-класу в обох копіях пілюлі; `aria-label` на `#verifyInput`).
  **Коло 2 фронтендера ЗАКОМІЧЕНО** (`a348782` D-01 z-index 10 у десктопному блоці · `23bd7fc`
  D-02 власний клас `.navprog-label` замість JIT · `3c1d9a1` D-03 `aria-label` на `#verifyInput`;
  заміри — `03-frontend/report-round2.md`). **QA коло 2 — ПРОЙДЕНО** (`8f4b831`, 2026-09-06, після
  зупинки дня й нового рішення власника «продовжуй роботу, запускай субагентів»). Усі три дефекти
  кола 1 закриті власними замірами на превʼю: **D-01** — 0 із 25 проб у смузі шапки влучають у
  сайдбар, а з тимчасово поверненим `z-index: 50` **25 із 25** (A/B доводить, що зонд бачить
  дефект, а не «нічого не помічає»); три курси × чотири висоти; **D-02** — зсув шапки
  64,81 px → **0**, `.navprog-label` у CSSOM ще до першого малювання; **D-03** — `textbox "Код
  сертифіката"`, бокс не змінився. Регресій немає: мобільна шторка досі над шапкою (16/16 проб,
  справжній клік), sticky-колонка ціла, три лендінги однакові, `/verify` цілий. Ризик JIT-класів
  у межах 006 закритий грепом: задача додала рівно два класи, обидва мають правила у справжніх
  файлах стилів. **Не перевірено наживо:** гість (критерій 18) — агент не логіниться, а спосіб
  кола 1 із підміною токена свідомо не повторювався, бо supabase-js ротує refresh-токен і сесію
  власника можна було вбити; рух шторки — вкладка віддає `visibilityState: hidden`, доведено лише
  кінцевий стан. Звіт (296 рядків) записала коренева сесія з фінального повідомлення агента —
  `04-qa/report-round2.md`, скріншоти в `04-qa/shots/`. **QA рекомендує мерж; рішення власника
  «мержу поки не буде» лишається чинним.** **Рішення власника 2026-09-06: слід тестового
  акаунта НЕ прибирати** — на відміну від `005-4`, міграція-чистка не пишеться й не заводиться.
  Акаунт лишається в продових `profiles` як є. Хвіст, що лишається: версії й `site.updated` не
  підняті (потрібні перед релізом).
  **Три знахідки поза обсягом 006, усі старші за неї й живі в проді:** модалка «Написати нам»
  перші ~50 мс існує нестилізованою і лягає 394 px у кінець потоку документа (та сама причина, що
  D-02); `#syllabusList` на `/` перебудовується на 328 px; перехід шторки не вимкнений під
  `prefers-reduced-motion`. Разом — **28 Tailwind-утиліт, що приходять у DOM лише з JS**.
- **007 `quiz-distractors` — ЗРОБЛЕНО на `dev` 2026-09-06.** 11 партій `aia-quiz-editor`
  (Opus 5 / xhigh) за один день: 57 файлів, «правильна = найдовша» 87,9 % → 28,7 % (кожен курс
  ≤ 33 %), «найкоротша» 27,2 %, індекси `answer` розкидані `--rotate`, повний `--verify` чистий.
  Обрив лімітом API о ~14:20 коштував перезапуску трьох партій; робота на диску вціліла.
  Дірка закрита в `check-lessons.py`, контракті §6 і чеклисті редактора (`1ed52f9`). Зведення,
  таблиця «до/після» і знахідки для власника — `007-quiz-distractors/report.md`. Поза 007:
  маркери в трьох правильних відповідях, іспит `c23` на одне питання над порогом.
- **008 `terminal-content-review` — ЗРОБЛЕНО на `dev` 2026-09-06.** `aia-content-reviewer` × 26
  сторінок (по одному на агента, шість хвиль) + `aia-build-frontend` на реєстрі наскрізних правок.
  **Усі 26 — «готово з зауваженнями»**, жодна не повернута автору. Виправлено ~60 фактичних
  неточностей із показаним джерелом і 120+ непояснених слів; вигаданих фактів знайдено мало —
  більшість дефектів виявилась дрейфом продукту (2.1.236 → 2.1.263) або суперечністю тексту із
  самим собою.
  **Пʼять найдорожчих:** `c17` тлумачив `Hooks: Found 0 total hooks in registry` як «скільки
  хуків завантажилось» — A/B-запуск довів, що `0` друкується й тоді, коли хук відпрацював
  (лічильник рахує відкладені асинхронні), тобто людина з робочими хуками лагодила б те, що не
  зламано, **в уроці, який учить не гадати** · `c16` про `ConfigChange` суперечив **власному
  довіднику курсу** · `c18` радив прапорці, яких у `claude mcp login` немає (`unknown option`) ·
  `c19` суперечив сам собі у пʼяти місцях і мав зламану розмітку `<p><p>` · `c13` вів до тексту
  помилки JSON, якого на сучасних збірках не буває.
  **Тричі довідник виявився правішим за урок** (`ConfigChange`, «19 контекстів», `Settings Error`)
  — аргумент тримати довідники джерелом правди, а не навпаки.
  **Фронтендер закрив реєстр `fixes.md` шістьма комітами** (`1ca5476`, `b4e8cd2`, `1147258`,
  `1b7a795`, `18a122f`, `25ae3f6`), звіт — `03-frontend-report.md`: П-1 «стенограма» → «транскрипт»
  (одна правка, не десять) · П-2 «змінна оточення / середовища» (одна правка) · П-3 `mkdir -p`
  у PowerShell — два хибні твердження + шість застережень за еталоном `c17` · **П-4 кнопка
  «Копіювати» знята з 9 блоків із 34** за критерієм шкоди, кнопок у курсі 57 → 48, рішення по
  кожному блоку в таблиці звіту · П-5 «19 контекстів» → 20 · П-6 `Settings Warning` →
  `Settings Error`. Чекери до і після ідентичні; `#quizData` не змінений у жодному коміті.
  **Два блоки фронтендер відніс до «прибрати» самостійно** (`c02#t1`, `c07#p5`) — обидва за тим
  самим критерієм; `c07#p5` цікавий тим, що **шкода зʼявилась із часом**: на 2.1.236 `--restricted`
  падав, а з 2.1.248 прапорець існує і в читача відкрилася б сесія. Відкотити ці два можна окремо.
  **Поза реєстром, за прямим проханням власника:** кнопка «Відкрити ↗» на `/claude-code` дістала
  колір реквізитів (`--c-muted` → `--c-sand`, hover на `--c-ivory`) — на `index.html` та сама
  кнопка успадковує основний колір, тож приглушений відтінок був розсинхроном, а не задумом
  (`9ad16a6`, звірено на превʼю: кнопка й реквізит `rgb(232,220,195)`).
  **Рішення власника по хвостах: маркер-форма квізів, `p_score: 100` і датовані екрани лишаються
  як є, задачі не заводяться.** Наслідок, який виглядає як дефект: `check-lessons.py` назавжди дає
  «З помилками: 1 із 23» на `claude-code-23.html`.
  Зведення — `008-terminal-content-review/report.md`, реєстр — `fixes.md`, фронтенд —
  `03-frontend-report.md`.

Рішення власника цього дня: бренд сертифіката за курсом (П-10) · поріг квіза не чіпати ·
непрозора шапка уроку лише при висоті вікна ≤ 820 px (П-01) · на < 640 px пілюля показує
лише «3/12» (П-08) · **агенти конвеєра на Opus 5 / `high`** (frontmatter змінено з `xhigh`).
Уточнення власника того ж дня: **мержу в `main` поки не буде** — усе лишається на `dev`,
реліз відкладено · **Supabase завжди робить бекендер** («супабейзом завжди займається
бекендер») — leaked password protection вмикає `aia-build-backend` через Chrome, не власник ·
тестовий акаунт для QA **вже залогінений** у Chrome.

## Зроблено

### 2026-09-05 · 005 · AI Термінал — у проді

Третій курс (22 уроки + іспит + три довідники, лендінг `/claude-code`), навігація «Курси» на
трьох лендінгах, закрита 004 і PDF-фікси сертифіката для трьох курсів (0,42 МБ, `doc.link`,
робочий QR). Конвеєр за один день: PM → бекендер (`005-2` через Chrome) ∥ фронтендер → QA три
кола (63/63, перший сертифікат в історії проєкту). Змержено в `main` 2026-09-05 (`cdf71a8`,
`--no-ff` — щоб не повторити пастку fast-forward з 003), версії `1.0.1` / `0.4.1` / `1.3.1`,
прод звірено по хешах. Подробиці — `005-ai-terminal/task.md` п. 8–25, `01-plan.md`,
`04-qa/report.md`, `../design/005-ai-terminal/SUMMARY.md`.

### 2026-09-04 · 004 · Борг доступності — закрито

Зум (`user-scalable=no` у 38 HTML), контраст третинного тексту, ціль кнопки імені й
блокування фону під модалкою — усе одним проходом разом із версткою 005 (`98281b7`).
Контраст виявився не у «двох місцях», а в **51 входженні по 40 файлах**. Хвіст: футерні
посилання на двох живих лендінгах — 20px, не чіпались, бо це видима зміна.


### 2026-08-26 · 003 · Роадмап розробки — у проді

Окрема анімована сторінка `/roadmap` (варіант A «Лінія», перше підключення GSAP у проєкті)
плюс точка входу у футерах обох платформ. Конвеєр пройдено за один день: PM → фронтендер →
QA → коло фіксів (дефект D-01 закритий у `dc8cd12`), Supabase не чіпався ніде. Змержено в
`main` 2026-08-26 (`9f0f1be`, fast-forward через `git push origin dev:main`), версії підняті
до `0.3.0` / `1.2.0`. Подробиці — `003-roadmap/SUMMARY.md`, там же дві латентні пастки в
`js/roadmap-render.js` і дефект, що лишився в макеті.

### 2026-08-25 · 001 · Вхід і реєстрація через Google — у проді

Перша задача, що пройшла конвеєр повністю (PM → бекенд ∥ фронтенд → QA → коло фіксів → QA):
вхід через Google, `js/auth.js` як шар даних плюс новий `js/auth-ui.js`, редагування імені
для сертифіката, слот авторизації і токени макета в 38 HTML. Змержено в `main` (`661cf45`,
`d7febfd`), версії підняті до `0.2.0` / `1.1.0`, прод перевірено після деплою: сторінки 200,
регістрові пастки 404, приватне не роздається. Подробиці — `001-oauth-google/SUMMARY.md`,
незакриті хвости — у розділі «Активне» вище.

### 2026-08-24 · 002 · Ескалація привілеїв через `profiles.role` — закрито

Дірку знайдено 2026-08-23 плановою звіркою живої бази: `anon`/`authenticated` мали `UPDATE`
на всі колонки `profiles`, а `profiles_update` не мала `WITH CHECK` — один `PATCH` робив
будь-якого учня адміном. Власник застосував міграцію `002-lock-profiles-role.sql`; звірено
з базою після застосування: лишилось рівно `authenticated / full_name / UPDATE`, `INSERT`
забраний, `with_check` тепер явний. Головний висновок на майбутнє — **RLS не обмежує
колонки, це робота `GRANT`**, тому коректний екран політик у Dashboard нічого не доводить.

### 2026-08-23 · хвіст після створення майстерні — закрито

Supabase MCP піднявся й пройшов OAuth; звірку живої бази зроблено (результат — задача 002
вище, розділ «Стан бекенду» в `CLAUDE.md` переписаний за фактами). Загадку `payments`
закрито: заготовка під платні курси, 0 рядків, ніколи не використовувалась — не чіпали.
Дублікат `dev/.mcp.json` прибрано (канонічний — кореневий: ключ проєкту в `~/.claude.json`
саме `/Users/ander1.sage/Downloads/AIA`). Бекендеру доданий `mcp__claude-in-chrome` як
запасний шлях до Dashboard, з тією ж межею «дивитись — так, змінювати — ні».

### 2026-08-23 · майстерню створено

Створено `dev/build/` за зразком `dev/design/`: конвеєр із чотирьох агентів
(`aia-build-pm`, `aia-build-backend`, `aia-build-frontend`, `aia-build-qa`), усі Opus 5 /
effort `high`, 19 вендорених скілів у `.agents/skills/`. Рішення власника, зафіксовані в
`CLAUDE.md`: папка їде в публічний гіт із фільтром секретів · агенти правлять реальний код
і пушать у `dev`, `main` заборонений категорично · Supabase — читання через MCP, зміни
SQL-файлами на затвердження · нумерація задач спільна з `design/` · тестування живим
браузером без інсталяцій · фронтендер точно втілює макет і не вигадує візуал.

**Видимість агентів — закрито.** Агенти й скіли фізично лежать у `dev/build/.claude/`,
але харнес шукає їх у кореневій `.claude/` проєкту, тому звідти їх не було видно.
Створено відносні симлінки в `AIA/.claude/agents/` (4 агенти) і `AIA/.claude/skills/`
(19 скілів) — джерело правди лишилось одне, симлінки переживають клон. Деталі й порядок
оновлення — у `CLAUDE.md`, розділ «Як харнес знаходить цих агентів».

**Dev-база — вирішено: не робимо.** Розглядався Supabase-бранчинг: перевірено, що він
доступний лише від плану Pro ($25/міс) + $0.01344 за бранч на годину (≈$35/міс разом), а
бранч приходить без даних. Безкоштовну альтернативу (другий Free-проєкт, у власника є
вільний слот із двох) власник свідомо відхилив. **Рішення: одна база на все, вона ж прод.**

Наслідок, який довелось прописати в агентах: **dev-превʼю працює з продовою базою** —
у `config.json` один `supabase.url`, вибору середовища за хостом у `js/auth.js` немає.
Тестувальник отримав жорсткі правила для тестів, що пишуть у БД (дозвіл власника на кожен
прогін, один призначений тестовий акаунт, сертифікати не генерувати, перелік створених
рядків у звіті); бекендер — застереження, що MCP дивиться в прод і міграцію ніде не
обкатати. План на випадок, якщо колись робитимемо dev-базу, записаний у `CLAUDE.md`,
щоб не переоткривати питання.

**Supabase MCP вимкнений — закрито того ж дня, див. запис «хвіст після створення
майстерні» вище; нижче лишено як опис проблеми і способу її зняти.** У `AIA/.claude/settings.local.json` стоїть
`"disabledMcpjsonServers": ["supabase"]`, тож інструментів `mcp__supabase__*` немає
(перевірено пошуком інструментів). Сам сервер у `.mcp.json` прописаний правильно
(`project_ref=hpcyrnxschpxlrxudmqk`). Спроба увімкнути з сесії заблокована класифікатором
дозволів — і це правильно: доступ до живої бази вмикає людина. Власнику: `/mcp` →
увімкнути `supabase` → перезапуск. `claude mcp add` не допоможе — запис у `.mcp.json`
уже є й правильний (URL ідентичний), блокує саме рядок у `settings.local.json`. До того бекендер працює без звірки схеми з живою базою
і зобов'язаний позначати це у звіті.
