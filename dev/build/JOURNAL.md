# Журнал подій інженерної майстерні

Що відбувалося в `dev/build/` і чого чекаємо далі. Зворотно-хронологічний: **найновіше
згори**. Аналог `dev/design/JOURNAL.md`, але про реалізацію, а не про дизайн.
Веде коренева сесія; PM-агент дописує по своїх задачах.

## Правило журналу

**Поки задача в роботі — розписуємо детально.** Статуси, на якому агенті стоїмо, які
дефекти знайдено, яке коло циклу QA, чого чекаємо і від кого (особливо: які міграції
лежать незастосовані й чиє рішення блокує).

**Коли задача в проді — увесь опис стирається** і замінюється одним записом:
**дата · що за задача · що зроблено. Максимум 2–3 речення.** Задача переїжджає з
«Активного» у «Зроблено».

Сенс правила: хроніка потрібна, поки нею користуються. Після здачі подробиці лишаються
в папці задачі (`01-plan.md`, `02-backend/`, `03-frontend/`, `04-qa/`, `SUMMARY.md`).

**Репозиторій публічний:** ні ключів, ні даних учнів, ні фінансів — і в журналі теж.

---

## Активне

### 012 «Jira з нуля» — в роботі: контент — **j01–j22 написані й відрецензовані** (хвиля 5 закрита 2026-09-19, ~14:00); далі — **іспит j23** → три довідники, кожне за словом власника; фірма сюжету з 2026-09-19 — **«Марципан»**; каркас до дизайну готовий

Четвертий курс за зразком 005: Jira Cloud Free для новачків не з ІТ, 22 уроки + іспит + 3
довідники. **Порядок — рішення власника:** програма → контент і каркас → дизайн з повним входом →
білд як чисте перенесення (урок 005: дизайн ішов раніше за програму, білд «дознаходив» компоненти).
**Дизайн-конвеєр запускає власник сам у дизайн-сесії — звідси не стартувати.**

**⏱ ТОЧКА ВІДНОВЛЕННЯ (оновлено ~14:00 2026-09-19 — ХВИЛЯ 5 ЗАКРИТА ПОВНІСТЮ, СЕСІЮ ЗАКРИТО ЗА СЛОВОМ ВЛАСНИКА; агентів у роботі немає; коренева сесія з cwd `AIA/dev`, памʼять агентів — через симлінки; усе в `dev` і запушено, дерево чисте):**
- **Рецензія хвилі 5 — ГОТОВА (~13:30):** 4 із 4 «готово з зауваженнями», **43 правки рецензентів + 12 правок кореневої сесії**,
  203 цитати звірені посимвольно й регістрочутливо — 0 розбіжностей; чекер 0 ✗ **на всіх 22 уроках**, `check-quiz-markers.py` —
  0 на 22. Коміти: `6697a1f` j19 · `222c57b` j21 · `0f46866` j20 · `a689843` j22 · хвости кореневої сесії `559f306` (j19:
  `Add internal note`), `9e8bcac` (j19: календар «Марципан: Пн–Сб»; §3: дедлайни `MY`), `34d0618` (j20: ⚠ `Access requests`,
  `Grant access` прибрано з `•••`, flows із копії; j22: два ⚠ і `editJiraIssue`). Рецензент ≈ 288–319 тис. токенів, 16–20 хв;
  8 агентів хвилі разом ≈ 2,95 млн; ліміт витрат не спрацював. Документи (`3a241db`): `cross-findings.md` «Від рецензентів
  хвилі 5», `program.md` «Уточнення» **п. 18**, `facts-free-plan.md` «Дописано після рецензії хвилі 5»; шаблони промптів —
  `3f5c1c9`. ⚠ Після останнього рецензента процес Claude Code перезапускався: прийшло хибне сповіщення «рецензент j22 не
  закінчив» — його звіт (31 КБ) і правки на диску повні й закомічені (`a689843`); **скретчпад попереднього процесу зник**
  (скрипт перейменування `rename_firm.py` не зберігся — правила описані в коміті `189a0ff`).
- **Найважливіше з рецензії хвилі 5:** у цитатах і вікнах знову 0 розбіжностей; **головний клас дефектів — внутрішні
  суперечності уроку й перефрази, що кажуть більше за цитату поруч** (j19: черга «Нові» відбирає за статусом, а урок обіцяв,
  що заявка з неї «перетікає»; j21: «п'ять груп» у прозі проти шести в діаграмі; j20: «ім'я лишиться» проти власного джерела —
  `Former user`), другий — **твердження про екран, якого ніхто не бачив** (j19, j20, j22 — переписані на цитату або подані
  з ⚠) · рецензент j21 упіймав помилку **кореневої сесії**: дедлайни `MY` у §3 були записані зі зсувом на позицію (виправлено
  за вікном уроку) · мітка в JQL — у лапках, зведено в j07/j20/j21 · дім `Ask AI`/`Improve` — урок 3, не урок 2.
- **Довершено 2026-09-20 (власник: «продовжуй підготовку до наступної сесії»):** фінальна звірка — гілка `dev`, дерево чисте,
  три скрипти на 22 уроках: 0 ✗, 0 маркерів, «найдовша» 5/132; чеклист релізу зведено в `012-jira/task.md` (п. 7, рядки
  (а)–(є)); рішення власника внесені в бриф дизайну й рядок індексу дизайну; `jira.config.json` — примітка `author._note`
  (секцію не публікувати, поки `text` і `href` порожні); у фактах вісім блоків «Дописано після…».
- **Лишилось у текстах (не блокує іспит, зробити в наступній сесії):** **квіз j20 не покриває трьох тем картки** (`Open access`;
  «на Free усі — адміністратори спейсів»; two-step verification) — переписати 1–2 питання з обома квіз-скриптами · j19 — поле
  «Вкладення» (після знімка JSM) · j22 — бейджі й платність сертифікації (знімком у браузері), `Assignee` у W1 · j21 — що
  робити з першим, зіпсованим заїздом імпорту (дублі) · **перед релізом — повторна посимвольна звірка цитат усіх 22 уроків**
  (Atlassian переписує сторінки на ходу: KB про видалені роботи змінилась за добу) і перевірка посилань j22 після перезапуску
  форумів спільноти 2026-09-22.
- **(історія) Стан на ~11:45 2026-09-19 — автори хвилі 5 готові, рецензенти ×4 у роботі; останній коміт перед їхнім стартом `d3b684b`:**
- **Хвиля 5, автори — ГОТОВІ (~11:00):** 4 із 4 уроків `jira-19…22.html` написані, чекер 0 ✗, `check-quiz.py` найдовша 0/6 на
  всіх, маркерів 0; закомічено адресно (`f9b4cec` j19 · `b424a0f` j20 · `832aeae` j22 · `9392dc7` j21). Автори ≈ 378–476 тис.
  токенів (разом ≈ 1,75 млн), 28–32 хв кожен; 214 цитат хвилі звірені авторами посимвольно — 0 розбіжностей. Ліміт витрат
  не спрацював. Документи (`d3b684b`): `cross-findings.md` «Від авторів хвилі 5», `program.md` §3 + «Уточнення» **п. 17** +
  закриті питання власнику №1–4, 8, 9, `facts-free-plan.md` «Дописано після хвилі 5».
- **Найважливіше з хвилі 5:** `facts` **№14 і №14b закриті** (живі обидві адреси MCP — 401; в уроці v2, v1 переходить на
  v2 з 2027-03-01; виклики в межах одного продукту кредитів Rovo не потребують; докси MCP переїхали в
  `atlassian-ai-gateway`) · у team-managed службових спейсах **поля `resolution` немає** — приклад черги в самих доксах
  стоїть на ньому (j19 показує це плашкою) · листа-підтвердження клієнтові немає («They won't get notifications for their
  own activity») · Forms на Free **не закрито** · `Deactivate` — лише керовані акаунти, на Free `Remove user` / `Suspend
  access` · імпорт/експорт **не входять** у право `Administer Jira` · 🔴 **Atlassian переписує сторінки на ходу**: KB про
  видалені роботи оновлена 2026-09-18, цитата j05 зникла за добу — виправлено кореневою сесією (`f5478b7`, 3 із 3 цитат
  звірені `curl`-ом); **перед релізом — повторна посимвольна звірка цитат усіх 22 уроків** · j22: екран OAuth-згоди не
  намальований (написів у доксах немає) → одне вікно; сценарій живого прогону — `reports/j22.md` §5.2.
- **Перейменування «Кориця» → «Марципан» — ЗРОБЛЕНО (`189a0ff`, окремим комітом після авторів, до рецензентів):** 22 уроки,
  136 рядків по відмінках скриптом + узгодження роду руками (9 місць), адреса сайту сюжету `marcypan.atlassian.net`
  (j02, j03, j20); `program.md` §3 з нотаткою про відмінки, контракт, обидва шаблони промптів, `task.md`, бриф дизайну
  `dev/design/012-jira/task.md` (2 рядки — **сказати власнику**), приватний план. Лишків старої назви в уроках — 0; чекер
  0 ✗ на 22, маркерів 0, статистика квізів до й після ідентична. У `reports/*` і старих записах `cross-findings.md` —
  стара назва (історія). **Не зроблено:** кореневий `CLAUDE.md` (1 згадка) — у фінальному проході документів.
- **Рецензенти ×4 стартували ~11:40 (слово власника «запусти рецензію»):** `prompt-reviewer.md` + блок «Особливість jNN» зі
  звітів авторів §4–§6 + блок «Додано після хвилі 5» (нова назва фірми — перевірити узгодження роду; рішення власника — не
  дефект). **Зведення паралельних сусідів:** форма мітки в JQL — **у лапках** (канон j07: `labels = "дозволи"`): рецензент
  j20 править `labels != архів` → `labels != "архів" OR labels IS EMPTY` скрізь, **зокрема у квізі** (єдиний дозволений
  виняток із правила «`q` не чіпати» — механічно лапки, у JSON екрановані); рецензент j21 тримає ту саму форму і звіряє
  всі 20 кроків чеклиста з уроками, на які вони посилаються; рецензент j21 окремо перевіряє «хто робить імпорт» проти
  знахідки j20 (`Administer Jira` не дає імпорту). Рецензент j22 — «жодного вигаданого виводу» по кожному з 7 `term`;
  реальний `claude mcp add/remove/login/list` заборонений.
- **Після рецензентів (не забути):** три скрипти на кожному → `git add` + `git commit -- <шляхи>` адресно (урок + звіт) →
  `cross-findings.md` «Від рецензентів хвилі 5», `program.md` п. 18, `facts-free-plan.md`, цей запис, `task.md`, README,
  кореневий `CLAUDE.md` (зокрема назва фірми), памʼять агентів окремим комітом → push. **РІШЕННЯ ВЛАСНИКА ~11:55: «після
  рецензії все готуй до наступної сесії, оновлюєш відповідні доки, журнал подій і т. д. Цю сесію після рецензії ми
  закриваємо» — іспит j23 у ЦІЙ сесії НЕ запускати.** Іспит j23 (окремий автор, читає всі 22 уроки, ≈ 500 тис. токенів; теми
  Ф5 від автора j22 — у `cross-findings.md`) і довідники ×3 — у наступній сесії, кожне за словом власника.
- **Якщо сесія обірвалась під час рецензії:** `git status` покаже змінені `modules/jira-19…22.html` і untracked
  `reports/jNN-review.md`; файл змінений, а звіту немає = рецензент не дійшов до кінця → новий рецензент за
  `prompt-reviewer.md` (частково виправлений файл — не перешкода); зупинених лімітом будити `SendMessage` на той самий agentId.
- **(історія) Старт авторів хвилі 5 — ~09:15 2026-09-19; дерево перед стартом чисте, останній коміт був `ceede53`:**
- **Хвиля 5 стартувала ~09:15 2026-09-19 (слово власника «запускай хвилю 5»):** автори ×4 на `j19`–`j22` паралельно,
  кожен пише рівно два файли — `modules/jira-NN.html` + `01-authoring/reports/jNN.md`. Промпт = `prompt-author.md` (тіло
  без змін, підстановка номера/назви/«Фаза 5») + рядок про сусідів + дата + блок «Особливе для jNN» (зібраний із
  «Старт наступної сесії» п. 2 нижче і `grep 'jNN' cross-findings.md`). **Стан середовища на старті:** розширення Chrome
  **не підключене** (`tabs_context_mcp` → «Browser extension is not connected»), конектора `claude.ai Atlassian` у сесії
  немає, JSM і Confluence на sandbox не додані → **j19 — усі чотири вікна «за документацією»; j22 — без живого запуску**:
  вивід терміналу й екран OAuth-згоди **не вигадувати** (лише те, що є в живих доксах, решта — словами з ⚠ і проханням
  зняти у звіті §5); дозволено зняти самому `claude --version`, `claude mcp --help`, `claude mcp add --help` і перевірити
  обидва endpoint-и `curl`-ом без OAuth (401 = живий, 404 = немає); **реальний `claude mcp add` заборонений** (міняє конфіг
  власника). **Рішення кореневої сесії для паралельних сусідів (щоб не розійшлись):** (1) «хто створює спейс» — канон j09:
  team-managed за замовчуванням створює будь-хто, поки адміністратор Jira не змінить глобальне право; company-managed —
  лише адміністратор Jira; дім теми — j20, у j19 — щонайбільше одне речення; (2) «гігієна замість архівації» (`Done` +
  мітка `архів`) — дім j20 (чому архівації немає і що замість), j21 лише застосовує кроком чеклиста з відсиланням;
  (3) фільтр «без архіву» — у формі з j07: `labels != архів OR labels IS EMPTY` (голе `!=` губить роботи без міток);
  (4) j19: статуси — робочий канон §3, `FIN-1` — `Done`, `FIN-2` — `Waiting for customer`; «два листи клієнтові» з картки —
  лише з дослівним джерелом; (5) j21: грабля картки «ключі не змінити» хибна (ключ спейсу змінюється, j03/j09) — переписати
  за доксами; крок 16 «sample data геть» без опори — замінити або зняти; (6) j22: число викликів на годину — ліміт плану,
  в урок не йде; посилання на профіль Champion не ставити (відкрите питання власнику №2).
  **Після авторів (не забути):** три скрипти → `git add` + `git commit -- <шляхи>` адресно → §8 звітів у `cross-findings.md`
  «Від авторів хвилі 5», ⚠ → `facts-free-plan.md` «Дописано після хвилі 5», уточнення карток → `program.md` п. 17 → цей
  запис → push → рецензенти ×4. **Якщо сесія обірвалась:** `git status` покаже untracked `modules/jira-19…22.html` і
  `reports/j19…22.md`; файл уроку без звіту = автор не дійшов до кінця (перечитати чекером, дописувати новим автором з
  інструкцією «допиши файл»); зупинених лімітом будити `SendMessage` на той самий agentId.
- **Доручення власника ~09:25 (під час роботи авторів): «після авторів давай розберемо рішення, що чекають» — по ОДНОМУ
  питанню через `AskUserQuestion`, з варіантами на вибір, рекомендований — першим** (відповідь клавішею 1–4, у кінці
  Submit). Перелік: (1) канон статусів `FIN` (`Open → Work in progress → Waiting for customer → Done` проти картки
  `In Progress`/`Resolved` проти «чекати знімка JSM»); (2) авторка заявок `FIN-1`/`FIN-2` — Соломія; (3) назва
  постачальника «млин «Золотий колос»» — лишити чи нейтральна; (4) термін «службовий спейс (service space)»; (5) «Кориця»
  і 8 ролей; (6) профіль Champion (блок `author` у `jira.config.json` — зараз заглушки, `href` порожній); (7) монограма
  сертифіката (чинні: `AIA` · `AIA` · `AIT`; кандидат `AIJ`); (8) текст пункту меню «Курси» (чинні: «Базовий» · «AI
  Architect» · «Claude Code CLI»); за потреби — (9) іспит j23 після рецензії j19–j22. Відповіді → `program.md` «Відкриті
  питання власнику» (закривати пунктами), §3, `jira.config.json` — лише `author`/тексти, без `css/`/`js/`.
- **РІШЕННЯ ВЛАСНИКА 2026-09-19 (~09:45–…, розбір по одному питанню, поки автори пишуть) — не переоткривати:**
  (1) **фірма сюжету перейменовується: «Кориця» → «Марципан»** (власник написав «Марцепан», після уточнення про словникову
  форму обрав «Марципан» через «и»; відмінки: «бухгалтерія Марципану», «у Марципані», «з Марципаном»). **Перейменування
  робить коренева сесія ОДНИМ окремим комітом ПІСЛЯ авторів хвилі 5 і ДО рецензентів** (автори зараз пишуть із «Корицею»,
  §3 під ними не міняти): 22 уроки (в j01–j18 — 84 «Кориці» · 21 «Кориця» · 6 «Корицю» · 1 «Корицею», кожне входження —
  за відмінком, не sed-ом наосліп; один латинський слід `korytsia`), `jira.config.json`, `program.md` (§3 і всі згадки),
  `lesson-contract.md`, обидва шаблони промптів, `glossary.md`/`facts-map.md` якщо є, бриф `dev/design/012-jira/task.md`
  (сказати власнику), кореневий `CLAUDE.md`, памʼять; звіти `reports/*` — історія, не чіпати; люди, ключі й назви спейсів
  не міняються; (2) авторка заявок `FIN-1` і `FIN-2` — **Соломія Бойко, обидві** (підтверджено); (3) постачальник у формі
  `FIN-2` — **лишається «млин «Золотий колос»»**; (4) термін — **«службовий спейс (service space)»** (підтверджено);
  (5) блок «Хто веде» (`author` у `jira.config.json`) — **лишити заглушкою**: «потім буде сторінка «про власника» (є в
  ідеях) — тоді посилатись на неї», тобто посилання піде на власну сторінку сайту, а не на профіль Atlassian Community; у
  j22 посилання на профіль не ставити; (6) монограма сертифіката — **`AIJ`** (один рядок `BRANDS` у `js/certificate.js` —
  на етапі білду, не зараз); (7) пункт меню «Курси» — **«Jira з нуля»** (білд, чотири лендінги); (8) **іспит j23 — ПІСЛЯ
  рецензії j19–j22**; (9) статуси `FIN` — **робочий канон стає каноном**: `Open → Work in progress → Waiting for customer →
  Done`; міняти лише якщо знімок живого JSM покаже інший набір (тоді j18 і j19 разом). **Лишились відкритими в
  `program.md` «Відкриті питання власнику»:** №5 (дозвіл на наповнення sandbox через MCP), №6 (заголовок hero — для
  дизайну), №7 (JSM і Confluence на sandbox). **Куди рознести після авторів:** `program.md` — §3 (назва фірми, «робочий
  канон» → «канон»), «Відкриті питання власнику» (закрити №1–4, 8, 9), «Уточнення» п. 17; `jira.config.json` —
  `author._note`; чеклист білду — `AIJ` і «Jira з нуля»; бриф дизайну — назва фірми й рішення про блок автора.
- **(історія) Стан до старту хвилі 5 — ~01:15 2026-09-19: хвиля 4 закрита повністю, агентів у роботі не було.**
- **Рецензія хвилі 4 — ГОТОВА (~01:00 2026-09-19):** 4 із 4 «готово з зауваженнями», **36 правок рецензентів + 12 правок
  кореневої сесії**, 224 цитати звірені посимвольно й регістрочутливо — 0 розбіжностей; чекер 0 ✗ **на всіх 18 уроках**,
  `check-quiz-markers.py` — 0 на 18. Коміти: `ea376fd` j15 · `4346c3c` j16 · `7639657` j17 · `243ee8f` j18 · `be0e202`
  (хвости в j01 і j09). Рецензент ≈ 295–330 тис. токенів, 19–21 хв. Документи: `cross-findings.md` «Від рецензентів
  хвилі 4», `program.md` §3 + «Уточнення» **п. 16** + «Відкриті питання власнику» п. 7–9, `facts-free-plan.md` «Дописано
  після рецензії хвилі 4», обидва шаблони промптів, `task.md`, README, `CLAUDE.md`. **Стан: j01–j18 написані й
  відрецензовані, усе в `dev` і запушено; дерево чисте; агентів у роботі немає.**
- **Найважливіше з рецензії хвилі 4:** у цитатах і вікнах — 0 розбіжностей, а **головний клас дефектів — твердження «зі
  здорового глузду» без джерела** («лист усе одно надсилається», «адміністратор не може ввімкнути листи» — суперечило
  довідці, «резервна копія не рятує дані застосунку», «категорія є лише тоді, коли продукт доданий» — спростував
  `screens/23`, «у довідці скорочують до JSM») — усе виправлено або знято · суперечність j16/j17 про Slack і Teams закрита
  (картка Slack-застосунку на Marketplace **є** за ключем, хоча текстовий пошук REST її не повертає) · `endOfWeek()`
  вважає останнім днем тижня суботу · листа-підтвердження клієнтові при створенні заявки в доксах **немає** (картка j19
  на нього розраховує) · `Add agent` — на редакторі workflow, на картці агента — розділ `Agents` (Rovo).
- **ДАЛІ — за словом власника:** хвиля 5 = автори **j19–j22** (чотири; чеклист — «Старт наступної сесії» нижче) →
  рецензенти → **іспит j23 окремим автором ПІСЛЯ рецензії j19–j22** (рекомендація кореневої сесії: пʼять питань Ф5
  спираються на тексти цих уроків; рішення — за власником) → довідники ×3 (третій — після JSM/Confluence на sandbox).
- **Хвиля 4, автори — ГОТОВІ (~23:40):** 4 із 4 уроків `jira-15…18.html` написані, чекер 0 ✗, `check-quiz.py` найдовша
  0/6 на всіх, маркерів 0 (у j15 П3 двокрапка стояла лише в правильному — розведено кореневою сесією), закомічено
  адресно (`295177c` j15 · `fede0ab` j18 · `d712910` j16 · `8c887d3` j17; памʼять авторів — `78a4249`). Автори ≈ 390–415
  тис. токенів, 33–34 хв кожен; 234 цитати хвилі звірені посимвольно, 0 розбіжностей. Документи (`e139bfc` + наступний
  коміт): `cross-findings.md` «Від авторів хвилі 4», `program.md` §3 (`REM-6`, автор `FIN-1`/`FIN-2` — СБ, деталі форми
  `FIN-2`, робочий канон статусів `FIN`) і «Уточнення» **п. 15**, `facts-free-plan.md` «Дописано після хвилі 4» (+ два
  закриті рядки: база знань JSM, фільтр `Free up to 10 users`).
- **Найважливіше з хвилі 4:** картки фаз 4–5 відстали від продукту (гілок «за JQL» і `For each` немає; картка j18 про
  JSM Free застаріла; «календар через експорт» не підтвердився) — заміни зведені в п. 15 як рішення · база знань JSM:
  «at a minimum, you'll need to have a Free plan of Confluence on the same site» (слів «купувати не треба» не вживати) ·
  `Cloud Fortified` закривається 2026-12-31 · `Pages → Docs` у польоті · статуси `FIN` із §3 (`In Progress`, `Resolved`)
  у доксах не знайдені — робочий канон `Open → Work in progress → Waiting for customer → Done` до знімка JSM ·
  суперечність j16/j17 про те, звідки ставляться застосунки Slack і Teams, — віддана обом рецензентам.
- **(історія, зразок рядків «особливе») Хвиля 4 стартувала ~23:00 2026-09-18 (слово власника «запускай четверту хвилю»):** автори ×4 на `j15`–`j18` паралельно,
  кожен пише рівно два файли — `modules/jira-NN.html` + `01-authoring/reports/jNN.md`. Промпт = `prompt-author.md`
  + рядки «особливе»: **j15** — `REM-6` створює саме цей урок (дані з §3, 10 жовтня), підписи журналу `Successful` /
  `No action` / `Some errors` тримати як у j14 (Ф-2 рецензента j14; sandbox дасть інше — правимо j14 і j15 разом),
  жива дія `Send customized email` (не Deprecated `Send email`), слаги доксів від автора j14, `screens/14`;
  **j16** — оглядово (рішення власника), Confluence на sandbox немає → стан «після підключення» лише «за
  документацією», `screens/09`, `17`, екран `Emails and notifications` проти доксового `Notification settings`;
  **j17** — `screens/20`, `22`, поіменний список несумісних із Free застосунків (знахідка j02), `Manage apps` не
  знятий; **j18** — **Фаза 5**, JSM на sandbox немає → усі три вікна «за документацією», `screens/23`, `24`;
  картка застаріла щодо JSM Free (facts №3 ✅: multi-channel, SLA, custom reports Є) — факти мають пріоритет;
  термін — «службовий спейс (service space)» як у j09 (у j01 — «службовий простір», у картці — «сервіс-спейс»:
  звести після хвилі).
  Перед стартом окремим комітом `757fb97` закомічена памʼять агентів хвиль 2–3 (18 файлів). Рецензенти стартували ~00:10
  2026-09-19 з блоком «Особливість» зі звіту автора §4–§6 і вказівкою звести суперечність j16/j17.
- **Рецензія хвилі 2 — ГОТОВА:** 7 із 7 «готово з зауваженнями», 72 правки, чекер 0 ✗ на всіх, закомічено
  адресно (`d77f0e5` j04 · `f41790e` j08 · `5c05203` j09 · `f07ff3b` j06 · `a041ca6` j07 · `3bb9a34` j10 ·
  `71cd305` j05). Кожен рецензент ≈ 230–275 тис. токенів. Документи: `cross-findings.md` «Від рецензентів
  хвилі 2» (наскрізне + по рецензенту), `program.md` «Уточнення» п. 11, `facts-free-plan.md` «Дописано після
  рецензії хвилі 2» (🔴 старе посилання після Move — на ньому квіз j05), `task.md`.
- **Найважливіше з рецензії:** шаблон task management дає 2 статуси, project management — 3 (Ф-1 у j10;
  звірити j03/j09/j12) · цитати треба звіряти посимвольно й за банером сторінки (j06: вставлене «Cloud»,
  software-цитати про business-дошку) · маркери стилю у квізах, яких `check-quiz.py` не бачить (j06 — 5 із 6) ·
  два «адміністратори» (спейсів ≠ `Administer Jira`).
- **Рішення власника, які чекають:** `REM` у вікнах j07/j08 до j09 (правило чи прибрати) · блок 3 j07 = 42 %
  уроку (девʼятий блок «Фільтри» в контракті?) · «виконавець рівно один» без цитати (факт продукту чи зняти).
- **Хвости закриті кореневою сесією (`5e43863`, ~12:00):** легенди `jira-01` за §4.1 · j03 «`status = Done`» →
  помилка «does not exist» (Mermaid + проза) · j09 довідник за назвою · j06 — `View settings → Hide done work
  items after` (jira-core-cloud) і `Search board` (`screens/03`). **Лишається:** j03 + j09 «service management»
  у `Type` — парою після знімка JSM-спейсу (без sandbox не закрити).
- **Хвиля 3 — ГОТОВА (~13:00):** 4 із 4 уроків `jira-11…14.html` написані, чекер 0 ✗, закомічені адресно
  (`8fd7fe6` j12 · `c6515d5` j14 · `fd6087c` j13 · `0f0e880` j11 — автор j11 завис на фінальному
  повідомленні, файл і звіт повні). Автори ≈ 330–360 тис. токенів, 19–22 хв (після відновлення). Документи:
  `cross-findings.md` «Від авторів хвилі 3», `program.md` «Уточнення» п. 12 + `WEB-4`/`WEB-5` у §3,
  `facts-free-plan.md` «Дописано після хвилі 3», `task.md`.
- **Найважливіше з хвилі 3:** `jira-core-cloud` — аліас, канонічний префікс `jira-software-cloud/docs/` ·
  докси автоматизації вже кажуть `flow`/`step`, `Send email` Deprecated · епіка в бізнес-спейсі за
  замовчуванням немає (`+ Add work type`) · тип поля `URL` у team-managed не існує · `Group by Epic` для ділових
  дошок не існує · `win__chain` уперше вжитий (j14).
- **Рецензія хвилі 3 — ГОТОВА (~14:00):** 4 із 4 «готово з зауваженнями», 49 правок, чекер 0 ✗, закомічено
  (`268adde` j13 · `102cdb3` j12 · `76163f3` j11 · `4687992` j14). Документи: `cross-findings.md` «Від рецензентів
  хвилі 3», `program.md` «Уточнення» п. 13, `facts-free-plan.md`, `task.md`. **Стан: j01–j14 написані й
  відрецензовані, усе в `dev`; дерево чисте.**
- **Хвости до j14 ЗАКРИТІ кореневою сесією (~15:30, доручення власника «закривай усі хвости»)** — `program.md`
  «Уточнення» п. 14 і правила 2а–2в: дати сюжету зведені до канону (§3; j08/j12/j14 виправлені, `MARK-6` → МЛ) ·
  `REM` у вікнах до j09 — правило «вікна показують фірму цілком» · числа поведінки продукту з цитатою — дозволені ·
  j12 «стан» → «статус», плитки `Kanban`/`Scrum`, перейменування статусів англійською · j11 `Story`/`Bug`, дім
  поля `Team`, виноска ⚠ про `+ Add work type` · j04 `type` замість `issuetype`, опора «виконавець один» (тип
  `USER`) · j07 `Default access` · j13 квіз П6, вкладки W2 · j14 назва flow, 10 жовтня · `Sprint`/`openSprints()` —
  лише довідник · блок 3 j07/j11 — лишається · Summary 7 днів / 2 тижні — обидва в доксах · **дзеркальні маркери
  квізів у 11 питаннях 7 уроків** (j03, j04, j06, j07, j08, j10) — розведені; тепер 0 в обидва боки на 14 уроках.
  **Лишається лише після sandbox:** `+ Add work type`/`Epic` у діловому спейсі; підписи журналу (j14/j15);
  «service management» у `Type` (j03/j09); `Export` у W1 j07; старе посилання після `Move` (j05); двоє в `Assignee`.
- **ДАЛІ — за словом власника:** хвиля 4 = автори j15–j18 (`prompt-author.md` + указівка на «Від рецензентів хвилі 3»
  і «Уточнення» п. 13; для j15 — Ф-2 j14 про підписи журналу, `REM-6` створює j15) → рецензенти j15–j18 → хвиля 5
  j19–j23 (+ іспит; j19 — `stateDiagram-v2`, `useMaxWidth` для білду) → довідники ×3 (третій — після JSM/Confluence на
  sandbox; обіцянки уроків довідникам — у наскрізному «Від рецензентів хвилі 3»).

- **Двічі за ніч спрацював місячний ліміт витрат (429, ~00:45 і ~05:30)** — усі 11 агентів (рецензенти ×7 на
  j04–j10 ∥ автори ×4 на j11–j14) зупинялись посеред роботи. Дерево ціле: чекер 0 ✗ на `jira-01…10`, правки
  рецензентів у семи файлах (не закомічені, `git diff --stat`: +178/−129), звіти `j04-review.md` і `j08-review.md`
  записані, решта пʼять — ні; `jira-11.html` — 14,7 KB, обривається на блоці 2; `jira-12…14` не створені.
  **Агенти, зупинені 429, відновлюються `SendMessage` на той самий agentId з їхнього транскрипта** (перевірено
  тричі; на стрімі буває «no progress 600s» — повторити надсилання). **Рішення власника ~10:00: спершу лише сім
  рецензентів; авторів j11–j14 будити після.** Якщо ця сесія обірветься — рецензенти вже не відновляться:
  тоді нові рецензенти на j05, j06, j07, j09, j10 за `prompt-reviewer.md` (файли вже частково виправлені —
  це не перешкода), j04/j08 — перевірити звіти на повноту; автор j11 — новий, з інструкцією дописати файл.

- **Хвиля 2 — ГОТОВА:** 7 із 7 уроків `jira-04…10.html` написані, чекер 0 ✗ / 0 !, «найдовша» 0–2 з 6, закомічені
  адресно (`496ff0c` j06 · `c3aed5c` j09 · `dbebe1d` j08 · `dccd2ed` j05 · `6f1db76` j04 · `96dd192` j10 ·
  `52f85df` j07). Кожен автор ≈ 300–340 тис. токенів, 27–31 хв. Документи після хвилі: `cross-findings.md`
  (розділ «Від авторів хвилі 2» — наскрізне + по автору), `program.md` («Уточнення до карток після хвилі 2»,
  10 пунктів, пріоритет над картками), `facts-free-plan.md` (7 нових ⚠ + список дозйомки), `task.md`, README.
- **Зараз запущено (ніч 2026-09-18):** рецензенти ×7 на `j04`–`j10` (`prompt-reviewer.md`; правлять лише свій
  урок, звіт `reports/jNN-review.md`) ∥ автори ×4 на `j11`–`j14` (`prompt-author.md`). Після них — хвиля 4
  `j15`–`j18` ∥ рецензенти j11–j14 → хвиля 5 `j19`–`j23` ∥ рецензенти j15–j18 → рецензенти j19–j23 ∥ довідники.
- **Три знахідки хвилі 2, які варто знати до читання уроків:** (1) у JQL лишається `project` — докси `jql-fields`
  зіпсовані автозаміною на `space`, Atlassian сама вішає банер «try using the old term instead»; (2) `Summary` →
  `Title` у доксах (open beta), екран ще `Summary`; (3) `js/mermaid-theme.js` не має `useMaxWidth: false` для
  `state` — j10 (і майбутній j19) зі `stateDiagram-v2` стискатимуться; це для білду (чеклист релізу).
- **Для власника (sandbox, лише перегляд) — зведено у `facts-free-plan.md` «Дописано після хвилі 2»:** найважливіші —
  майстер `Create space` на Free (чи є company-managed), business-спейс (`screens/21`, чекають пʼять уроків),
  мітки глобальні чи ні, `Fix error` на Free.

- **Рішення власника цієї ночі:** агенти пишуть **лише контент** — дизайн робить дизайн-сесія, білд —
  dev-команда `dev/build`; нічого з `css/`, `js/`, `jira.html`, спільного коду звідси не чіпати.
  **Хвилі затверджені наперед, апрув на кожну не потрібен:** хвиля 2 = **7** авторів `j04`–`j10`
  (запущені ~23:50) → хвиля 3 = `j11`–`j14` → хвиля 4 = `j15`–`j18` → хвиля 5 = `j19`–`j23` (5, з іспитом)
  → потім **три довідники** `jira-ref-*` (довідник 3 про ліміти — після JSM/Confluence на sandbox).
  **Рецензенти — так**, на кожну написану партію, паралельно з наступною хвилею авторів
  (`prompt-reviewer.md`). Після кожної хвилі: чекер + `check-quiz.py` → `git commit -- <шляхи>` у `dev` →
  оновити цей запис, `012-jira/task.md` (статус), індекси README, `cross-findings.md` (§8 звітів),
  `facts-free-plan.md` (⚠).
- **Стан файлів (історія старту хвилі 2):** написані й відрецензовані `jira-01…03`; `jira-04…10` — пишуться (звіти
  `01-authoring/reports/j04…j10.md` з'являться після завершення; якщо файл уроку є, а звіту немає —
  автор не дійшов до кінця, урок перечитати чекером перед рецензією). Промпти — `prompt-author.md`
  з підстановкою номера/назви/фази; для j07 додано рядок про JQL, j08 — гаджети, j09 — назви типів
  спейсів, j10 — статуси й редактор workflow.
- **Пастка cwd закрита обходом:** стара памʼять Терміналу відкладена в
  `dev/.claude/agent-memory/aia-content-author.terminal-2026-09-04/`, замість неї симлінк на
  `dev/build/.claude/agent-memory/aia-content-author` (як у рецензента). Памʼять авторів іде в канонічну папку.
- **Якщо сесія обірвалась:** `git status` покаже untracked `modules/jira-NN.html` і `reports/jNN.md` —
  прогнати `check-lessons.py --pattern 'modules/jira-*.html'` і `check-quiz.py`, закомітити готові,
  запустити рецензентів на них і наступну хвилю за списком вище.

**Що є в `dev` (коміти `8f0279d` → `b174d16` → `abef643` → докси):** `00-research/program.md` (джерело
правди: 22 картки з вікнами, сюжет «Кориця», матриця 62 вікон), `facts-free-plan.md` (⚠ №3 про JSM Free
**закрито ✅** трьома методами; 5 рядків «для власника» в «Не вдалось перевірити»), `glossary.md`,
`inventory.md`, `screens/` — **23 знімки** (22 бічна панель · 23 галерея шаблонів · 24 таблиця планів JSM
дозняті ввечері; 21 business-спейс — після створення спейсу), `01-authoring/lesson-contract.md` +
`facts-map.md` + `check-lessons.py` (тепер `!` на пункт легенди > 90 знаків) + `cross-findings.md`
(секції авторів ×3 і рецензентів ×3), **шаблони промптів `prompt-author.md` і `prompt-reviewer.md`**,
`jira.config.json` (17 ключів, 23 модулі `draft`, 0.1.0), **уроки `modules/jira-01…03.html` — пройшли
рецензента** (`reports/jNN.md` авторів + `reports/jNN-review.md`), бриф `dev/design/012-jira/task.md` з
розділом «Вхід для дизайну — ГОТОВИЙ».

**Рецензія хвилі 1 (2026-09-17, вечір; 3 агенти паралельно, ~20 хв і ~240 тис. токенів кожен):** усі
три — «готово з зауваженнями»; 45 правок із показаним джерелом (ключ роботи **не вічний** — міняється
разом із ключем спейсу; статуси народжуються з шаблону; лист про неактивність іде адмінам сайту, не
«тому, хто створив»; легенди j03 94–157 → 41–62 знаки; маркери пунктуації в квізах). Два пропуски
програми закрила коренева сесія з готовим джерелом (Scrum у j01; архівація робіт у j02, facts №5) і
додала гloss «організація» в j03. Чекер 0 ✗ / 0 !, квізи «найдовша» 0/18. **Не закрито кодом:**
4 твердження j03 перевіряються лише живою реєстрацією (картка, кирилиця в назві сайту, лист з адресою,
клік Ask AI) — **власник**; напис «Team-managed business» у вікнах j02/j03 — після business-спейсу.

**Знахідки вечора, що визначають дизайн і наступні уроки:** sandbox стоїть на **новій навігації**
(Recent · Starred · Apps · Customize sidebar; Search і + Create — у верхній панелі; `screens/22`) — стару
доксову сторінку `navigate-to-your-work` не цитувати · шаблон задач у UI — **Task tracking** (докси: «task
management template», 2 статуси) · Work management — 30 business-шаблонів без Premium · JSM Free:
multi-channel ✓, SLA ✓, custom reports ✓, 1 250 steps, global automation 100 executions/month ·
**`win__topbar` (§4.3) не вжитий у жодному уроці** — усі кладуть верхню панель у `win__bar`; для Search поза
порталом класу немає — **дизайн зводить до одного рішення, білд робить sed-пас** · «один сайт» у j03
лишається (рішення кореневої сесії: One для Free/Standard/Premium, стабільно; власник може скасувати).

**Пастка сесії (cwd):** памʼять агентів — у `dev/build/.claude/agent-memory/`. Сесії 2026-09-17 (вечір) і
2026-09-18 працювали з `AIA/dev` через **симлінки** `dev/.claude/agent-memory/aia-content-{author,reviewer} →
../../build/.claude/agent-memory/…` (стара памʼять Терміналу відкладена в `aia-content-author.terminal-2026-09-04/`).
Обидва шляхи тепер робочі; чистіший — `cd dev/build && claude`.

**Старт наступної сесії (стан: j01–j22 написані й відрецензовані, хвости закриті, усе запушено в `dev`; останні коміти —
документи хвилі 5; браузер: розширення Chrome 2026-09-19 було НЕ підключене — перевірити `tabs_context_mcp` на старті;
конектора `claude.ai Atlassian` у сесії не було):**
1. `git status`, гілка `dev`, дерево має бути чисте. Shell перед спавном агентів — у `AIA/dev` (симлінки памʼяті) або в
   `dev/build`; **не заходити `cd` у папку задачі**. Прочитати цей запис, `012-jira/task.md` (статус), `program.md` §3 (фірма —
   **«Марципан»**, відмінки й латиниця там само) + «Уточнення» **п. 1–18** + «Наскрізні правила» 2а–2в + «Відкриті питання
   власнику» (відкриті лише №5–7), `cross-findings.md` (розділи хвилі 5 авторів і рецензентів — там рядки «→ j23» і «→
   довідник …»), `facts-free-plan.md` «Дописано після…» (вісім блоків).
2. **Іспит j23 — за словом власника** (рішення 2026-09-19: після рецензії j19–j22 — умова виконана): один автор за
   `01-authoring/prompt-author.md` (блок «Додано після хвилі 5» → «Для іспиту j23»): 26 питань, розподіл по фазах —
   `program.md` «Фінальний іспит», без вікон і Mermaid, `passing_score` 85, форма ситуаційна; автор читає всі 22 уроки в
   чинній редакції (≈ 500 тис. токенів); теми Ф5 — безпечні й заборонені — від рецензентів j21 і j22 у `cross-findings.md`
   («→ j23»); **не питати** про числа, покоління endpoint-а й усе, що в уроках стоїть під ⚠; обидва квіз-скрипти обовʼязкові.
   Потім рецензент (`prompt-reviewer.md`: кожне питання звіряти з чинним текстом уроку, а не з карткою).
3. **Дрібне до або разом з іспитом:** квіз j20 — переписати 1–2 питання під теми картки (`Open access`; «на Free усі —
   адміністратори спейсів»; two-step verification), JQL-питання прибрати (його дім — j07); перевірити трьома скриптами.
4. **Довідники ×3 — за словом власника, після іспиту:** `jira-ref-jql.html`, `jira-ref-automation.html`, `jira-ref-map.html`
   (третій — після JSM/Confluence на sandbox: у ньому всі числа лімітів і «Що змінилось»). Обіцянки уроків довідникам зібрані
   в `cross-findings.md`: «Від рецензентів хвилі 3» → «Наскрізне», «Від авторів хвилі 4/5», «Від рецензентів хвилі 4/5»
   (зокрема: SLA-функції JQL живуть на `write-jql-queries-for-slas`; тригер `SLA threshold breached`; інструмент
   `searchJiraIssuesUsingJql`; перевитрата кредитів і кроків автоматизації з однієї дати 2026-12-03; сезонні релізи
   задокументовані; «product» → «app» в адмінці; пастка порожньої клітинки в таблицях планів). Перша колонка `.ds-tbl` —
   лише ідентифікатор.
5. **Агенти, зупинені лімітом (429):** відновлювати `SendMessage` на той самий agentId; «no progress 600s» — повторити.
   Виміряно у хвилі 5: автор ≈ 378–476 тис. токенів і 28–32 хв, рецензент ≈ 288–319 тис. і 16–20 хв; 8 агентів разом ≈ 2,95
   млн. Ліміт у хвилях 4 і 5 не спрацював. **Перезапуск процесу Claude Code посеред хвилі** дає хибне «agent didn't finish» —
   спершу дивитись на диск (звіт + `git status`), а не перезапускати агента.
6. **Sandbox (лише перегляд), у порядку користі** — повні списки в `facts-free-plan.md` (блоки хвиль 4 і 5): `Settings →
   System` (чи є `Backup manager` — j20) · `+ Add work type` і `Epic` у діловому спейсі (j11–j13, j21) · `Audit log` із
   запуском (j14, j15) · майстер CSV-імпорту (j21) · A/B перемикача запитів доступу й `This space has 1 role` (j20) · кирилична
   мітка в JQL (j07, j20) · після додавання JSM: сторінка типу запиту (чи є Forms і поле вкладення), `SLAs` + `Add calendar`,
   `Create new queue`, портал `Requests`, `Customer notifications` на Free, налаштування службового спейсу цілком, колонка
   `Type` у `Spaces`, **які статуси дає шаблон** (канон `FIN`) · після Confluence: вкладка `Docs` + макрос `/jira` (j16).
7. **Живий прогін MCP (після `/mcp` → `claude.ai Atlassian`)** — сценарій у `reports/j22.md` §5.2 + доповнення рецензента
   (`facts-free-plan.md`): екран OAuth-згоди → `screens/25-oauth-consent.md`, вікно підтвердження інструмента, `Reporter` і
   `Assignee` створеної картки, чи з'явився новий елемент у Jira, запити українською, крос-продуктовий запит із Free.
8. **Скрипти перевірки (усі три — після кожного агента й після кожної власної правки):** `check-lessons.py --pattern
   'modules/jira-*.html'` · `007-quiz-distractors/check-quiz.py --files …` · `012-jira/01-authoring/check-quiz-markers.py`.
   Станом на 2026-09-19 — 0 ✗ і 0 маркерів на 22 уроках; «найдовша» 5/132.
9. **Чеклист білду (не контент; після `дизайн готовий`) — повна версія з 2026-09-20 живе в `012-jira/task.md` «В. Конвеєр
   реалізації» п. 7 («Чеклист релізу», рядки (а)–(є)); коротко:** монограма сертифіката **`AIJ`** (рядок `BRANDS` у
   `js/certificate.js`) · пункт меню «Курси» — **«Jira з нуля»** на чотирьох лендінгах · `js/mermaid-theme.js` —
   `useMaxWidth: false` для `state` (j10, j19) · `../jira.html` → 404 до білду · `win__topbar` — 0 вживань у 22 уроках ·
   заміри на 390 px: таблиці вікон на 4–7 колонок, багаторядковий `term` (j15), `term` з рядком до ~210 знаків і
   `sequenceDiagram` (j22), `ds-tbl` груп і `win__table` W3 (j20) · блок «Хто веде» — заглушка до сторінки «Про засновника» ·
   реєстр заявок дизайну — `cross-findings.md`, пункти «Дизайну» в наскрізних розділах хвиль 2–5.

**Чекаємо від власника:** слово на іспит j23 і на довідники ×3 · **JSM Free і Confluence Free на sandbox** (j16, j18, j19
написані «за документацією» й чекають дозйомки; довідник-карта — після цього), English, 2–3 тестові адреси · **`/mcp` →
`claude.ai Atlassian`** і підключене розширення Chrome (живий прогін j22; дозйомка sandbox) та разовий дозвіл на наповнення
sandbox (відкрите питання №5) · заголовок hero (№6 — для дизайну) · **передати бриф дизайн-сесії** (робить власник; у брифі
`dev/design/012-jira/task.md` 2026-09-19…20 оновлено: назва фірми — «Марципан», монограма `AIJ`, блок автора посилається на
майбутню сторінку «Про засновника» (до того — заглушка, секція має вимикатись без шкоди лендінгу), пункт меню «Jira з
нуля»; рядок 012 в індексі `dev/design/README.md` оновлено тим самим; реєстр заявок дизайну — `cross-findings.md`) ·
`MY-2` з двома канонічними датами (Sep 5 у j08, Oct 5 у j21 — щомісячна справа) — підтвердити або вирівняти вікна.
**Закрито власником 2026-09-19 (не переоткривати):** фірма «Марципан» і 8 ролей · авторка `FIN-1`/`FIN-2` — Соломія · «млин
«Золотий колос»» · «службовий спейс» · блок «Хто веде» — заглушка · `AIJ` · «Jira з нуля» · іспит після рецензії · канон
статусів `FIN`. Приватне про sandbox — `dev/jira-course-plan.local.md`.

## Зроблено

### 2026-09-10 · 011 «Реєстр відкладених хвостів» — у проді

Мерж `c2b0ef1` (`--no-ff`). За три сесії одного дня: рядки «Я» кореневою сесією → хвиля 1
«рішення» (4 PM-агенти, `05-decisions/`) → хвиля 2 «фікси» (фронтендер ∥ бекендер) → QA два
кола (D-01 маска гасила повзунок `--tall`, D-02 стан `left` — обидва закриті) → рядок 11
(PDF в акценті свого курсу) кореневою сесією → реліз `0.5.1 / 1.4.1 / 1.2.1`. Реєстр порожній:
23 рядки закриті кодом або рішеннями власника; 1a → `dev/ideas.local.md`. Подробиці — у папці
задачі, протокол прода — `tg/CHANGELOG.md` 2026-09-10.

### 2026-09-09 · Крок 4 уроку `claude-code-02` — таблиця стала списком, у проді

Власник знайшов у проді зламану таблицю «Поверхня»: друга колонка стиснута до одного
символа в рядок. Причина — `white-space: nowrap` на першій колонці `.ds-tbl` (правило для
команд і ключів), а там лежало речення на 91 символ: колонка розпиралась на 734 px.
Рішення власника — **компонент не чіпати**, а на цій сторінці перевести перелік у звичайний
`<ul>` формату «**Назва.** опис», як у кроці 2 того ж уроку. Текст збережено дослівно,
заголовок другої колонки перенесено у вступний абзац. Мерж `--no-ff` (`1277549`), прод
звірено побайтово.

**Замір, що лишився в спадок:** у власному Chrome по CDP пройдено **всі 180 таблиць сайту**
на 1280 px — та сама причина ламає ще 5 таблиць повністю і ~12 помірно. Виписано в реєстр
011 рядком 13a. **Пастка методу:** на сторінках уроків усі ширини виходять нульові, поки не
знято шлагбаум `#aiaGate` — він ставить `hidden` на всі діти `main` **після** відповіді
Supabase, тобто пізніше за `load`.


### 2026-09-08 · 010 — увесь сайт на дизайн-системі 009, у проді

Три платформи переведені на єдину систему: 66 публічних HTML на одному наборі токенів,
`css/custom.css` (2 535 рядків) видалений, 66 копій `tailwind.config` → один `js/tw-theme.js`,
540 альфа-модифікаторів → 0, акцент курсу через `data-course`. Конвеєр за два дні:
5 фронтендерів у три хвилі → QA коло 1 → коло фіксів → QA коло 2 (двоє агентів) → 4 фікси →
QA коло 3 «пройдено». **Закрито 16 дефектів.** Змержено `--no-ff` (`4d1edbb`), версії
`0.5.0` / `1.4.0` / `1.2.0`, прод звірено побайтово, 65 адрес sitemap → 200.
Подробиці — `010-page-templates/SUMMARY.md`; відкладене — задача 011.

**Три уроки, що переживуть задачу:** дві з чотирьох блокерів кола 1 були **регресіями від
правок, зроблених мимохідь** у самій кореневій сесії · три звіти поспіль звинувачували
Mermaid у злиплих підписах, а причина була в тому, що `<br/>` у розмітці не екранований і
`textContent` його викидав · правдивий резерв висоти плити **погіршив** CLS, бо прибрав
випадкову компенсацію — важелем виявився порожній `<h1>`, а не плита.

**Метод, який варто забрати далі:** QA кола 2 підняв **власний Chrome із видимим вікном**
(CDP + вимкнене приспання фонових вкладок) — і це зняло всі шість пасток заміру проєкту
одразу: справжній `Tab`, реальний вʼюпорт, чесний CLS, емуляція `prefers-reduced-motion`,
блокування CDN. Рецепт — у памʼяті агента `method_cdp_own_chrome.md`.



### 2026-09-06 · 006 / 007 / 008 — у проді одним релізом

Три задачі одного дня, змержені разом. **006** закрила платформні хвости після 005 (бренд PDF
за курсом, один запит `certificates`, самолікування імені, омогліфи `/verify`, кнопка банки,
футерні цілі 24 px, пілюля «3/12», модалка з пасткою фокуса, skip-link, шапка уроку) — QA два
кола, D-01 закрито з A/B-контролем. **007** прибрала підказку довжиною у квізах трьох курсів:
«правильна = найдовша» 87,9 % → 28,7 %, дірка закрита в чекері, контракті й чеклисті. **008**
провела редакторський прохід по всіх 26 сторінках AI Терміналу (26/26 «готово з зауваженнями»,
~60 фактичних правок із джерелом) і закрила реєстр наскрізних правок П-1…П-6.
Версії `0.4.2` / `1.3.2` / `1.1.0`. Подробиці — `SUMMARY.md` кожної задачі.

**Три висновки, що переживуть ці задачі:** клас, який існує лише в рядку JS, Tailwind CDN
генерує із затримкою — це видимий стрибок верстки, і такий самий корінь має ще 28 утиліт у
проді · **тричі довідник курсу виявився правішим за урок**, тож довідники варто тримати
джерелом правди · вирівняти довжину варіантів у квізі мало: правильну відповідь так само видає
**форма** — пунктуація й побудова фрази (лишено як є за рішенням власника).


### 2026-09-05 · 005 · AI Термінал — у проді

Третій курс (22 уроки + іспит + три довідники, лендінг `/claude-code`), навігація «Курси» на
трьох лендінгах, закрита 004 і PDF-фікси сертифіката для трьох курсів (0,42 МБ, `doc.link`,
робочий QR). Конвеєр за один день: PM → бекендер (`005-2` через Chrome) ∥ фронтендер → QA три
кола (63/63, перший сертифікат в історії проєкту). Змержено в `main` 2026-09-05 (`cdf71a8`,
`--no-ff` — щоб не повторити пастку fast-forward з 003), версії `1.0.1` / `0.4.1` / `1.3.1`,
прод звірено по хешах. Подробиці — `005-ai-terminal/task.md` п. 8–25, `01-plan.md`,
`04-qa/report.md`, `../design/005-ai-terminal/SUMMARY.md`.

### 2026-09-04 · 004 · Борг доступності — закрито

Зум (`user-scalable=no` у 38 HTML), контраст третинного тексту, ціль кнопки імені й
блокування фону під модалкою — усе одним проходом разом із версткою 005 (`98281b7`).
Контраст виявився не у «двох місцях», а в **51 входженні по 40 файлах**. Хвіст: футерні
посилання на двох живих лендінгах — 20px, не чіпались, бо це видима зміна.


### 2026-08-26 · 003 · Роадмап розробки — у проді

Окрема анімована сторінка `/roadmap` (варіант A «Лінія», перше підключення GSAP у проєкті)
плюс точка входу у футерах обох платформ. Конвеєр пройдено за один день: PM → фронтендер →
QA → коло фіксів (дефект D-01 закритий у `dc8cd12`), Supabase не чіпався ніде. Змержено в
`main` 2026-08-26 (`9f0f1be`, fast-forward через `git push origin dev:main`), версії підняті
до `0.3.0` / `1.2.0`. Подробиці — `003-roadmap/SUMMARY.md`, там же дві латентні пастки в
`js/roadmap-render.js` і дефект, що лишився в макеті.

### 2026-08-25 · 001 · Вхід і реєстрація через Google — у проді

Перша задача, що пройшла конвеєр повністю (PM → бекенд ∥ фронтенд → QA → коло фіксів → QA):
вхід через Google, `js/auth.js` як шар даних плюс новий `js/auth-ui.js`, редагування імені
для сертифіката, слот авторизації і токени макета в 38 HTML. Змержено в `main` (`661cf45`,
`d7febfd`), версії підняті до `0.2.0` / `1.1.0`, прод перевірено після деплою: сторінки 200,
регістрові пастки 404, приватне не роздається. Подробиці — `001-oauth-google/SUMMARY.md`,
незакриті хвости — у розділі «Активне» вище.

### 2026-08-24 · 002 · Ескалація привілеїв через `profiles.role` — закрито

Дірку знайдено 2026-08-23 плановою звіркою живої бази: `anon`/`authenticated` мали `UPDATE`
на всі колонки `profiles`, а `profiles_update` не мала `WITH CHECK` — один `PATCH` робив
будь-якого учня адміном. Власник застосував міграцію `002-lock-profiles-role.sql`; звірено
з базою після застосування: лишилось рівно `authenticated / full_name / UPDATE`, `INSERT`
забраний, `with_check` тепер явний. Головний висновок на майбутнє — **RLS не обмежує
колонки, це робота `GRANT`**, тому коректний екран політик у Dashboard нічого не доводить.

### 2026-08-23 · хвіст після створення майстерні — закрито

Supabase MCP піднявся й пройшов OAuth; звірку живої бази зроблено (результат — задача 002
вище, розділ «Стан бекенду» в `CLAUDE.md` переписаний за фактами). Загадку `payments`
закрито: заготовка під платні курси, 0 рядків, ніколи не використовувалась — не чіпали.
Дублікат `dev/.mcp.json` прибрано (канонічний — кореневий: ключ проєкту в `~/.claude.json`
саме `/Users/ander1.sage/Downloads/AIA`). Бекендеру доданий `mcp__claude-in-chrome` як
запасний шлях до Dashboard, з тією ж межею «дивитись — так, змінювати — ні».

### 2026-08-23 · майстерню створено

Створено `dev/build/` за зразком `dev/design/`: конвеєр із чотирьох агентів
(`aia-build-pm`, `aia-build-backend`, `aia-build-frontend`, `aia-build-qa`), усі Opus 5 /
effort `high`, 19 вендорених скілів у `.agents/skills/`. Рішення власника, зафіксовані в
`CLAUDE.md`: папка їде в публічний гіт із фільтром секретів · агенти правлять реальний код
і пушать у `dev`, `main` заборонений категорично · Supabase — читання через MCP, зміни
SQL-файлами на затвердження · нумерація задач спільна з `design/` · тестування живим
браузером без інсталяцій · фронтендер точно втілює макет і не вигадує візуал.

**Видимість агентів — закрито.** Агенти й скіли фізично лежать у `dev/build/.claude/`,
але харнес шукає їх у кореневій `.claude/` проєкту, тому звідти їх не було видно.
Створено відносні симлінки в `AIA/.claude/agents/` (4 агенти) і `AIA/.claude/skills/`
(19 скілів) — джерело правди лишилось одне, симлінки переживають клон. Деталі й порядок
оновлення — у `CLAUDE.md`, розділ «Як харнес знаходить цих агентів».

**Dev-база — вирішено: не робимо.** Розглядався Supabase-бранчинг: перевірено, що він
доступний лише від плану Pro ($25/міс) + $0.01344 за бранч на годину (≈$35/міс разом), а
бранч приходить без даних. Безкоштовну альтернативу (другий Free-проєкт, у власника є
вільний слот із двох) власник свідомо відхилив. **Рішення: одна база на все, вона ж прод.**

Наслідок, який довелось прописати в агентах: **dev-превʼю працює з продовою базою** —
у `config.json` один `supabase.url`, вибору середовища за хостом у `js/auth.js` немає.
Тестувальник отримав жорсткі правила для тестів, що пишуть у БД (дозвіл власника на кожен
прогін, один призначений тестовий акаунт, сертифікати не генерувати, перелік створених
рядків у звіті); бекендер — застереження, що MCP дивиться в прод і міграцію ніде не
обкатати. План на випадок, якщо колись робитимемо dev-базу, записаний у `CLAUDE.md`,
щоб не переоткривати питання.

**Supabase MCP вимкнений — закрито того ж дня, див. запис «хвіст після створення
майстерні» вище; нижче лишено як опис проблеми і способу її зняти.** У `AIA/.claude/settings.local.json` стоїть
`"disabledMcpjsonServers": ["supabase"]`, тож інструментів `mcp__supabase__*` немає
(перевірено пошуком інструментів). Сам сервер у `.mcp.json` прописаний правильно
(`project_ref=hpcyrnxschpxlrxudmqk`). Спроба увімкнути з сесії заблокована класифікатором
дозволів — і це правильно: доступ до живої бази вмикає людина. Власнику: `/mcp` →
увімкнути `supabase` → перезапуск. `claude mcp add` не допоможе — запис у `.mcp.json`
уже є й правильний (URL ідентичний), блокує саме рядок у `settings.local.json`. До того бекендер працює без звірки схеми з живою базою
і зобов'язаний позначати це у звіті.
