# Журнал подій інженерної майстерні

Що відбувалося в `dev/build/` і чого чекаємо далі. Зворотно-хронологічний: **найновіше
згори**. Аналог `dev/design/JOURNAL.md`, але про реалізацію, а не про дизайн.
Веде коренева сесія; PM-агент дописує по своїх задачах.

## Правило журналу

**Поки задача в роботі — розписуємо детально.** Статуси, на якому агенті стоїмо, які
дефекти знайдено, яке коло циклу QA, чого чекаємо і від кого (особливо: які міграції
лежать незастосовані й чиє рішення блокує).

**Коли задача в проді — увесь опис стирається** і замінюється одним записом:
**дата · що за задача · що зроблено. Максимум 2–3 речення.** Задача переїжджає з
«Активного» у «Зроблено».

Сенс правила: хроніка потрібна, поки нею користуються. Після здачі подробиці лишаються
в папці задачі (`01-plan.md`, `02-backend/`, `03-frontend/`, `04-qa/`, `SUMMARY.md`).

**Репозиторій публічний:** ні ключів, ні даних учнів, ні фінансів — і в журналі теж.

---

## Активне

### 003 — Роадмап розробки: окрема анімована сторінка · статус `у коді`

**2026-08-26: конвеєр пройдено повністю за один день.** PM → фронтендер → QA → коло фіксів.
Запускали **по одному агенту, кожного за окремою командою власника** — його пряме рішення.
Бекендера не було: Supabase не чіпався ніде.

**Де все спинилось: `main` НЕ мерджено.** Власник сказав «мерджити можна», але команду
`git checkout main && git merge` блокує класифікатор дозволів із сесії, і власник вирішив
залишити як є. Тобто задача жива тільки на `dev` і на dev-превʼю `/roadmap`; `main` відстає
на 21 коміт. **Наступний крок — мерж руками власника**, кроки й перевірки після нього —
`003-roadmap/SUMMARY.md`. Запису в `tg/CHANGELOG.md` свідомо ще немає — релізу не було.

**Контент сторінки складали окремо від макета.** Усі 16 пунктів у макеті були моками
(`design/00-decisions.md` §3); реальні **6 / 2 / 10** зібрані з власником із фактів
`tg/CHANGELOG.md`, git-історії, конфігів і `dev/ideas.local.md` і лежать у `content.md`.
Курси датовані `Q-2 2026`, решта зробленого `Q-3 2026` — тобто у «Зроблено» дві квартальні
групи. Задачу 002 на публічній сторінці не показуємо, у пункті про доступність немає ні
слова про email у сертифікаті.

**Порядок пунктів власник перевернув на «старіше вище»** (час тече вниз). Це породило дві
латентні пастки в `js/roadmap-render.js`, обидві **свідомо не виправлені**: згортання
квартальних груп іде за індексом (`gi >= 2`), тобто при третьому кварталі згорнеться
найсвіжіший; і гілка `dropped` сортує «Зроблено» у зворотному порядку. Обидві невидимі на
поточних даних, обидві описані у звітах і в переліку «чого QA не подає як дефект».

**QA завалив перший прогін — дефект D-01.** Голова лінії відривалась від змійки після
кожного `render()` і висіла окремим кільцем за 116px від лінії до першого скролу. Фікс
`dc8cd12` — хук `setTrailProgress(lit.p)` зсередини `render()`, симетрично наявному
`rebindPulse()`. Перевірку фіксу на живому превʼю робила **коренева сесія**, бо браузер до
підагента не дійшов (див. нижче). Критерій 20 після фіксу — ті самі 523.04 / 595.34.

**Два обмеження харнеса, які цього дня коштували часу:**
1. **Підагенти не можуть створювати report `.md`** — і фронтендер, і QA віддали повний текст
   звіту фінальним повідомленням, а файли записала коренева сесія. Це вже ловилось на 001;
   тепер правило «якщо харнес блокує — віддай текст» вписане у визначення фронтендера.
2. **Тулсет підагента фіксується при спавні.** Власник попросив дати фронтендеру браузер,
   `mcp__claude-in-chrome` додано в його визначення (`6f3105f`) — але скіл `claude-in-chrome`
   відповів, що інструменти в контекст агента не входять, бо тулсет зафіксовано до появи
   підключення. **Діятиме з наступного запуску сесії.**

**Задача 004 підросла на пʼятий пункт** (`4d49392`): `text-faint` (3.57:1) у футерах
`index.html` і `architect.html`. Знайшлось при плануванні 003 — футер роадмапу свідомо
зроблено контрастнішим, тож без цього пункту головні сторінки лишились би гіршими за
другорядну. Це єдиний пункт 004 із видимою зміною, межа «нуль візуальних змін» більше не
описує всю задачу.

**Хвіст у чужій майстерні:** макет лишився з дефектом D-01
(`design/003-roadmap/04-variants/_base/motion.js`). Ми в `dev/design/` не пишемо — рішення
власника, переказувати сесії `design_1` чи ні.

### 2026-08-26 · PM закінчив: `01-plan.md` готовий, **блокуючих питань нуль**

План розгорнутий із `task.md` + `content.md` (остання ревізія `3915bfa`) + `SUMMARY.md`
дизайну й читання реального коду. Головне, що з нього треба знати, не відкриваючи файл:

- **Контент фінальний: 6 / 2 / 10 = 18 пунктів.** У плані таблиця з **`id` кожного пункту**
  (їх макет не мав — вони стають фрагментами URL), а тексти беруться з `content.md`
  побайтово. Похідні числа, які мусять зійтися: дві квартальні групи в «Зроблено»
  (`Q-2 2026` → `Q-3 2026`), жодна не згорнута, 12 контролів «#», нуль пунктів без дати.
- **Три знахідки з коду, яких у макеті не було видно і які інакше стали б питаннями
  фронтендера:**
  **(1)** `js/config.js:66` перезаписує `document.title`, а `render.js:192` ставить свій —
  два асинхронні fetch змагаються за заголовок; закрито атрибутом `data-own-title` і однією
  умовою; **(2)** `js/contact.js:51` вішається на **єдиний** `#contactTrigger`, а роадмапу
  потрібні чотири точки виклику — закрито публічним `window.AIAContact.open`;
  **(3)** футер, скопійований з `index.html`, приносить `text-faint` (#7A7164, 3.57:1) у
  трьох місцях — це рівно те, що критерій приймання 4 забороняє; на роадмапі беремо
  `text-muted`, наявні дві сторінки не чіпаємо (борг поруч із 004).
- **Реверс порядку пунктів дав другу латентну пастку.** Крім уже відомої (`render.js:255`
  сортує «Зроблено» новіше-вгору при вмиканні `dropped`), є друга: `gi >= 2` тепер згортає
  **найсвіжіші** квартальні групи, а не найстаріші. На двох групах не видно взагалі;
  побачиться на третьому кварталі. Однорядковий фікс існує і **доказово нічого не змінює
  на поточних даних**, але дефолт плану — **не виправляти**, як власник вирішив щодо першої
  пастки. Обидві мусять бути у звіті фронтендера, інакше QA подасть їх як дефекти.
- **Сценарні перемикачі URL** (`?data=max` тощо) план пропонує **лишити**, але вичистити з
  них вигадані пункти: `inflate()` клонує реальні з префіксом `[тест]`. Без перемикачів
  критерії 7, 14, 15, 18 і `copy.noDate` на превʼю **неперевірювані** — QA має лише браузер
  і не править дані.
- **Ліцензія GSAP перевірена самостійно 2026-08-26** на `gsap.com/licensing`: безкоштовно
  для комерційного використання з 30.04.2025, атрибуції не вимагає, обмеження лише щодо
  конкурентів візуального конструктора Webflow. Три файли CDN 3.13.0 віддають 200
  (72 435 / 44 157 / 7 069 байтів).
- Критеріїв приймання **35**: 1–19 із нумерацією дизайну без змін, 20–35 нові (стиснений
  hero, недоторканий високий екран, порядок і групи, регресія на двох наявних сторінках,
  таблиця `curl` із регістровими пастками, побайтова палітра, деградація без GSAP).
  Окремим списком — пʼять речей, які **неможливо** перевірити на живих даних і які треба
  фіксувати як неперевірені, а не як пройдені.

**Чекаємо: команду власника запустити `aia-build-frontend`.** Вхід для нього — `01-plan.md`
(і тільки він: макет, контент і дизайн уже зведені там). Сім відкритих питань неблокуючі,
у кожного стоїть дефолт, за яким можна працювати.

---

### 004 — борг доступності після 001 і email у сертифікаті · статус `у плані`

Заведено 2026-08-25 рішенням власника: усі хвости 001 закриваються **одним прогоном
конвеєра**, а не по одному. Чотири пункти, усі дрібні:

| # | Що | Кому | Де саме |
| - | -- | ---- | ------- |
| 1 | `user-scalable=no` блокує зум — WCAG 1.4.4 (AA) | фронтендер | 38 HTML; ще 6 файлів уже мають нормальний viewport — розмітка розʼїхалась |
| 2 | Ціль кнопки імені ≈130 × 18 проти мінімуму 24 × 24 — WCAG 2.2 SC 2.5.8 | фронтендер | `.slot__name` у `css/custom.css`, кнопка `#aiaNameBtn` у `js/auth-ui.js` |
| 3 | Фон під модалкою прокручується — замка `body` немає | фронтендер | `.aia-scrim`, зараз лише `overscroll-behavior: contain` |
| 4 | При порожньому імені в сертифікат друкується email, а не «Студент» | бекендер | `maybe_issue_certificate`: `coalesce(full_name, email, 'Студент')` |

**Головна межа задачі — нуль візуальних змін.** Пункти 2 і 3 виглядають так, як у
затвердженому макеті 001, тому правити їх можна лише невидимо (зона дотику через падінг
або псевдоелемент, замок прокрутки без стрибка від смуги прокрутки). Якщо без зміни вигляду
не виходить — **стоп і назад у дизайн**, а не «трохи підправити». Саме через цю межу дизайн
на вході не потрібен.

**Пункт 4 — міграція**, тобто файл у `02-backend/db/` з блоком відкату, застосовує власник,
далі запис у `tg/CHANGELOG.md`. Виданих сертифікатів зараз 0, тож вікно виправити це «до
першої видачі» ще відкрите; `certificates.full_name` лишається знімком на момент видачі.

**Окремо для PM:** критерій приймання 15 із плану 001 у новий план не переносити — він був
нездійсненний за конструкцією (`signInWithOAuth` не робить мережевого запиту). Це п'ятий
хвіст 001, і закривається він формулюванням, а не кодом.

**Чекаємо: прогін конвеєра** — PM → бекенд ∥ фронтенд → QA. Запускає коренева сесія.

---

## Зроблено

### 2026-08-25 · 001 · Вхід і реєстрація через Google — у проді

Перша задача, що пройшла конвеєр повністю (PM → бекенд ∥ фронтенд → QA → коло фіксів → QA):
вхід через Google, `js/auth.js` як шар даних плюс новий `js/auth-ui.js`, редагування імені
для сертифіката, слот авторизації і токени макета в 38 HTML. Змержено в `main` (`661cf45`,
`d7febfd`), версії підняті до `0.2.0` / `1.1.0`, прод перевірено після деплою: сторінки 200,
регістрові пастки 404, приватне не роздається. Подробиці — `001-oauth-google/SUMMARY.md`,
незакриті хвости — у розділі «Активне» вище.

### 2026-08-24 · 002 · Ескалація привілеїв через `profiles.role` — закрито

Дірку знайдено 2026-08-23 плановою звіркою живої бази: `anon`/`authenticated` мали `UPDATE`
на всі колонки `profiles`, а `profiles_update` не мала `WITH CHECK` — один `PATCH` робив
будь-якого учня адміном. Власник застосував міграцію `002-lock-profiles-role.sql`; звірено
з базою після застосування: лишилось рівно `authenticated / full_name / UPDATE`, `INSERT`
забраний, `with_check` тепер явний. Головний висновок на майбутнє — **RLS не обмежує
колонки, це робота `GRANT`**, тому коректний екран політик у Dashboard нічого не доводить.

### 2026-08-23 · хвіст після створення майстерні — закрито

Supabase MCP піднявся й пройшов OAuth; звірку живої бази зроблено (результат — задача 002
вище, розділ «Стан бекенду» в `CLAUDE.md` переписаний за фактами). Загадку `payments`
закрито: заготовка під платні курси, 0 рядків, ніколи не використовувалась — не чіпали.
Дублікат `dev/.mcp.json` прибрано (канонічний — кореневий: ключ проєкту в `~/.claude.json`
саме `/Users/ander1.sage/Downloads/AIA`). Бекендеру доданий `mcp__claude-in-chrome` як
запасний шлях до Dashboard, з тією ж межею «дивитись — так, змінювати — ні».

### 2026-08-23 · майстерню створено

Створено `dev/build/` за зразком `dev/design/`: конвеєр із чотирьох агентів
(`aia-build-pm`, `aia-build-backend`, `aia-build-frontend`, `aia-build-qa`), усі Opus 5 /
effort `high`, 19 вендорених скілів у `.agents/skills/`. Рішення власника, зафіксовані в
`CLAUDE.md`: папка їде в публічний гіт із фільтром секретів · агенти правлять реальний код
і пушать у `dev`, `main` заборонений категорично · Supabase — читання через MCP, зміни
SQL-файлами на затвердження · нумерація задач спільна з `design/` · тестування живим
браузером без інсталяцій · фронтендер точно втілює макет і не вигадує візуал.

**Видимість агентів — закрито.** Агенти й скіли фізично лежать у `dev/build/.claude/`,
але харнес шукає їх у кореневій `.claude/` проєкту, тому звідти їх не було видно.
Створено відносні симлінки в `AIA/.claude/agents/` (4 агенти) і `AIA/.claude/skills/`
(19 скілів) — джерело правди лишилось одне, симлінки переживають клон. Деталі й порядок
оновлення — у `CLAUDE.md`, розділ «Як харнес знаходить цих агентів».

**Dev-база — вирішено: не робимо.** Розглядався Supabase-бранчинг: перевірено, що він
доступний лише від плану Pro ($25/міс) + $0.01344 за бранч на годину (≈$35/міс разом), а
бранч приходить без даних. Безкоштовну альтернативу (другий Free-проєкт, у власника є
вільний слот із двох) власник свідомо відхилив. **Рішення: одна база на все, вона ж прод.**

Наслідок, який довелось прописати в агентах: **dev-превʼю працює з продовою базою** —
у `config.json` один `supabase.url`, вибору середовища за хостом у `js/auth.js` немає.
Тестувальник отримав жорсткі правила для тестів, що пишуть у БД (дозвіл власника на кожен
прогін, один призначений тестовий акаунт, сертифікати не генерувати, перелік створених
рядків у звіті); бекендер — застереження, що MCP дивиться в прод і міграцію ніде не
обкатати. План на випадок, якщо колись робитимемо dev-базу, записаний у `CLAUDE.md`,
щоб не переоткривати питання.

**Supabase MCP вимкнений — закрито того ж дня, див. запис «хвіст після створення
майстерні» вище; нижче лишено як опис проблеми і способу її зняти.** У `AIA/.claude/settings.local.json` стоїть
`"disabledMcpjsonServers": ["supabase"]`, тож інструментів `mcp__supabase__*` немає
(перевірено пошуком інструментів). Сам сервер у `.mcp.json` прописаний правильно
(`project_ref=hpcyrnxschpxlrxudmqk`). Спроба увімкнути з сесії заблокована класифікатором
дозволів — і це правильно: доступ до живої бази вмикає людина. Власнику: `/mcp` →
увімкнути `supabase` → перезапуск. `claude mcp add` не допоможе — запис у `.mcp.json`
уже є й правильний (URL ідентичний), блокує саме рядок у `settings.local.json`. До того бекендер працює без звірки схеми з живою базою
і зобов'язаний позначати це у звіті.
