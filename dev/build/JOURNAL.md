# Журнал подій інженерної майстерні

Що відбувалося в `dev/build/` і чого чекаємо далі. Зворотно-хронологічний: **найновіше
згори**. Аналог `dev/design/JOURNAL.md`, але про реалізацію, а не про дизайн.
Веде коренева сесія; PM-агент дописує по своїх задачах.

## Правило журналу

**Поки задача в роботі — розписуємо детально.** Статуси, на якому агенті стоїмо, які
дефекти знайдено, яке коло циклу QA, чого чекаємо і від кого (особливо: які міграції
лежать незастосовані й чиє рішення блокує).

**Коли задача в проді — увесь опис стирається** і замінюється одним записом:
**дата · що за задача · що зроблено. Максимум 2–3 речення.** Задача переїжджає з
«Активного» у «Зроблено».

Сенс правила: хроніка потрібна, поки нею користуються. Після здачі подробиці лишаються
в папці задачі (`01-plan.md`, `02-backend/`, `03-frontend/`, `04-qa/`, `SUMMARY.md`).

**Репозиторій публічний:** ні ключів, ні даних учнів, ні фінансів — і в журналі теж.

---

## Активне

### 005 · конвеєр пройдено повністю — 2026-09-05, коренева сесія

**QA коло 2 — ПРОЙДЕНО** (`3a71507`): усі 63 критерії, наскрізний шлях «реєстрація → 23 модулі
по порядку → іспит → сертифікат → `/verify`» пройдений **уперше в історії проєкту** й працює.
**Того ж дня закриті два дефекти PDF і QR, QA коло 3 — ПРОЙДЕНО:** D-04 27 МБ → 0,42 МБ (JPEG,
`3d2eeac`), D-05 клікабельні області `doc.link` над кодом і URL (`2a1aeaa`) — клік доведений
переходом на `/verify` «Сертифікат дійсний»; QR не працював ніколи (`qrcode@1.5.3/build/` → 404,
у проді те саме) → `1.4.4` (`b6e2226`), QR читається jsQR і збігається з кодом. Усе — для трьох
курсів (`js/certificate.js`, `certificate.html` спільні). Тестовий сертифікат кола 2 видалений за
дорученням власника через `/tasks` (`6ee0dfc`), у колі 3 виданий новий; SQL для повного
прибирання акаунта — `02-backend/db/005-4-cleanup-qa-account.sql`, застосовує власник.
Мерж у `main` — **чекає рішення власника**; QA рекомендує мерджити.

**Що зроблено за день (усе на `origin/dev`):** міграція `005-2` застосована бекендером через
Chrome за разовим дозволом (`1cd372e`) · `01-plan.md` від PM, 63 критерії (`e23f67b`) · три
довідники доведені до еталона, вступні блоки **перенесені** в кінець дослівно, пілюля прогресу
на лендінгу (`f5d9583`…`cf5d81d`) · два текстові розриви виправлені за рішенням власника
(`c0527ac`) · QA коло 1 — не пройдено, D-01 (`389a06e`) · фікси: 100 таблиць уроків в
обгортку + `overflow-wrap` у спільному CSS, іспит посиланням, довідники у футері 23 уроків,
версії 1.0.1 / 0.4.1 / 1.3.1 (`02a7818`…`c6f8f06`) · QA коло 2 — пройдено.

**Чотири речі, які ламають інтуїцію:**
1. **QA помилився з причиною D-01 на 5 сторінках із 14** — там розпирали нерозривні рядки в
   інлайновому `<code>`, а не таблиці; фронтендер довів A/B через CSSOM. Правило переносу
   тепер спільне для трьох курсів, регресії немає.
2. **Агенти не реєструють акаунтів і не вводять паролів** — це межа інструменту, не дозволу.
   Реєстрацію тестового акаунта і зміну імені виконав власник у Chrome, QA продовжив у тій
   самій сесії. Перша спроба зупинена: імʼя в профілі було не «QA Тест 005», а сертифікат
   бере імʼя знімком і не перевидається.
3. **Перший сертифікат у базі одразу показав два платформні дефекти** `js/certificate.js`:
   **D-04** PDF важить 27 МБ (`html2canvas` scale 2 + PNG ×2), **D-05** URL перевірки в PDF
   існує лише як пікселі — не клікнути, не скопіювати, при переписуванні латиниця плутається з
   кирилицею, і `/verify` каже «не знайдено». Рішення власника (у діалозі з QA): у **всі**
   сертифікати — пряме клікабельне посилання (`doc.link(...)` поверх зображення).
4. **Власник розмовляє з підагентами напряму через `/tasks`** — два рішення дня (ширина
   лендінгу, посилання в сертифікаті) прийшли в кореневу сесію через звіти агентів, а не через
   діалог. Читати фінальні повідомлення агентів до кінця.

**Створені рядки в проді (звірено через MCP):** `profiles` 1 («QA Тест 005»), `enrollments` 3,
`progress` 23, `quiz_attempts` 23, `certificates` 1. Прибирання `certificates` — лише міграцією.

**Відкриті хвости:** D-04 + D-05 у `js/certificate.js` (зона бекендера, три курси) · рішення
про мерж · П-01 сайдбар уроку налазить на шапку при висоті < 785 px (**є в проді**) · П-02
модалка «Написати нам» без пастки фокуса · П-08 пілюля на 390 px переносить назву курсу ·
`aia-content-reviewer` так і не запускався · ширина лендінгу — власник передумав, лишається.

### 005 · план нарешті існує — 2026-09-05, PM-агент

`01-plan.md` написаний **уперше за всю задачу** (до цього конвеєр ішов без нього — Д5.2).
Обсяг прогону — чотири речі разом, за рішенням власника п. 8–16 у `task.md`: три довідники
за Д5.8 · `#navProgress` на лендінгу · міграція `005-2` · **перший QA курсу на живому превʼю**.
**63 критерії приймання**, блокуючих питань до власника немає (чотири неблокуючі з дефолтами).

**Три знахідки плану, яких не було ні в Д5.8, ні у звітах** — усі зняті ґрепом 2026-09-05:

1. 🔴 **Еталон сам провалює WCAG 2.1.1.** У `claude-code-ref-commands.html` **28 обгорток
   `overflow-x` і нуль `role="region"`**, `tabindex="0"` лише два (обидва на `.term__body`).
   У «поганих» hooks і settings цей патерн якраз є (14 і 20). Тобто це не «два підтягнути до
   еталона», а **зустрічний рух**, і Д5.8.1 його прямо дозволяє.
2. 🔴 **`-ref-hooks` має 0 `scroll-mt` при липкій шапці** — усі 19 його якорів ведуть під
   шапку. Полагодиться саме, коли `id` переїде на `<section class="scroll-mt-24">`.
3. **`#navProgress` відсутній саме в еталоні** (є на двох інших довідниках, на трьох лендінгах
   і на 23 уроках), хоча `js/config.js` там підключений і заповнив би пілюлю сам.

**Контракт пілюлі, щоб не переоткривати:** текст рівно `Прогрес: N/23` (23 = усі модулі
конфіга разом з іспитом — стільки ж показують сторінки уроків того ж курсу), N — **перетин**
`completedSet()` зі списком конфіга; ключ резерву `aia:navProgress:claude-code.config.json`
**той самий**, що вже пишуть уроки; `js/config.js` на лендінг **не підключати** (подвійний
рендер по `[data-site]`/`[data-link]`), шість функцій копіюються дослівно.

**Ще один факт для QA, звірений у коді:** `js/quiz.js` **не звертається до `AIAProgress`
жодного разу** — завершення ставить кнопка, `js/progress.js` шле `p_score = 100`, тому
`passing_score` 70/85 не перевіряється ніколи. Це поведінка платформи (п. 15), не дефект 005.

**Стан на кінець 2026-09-05:** бекендер свою частину **закрив паралельно** з написанням плану —
`005-2-module-titles.sql` написаний і **застосований**, коди `c04/c08/c14/c16/c19/c22` звірені
з живою базою через `md5(title)` (розійшлися рівно 6 із 23, усі шість кодів із SUMMARY —
правильні), запис у `tg/CHANGELOG.md` є. **Хвіст п. 8в закритий.**

**Чекаємо:** фронтендер — Ф1…Ф10 з комітом після кожного пункту; далі QA (критерії 1–63,
перший QA цього курсу взагалі). **Один Chrome на всіх — у браузері один агент за раз.**

### 005 — AI Термінал: курс Claude Code CLI · статус `в роботі`

Третій блок платформи: **22 модулі + фінальний іспит + три довідники**, коди `c01`–`c23`.
Заведено 2026-09-03.

**Уже зроблено:** міграція `005-1` у проді 2026-09-03 (курс і 23 модулі в базі) · дизайн
закрито, варіант B «Робота» · **лендінг, навігація «Курси» і компонент «термінал»
перенесені в код 2026-09-04** (`98281b7`, `b6bb63d`) · **задачу 004 закрито тим самим
проходом** · роадмап і версії оновлені (`4d5e422`).

**Авторинг закінчено: 26 сторінок із 26** — 22 уроки `c01`–`c22`, фінальний іспит `c23`
і три довідники. Усі проходять `01-authoring/check-lessons.py` без помилок і попереджень;
у `claude-code.config.json` **усі 23 модулі в `ready`**, дірок немає. Довідники лежать
**у корені сайту**, не в `modules/` — вони поза прогресом і поза сертифікатом.

**Що з конвеєра так і не проходило:** `aia-build-pm` (`01-plan.md` не існує),
`aia-build-qa` (у `04-qa/` лише порожня тека `shots`) і `aia-content-reviewer` — **на жодній
із 26 сторінок**. Замість редактора спрацював перехресний контроль авторів, і саме він дав
усі великі знахідки двох днів.

**Вхід для того, хто продовжує — папка `01-authoring/`:**
`lesson-contract.md` (розділ 0 головний) · `facts-map.md` · `cross-findings.md` (309 р.,
розділ під кожен модуль) · `check-lessons.py` · **файл промптів хвилі** (прибраний
2026-09-04, коли курс дописано).

**Чому мапа джерел існує:** фактуру збирали під 16 модулів, програму затвердили на 23,
зсув нерівномірний. Три модулі (`c01`, `c17`, `c20`) джерела не мають взагалі.

**Що зʼясувалось і змінює роботу:**
- **докси переїхали:** `docs.claude.com/en/docs/claude-code/<стор>` → 301 на
  `code.claude.com/docs/en/<стор>`, а **WebFetch крос-хостові редіректи не проходить**.
  Беремо `curl -sL …/<стор>.md` — WebFetch ще й обрізає великі сторінки (`env-vars.md`
  480 КБ, цілий розділ у нього не потрапляє). Індекс — `code.claude.com/docs/llms.txt`;
- **`--help` глушить помилку про невідомий прапорець**, але не про невірне значення
  відомого — перевірено власним запуском на 2.1.236;
- **фактура застаріває за добу:** у порогах компакції вже Opus 4.8 і 5 · `/vim` видалена
  у v2.1.92 · команд не «~90», а **111** · прапорців не «~90», а 62 + 13 підкоманд.
  Підпис довідника на лендінгу виправлено.

### 005 · доручення власника по довідниках — 2026-09-04, пізній вечір

Власник переглянув три довідники наживо й дав **три правки, які виконує фронтендер**. Дизайн
цих сторінок ніколи не малювала (їх не було в брифі 005) — тому їхній вигляд ні на що не
спирається, і саме тому вони власнику не сподобались. Записано в
`../design/005-ai-terminal/SUMMARY.md`, **новий розділ Д5.8** (він і є ТЗ), плюс пункт 11
у списку хвостів Д5.4.

1. **Еталон — `claude-code-ref-commands.html`, два інші довести до нього.** Різниця виміряна
   ґрепом, не на око: у `-ref-hooks` **жодного `<section>`** (нема за що чіпляти ні зміст, ні
   якорі), у `-ref-settings` **немає змісту взагалі** при 223 ключах і 24 розділах, і там же
   **4 таблиці з 24 без `overflow-x`** — це вже поламана мобілка, а не смак. Покращення й
   адаптивність — на розсуд фронтендера, рамка навмисно широка.
2. **Прибрати вступний блок про дату й версію з усіх трьох** — дубль того, що й так є в кінці.
   ⚠️ Блок називається на кожній сторінці інакше, шукати за роллю. І в `-ref-commands` у
   ньому сидить **пояснення третьої колонки таблиць** («потребує v2.1.212»), якого немає
   більше ніде, а колонка стоїть у 30 таблицях — абзац треба перенести **дослівно**, не
   переказом.
3. **Зняти оранжеві підкреслення з навігаційних карток `-ref-hooks`.** 🔴 Корінь — `.prose-aia a`
   (`css/custom.css` р. 206–210), а `.prose-aia` стоїть **на 62 сторінках**. Правити саме це
   правило = зняти підкреслення з посилань у всіх уроках трьох курсів. Фікс мусить бути
   локальним; класу `not-prose` у проєкті немає.

**Межа Д5.0 («текст не чіпати») тут не порушена:** видалення трьох названих блоків — рішення
власника, а не самодіяльність агента. Решта тексту лишається недоторканою, включно з
кінцівками, форму яких рівняти можна, а слова — ні.

### 005 · авторинг, стан на 2026-09-04 (вечір)

**Метод, який працює:** хвилями по 4–5 авторів, кожен пише **свій новий файл** і не має
спільної зони запису ні з ким. Обрив тоді коштує одну недописану сторінку, а не
пошкоджений спільний код.

| Хвиля | Сторінки | Результат |
| --- | --- | --- |
| 1 (`c08`–`c11`) | 4 | усі 4, без обривів (`e4e7d6b`) |
| 2 (`c12`–`c15`, `c18`) | 5 | усі 5, без обривів (`ad0aa3e`) |
| 3а (`c16`, `c17`) | 2 | обидва, без обривів — **11 авторів поспіль без жодної втрати** |
| 3б (`c19`, `c20`) | 2 | обидва, без обривів — **13 авторів поспіль без втрат** |
| 3в (`c21`, `c22`) | 2 | обидва, без обривів (`b20b37f`) |
| 4 (`c23` + 3 довідники) | 4 | усі 4 (`4dee1dd`) — разом **19 авторів за два дні без жодного обриву** |

**Ліміт на 2026-09-04, 81% сесії:** пару `c19`+`c20` запущено вже після цієї позначки й
обидва дійшли до кінця. Тижневий ліміт на той момент — 27%.

**⛔ Стоп-правило власника:** після пари `c16`+`c17` **зупинитись**. Він перевіряє
залишок ліміту сам і вирішує, коли далі. Причина — партію вже одного разу зупинили на
**96% ліміту**, і чотири агенти з шести загинули по watchdog.

**Пропускна здатність, виміряна:** 4 і 5 одночасних — стабільно. Шість — межа, дала
4 обриви з 6. Обірваний автор зʼїдає токени й не лишає нічого.

**Що партії полагодили в уже написаному** (жоден дефект не був видимий авторам «своїх» уроків):
1. **`c05` три дні мав невидимі заголовки** — чотири `<h4>` без правила в `css/custom.css`;
   Tailwind preflight скидає `h1`–`h6`. Знайшов автор `c08`, поставивши `h4` у себе.
2. **20 пояснень квіза в пʼяти уроках вказували не туди.** `js/quiz.js` тасує варіанти
   (Фішер–Єйтс), а `explain` писали «перший варіант», «третій». Знайшов автор `c18`,
   читаючи чужий JS. Виправлено на посилання за змістом; у контракт додано §9.1.
3. **`c02` суперечив `c11`** — «конкретних чисел курс не наводить» проти уроку з цінами.
   Звужено до **лімітів**: їх немає в самій документації («no fixed message count»).
4. **`c10` і `c11` були написані, але лишались `soon`** у конфізі — тобто недоступні.
5. **`check-lessons.py` не бачив «просто » через перенос рядка** — полагоджено, одразу
   знайшлось пропущене входження.
6. **Регресія доступності в 12 уроках.** Задача 004 замінила `faint` `#7A7164` → `#9A9184`
   по всьому сайту заради контрасту AA, але шаблон, з якого автори курсу копіювали шапку,
   ніс **старе** значення — і половина уроків приїхала зі старим кольором, половина з новим.
   Знайшов автор `c16`, звіряючи власну шапку з чужими. Виправлено в усіх 12; живих
   входжень `#7A7164` у проєкті більше немає (два, що лишились у `css/`, — це коментарі,
   які описують саму задачу 004).

**Головні знахідки, що змінюють майбутні уроки:**
- **hooks: 33 події в доксах, 31 у встановленій 2.1.236** — розрив закрито до кінця:
  `PreModelSwitch` і `PostModelSwitch` потребують **v2.1.251**, а `claude-code@latest` у
  Homebrew того ж дня = 2.1.251. Доведено двічі (`claude doctor` + `strings`). Підпис
  довідника «усі 33» правильний;
- **`claude doctor` виходить із кодом `0` навіть коли знайшов `Invalid settings`** — тобто
  як гейт у CI він непридатний. У доксах цього немає;
- **у програмі курсу був неіснуючий пункт:** рядок `c20` містив «tunnels», а такої функції
  в Claude Code немає взагалі — слово трапляється лише про корпоративний проксі, інтерфейси
  `utun` і як **заборонену** дію. `program.md` виправлено. Урок на майбутнє: пункт програми
  не є доказом існування функції — її писали під стару фактуру;
- **`--permission-prompts` (docs) не існує у 2.1.236**, а **`--channels` навпаки — існує,
  але свідомо прихований із `--help`**. Разом із hooks-подіями це вже **пʼять** знайдених
  розривів між документацією і встановленою збіркою;
- **`hooks.md` 318 КБ, `errors.md` 352 КБ** — читати частинами (`curl` на диск → `grep '^#'`
  → `sed`), інакше контекст забитий до першого рядка уроку. `hooks-reference.md` **не існує**;
- **з `c17` знято 🔴**: «джерела немає» стосувалось фактури, а в доксах є цілий розділ
  Troubleshooting із чотирьох сторінок;
- **цін у доксах Claude Code немає взагалі** — вони на `platform.claude.com`. Числа лімітів
  публічно відсутні **за задумом**;
- **аліас `fable` до v2.1.255 означав іншу модель**, ніж тепер; `opus` переїхав на v2.1.219.
  Аліас — рухома ціль;
- **пісочниця Bash не накриває ані hooks, ані сервери MCP**;
- **223 ключі `settings.json`, а не 222** — перелік застаріває за добу.

**Вхід для того, хто продовжує:** файл промптів хвилі (`c16`, `c17` + розвідка розмірів
сторінок; прибраний 2026-09-04) · `cross-findings.md` (640 р., два нові розділи зі
знахідками партій) · `lesson-contract.md` §9 (уточнення, які коштували авторам часу).

**Три речі, яких з коду не видно:**
1. **Коди модулів мусять бути глобально унікальними** — `js/auth.js` будує мапу без фільтра
   за курсом. Зайнято `m01`–`m12`, `a01`–`a22`, `c01`–`c23`.
2. **Скріншот реальної сесії Claude Code — хвіст на власнику.** Без нього гліфи `⏺` і `└─`
   малюються за прототипом; живим виводом підтверджений лише `❯`.
3. **Міграція `005-2` не застосована** — вирівнює назви шести модулів у базі з конфігом.
   Готовий SQL — у `../design/005-ai-terminal/SUMMARY.md`, розділ Д5.4. Користувач цього не бачить (назви беруться з конфіга),
   але база й конфіг розходяться.

## Зроблено

### 2026-09-04 · 004 · Борг доступності — закрито

Зум (`user-scalable=no` у 38 HTML), контраст третинного тексту, ціль кнопки імені й
блокування фону під модалкою — усе одним проходом разом із версткою 005 (`98281b7`).
Контраст виявився не у «двох місцях», а в **51 входженні по 40 файлах**. Хвіст: футерні
посилання на двох живих лендінгах — 20px, не чіпались, бо це видима зміна.


### 2026-08-26 · 003 · Роадмап розробки — у проді

Окрема анімована сторінка `/roadmap` (варіант A «Лінія», перше підключення GSAP у проєкті)
плюс точка входу у футерах обох платформ. Конвеєр пройдено за один день: PM → фронтендер →
QA → коло фіксів (дефект D-01 закритий у `dc8cd12`), Supabase не чіпався ніде. Змержено в
`main` 2026-08-26 (`9f0f1be`, fast-forward через `git push origin dev:main`), версії підняті
до `0.3.0` / `1.2.0`. Подробиці — `003-roadmap/SUMMARY.md`, там же дві латентні пастки в
`js/roadmap-render.js` і дефект, що лишився в макеті.

### 2026-08-25 · 001 · Вхід і реєстрація через Google — у проді

Перша задача, що пройшла конвеєр повністю (PM → бекенд ∥ фронтенд → QA → коло фіксів → QA):
вхід через Google, `js/auth.js` як шар даних плюс новий `js/auth-ui.js`, редагування імені
для сертифіката, слот авторизації і токени макета в 38 HTML. Змержено в `main` (`661cf45`,
`d7febfd`), версії підняті до `0.2.0` / `1.1.0`, прод перевірено після деплою: сторінки 200,
регістрові пастки 404, приватне не роздається. Подробиці — `001-oauth-google/SUMMARY.md`,
незакриті хвости — у розділі «Активне» вище.

### 2026-08-24 · 002 · Ескалація привілеїв через `profiles.role` — закрито

Дірку знайдено 2026-08-23 плановою звіркою живої бази: `anon`/`authenticated` мали `UPDATE`
на всі колонки `profiles`, а `profiles_update` не мала `WITH CHECK` — один `PATCH` робив
будь-якого учня адміном. Власник застосував міграцію `002-lock-profiles-role.sql`; звірено
з базою після застосування: лишилось рівно `authenticated / full_name / UPDATE`, `INSERT`
забраний, `with_check` тепер явний. Головний висновок на майбутнє — **RLS не обмежує
колонки, це робота `GRANT`**, тому коректний екран політик у Dashboard нічого не доводить.

### 2026-08-23 · хвіст після створення майстерні — закрито

Supabase MCP піднявся й пройшов OAuth; звірку живої бази зроблено (результат — задача 002
вище, розділ «Стан бекенду» в `CLAUDE.md` переписаний за фактами). Загадку `payments`
закрито: заготовка під платні курси, 0 рядків, ніколи не використовувалась — не чіпали.
Дублікат `dev/.mcp.json` прибрано (канонічний — кореневий: ключ проєкту в `~/.claude.json`
саме `/Users/ander1.sage/Downloads/AIA`). Бекендеру доданий `mcp__claude-in-chrome` як
запасний шлях до Dashboard, з тією ж межею «дивитись — так, змінювати — ні».

### 2026-08-23 · майстерню створено

Створено `dev/build/` за зразком `dev/design/`: конвеєр із чотирьох агентів
(`aia-build-pm`, `aia-build-backend`, `aia-build-frontend`, `aia-build-qa`), усі Opus 5 /
effort `high`, 19 вендорених скілів у `.agents/skills/`. Рішення власника, зафіксовані в
`CLAUDE.md`: папка їде в публічний гіт із фільтром секретів · агенти правлять реальний код
і пушать у `dev`, `main` заборонений категорично · Supabase — читання через MCP, зміни
SQL-файлами на затвердження · нумерація задач спільна з `design/` · тестування живим
браузером без інсталяцій · фронтендер точно втілює макет і не вигадує візуал.

**Видимість агентів — закрито.** Агенти й скіли фізично лежать у `dev/build/.claude/`,
але харнес шукає їх у кореневій `.claude/` проєкту, тому звідти їх не було видно.
Створено відносні симлінки в `AIA/.claude/agents/` (4 агенти) і `AIA/.claude/skills/`
(19 скілів) — джерело правди лишилось одне, симлінки переживають клон. Деталі й порядок
оновлення — у `CLAUDE.md`, розділ «Як харнес знаходить цих агентів».

**Dev-база — вирішено: не робимо.** Розглядався Supabase-бранчинг: перевірено, що він
доступний лише від плану Pro ($25/міс) + $0.01344 за бранч на годину (≈$35/міс разом), а
бранч приходить без даних. Безкоштовну альтернативу (другий Free-проєкт, у власника є
вільний слот із двох) власник свідомо відхилив. **Рішення: одна база на все, вона ж прод.**

Наслідок, який довелось прописати в агентах: **dev-превʼю працює з продовою базою** —
у `config.json` один `supabase.url`, вибору середовища за хостом у `js/auth.js` немає.
Тестувальник отримав жорсткі правила для тестів, що пишуть у БД (дозвіл власника на кожен
прогін, один призначений тестовий акаунт, сертифікати не генерувати, перелік створених
рядків у звіті); бекендер — застереження, що MCP дивиться в прод і міграцію ніде не
обкатати. План на випадок, якщо колись робитимемо dev-базу, записаний у `CLAUDE.md`,
щоб не переоткривати питання.

**Supabase MCP вимкнений — закрито того ж дня, див. запис «хвіст після створення
майстерні» вище; нижче лишено як опис проблеми і способу її зняти.** У `AIA/.claude/settings.local.json` стоїть
`"disabledMcpjsonServers": ["supabase"]`, тож інструментів `mcp__supabase__*` немає
(перевірено пошуком інструментів). Сам сервер у `.mcp.json` прописаний правильно
(`project_ref=hpcyrnxschpxlrxudmqk`). Спроба увімкнути з сесії заблокована класифікатором
дозволів — і це правильно: доступ до живої бази вмикає людина. Власнику: `/mcp` →
увімкнути `supabase` → перезапуск. `claude mcp add` не допоможе — запис у `.mcp.json`
уже є й правильний (URL ідентичний), блокує саме рядок у `settings.local.json`. До того бекендер працює без звірки схеми з живою базою
і зобов'язаний позначати це у звіті.
